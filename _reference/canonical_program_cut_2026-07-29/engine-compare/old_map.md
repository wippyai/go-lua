# Repo map: .engine-compare/old/types

_142 files, leaf model `gemma-4-26b`, reduce model `gemma-4-26b`._

> This package contains the core type representations, constraint solvers, and flow-sensitive analysis engines. It integrates an incremental computation database with specialized logic for type narrowing, subtyping, and effect tracking.

## Architecture

- **types/** — This directory provides the foundational type system, dataflow analysis engines, and incremental computation infrastructure required for the engine's static analysis.
  - **cfg/** — This directory defines the core data structures and type definitions for control flow graphs, SSA versioning, and symbol identity used in dataflow analysis.
    - `cfg.go` — Provides control flow graph (CFG) structures and traversal utilities for dataflow analysis.
    - `identity.go` — Defines the BoundIdent type used to represent identifiers resolved to specific symbols and locations.
    - `ssa.go` — Defines types and utilities for managing unique variable identities and SSA (Static Single Assignment) versioning.
    - `versioned.go` — Defines interfaces for SSA versioning, symbol scoping, and parameter metadata required for flow-sensitive type checking within a CFG.
  - **constraint/** — This directory provides the core types, expression logic, and solver mechanisms for a constraint-based type refinement and inference system.
    - `atom.go` — Defines the core data structures for terms and atomic constraints used in the constraint language.
    - `condition.go` — Manages logical conditions represented in disjunctive normal form (DNF) using constraints.
    - `constraint.go` — Defines the types and interfaces for multi-path narrowing constraints used in type refinement.
    - `doc.go` — Provides documentation and high-level definitions for the multi-path type narrowing constraint system.
    - `effect.go` — Defines types and logic for modeling how function calls produce type refinements and effects.
    - `expr.go` — Defines the interface and concrete implementations for arithmetic expressions used in constraint evaluation.
    - `expr_compare.go` — Defines types and constructor functions for relational comparison expressions.
    - `infer.go` — Implements constraint-based generic type inference and pattern matching using subtype relationships and Tarjan's SCC algorithm.
    - `intern.go` — Provides a thread-safe mechanism to intern common constraint objects to reduce memory allocations.
    - `numeric.go` — Defines types and interfaces for representing arithmetic relationships between paths as numeric constraints.
    - `path.go` — Defines types and methods for representing and manipulating access paths to runtime values using SSA-style symbols and segments.
    - `path_helpers.go` — Provides utility functions for parsing and constructing path representations for parameter placeholders and return values.
    - `path_visit.go` — Provides utility functions to traverse and extract paths from various constraint types.
    - `solver.go` — Implements a deterministic constraint solver that applies type narrowing to program paths using an environment of resolvers.
    - `sort.go` — Provides a utility function to return map keys in a deterministic sorted order.
    - `visit.go` — Implements the visitor pattern to dispatch operations on constraint and expression type variants.
    - **constraint/theory/** — This directory provides specialized SMT-style constraint solving theories, including difference logic, modular arithmetic, and E-graph equality reasoning.
      - `difference.go` — Implements a difference logic constraint solver using a weighted directed graph and the Floyd-Warshall algorithm.
      - `doc.go` — Provides documentation and architectural overview for modular SMT-style constraint solving theories.
      - `egraph.go` — Implements an E-graph for path equality with congruence closure using Union-Find.
      - `modular.go` — Defines types and logic for solving and checking modular arithmetic constraints based on concrete values and variable ranges.
      - `result.go` — Defines the three-valued logic type and lattice operations for constraint satisfiability results.
  - **contract/** — This directory defines the formal Hoare-style behavioral specifications, including preconditions, postconditions, and effects, for function contracts.
    - `spec.go` — Defines Hoare-style function behavioral specifications including preconditions, postconditions, effects, and callback details.
  - **db/** — This directory implements a thread-safe, incremental computation database with structural interning and dependency tracking.
    - `attachment.go` — Provides a type-safe mechanism for attaching and retrieving arbitrary data from a QueryContext using generic keys.
    - `db.go` — Implements a thread-safe, Salsa-style incremental computation database with structural interning and manifest management.
    - `input.go` — Manages the leaf nodes of the dependency graph by storing and tracking revisions of input key-value pairs.
    - `query.go` — Implements an incremental computation system using memoized queries with dependency tracking and cycle detection via fixpoint iteration.
  - **diag/** — This directory manages the definition, collection, and visual rendering of type-checking diagnostic messages.
    - `collector.go` — Manages the accumulation, deduplication, and contextual help generation for type-checking diagnostics.
    - `render.go` — Provides functionality to parse source code into lines and render diagnostic messages in Rust-style text formats with optional ANSI coloring.
    - `types.go` — Defines the core data structures and types for representing diagnostic information, such as errors, warnings, and hints, within the engine.
  - **effect/** — This directory defines the core type system and serialization logic for modeling and propagating function effects and row-polymorphic behaviors.
    - `codecs.go` — Registers and implements codecs for serializing and deserializing various effect labels to and from a manifest format.
    - `compose.go` — Implements set-theoretic operations (union, intersection, subset) for effect rows to model effect propagation and compatibility.
    - `label.go` — Defines the interface and concrete implementations for effect labels used to describe function behaviors and type-level side effects.
    - `linflow.go` — Defines effect types for tracking how function parameters flow into return values for type refinement.
    - `param_ref.go` — Resolves a parameter reference index into an absolute index based on a runtime argument count.
    - `registry.go` — Manages the global registry and serialization interfaces for effect label codecs.
    - `row.go` — Defines row-polymorphic effect types and set-theoretic operations for tracking function behaviors in a Lua type checker.
    - `visit.go` — Provides visitor pattern implementations for dispatching on Label, TypeTransform, and ReturnType variants.
  - **flow/** — This directory implements a flow-sensitive type and numeric constraint solver that performs dataflow analysis across a control flow graph.
    - `edge.go` — Constructs a map of type constraints that guard control flow edges by merging multiple conditions via logical AND.
    - `flow.go` — Defines the data structures and types required for performing flow-sensitive type propagation and constraint solving across a control flow graph.
    - `graph_readonly.go` — Provides read-only access wrappers for traversing graph predecessors and successors.
    - `inference.go` — Implements function refinement inference to describe how return values relate to parameters for interprocedural narrowing.
    - `normalize.go` — Provides a method to enforce deterministic ordering of slice-based fields within an Inputs struct to ensure reproducible analysis.
    - `numeric.go` — Implements worklist-based dataflow analysis to propagate numeric constraints and detect unreachable edges in a CFG.
    - `path.go` — Provides functionality to parse path strings into structured constraint.Path objects for display purposes.
    - `product_domain.go` — Orchestrates type, numeric, and shape subdomains to perform flow-sensitive constraint narrowing and congruence closure.
    - `query.go` — Provides query methods for a solution to retrieve types, conditions, constant values, and numeric bounds at specific CFG points.
    - `solver.go` — Implements a flow-sensitive type analysis solver using a worklist algorithm to compute narrowed types across a CFG.
    - `solver_helpers.go` — Provides helper methods for initializing symbol types and building dependency maps for incremental worklist propagation in a flow analysis solver.
    - `transfer.go` — Implements transfer functions for a flow analysis worklist algorithm, handling type changes at CFG points via phi nodes, assignments, and widening.
    - `type_facts.go` — Defines the TypeFacts interface and its implementation to separate declared types from flow-refined types to prevent early synthesis poisoning.
    - **flow/domain/** — This directory defines the abstract interfaces and core logic for managing type and numeric constraint domains within the flow solver.
      - `domain.go` — Defines abstract interfaces and classification logic for type and numeric constraint domains used in the flow solver.
      - `helpers.go` — Provides utility functions for parsing and comparing hierarchical path keys within the domain layer.
      - `shape_domain.go` — Manages structural type narrowing and constraint propagation for tables and records.
      - `type_domain.go` — Manages type narrowing by applying constraints like truthiness, type membership, and nil equality to path-based types.
    - **flow/join/** — This directory provides logic for computing type unions during phi node merging by applying coalescing rules to records and maps.
      - `join.go` — Computes the union of multiple types for phi node merging by applying coalescing rules for records and maps.
    - **flow/numeric/** — This directory implements the numeric subdomain for the flow solver, managing integer constraints through state tracking and difference-bound matrix theory.
      - `domain.go` — Implements the numeric subdomain for the flow solver, managing integer constraints through state tracking and a difference-bound matrix theory solver.
      - `state.go` — Implements an abstract interpretation state for numeric constraints including intervals, modular residues, and difference constraints.
      - `theory.go` — Provides SMT-style theory solving for numeric constraints using difference graphs and modular arithmetic.
    - **flow/pathkey/** — This directory provides the core logic for constructing, parsing, and resolving canonical path keys used to identify symbol locations within flow analysis.
      - `format.go` — Provides utility functions for formatting canonical string representations of symbol roots and versioned symbol roots.
      - `key.go` — Provides utilities for constructing, parsing, and comparing canonical path keys and filtering constraints based on path relationships.
      - `resolver.go` — Generates canonical SSA-versioned path keys for flow analysis.
      - `root.go` — Parses a path key into its canonical root and remaining suffix.
      - `util.go` — Provides low-level parsing and validation utilities for Lua-style identifiers, integer literals, and safe float-to-int conversions.
    - **flow/propagate/** — This directory implements the dataflow analysis logic for propagating type constraints through a control flow graph.
      - `propagate.go` — Computes type constraints at CFG points using forward dataflow propagation and constraint killing.
  - **io/** — This directory manages the binary serialization and deserialization of complex type signatures, manifests, and behavioral specifications.
    - `manifest.go` — Defines the Manifest structure and FunctionSummary for capturing and serializing module type signatures and behavioral specifications for cross-module analysis.
    - `manifest_lookup.go` — Provides functions to resolve manifests and their enriched export types from a manifest querier using a specific fallback policy.
    - `predicates.go` — Provides binary encoding and decoding logic for type constraints and expressions using typeWriter and typeReader.
    - `reader.go` — Implements a binary deserializer for complex type definitions, contracts, and effect rows.
    - `serialize.go` — Provides binary serialization and deserialization for Lua type system constructs.
    - `writer.go` — Provides a binary serialization mechanism for encoding complex type definitions, contracts, and effects into an io.Writer.
  - **kind/** — This directory defines the core enumeration and classification logic for type kinds used throughout the engine's type discrimination system.
    - `kind.go` — Defines the enumeration and classification methods for type kinds used in type discrimination.
  - **narrow/** — This directory provides specialized logic for refining and narrowing Lua types through predicates, literal matching, and structural identity.
    - `doc.go` — Provides type narrowing operations for control flow refinement based on Lua's truthiness and type semantics.
    - `filter.go` — Provides functions and types for narrowing Lua types through predicates, field truthiness, and literal matching.
    - `key_narrow.go` — Provides functions to narrow or exclude types based on built-in Lua kinds or hash-based type identities.
    - `match.go` — Provides logic to determine if a specific field within a type can match a given literal value for discriminated union narrowing.
    - `narrow.go` — Provides logic for narrowing and transforming types based on Lua truthiness, kind checks, and set operations.
    - `resolver.go` — Defines an interface for context-free type queries regarding field and index access to decouple narrowing logic from the full type checker.
    - `type_key.go` — Defines a compact representation for identifying types via built-in names or structural hashes for use in narrowing operations.
  - **numparse/** — This directory provides utility functions for parsing string representations of integers and floating-point numbers.
    - `numparse.go` — Provides functions for parsing string representations of integers and floating-point numbers.
  - **query/** — This directory provides the core logic for performing deep type inspections, method resolution, and complex type structure navigation.
    - **query/core/** — This directory provides the core logic for performing complex, high-level queries and resolutions against the Lua type system.
      - `codegen.go` — Defines types and logic for determining code generation strategies regarding method dispatch, memory layout, and optimization eligibility.
      - `cycle.go` — Defines types and logic for classifying Lua types into garbage collection categories based on their potential to form reference cycles.
      - `discriminator.go` — Provides logic to identify and extract discriminator fields from union types for type narrowing.
      - `doc.go` — Provides foundational type inspection, query, and lookup operations for the Lua type system.
      - `engine.go` — Provides a memoized engine for performing complex type queries, operator resolutions, and subtype checks.
      - `field.go` — Provides pure, non-memoized recursive field resolution logic for various type structures.
      - `index.go` — Provides logic for resolving index lookups (t[key]) on various type containers without an engine.
      - `instantiate.go` — Handles the substitution and instantiation of generic type parameters into concrete types.
      - `method.go` — Provides logic for resolving method signatures, callability, and metamethods within a Lua type system.
      - `operator.go` — Resolves the resulting types of Lua unary and binary operators, including metamethod fallback and union type distribution.
      - `ops.go` — Defines the TypeOps interface for abstracting type resolution and transformation operations.
      - `reach.go` — Provides logic for computing transitive reachability and detecting potential cycles within record type structures.
      - `resolver.go` — Provides a functional adapter for field and index resolution to enable dependency injection and testing without a full engine instance.
      - `special_access.go` — Provides logic to determine canonical access types for special top-level types during queries.
      - `table_match.go` — Provides logic to identify which specific member of a union type a table literal matches based on discriminator fields.
      - `traverse.go` — Provides utility functions for recursively traversing and inspecting complex type structures like unions, intersections, and optionals.
      - `union.go` — Provides logic to select or merge compatible function signatures from a union type based on parameter count.
      - `util.go` — Provides utility functions for inspecting and querying properties of Lua types, such as fields, methods, iterability, and containment.
  - **subtype/** — This directory implements the core logic for structural subtype comparison, type normalization, and variance calculations.
    - `guard.go` — Provides utility functions to check if recursion depth limits have been reached during subtype comparison.
    - `normalize.go` — Provides logic for simplifying and normalizing union and intersection type structures.
    - `subtype.go` — Implements structural subtype checking with coinductive cycle detection and type widening rules.
    - `variance.go` — Defines the Variance type and logic for calculating variance transformations and compositions.
    - `widen.go` — Provides functions to convert specific types into more general forms through shallow or deep widening.
  - **typ/** — This directory defines the core type system, providing the fundamental structures, interfaces, and algebraic logic for representing and manipulating complex types.
    - `annotated.go` — Defines types and logic for wrapping base types with runtime validation annotations.
    - `capture.go` — Defines types and logic for representing how variables are captured within closures.
    - `container.go` — Defines container type structures for Arrays, Maps, and Tuples within the type system.
    - `dedup.go` — Provides a utility function to remove duplicate Type instances from a slice using hash-based bucketing and structural equality.
    - `deferred.go` — Defines types for representing deferred field and index access operations in generic type contexts.
    - `equals.go` — Implements structural equality comparison for types with coinductive cycle detection and string representation with depth limiting.
    - `field_shape.go` — Provides logic to separate nilability from a type to determine if a field is optional.
    - `format.go` — Provides bounded string rendering for type definitions to prevent excessive output in diagnostics.
    - `function.go` — Defines the structures and builder for representing function types, including parameters, return types, generics, effects, and contracts.
    - `function_arity.go` — Calculates the minimum number of positional arguments required to call a function based on its parameter definitions.
    - `generic.go` — Defines structures and methods for handling generic type parameters, generic definitions, instantiated types, and type variables for type inference.
    - `guard.go` — Provides factory functions and constants for managing recursion depth guards.
    - `info.go` — Defines marker interfaces for function metadata including effects, specifications, and type refinements.
    - `intersection.go` — Implements the Intersection type logic, including normalization, deduplication, and hashing.
    - `literal.go` — Defines the Literal type and factory functions for singleton types representing constant values.
    - `metamethod.go` — Defines Lua metamethod identifiers and provides utility functions for categorizing and mapping them to operators.
    - `nominal.go` — Defines the structures and methods for sum types (tagged unions) and structural interfaces.
    - `optional.go` — Defines the Optional type representing a nullable type union (T | nil).
    - `policy.go` — Implements type joining and merging logic for records, unions, and branch outcomes.
    - `predicates.go` — Provides predicate functions for inspecting and categorizing Type instances and slices of Types.
    - `rebuild.go` — Provides factory functions to construct and hash complex type structures like functions and records.
    - `record.go` — Defines the Record type and a builder for representing Lua table structures with named fields and optional map components.
    - `record_extend.go` — Provides functionality to extend an existing record type with a new or updated field.
    - `recursive.go` — Defines the Recursive type and provides mechanisms for constructing and hashing self-referential types with cycle detection.
    - `ref.go` — Defines specialized type implementations including unresolved references, type aliases, platform-specific types, and metatypes.
    - `rewrite.go` — Implements a bottom-up tree traversal mechanism to transform type structures using a provided callback function.
    - `soft.go` — Provides logic for detecting and pruning soft-placeholder types within the type system based on configurable policies.
    - `soft_flags.go` — Provides utility functions to determine if types or their components contain flags indicating they can be rewritten during soft pruning.
    - `string_cache.go` — Provides a thread-safe mechanism to lazily initialize and cache a single string value.
    - `type.go` — Defines the core Type interface and provides singleton implementations for primitive and special types.
    - `union.go` — Implements the Union type, providing logic for normalization, flattening, deduplication, and subsumption of member types.
    - `visit.go` — Implements the visitor pattern for traversing and dispatching operations on various type implementations.
    - **typ/join/** — This directory provides type join operations for merging control flow paths and function signatures.
      - `join.go` — Provides type join operations for merging control flow paths and function signatures.
    - **typ/subst/** — This directory provides type substitution and expansion operations for generic type instantiation and Self type resolution.
      - `subst.go` — Provides type substitution and expansion operations for generic type instantiation and Self type resolution.
    - **typ/union/** — This directory provides specialized logic for analyzing, extracting, and merging data from union types.
      - `union.go` — Provides operations for analyzing, extracting, and merging information from union types.
    - **typ/unwrap/** — This directory provides pure utility operations for unwrapping, extracting, and querying specific type structures.
      - `unwrap.go` — Provides pure operations for unwrapping, extracting, and querying specific type structures from a type system.

## Anomalies

- **[cycle]** constraint -> narrow -> subtype -> typ
- **[hub:fan-out]** flow/transfer.go  (6) — imports many modules
- **[hub:fan-out]** flow/solver.go  (6) — imports many modules
- **[hub:fan-out]** constraint/solver.go  (5) — imports many modules
- **[hub:fan-out]** io/writer.go  (5) — imports many modules
- **[hub:fan-out]** flow/product_domain.go  (5) — imports many modules
- **[hub:fan-out]** query/core/engine.go  (5) — imports many modules
- **[hub:fan-out]** flow/query.go  (5) — imports many modules
- **[hub:fan-in]** kind/kind.go  (46) — imported by many
- **[dir]** constraint: The package contains duplicated logic in intern.go and boilerplate type-switch patterns in visit.go, alongside a potential precision loss issue in atom.go.
- **[dir]** db: The Interner implementation has a potential loss of deduplication due to a hard limit on bucket size.
- **[dir]** diag: collector.go contains a structural smell where contextual help generation relies on fragile string matching on format strings.
- **[dir]** effect: `codecs.go` contains multiple empty stubs, and `visit.go` exhibits duplicated type-switch boilerplate logic.
- **[dir]** flow/numeric: theory.go contains a stubbed method (AddModular) that performs no action.
- **[dir]** typ: rebuild.go contains risky error handling that may panic on nil returns.
- `constraint/atom.go`: NumericConstraintToAtom contains a simplification for Le constraints that may lose precision or correctness if c != 0.
- `constraint/intern.go`: duplicated logic
- `constraint/visit.go`: Duplicated logic in type switch boilerplate for handling pointer vs. value receivers.
- `db/db.go`: Interner limits bucket size to maxBucketSize (8) which may cause loss of deduplication for high-collision hashes.
- `diag/collector.go`: ContextualHelp uses fragile string matching on format strings to determine error explanations.
- `effect/codecs.go`: Multiple stubs with empty Encode/Decode implementations (throwCodec, ioCodec, divergeCodec, borrowAllCodec, moduleLoadCodec, variadicTransformCodec, typePredicateCodec, typeValueMethodCodec, callableTypeCodec).
- `effect/visit.go`: Duplicated logic in type switch boilerplate for handling pointer vs. value receivers.
- `flow/numeric/theory.go`: AddModular is a stub that does nothing.
- `typ/rebuild.go`: risky error handling (panic on nil return entry)

## Coupling

Resolved 168 file→file edges across 77 directory→directory edges.

- Module cycles: 1
- Top external deps: github.com/wippyai/go-lua/internal (31), strings (18), fmt (16), sync (13), strconv (10), math (7), slices (4), sync/atomic (4), bytes (3), errors (3), encoding/binary (1), github.com/wippyai/go-lua/compiler/ast (1)
