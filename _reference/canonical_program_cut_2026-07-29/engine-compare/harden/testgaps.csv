path,query_id,response,completion_tokens,latency_ms,error
analysis/check/body/module_type_alias.go,q0,NONE,2,836,
analysis/check/body/expression_operation.go,q0,NONE,2,961,
analysis/check/body/object_literal_sourcevalue.go,q0,NONE,2,1047,
analysis/check/body/api.go,q0,NONE,2,1498,
analysis/check/body/result_accessors.go,q0,NONE,2,1567,
analysis/check/body/signature_return_type_ops.go,q0,NONE,2,121,
analysis/check/body/visibility.go,q0,NONE,2,95,
analysis/check/body/return_type_values.go,q0,"21: `resolver.Type(expr)` + A type expression that triggers infinite recursion in the resolver (e.g., a recursive table definition or a self-referential type alias) that might cause a stack overflow.

23: `product.Top()` + A malformed or unresolvable type expression (e.g., a syntax error in a type annotation or a reference to a non-existent global type) that forces the analyzer to fallback to `Top`.

31: `types[0].(*ast.TupleTypeExpr)` + A single-element slice containing a `TupleTypeExpr` that is empty or contains nil elements, testing the flattening logic's robustness.

32: `tuple.Elements...` + A deeply nested tuple structure (e.g., a tuple within a tuple within a tuple) that tests whether the flattening is shallow or if it fails to unpack nested structures correctly.",191,3056,
analysis/check/body/result_parameter_readmodel.go,q0,"16: Nil/Empty Symbol Handling: A `slot.Symbol` that resolves to an empty string via `statekey.SymbolValue` tests if the loop correctly skips ""ghost"" parameters without crashing or returning invalid empty slots.

35: Nil Graph/Empty RPO: A `Result` that has valid bindings but a `nil` graph (or an empty RPO traversal) tests the boundary where a function has parameters but no control-flow graph available for analysis.

40: Non-Ordinary Assignment Kinds: An assignment that exists but has a `Kind` other than `RootAssignmentOrdinaryRootWrite` (e.g., a global write or a non-root assignment) tests if the re-assignment logic correctly ignores non-local or non-standard side effects.

43: Target Symbol Collision: An assignment where `assignment.TargetSymbol()` returns a symbol that is not present in the `params` map tests the intersection logic between the function's formal parameters and the actual writes occurring in the graph.",213,4135,
analysis/check/body/config.go,q0,"41: mergeExpressionValues + Nil/Empty handling: If both `base` and `override` are non-nil but empty, it returns `nil`, which might cause a panic in a caller expecting a non-nil map for subsequent writes.

49: exprRefSet + Nil/Empty handling: If `paths` is an empty map (not nil), it returns `nil`, potentially breaking callers that rely on the distinction between an empty set and a null set.

58: configGlobals + Malformed Input: If `signaturelookup.StdlibSignatures()` returns a key that is just a dot `"".""` or starts with a dot, `root` becomes an empty string, which is then appended to `globals` via the `root != """"` check, but the logic for handling complex nested stdlib paths might be shallow.

58: configGlobals + Rare Type Combinations: If `IncludeStdlib` is true but `StdlibSignatures()` returns an empty map, the function returns only the user-defined globals, which might be an unexpected state for a ""stdlib-enabled"" config.",231,4363,
analysis/check/diagnostics/assignment_call_owner.go,q0,NONE,2,726,
analysis/check/body/generic_for_transfer.go,q0,"44: nil/empty handling + If `sem`, `sources`, or `signatureID` are nil, the transfer function returns the input state without error, potentially masking missing analysis data.
48: malformed input + If `fact.VariableIndex` is out of bounds of `fact.Symbols`, the function silently returns the input state.
65: malformed input + If `sourceIndex` is negative or exceeds the number of arguments provided at the call site, the function returns an empty value.
101: huge unions/intersections + If a literal container has many entries, `product.Join` is called repeatedly, which could lead to exponential complexity or memory exhaustion in a large union.
123: rare type combinations + If `object.StaticMembers()` contains keys that are not simple segments (e.g., complex table keys), they are silently skipped.
134: malformed input + If an object entry has multiple segments (e.g., `t[""a""][""b""]`), it is ignored by `genericForDirectContainerElement`.
163: recursion/depth limits + If `ast.NonNilAssertExpr` is nested deeply (e.g., `assert(assert(assert(x)))`), this recursive call could cause a stack overflow.
165: nil/empty handling + If a `CastExpr` resolves to an `Any` or `Unknown` type, the assertion is treated as invalid/false.",309,4960,
analysis/check/diagnostics/assignment_diagnostic.go,q0,NONE,2,639,
analysis/check/body/function_value_types.go,q0,"51: nil/empty handling: `r.ExpressionPath(expr)` returning an empty path key that exists in the map but represents a non-existent or ""bottom"" location.
65: nil/empty handling: `r.funcTypes.ByPath[p.Key()]` returning a `nil` pointer for a key that exists in the map, potentially causing downstream panics if not checked.
73: malformed input: `product.Get` returning an identity key for a value that is actually `product.Bottom`, leading to invalid identity lookups.
78: rare type combinations: `functionContextEntryHolds` returning `true` when `current` state is partially uninitialized or contains conflicting refinements for the same `id`.
86: overflow/logic error: `product.LessOrEq` returning true for a `want` value that is actually a subtype of a different branch in a massive union.
93: rare type combinations: `valueHasIdentity` returning true for a `product.Bottom` value if the registry's identity mapping is corrupted or improperly initialized.
97: metatable edge cases: `entry.PathStaticMembersSnapshot()` returning a set of members that are technically present but belong to a metatable rather than the object itself.
103: recursion/depth limits: `current.HeapTableObjectsSnapshot()` returning a deeply nested or cyclic heap structure that causes `heapidentity.MapDomain(...).LessOrEq` to hang or stack overflow.
104:",320,5069,
analysis/check/body/read_boundary_state.go,q0,"23: Nil Receiver + Nil Registry/Graph/Xfer: Calling `nodeOutputAt` on a non-nil `Result` where `r.registry` or `r.boundaryXfer` is nil, testing if the function gracefully returns an empty state instead of panicking.
38: Nil Receiver: Calling `nodeOutputAt` on a nil `Result` pointer.
44: Nil Receiver: Calling `nodeOutputAt` on a nil `Result` pointer.
45: Map Initialization Race/Nil: Accessing `r.boundary[point]` when `r.boundary` is nil (though handled by the `if r == nil` check, the logic assumes `r` is valid but `r.boundary` might not be initialized).
53: Recursive Boundary Dependency: A `boundaryXfer` function that calls `nodeOutputAt` for the same `point`, potentially causing infinite recursion/stack overflow.
58: Lazy Map Initialization: The first time a boundary is computed, `r.boundary` is initialized; testing if this side-effect is safe during concurrent reads or if it causes issues in a read-only context.
74: Nil `callOutcome`: Calling `hasNodeLocalBoundaryEffects` when `r.callOutcome` is nil, testing the short-circuiting logic.
85: Empty Postcondition Collections: Testing if `len()` checks on `PostconditionRefinements` or `PostconditionPathRelations` correctly handle nil or empty slices without error.
91",320,5067,
analysis/check/body/import_outcomes.go,q0,"42: nil/empty handling + A `site.CalleePath()` that returns an empty path object but is not `nil` could bypass the first check and hit `readexpr.Project` with an empty path.
51: malformed input + A `p.Segments` slice that is non-empty but contains nil or invalid path segments could cause a panic in `readexpr.Project`.
58: recursion/depth limits + A `p.Segments` slice that is extremely deep could cause a stack overflow during the `root.Segments = nil` logic or subsequent projection.
74: recursion/depth limits + A `p.Parent()` loop that encounters a cyclic path structure (where a parent is itself) would cause an infinite loop.
78: rare type combinations + A `prefixType` that is a complex union/intersection where `typ.IsAny` is false but the type is logically ""top"" could lead to incorrect projection.
81: malformed input + `luatypeprojection.ApplySegments` receiving a `suffix` that contains indices or keys incompatible with the `prefixType` structure.
113: nil/empty handling + A `site.ReceiverPath()` that returns `ok=true` but an empty path could lead to an invalid `readexpr.Project` call.
135: nil/empty handling + A `sources` object that is non-nil but contains a `source` that resolves to a `nil` value via `ValueOfSource`.
143",320,5069,
analysis/check/body/result_return_readmodel.go,q0,"22: nil/empty handling + Calling `ReturnPoints` on a `Result` where `r.Graph()` returns `nil` (e.g., an uninitialized or partially constructed analysis).
34: nil/empty handling + Calling `ReturnValueSources` on a `nil` receiver, which is a common edge case in pointer-heavy Go code.
39: nil/empty handling + Calling `ReturnValueSources` on a valid `Result` but requesting a `point` that has no associated facts in the map.
46: nil/empty handling + Calling `ReturnPresenceRelations` on a `nil` receiver.
51: nil/empty handling + A `point` that exists in the graph but has no return facts associated with it.
53: nil/empty handling + `openTailReturnPresenceRelations` returning a non-nil slice that is immediately appended to a nil `relations` slice.
57: nil/empty handling + Calling `openTailReturnPresenceRelations` on a `Result` where `r.callOutcome` is `nil` (e.g., a non-function return).
60: nil/empty handling + A `point` that is valid but has no return facts, causing `ret.Sources()` to potentially panic if `ret` is nil (though `ok` check handles it).
63: malformed input + `openTailReturnCallSources` receiving a `sources` slice where the first element is not a `ValueSourceCall`.
67:",320,4275,
analysis/check/checktest/checktest.go,q0,"51 + nil/empty handling + Passing a `nil` manifest to `WithManifest` should be safely ignored without panicking.
63 + nil/empty handling + Passing a `nil` module result to `WithModule` should be safely ignored without panicking.
71 + nil/empty handling + Passing a `nil` diagnostic policy to `WithDiagnosticPolicy` might cause a panic in `cloneDiagnosticPolicy` if not handled.
77 + nil/empty handling + Passing a `nil` rule to `WithDiagnosticRule` could lead to a nil pointer dereference during diagnostic application.
103 + malformed input + Providing a `nil` result from `checkSource` (if it were possible) would cause a panic when accessing `result.checked`.
158 + nil/empty handling + Passing a slice of `nil` options to `Check` could cause a panic in the `applyOptions` loop.
175 + nil/empty handling + If `policy.Rules` is nil, `len(policy.Rules)` is 0, but the logic must ensure it doesn't attempt to iterate over a nil map.
214 + malformed input + If `c.manifests` or `c.modules` are nil, the `range` loops and `len()` calls must not panic.",283,3538,
analysis/check/body/entry_contextual_function.go,q0,"28: nil/empty handling + A parent function exists but has an empty `ReturnTypes` slice, causing an out-of-bounds or immediate failure.
31: malformed input + A return statement exists but the expression at the target index is `nil` instead of a valid type expression.
35: rare type combinations + The declared return type is a complex type (like a table or union) that is not a `*typ.Function`, testing the type assertion failure.
46: recursion/depth limits + A deeply nested `if-else` or `do` block structure that could cause a stack overflow during the recursive `returnPositionIndex` calls.
48: cyclic/self-referential + A function literal that is returned within its own definition (if the AST allows such a cycle), testing the search logic.
63: nil/empty handling + A function signature with a `nil` parameter type at a specific index, testing the robustness of the parameter resolution.
64: rare type combinations + A parameter type that is `any`, `unknown`, or `never`, which should trigger a fallback to gradual-top rather than returning the type.
75: rare type combinations + A method receiver that resolves to `any`, `unknown`, or `never`, testing the logic that prevents unsound projection.
76: malformed input + A method receiver that exists in the bindings but fails to resolve in the `resolver`, testing the error path for unresolved symbols.",312,5633,
analysis/check/body/internal/readexpr/readexpr.go,q0,"45 + recursion/depth limits + A path `p` with extremely deep nesting could cause a stack overflow due to the recursive calls to `Project(config, point, p.Parent(), in)`.

68 + nil/empty handling + If `p.Segments` is empty but `p.IsEmpty()` is false, `p.Segments[len(p.Segments)-1]` will panic with an index out of bounds.

101 + recursion/depth limits + The recursive call to `Project` inside the `hasExactPresent` block can trigger a stack overflow on deeply nested paths.

125 + recursion/depth limits + The recursive call to `Project` inside the `!hasExactPresent` block can trigger a stack overflow on deeply nested paths.

143 + recursion/depth limits + The recursive call to `Project` inside the `projectFromHeapIdentity` block can trigger a stack overflow on deeply nested paths.

163 + recursion/depth limits + The recursive call to `Project` inside the `projectFromStructuralEvidence` block can trigger a stack overflow on deeply nested paths.

198 + malformed input + If `suffix` contains segments that are valid but logically impossible for the `parentType`, `ApplySegments` might return unexpected results or hit internal limits.

213 + rare type combinations + If `parentType` is a complex union/intersection, `ApplySegments` might encounter extreme complexity or non-terminating type resolution.",311,5676,
analysis/check/body/call_identity.go,q0,"53: nil/empty handling: `r.graph.RPO()` returns an empty slice or nil, causing the loop to skip without error.
58: nil/empty handling: `facts.CallSite(point)` returns `ok=false` for a point that exists in the RPO but has no associated call fact.
62: nil/empty handling: `site.Expr()` returns `ok=false`, representing a call site where the expression cannot be resolved to a reference.
66: nil/empty handling: `r.callPoints` is nil on first valid call site, triggering lazy initialization.
75: nil/empty handling: `r` is nil, returning an empty identity instead of panicking.
84: malformed input: `r.imports.SignatureName` is called with a `ctx.Point` that might not exist in the graph, testing the robustness of the projection lookup.
93: nil/empty handling: `r.graph` is nil, causing `ctx.Node` to be assigned a nil value from `r.graph.Node(point)`.
101: nil/empty handling: `r.callPoints` is empty or `site.Expr()` is invalid, returning a zero-value `cfg.Point`.
111: malformed input: `calleePath.Symbol` is 0 (null symbol), which is a valid ID in some systems but should be treated as an invalid root.
117: rare type",320,6369,
analysis/check/body/entry_state.go,q0,"54: Nil/Empty handling + `bindings.ParamSlots(fn)` returning a slice containing a slot with a `Symbol` of `0` or an empty `valueSlot` string, potentially causing downstream key lookups to fail or return incorrect results.

63: Rare type combinations + `slot.Type == nil` combined with `slot.Name == ""self""` where `methodReceiverType` returns a type that is actually `product.Bottom`, testing if the analysis correctly handles ""self"" being uninitialized or invalid.

74: Malformed input + `hasExpectedSig` is true but `slot.SourceIndex` points to an out-of-bounds or invalid index in the `expectedSig` structure, testing the robustness of `contextualParamType`.

83: Nil/Empty handling + `resolver.Type(slot.Type)` returning `ok=false` for a non-nil type, which silently skips the seed; this tests if the analysis fails to report a type error when a parameter has an unresolvable type.

103: Malformed input + `m.Path` or `m.Export` being partially initialized (e.g., `m.Export` is a nil pointer), testing if the loop continues safely or panics.

105: Rare type combinations + `bindings.IsImplicitGlobalSymbol(id)` returning true for a symbol that is actually a critical module export, testing if the distinction between implicit and explicit globals is strictly enforced.

118: Overflow/Complexity",320,6371,
analysis/check/body/read_boundary.go,q0,"44: nil/empty handling + `r.boundarySources()` returning nil could cause a panic if `ValueOfSource` is called on a nil receiver (though here it checks `sources == nil` first, the `r.boundaryRead` field is passed blindly).
44: equality edge case + `product.Equal` with `product.Bottom` might return true for unexpected types if the registry is in an inconsistent state.
63: type combination + `refinement.ContainsFreeTypeParam(t)` might return true for complex generic types that are actually concrete in a specific context, causing false negatives in recovery.
64: type combination + `ev.IsExplicitTop()` or `IsGradualTop()` might fail to distinguish between a ""true"" top and a ""locally unknown"" type in highly dynamic Lua code.
105: malformed input + `p.IsEmpty()` check is present, but a path with a single invalid/malformed segment might bypass this and cause `readexpr.Project` to panic.
118: overflow + `in.ReadLenFloor(pathKey)` returns `int64`; if the Lua array is theoretically larger than `math.maxint64` (unlikely but possible in some IRs), it could overflow.
131: malformed input + `indexPath.IsEmpty()` or `arrayPath.IsEmpty()` handles empty paths, but a path that is ""self-referential"" (e.g., `x[x]`) might cause `visibility",320,6414,
analysis/check/diagnostics/call_param_obligation.go,q0,NONE,2,1499,
analysis/check/body/run.go,q0,"148 + nil `built` or `built.Graph` + A CFG builder returning a nil graph (e.g., due to an internal error or empty file) would trigger `ErrUnsupportedCFG`.
188 + nil `config.ExpressionValue` + A configuration where `ExpressionValue` is nil, forcing the engine to fallback to a default `readexpr.Provider`.
204 + nil `config.ExpressionValues` + A configuration where `ExpressionValues` is nil, potentially causing issues if `mergeExpressionValues` doesn't handle nil inputs.
238 + nil `config.TypeValues` + A configuration where `TypeValues` is nil, forcing the solver to initialize a new `typevalue.NewCache()`.
243 + nil `s.cfg.Graph` + A `Static` object with a nil graph, which would cause a panic in `parameterEntryState`.
255 + nil `s.semantics.Function()` + A `Static` object where the function semantics are missing, causing a panic in `parameterEntryState`.
278 + nil `config.SignatureArgumentTypeFactory` + A configuration where the factory is nil, requiring the logic to fall back to the base `signatureArgumentType`.
281 + nil `factoryArgumentType` + A factory that returns `nil, false`, testing the fallback mechanism to the `baseArgumentType`.
301 + nil `s.signatures` + A `Static` object with no signatures, testing",320,5953,
analysis/check/checktest/internal/precheck/precheck.go,q0,"48: nil function expression + A `FunctionExpr` with a `nil` body or `nil` statements could cause a panic in `scanStmts`.
74: nil table field + A `TableExpr` containing a `nil` element in the `Fields` slice (e.g., `{[1] = nil}`) could cause a panic during field iteration.
103: nil function receiver + A `FuncCallExpr` where the `Receiver` is `nil` might be valid in some ASTs but could trigger unexpected behavior in recursive scanning.
134: nil function name + A `FuncDefStmt` where `stmt.Name` is non-nil but `stmt.Name.Func` is `nil` would cause a panic in `scanExpr`.
158: nil expression in table key + A `TableExpr` where a field's `Key` is `nil` (if `KeySyntax` is not `Dot`) would cause a panic in `scanExpr`.
188: nil expression in logical op + A `LogicalOpExpr` with a `nil` `Lhs` or `Rhs` would cause a panic in `scanExpr`.
214: nil expression in unary op + A `UnaryMinusOpExpr` (or similar) with a `nil` `Expr` would cause a panic in `scanExpr`.
233: nil expression in cast + A `CastExpr` with a `nil` `Expr` would cause a panic in `",320,5413,
analysis/check/diagnostics/diagnostics.go,q0,"78: recursion/depth limits + infinite function nesting: A result containing a circular chain of function results (e.g., `fnA` contains `fnB` which contains `fnA`) would cause a stack overflow in the recursive `produceWithResolver` calls.",57,2335,
analysis/check/diagnostics/return_contract.go,q0,NONE,2,806,
analysis/check/diagnostics/evidence.go,q0,"21: nil/empty handling: `result`, `result.Registry()`, or `read` are nil, which tests the early exit guard against uninitialized analysis states.
24: malformed input/missing value: `read(result, point)` returns `ok=false`, testing how the system handles a requested point that exists in the CFG but has no associated value in the read model.
30: rare type combinations (Top/Any): `proof.IsExplicitTop()` handles the case where a value is explicitly typed as `any` or `unknown`, preventing the system from attempting to find structural proofs for types that lack them.
38: malformed input/proof mismatch: `ValueProofAdmissible` returns `false`, testing the edge case where a value exists but its type-level proof is insufficient or incompatible with the expected `want` type.",183,3865,
analysis/check/diagnostics/dead_assignment.go,q0,"82 + recursion/depth limits + A deeply nested function structure with many captures could cause a stack overflow in the recursive `walk` function.
118 + nil/empty handling + If `result.FunctionResults()` returns a slice containing `nil` elements, `child.Function()` will panic.
218 + recursion/depth limits + A deeply nested tree of `ast.TableExpr` or `ast.FuncCallExpr` could cause a stack overflow in the recursive `expr` function.
268 + recursion/depth limits + A cyclic CFG (Control Flow Graph) would cause an infinite loop/stack overflow in `firstOverwritesBeforeRead` if the `visiting` map logic fails to catch a specific cycle type.
278 + recursion/depth limits + A CFG with a cycle that does not contain a read or a write of the target symbol could cause the `walk` function to exhaust the stack.
314 + nil/empty handling + If `stmt.NamePositions` is shorter than `index`, `localNameSpan` will panic with an out-of-bounds error.",237,4752,
analysis/check/diagnostics/condition_reachability.go,q0,"33: nil/empty handling + `result.Graph()` returning `nil` is handled, but if `result` itself is `nil`, the function will panic.
43: rare type combinations + if `graph.RPO()` returns an empty slice or a slice containing `nil` points, the loop behavior depends on the graph implementation's stability.
101: malformed input + if `check.Path` is a malformed or deeply nested path that causes `check.Path.String()` to panic or overflow during string concatenation.
138: rare type combinations + if `env.constraints` contains a constraint where `c.target` is a self-referential or cyclic path structure, `c.target.Equal(check.Path)` might enter infinite recursion.
158: rare type combinations + if `env.typeChecks` contains a type name that is an empty string or a special Lua keyword, the equality check might produce misleading diagnostics.
178: malformed input + if `fact.Condition` or `fact.Stmt` are malformed AST nodes with invalid/zeroed spans, `ast.SpanOf` might return an invalid span, leading to zero-valued diagnostic positions.",259,5077,
analysis/check/diagnostics/assignment.go,q0,"34: nil/empty handling + `result == nil` or `graph == nil` returns early, but if `graph.RPO()` returns an empty slice, the loop is skipped without error.
43: nil/empty handling + `fact.Type == nil` or `fact.Expr == nil` returns early, preventing panics on incomplete assignment facts.
148: recursion/depth limits + `memberRead(...).walk` uses a map for visited nodes; if the AST has a cycle (malformed input), it could cause a stack overflow or infinite loop.
168: malformed input + `directCallResultAssignmentWouldReport` checks `source.HasCallPoint`; if a `SourceCall` kind is provided without a valid `CallPoint`, it returns false.
188: rare type combinations + `directCallContractWouldReport` checks `site.CalleeSymbol() == 0`; if a call site exists but the symbol is unresolved/zero, it avoids processing.
216: rare type combinations + `callResultWitnessProvenMismatchType` returns `nil, false` if `want` is nil, preventing downstream type comparison panics.
253: huge unions/intersections + `objectLiteralAdmissibleToAnyArm` (called via `closedRecordUnionArms`) must handle extremely large union types without exponential complexity.
258: huge unions/intersections + `objectLiteralEntryMismatchType` must handle deep/nested record structures within a union arm.
2",320,10589,
analysis/check/diagnostics/assignment_projection.go,q0,"148: recursion/depth limits + A deeply nested `typ.Union` or `typ.Intersection` containing many `typ.Optional` wrappers that hits `typ.DefaultRecursionDepth`.
168: recursion/depth limits + A `typ.Union` where every member is a `typ.Optional` that triggers a stack overflow or depth limit during `dynamicIndexWriteValueType`.
183: recursion/depth limits + A `typ.Intersection` where every member is a `typ.Optional` that triggers a stack overflow or depth limit during `dynamicIndexWriteValueType`.
204: recursion/depth limits + A `typ.Record` with a very large number of fields where `closedRecordDynamicWriteValueType` performs a massive intersection.
233: recursion/depth limits + A `typ.Union` containing many members that all require recursive calls to `hasExplicitNilToNilableMember`.
263: recursion/depth limits + A `typ.Alias` or `typ.Recursive` type that points to itself, testing the `tt.Inner == t` or `tt.Body == t` safety checks.
278: recursion/depth limits + A `typ.Annotated` type that points to itself, testing the `tt.Inner == t` safety check.
279: recursion/depth limits + A `typ.Alias` that points to itself, testing the `next == t` safety check.
280: recursion/depth limits +",320,10361,
analysis/check/diagnostics/channel_select_exhaustiveness.go,q0,"48: nil/empty handling + If `result.Graph()` returns nil, the function returns nil immediately, but if `result.Call(point)` returns a nil `call` object, it could panic if not for the `!ok` check.
68: malformed input + If `selectFact.ResultTarget.Path` is an empty path or `selectFact.Cases` is an empty slice, the diagnostic info is skipped, potentially masking a malformed AST.
101: recursion/depth limits + `nestedElseIfStatements` only checks one level deep (`Else[0]`), so a deeply nested `if-elseif-elseif` structure might fail to identify nested branches correctly.
113: recursion/depth limits + `ifElseIfChain` uses a linear loop to traverse `stmt.Else[0]`, which is safe from stack overflow but assumes a perfectly linear chain; it doesn't handle non-linear branching logic.
131: rare type combinations + If `branch.Check.Kind` is not `branchcond.CheckPathEqual`, the entire chain is ignored, even if other types of checks (like type-based checks) might imply exhaustiveness.
148: malformed input + If `info.hasDefault` is true, the function returns early; an adversarial case would be a `select` with a `default` that is logically unreachable or incorrectly flagged.
168: recursion/depth limits + `ifElseIfChain` could enter an infinite loop if",320,10263,
analysis/check/diagnostics/direct_call.go,q0,"28: `result == nil` or `graph == nil` — Returns `nil` immediately, but if the caller expects a non-nil slice, this could cause a panic in the consumer.
38: `site.CalleeSymbol() == 0` — Handles calls to an undefined or zero-valued symbol, which might occur in malformed ASTs or broken symbol tables.
41: `callMemberAccess(fact)` — Checks if the call is a member access (e.g., `obj:method()`); an edge case is a call where the ""member"" is actually a nil-valued field.
118: `typ.IsAny(calleeType) || typ.IsUnknown(calleeType) || typ.IsNever(calleeType)` — Handles ""bottom"" types (`never`) or ""top"" types (`any`) which can cause infinite loops in type checkers if not short-circuited.
148: `result.ExpressionValueAtBoundary(point, callee)` — Boundary condition where a callee is an expression that evaluates to a type, but the flow analysis has no data for that specific CFG point.
178: `projectionHasNil(calleeType)` — Handles complex union types where one branch is a table and another is `nil`, testing the robustness of the nil-presence logic.
214: `len(args) < required` — Boundary condition for functions with many optional parameters where the number of arguments is exactly `required -",320,10278,
analysis/check/exportmanifest/effect_boundary.go,q0,NONE,2,245,
analysis/check/exportmanifest/object_type.go,q0,NONE,2,1711,
analysis/check/exportmanifest/product_type.go,q0,NONE,2,2156,
analysis/check/diagnostics/numeric_for.go,q0,"24 + nil result + A `result` pointer that is non-nil but contains a nil `Graph()` would cause a panic if not for the check on line 28.
36 + non-numeric step + A `numeric-for` loop where the `step` is a complex union type that contains a number but is not *exclusively* a number.
83 + recursion depth + A deeply nested or cyclic type (e.g., `type T = T`) that exceeds `typ.DefaultRecursionDepth` to test the safety fallback.
84 + never type + A type that is explicitly `Never` (bottom type) to ensure it doesn't trigger a false positive ""may contain number"" via the depth limit.
94 + union member exhaustion + A massive `Union` type where the first 10,000 members are non-numbers and only the last one is a `Number`.
98 + instantiated expansion + An `Instantiated` type that expands to itself (infinite expansion) to test the `expanded == t` cycle detection.
101 + kind mismatch + A type with a `Kind` that is not explicitly handled in the switch (e.g., a custom or future kind) to ensure it defaults to `true` (safe).",274,13295,
analysis/check/diagnostics/expression_type.go,q0,"51: recursion/depth limits + A deeply nested `CastExpr` or `NonNilAssertExpr` chain that exceeds `typ.DefaultRecursionDepth` to trigger the depth guard.
54: nil/empty handling + An `ast.Expr` that is explicitly `nil` passed to `typeOfDepth` to ensure it returns `nil, false` rather than panicking.
74: recursion/depth limits + A self-referential or infinitely nested `CastExpr` (e.g., `cast(cast(cast(...)))`) to test the recursion depth protection.
101: malformed input + An `ast.IdentExpr` where `ExpressionPath` returns a valid path but the `Symbol` is `0` (uninitialized/invalid symbol).
115: rare type combinations + A `SymbolTypeAnnotation` that exists but fails `lowerType`, testing the error propagation for invalid type definitions.
135: malformed input + A `broadType` call where `ExpressionPath` is valid but `SymbolValueAtBoundary` returns `ok=false`, testing the fallback logic.
154: malformed input + A `flowOriginType` call where `p.result` is `nil`, testing the safety check against nil receivers.
178: rare type combinations + An `expressionSegmentType` call with a `SegmentField` where the field name is empty or the type is a primitive that doesn't support field access.
184: rare type combinations",320,14254,
analysis/check/diagnostics/guard_env.go,q0,"88 + recursion/fixpoint + A cyclic CFG with infinite state growth could cause the `changed` loop to never terminate if `joinGuardEnvs` doesn't strictly reduce the state.
104 + nil/empty handling + A `graph` with no points (empty RPO) returns an empty map, but a `nil` graph returns `nil`, potentially causing caller panics.
134 + nil/empty handling + `joinPredecessorGuardEnvs` returns an empty `guardEnv{}` for nodes with no predecessors, which might be semantically different from a ""starting"" environment.
158 + nil/empty handling + `applyGuardEdge` returns the input `env` if `result` is `nil`, which might mask errors in the analysis engine.
178 + nil/empty handling + `applyGuardNode` returns the input `env` if `result` is `nil`, potentially skipping critical invalidation logic.
198 + malformed input + `directDynamicIndexAssignment` assumes `fact.Target` is an `*ast.AttrGetExpr`; if the AST is malformed/unexpected, it returns `false` silently.
213 + malformed input + `applyCallGuardInvalidation` returns an empty `guardEnv{}` if `hasSite` or `hasOutcome` is false, which effectively marks the path as ""not unreachable"" but with zero knowledge.
243 + recursion/depth limits + `callGuardArgumentBindings` and",320,14737,
analysis/check/diagnostics/object_literal_mismatch.go,q0,"41: closedRecordUnionArms + Single-arm Union: A union containing only one member (e.g., `type T = {a: int}`) might bypass the ""closed record"" logic and return `false`, potentially causing valid single-member unions to be treated as open/generic.

58: objectLiteralAdmissibleToRecord + Missing Required Field: A literal that contains all the *correct* types for the fields it has, but is missing a field marked as required by the record, tests the boundary between ""type match"" and ""structural completeness.""

78: objectLiteralMemberMismatch + Nil/Empty Inputs: Passing `nil` for `result`, `expr`, or `want` tests the robustness of the error reporting path to prevent panics during diagnostic generation.

88: objectLiteralMemberMismatch + Non-Admissible Union: A literal that matches *none* of the arms in a closed union, testing the fallback logic that must decide whether to report a specific member mismatch or a general shape mismatch.

104: objectLiteralMemberMismatchInFact + Partial Segment Match: A literal with entries that have deep/complex segment paths that do not exist in the `want` record, testing if the loop correctly skips non-matching paths without erroring.

116: objectLiteralEntryMismatchType + Unknown/Any Expected Type: If the `expected` type is `Any` or `Unknown`, the function must return `false` (no mismatch) to avoid false-positive errors on unconstrained types.

11",320,16252,
analysis/check/diagnostics/resolver.go,q0,"148 + nil/empty handling + A `result` with a non-nil `Graph()` but where `result.TypeDefinition(point)` returns `ok=false` for all points.
156 + malformed input + An alias definition where `fact.Type` is non-nil but `fact.Type.Type` is `nil`, potentially causing a panic in downstream consumers.
171 + malformed input + An interface definition where `fact.Interface.Name` is an empty string.
193 + recursion/depth limits + A `path` slice containing a single element that refers to a type which triggers an infinite loop of `resolveInParentScope` calls.
197 + recursion/depth limits + A `path` with length > 1 where `RequireAliasModulePath` returns a path that, when combined with `path[1:]`, creates a cycle in module resolution.
218 + recursion/depth limits + A `TypeRefResolved` call where `ref.Path` is empty, though the code checks `len == 0`, it relies on `ResolveTypeRef` to handle the logic.
234 + malformed input + A `PrimitiveTypeExpr` with a name that is not a builtin but is not found in the result or parent scope.
254 + recursion/depth limits + `hasKnownTypeName` calling itself via `parent.hasKnownTypeName` in a deep or cyclic scope hierarchy.
266 + malformed input + `decl",320,16197,
analysis/check/diagnostics/member_call.go,q0,"116: `colonMemberCallConsumesReceiver` + `nil` params: A function type with a `nil` parameter list or a malformed `contract.params` slice could cause a panic.
123: `colonMemberCallConsumesReceiver` + `self` mismatch: A function where the first parameter is named `""self""` but is not actually a subtype of the receiver, testing the logic's reliance on name-based heuristics.
133: `memberContractWithoutReceiver` + `source` nil: If `contract.source` is non-nil but `contract.source.Params` is empty, `contract.source.Params[1:]` will panic.
143: `receiverType` + `newFlowExpressionTyper` recursion: A deeply nested expression (e.g., `a[b[c[d...]]]:method()`) could trigger a stack overflow in the expression typer.
158: `callMemberAccessInfo` + empty `CalleePath`: A call where `fact.CalleePath` exists but has zero segments could lead to an out-of-bounds access if not for the length check.
178: `suffixFromReceiver` + cyclic paths: If `receiver` and `target` paths are constructed via cyclic references in the symbol table, the loop could run indefinitely.
198: `applyLiteralNarrowing` + massive `env.constraints`: A huge number of literal constraints (e.g., from a massive switch",320,17287,
analysis/check/fixpoint/program/internal/projectsummary/project_heap_table_objects.go,q0,NONE,2,503,
analysis/check/diagnostics/internal/readmodel/readmodel.go,q0,"28: nil/empty handling + `r.result == nil` check prevents panics when the reader is initialized with a nil result.
48: malformed input + `source.Expr == nil` handles cases where the AST source is partially constructed or corrupted.
54: rare type combinations + `proofTransparent` check handles cases where a type witness is hidden behind a non-transparent function expression.
63: malformed input + `sourcebridge.ValueSourceFromASTSource(source)` handles cases where the AST source cannot be mapped to a valid value source.
76: nil/empty handling + `r.result.Registry() == nil` prevents panics when the registry is missing from a valid result.
101: nil/empty handling + `r.result.Registry() == nil` prevents panics when checking untrusted origins.
123: nil/empty handling + `r.result.Registry() == nil` prevents panics when checking variant origins.
143: nil/empty handling + `r.result.Registry() == nil` prevents panics when checking full family types.
161: nil/empty handling + `declared == nil` handles cases where a user attempts to refine a non-existent type.
183: nil/empty handling + `r.result.Registry() == nil` prevents panics during complex refinement logic.
218: nil/empty handling + `r.result.Registry() == nil` prevents panics when checking",320,17388,
analysis/check/diagnostics/member_read.go,q0,"81: recursion/depth limits + A deeply nested `ast.TableExpr` or `ast.LogicalOpExpr` chain that exceeds `typ.DefaultRecursionDepth` to trigger a silent return.
113: recursion/depth limits + A `typ.Recursive` type with a self-referential `Body` that exceeds `typ.DefaultRecursionDepth` during `multiArmUnionDepth`.
148: recursion/depth limits + A `typ.Recursive` type that exceeds `typ.DefaultRecursionDepth` during `fullyNarrowedType` resolution.
178: malformed input + An `ast.AttrGetExpr` where `Key` is an empty string or `KeySyntax` is not `AttrKeyDot`, potentially bypassing logic.
203: recursion/depth limits + A `typ.Recursive` type that exceeds `typ.DefaultRecursionDepth` during `multiArmUnionDepth`.
225: recursion/depth limits + A `typ.Recursive` type that exceeds `typ.DefaultRecursionDepth` during `closedRecordLacksField`.
234: recursion/depth limits + A `typ.Union` containing many members that, when traversed via `closedRecordLacksField`, exceeds the recursion limit.
243: recursion/depth limits + A `typ.Alias` pointing to a `typ.Recursive` type that exceeds `typ.DefaultRecursionDepth` during `closedRecordLacksField`.
244: recursion/depth limits +",320,17388,
analysis/check/diagnostics/runtime_guard.go,q0,"29: nil/empty handling: `result.Graph()` returning `nil` or `point` being `0` (the entry node) might bypass logic that assumes a non-empty control flow graph.
32: cyclic/self-referential types: `dominance.ComputeImmediateDominatorInfo(graph)` may hang or panic if the CFG contains a cycle that isn't a standard loop (e.g., a malformed graph with a self-looping edge).
41: malformed input: `fact.Check.Path.Equal(p)` might fail or panic if the path object contains unexpected depth or non-canonical representations of the same logical path.
45: rare type combinations: `runtimeTypeName` only handles primitives; if a type name is passed that is a valid Lua type but not in the switch (like `table` or `userdata`), it returns `false`, potentially masking a valid guard.
46: rare type combinations: `subtype.IsSubtype(t, want)` may panic if `t` is `nil` (returned by `runtimeTypeName` on default) and `want` is a complex type.
50: cyclic/self-referential types: `graph.Successors(branch)` returning a successor that is the `branch` itself could lead to infinite loops if the `reachable` check isn't robust.
78: cyclic/self-referential types: `reachable` uses a `seen` map, but if the `graph.Successors",320,17469,
analysis/check/diagnostics/unused_local.go,q0,"21: nil graph handling + A `result.Graph()` returning `nil` (e.g., from a failed or empty compilation pass) to ensure the producer doesn't panic.
26: empty/invalid local name + A local variable with an empty string name `""""` (malformed AST) to ensure `ignoredUnusedLocalName` prevents a diagnostic.
26: underscore prefix convention + A local named exactly `""_""` or `""__""` to ensure the ""intentional unused"" convention is respected.
28: symbol read/write race + A local that is assigned a value but whose symbol is never read due to a control flow path that is unreachable or shadowed.
48: zero-span/malformed span + An AST node with a `0` or negative line/column number to see if `unusedLocalDiagnostic` produces a valid position.
58: empty name in explanation + A local with a name containing special characters or control codes that might break the `fmt.Sprintf` formatting in the evidence message.",218,22476,
analysis/check/diagnostics/trust.go,q0,"41: nil/empty handling + `result.Registry()` returning nil while `result` itself is non-nil could cause a panic if not for the explicit check.
44: nil/empty handling + `value` being a zero-value `product.Value` (e.g., nil internal pointer) might cause `product.PresenceOf` to panic.
47: nil/empty handling + `product.Get` returning a nil `escape` value could cause a panic in `escape.Equal`.
50: nil/empty handling + `product.Get` returning a nil `typewitness` value could cause a panic in `witness.IsTop()`.
53: nil/empty handling + `witness.Type()` returning `(nil, false)` might lead to unexpected behavior in `subtype.IsFreshAssignable` if it doesn't handle nil `got`.
78: nil/empty handling + `cast.Type` being nil is handled, but a `cast` with a circular type definition could cause infinite recursion in `lowerType`.
101: nil/empty handling + `accessPath.Symbol == 0` is handled, but a symbol that exists but has a circular dependency in its type annotation could cause a stack overflow.
113: nil/empty handling + `result.ExpressionValueAtBoundary` returning a value that is part of a cyclic graph could cause infinite recursion in `ValueHasUntrustedTopOrigin`.
125: nil/empty handling + `call.",320,24207,
analysis/check/exportmanifest/export.go,q0,"74: nil/empty handling + `root` being nil causes `rootReturnType` to return `nil, false`, potentially bypassing logic if the caller expects a valid type.
84: nil/empty handling + `summary.Returns` being empty or `summary.Returns[0]` being an unknown type results in a silent failure to identify the export type.
101: recursion/depth limits + `hasRecordMembers` on a deeply nested or cyclic `typ.Union` could cause a stack overflow during the recursive member check.
118: metatable edge cases + `mergeRecordMembers` uses `preferredType` which might favor a `nil` metatable from a source over a valid one in a summary, or vice versa, depending on the desired precedence.
134: malformed input + `mergeFields` uses a map to deduplicate; if `field.Name` is an empty string or contains non-standard characters, it may lead to unexpected manifest collisions.
148: overflow + `keyForStaticMember` uses `index int64`; if the underlying analysis produces an index that overflows or is negative, the map keying might behave unexpectedly.
176: recursion/depth limits + `rootReturnType` calls `unionType` on a slice of candidates; if the candidates themselves contain deeply nested types, this could trigger a stack overflow.",290,18703,
analysis/check/diagnostics/type_reference.go,q0,"31: nil/empty handling + `result.Function()` returning nil while `result.Graph()` is non-nil could skip parameter/return type checks entirely.
48: nil/empty handling + `result.Graph()` being nil returns early, potentially missing unresolved types in `LocalAssignment` or `TypeDefinition` if they were expected to be processed.
53: nil/empty handling + `result.LocalAssignment(point)` returning a fact with a `nil` `fact.Type` would cause a panic in `emitExpr`.
56: nil/empty handling + `result.TypeDefinition(point)` returning a fact with a `nil` `fact.Type` (for Alias) or `nil` `fact.Interface` (for Interface) is partially guarded but relies on `emitTypeDefinitionRefs` logic.
60: nil/empty handling + `fact.Call` being non-nil but `fact.Call.TypeArgs` being nil/empty could lead to unexpected behavior in `emitExprs`.
71: nil/empty handling + `fact.Type` being nil in `TypeDefinitionAlias` is guarded, but `fact.Type.TypeParams` being nil could cause a panic.
77: nil/empty handling + `fact.Interface.Extends` or `fact.Interface.Fields` being nil/empty is handled by range, but `field.Type` being nil would cause a panic in `emitExpr`.
81: nil/empty handling + `method",320,24737,
analysis/check/fixpoint/program/internal/projectsummary/project.go,q0,"48: nil result handling + A `nil` ResultReader passed to `FromResult` which should return an empty summary instead of panicking.
52: incomplete result state + A ResultReader where `ExitState()` returns `ok=false` (e.g., a function that never reaches a return point or crashes), testing the early exit path.
52: nil registry/graph + A ResultReader that returns `nil` for `Registry()` or `Graph()`, testing the robustness of the projection logic against partially initialized results.
83: mismatched return arity + A function where `ReturnTypeValues()` returns 1 value but a specific `ReturnPoint` provides 5 values, testing the `arity` maximization logic.
87: missing return value sources + A `ResultReader` that does not implement `returnValueSourceReader`, testing the type assertion fallback in `resultReturnSourceArity`.
89: empty source list + A `returnValueSourceReader` that returns an empty slice for a specific `cfg.Point`, testing the `ok` flag and length logic.
94: summary normalization + A complex summary with cyclic dependencies or massive unions that might cause `summary.Normalize` to hang or stack overflow.",255,14591,
analysis/check/diagnostics/value_reference.go,q0,"48: recursion/depth limits + A deeply nested `ast.TableExpr` or `ast.FuncCallExpr` chain that could cause a stack overflow during `walkValueExpr`.
68: recursion/depth limits + A self-referential AST structure (e.g., an expression that contains itself) causing infinite recursion in `emitAssignmentTargetReads`.
83: recursion/depth limits + A deeply nested `ast.TableExpr` or `ast.FuncCallExpr` chain that could cause a stack overflow during `walkValueExpr`.
103: malformed input + A `FuncCallExpr` where `Args` contains a `nil` element, potentially causing a panic in `walkValueExpr`.
118: malformed input + A `LogicalOpExpr`, `RelationalOpExpr`, or `ArithmeticOpExpr` where `Lhs` or `Rhs` is `nil`.
123: malformed input + A `StringConcatOpExpr` where `Lhs` or `Rhs` is `nil`.
126: malformed input + A `UnaryMinusOpExpr`, `UnaryNotOpExpr`, `UnaryLenOpExpr`, or `UnaryBNotOpExpr` where `Expr` is `nil`.
130: malformed input + A `CastExpr` or `NonNilAssertExpr` where `Expr` is `nil`.
143: nil/empty handling + A `FuncCallExpr` where `Func` or `Receiver` is `",320,20396,
analysis/check/exportmanifest/type_defs.go,q0,"14: nil/empty handling: A `manifest.Manifest` that is non-nil but contains a `result.Graph()` that returns `nil` (testing the early exit guard).
23: malformed input: A `TypeDefinitionAlias` where `fact.Type` is non-nil but `fact.Type.Name` is an empty string (testing the name validation guard).
28: malformed input: A `TypeDefinitionAlias` where the `fact.Type` reference exists but `result.TypeDef(fact.Type)` fails to find the corresponding declaration (testing broken internal graph links).
33: malformed input: A `TypeDefinitionAlias` where the declaration is found but `resolver.Decl(decl)` fails (testing unresolved or circular type references).
39: malformed input: A `TypeDefinitionInterface` where `fact.Interface` is non-nil but `fact.Interface.Name` is an empty string.
44: malformed input: A `TypeDefinitionInterface` where the interface reference exists but `result.InterfaceDef(fact.Interface)` fails to find the definition.
49: malformed input: A `TypeDefinitionInterface` where the interface declaration is found but `resolver.Decl(decl)` fails to resolve the interface structure.",273,15442,
analysis/check/fixpoint/program/program.go,q0,NONE,2,2598,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_reachability.go,q0,"39: nil/empty handling + A `result` that implements `stateAtReader` but returns `ok=false` for all points, testing if the engine incorrectly assumes reachability via an empty state.
40: nil/empty handling + A `result` that implements `stateAtReader` but returns a `state.State{}` (empty state) which might be semantically different from an ""unreachable"" state in certain domain implementations.
47: cyclic/self-referential types + A CFG containing a tight loop where the `visiting` map must prevent infinite recursion by returning `true` (assuming reachability through the cycle).
51: malformed input + A CFG where `r.graph.Exit()` returns a point that is also a successor of itself, testing the interaction between the exit condition and the cycle detection.
54: rare type combinations + A `noNormal` reader that returns `true` for the `Exit()` node, testing if the ""no normal return"" override takes precedence over the ""exit node"" success condition.
58: recursion/depth limits + A deeply nested or extremely large CFG (e.g., a long chain of basic blocks) that could trigger a stack overflow in the recursive `canCompleteNormally` calls.",271,15816,
analysis/check/fixpoint/program/internal/callresult/provider.go,q0,"148 + cyclic/self-referential heap objects + `writeNestedHeapObjectStaticMember` could enter an infinite loop or stack overflow if `segments` describes a path that cycles back to a previously visited `currentID`.

188 + huge unions/intersections + `joinPossibleCallSummaries` could cause memory exhaustion or extreme latency when joining two summaries with massive, complex type unions in their return slots.

213 + malformed/out-of-bounds return indices + `materializeReturnRootTypesFromFacts` could panic if `fact.Path.PlaceholderIndex()` returns a value that is extremely large or negative, though it checks `maxIndex < 0`.

245 + nil/empty return slots + `returnSlotNeedsFactType` could behave unexpectedly if `sum.Returns` is empty or contains uninitialized values, though it checks `maxIndex`.

278 + malformed path segments + `returnRecordTypeFromFacts` could fail to reconstruct a record if `path.DirectFieldName()` or `path.DirectIntIndex()` returns inconsistent data for the same index.

323 + deep recursion in heap paths + `writeNestedHeapObjectStaticMember` could cause a stack overflow if `segments` is an extremely long list of nested table accesses.

365 + nil/empty heap objects + `writeHeapObjectStaticMember` could lead to inconsistent state if `objects[id]` is accessed when `id` is valid but the map entry is missing or partially initialized.

415",320,19715,
analysis/check/exportmanifest/function_signatures.go,q0,"168 + cyclic/self-referential types + A table containing itself as a member could cause infinite recursion in `projector.visit` if the `visiting` map logic is bypassed or fails.
218 + recursion/depth limits + A deeply nested table structure (e.g., `a.b.c...`) could cause a stack overflow during the recursive `projector.visit` calls.
245 + malformed input + A `heapStaticMember` with a key that `segment.ParseFormattedSegments` cannot parse would be silently skipped.
268 + malformed input + A `heapDynamicEntry` with a key that cannot be converted to a string or has an invalid format might cause unexpected behavior in sorting.
311 + rare type combinations + A `product.Value` that is not a `*typ.Function` but is passed to `functionSignatureType` is handled, but a `nil` pointer dereference in `t.(*typ.Function)` is a risk if `t` is not checked.
348 + overflow/bounds + A `summary.Summary` with `ReturnPresenceRelations` containing indices that are extremely large or negative could cause out-of-bounds access in `errorReturnLabels`.
378 + rare type combinations + A `summary.Summary` where `NormalReturnParams` or `NormalReturnFacts` contain indices that do not align with the actual function arity.
413 + recursion/depth limits + A circular reference between `Return",320,24312,
analysis/check/exportmanifest/source_type.go,q0,"23: nil/empty handling + `source.Expr` being nil while `source.Kind` is `SourceExpression` could lead to unexpected false returns in complex analysis flows.
34: malformed input + `sourcebridge.ValueSourceFromASTSource` returning `ok=false` for an AST node that should logically map to a value.
40: malformed input + `result.SourceValueWithRootDeclarationRecoveryAtBoundary` failing to recover from a broken or deeply nested declaration chain.
54: rare type combinations + `valueexpr.RuntimeKind` returning a kind that, when wrapped in a `product.NewWithPresence`, creates a type that doesn't satisfy subsequent type constraints.
63: malformed input + `fn.ParList.Types` having a different length than `fn.ParList.Names`, causing `typeExprAt` to return `nil` and aborting type resolution.
67: malformed input + `fn.ParList.HasVargs` being true but `fn.ParList.VarargType` being `nil`, representing an invalid AST state.
76: recursion/depth limits + `typeresolve.NewWithExternal` potentially entering an infinite loop if `result.ModuleTypes()` contains cyclic type definitions.
81: nil/empty handling + `typeExprAt` receiving an out-of-bounds index due to a mismatch between parameter names and type expressions.",304,21371,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_return_conditions.go,q0,"31-33: Nil/Empty Result Handling: A `ResultReader` that implements `branchConditionReader` but returns a `nil` graph, testing the early exit for incomplete analysis results.

43-45: Empty Parameter Set: A function with no parameters (e.g., `function f()`) where `params` is empty, ensuring the loop doesn't attempt to initialize an empty slice.

53-55: Missing Branch Condition: A graph node that is a valid `cfg.Point` but lacks an associated `BranchCondition` in the reader, testing the robustness of the RPO traversal.

58-60: Non-Branching Point: A graph node that is not a branch (e.g., a basic block entry or a simple assignment), ensuring the logic doesn't attempt to analyze control flow at non-decision points.

61-63: Reachability Failure: A branch where the `newNormalReturnReachability` analysis fails or returns `ok=false`, simulating a broken or disconnected CFG.

76-78: Unbalanced Branching: A branch node that only has one outgoing edge (e.g., a `goto` or a single-path conditional), which should be ignored as it doesn't represent a true decision.

79-81: Identical Completion Paths: A branch where both the `true` and `false` edges lead to the exact same reachability outcome, preventing the inference of a parameter constraint.

96-9",320,18473,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_return_equalities.go,q0,"21: Nil Graph/Result - A `ResultReader` that returns a non-nil `Graph()` which is itself `nil` (testing the second guard clause).
34: Missing Branch Condition - A graph node that exists in RPO but has no associated branch condition in the `branchReader` (testing the `!ok` skip).
38: Non-Normal Return Path - A branch condition that exists but does not represent a ""normal"" return path (testing the `normalReturnBranchCondition` failure).
42: Invalid Equality Check - A `branchcond.Check` that is neither `CheckPathEqual` nor `CheckPathNot` (testing the `default` case in the switch).
43: Inverted Logic - A `CheckPathEqual` condition that is only met on an error/exceptional branch rather than the normal return branch.
44: Inverted Logic - A `CheckPathNot` condition that is only met on the normal return branch.
51: Out-of-Bounds/Invalid Path Index - A `check.Path` that refers to a variable or register index that does not exist within the provided `params` slice.
55: Out-of-Bounds/Invalid Path Index - A `check.OtherPath` that refers to a variable or register index that does not exist within the provided `params` slice.
59: Trivial/Self-Equality - A condition that compares a path to itself (e.g., `x == x`), which should be discarded to",320,18469,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_return_facts.go,q0,"168 + recursion/depth limits + A deeply nested table structure (e.g., `a.b.c.d...`) could cause the BFS in `frozenTablePlaceholderPaths` to consume excessive memory or time if the `seen` map logic fails to catch a cycle.
188 + cyclic/self-referential types + A table that contains itself as a static member could cause an infinite loop in the `queue` processing if `candidate.hasSeen(childID)` is bypassed.
214 + malformed input + A `pathKey` that decodes to a `PlaceholderIndex` that is negative or out of bounds of the `params` slice.
235 + rare type combinations + A `stateProof.Kind` that is not one of the three expected `pathevidence` constants, testing the `default` branch of the switch.
263 + malformed input + A `stateFact.Kind` that is not a valid `channelselectfact.Kind`, testing the `ok` return of `projectChannelSelectKind`.
284 + malformed input + A `stateFact.Result` or `stateFact.Case` that is a non-empty string but fails to resolve via `projectPath`.
311 + malformed input + A `stateKey.Target` that is a valid key but `projectPath` returns `ok=false` due to an invalid placeholder index.
335 + malformed input + A `relation.Source` or `relation",320,18489,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_return_params.go,q0,"22: nil/empty handling: A `result` that implements `parameterValueSlotReader` but returns an empty slice of slots, testing the early exit logic.
31: malformed input: A `slot` string that is empty (`""""`), which should be skipped without affecting the `out` slice length or content.
34: rare type combinations: A `slot` that exists in the `reassigned` map, testing if the function correctly ignores parameters that were modified during execution.
37: overflow/boundary: A `slot` where `entry.ReadValue` and `exit.ReadValue` return extremely complex/deeply nested product types that might cause `normalReturnParamConstraint` to hang or stack overflow.
45: rare type combinations: `normalReturnParamConstraint` returning `false` (e.g., when `exit` is not a subset of `entry`), testing if the `out[i]` remains `product.Top()`.
51: malformed input: `exit` being `product.Top()` or `product.Bottom()`, which triggers the early `false` return in the constraint logic.
54: rare type combinations: `exit` being exactly equal to `entry`, which is a valid mathematical state but a specific edge case in the constraint logic.
57: rare type combinations: `exit` being a strict subset of `entry` (e.g., `exit` is `int` and `entry` is `number`), testing the `LessOrEq`",320,18444,
analysis/check/fixpoint/summary/frozen_table_facts.go,q0,NONE,2,108,
analysis/check/fixpoint/summary/param_condition.go,q0,NONE,2,682,
analysis/check/fixpoint/program/internal/projectsummary/project_param_obligations.go,q0,"148 + `delegatedReturnCallSlots` + If `sources` contains multiple elements where `source.CallPoint` is identical but `source.Expanded` is false, it returns `false` even if the call is valid.
168 + `delegatedReturnCallSlots` + If `outcome.Results` contains negative indices (malformed data), it silently skips them, potentially returning an empty `slots` map.
178 + `delegatedReturnCallSlots` + If `sources` has multiple elements but they all point to the same `CallPoint` with different `ResultIndex` values, the `exists` check triggers a failure.
203 + `addCallOutcomeObligations` + If `obligation.ParamIndex` is exactly `len(fact.Args)`, it triggers an out-of-bounds check (handled, but edge case for malformed facts).
223 + `addTypedCallObligations` + If `i` reaches `len(fact.Args)` during iteration of `params`, it breaks, which might hide type mismatches in variadic-like scenarios.
253 + `directCallable` + If `sym` is 0 (nil symbol), it returns `false`, which is a boundary condition for uninitialized or anonymous functions.
273 + `receiverType` + If `receiver.Segments` is non-empty but `luatypeprojection.ApplySegments` fails (e.g., due to a deep/invalid path), it returns `false`.",320,21123,
analysis/check/fixpoint/summary/param_obligation.go,q0,NONE,2,358,
analysis/check/fixpoint/program/internal/projectsummary/project_return_condition_refinements.go,q0,"24: Empty ReturnPoints - A function that returns immediately without any return points (e.g., an infinite loop or error-only path) might bypass logic if `ReturnPoints()` returns an empty slice.
31: Non-Expression Return Sources - A return source that is a `ValueSourceCall` but has a `ResultIndex != 0` (e.g., returning the second element of a multi-value function call) will be silently ignored.
43: Massive Refinement Sets - A `condition.RefinementsForValue` call returning an extremely large slice could cause significant memory pressure or latency during the `append` loop.
58: Target Path Mismatch - A refinement where the `TargetPath` refers to a symbol not present in the function's `params` (e.g., a global or a local from an outer scope) will be silently dropped.
60: Unconstrained Refinement Values - A refinement that exists but has a `Constraint()` returning `ok=false` (e.g., a top/bottom type or an unresolvable constraint) will be skipped.
77: Target Path Empty - A `target.IsEmpty()` check handles the case where a refinement points to nothing, but if `target.Segments` is non-empty while `target.Symbol` is 0, it may behave unexpectedly.
81: Symbol Collision/Shadowing - If `target.Symbol` matches a `param.Symbol` but the path structure is deeply nested or cyclic, `path.New",320,16894,
analysis/check/fixpoint/summary/param_obligation_lattice.go,q0,"23 + Valid (paramMemberCallObligation) + An obligation with a negative index or empty member string that bypasses initial construction but enters the lattice.
43 + Valid (paramMemberReturnSlot) + A return slot with a negative index or empty member string that could corrupt intersection logic.
58 + Valid (returnParamPathAlias) + An alias with an empty source or member string that could cause identity collisions in the must-set.",94,3950,
analysis/check/fixpoint/ref/ref.go,q0,"39: FromCFG + Nil/Zero ID handling: A `cfg.Graph` that is non-nil but returns `0` for `ID()` must be treated as a zero reference to prevent collision with valid IDs.

47: FromSymbol + Zero ID handling: A `symbol.ID` of `0` must be normalized to a `KindZero` reference to ensure consistency in map lookups.

58: IsZero + Dual-Condition check: A `FuncRef` with `KindZero` but a non-zero `ID` (or vice versa) must both evaluate to true to prevent logic errors in identity comparisons.

63: Less + Kind/ID boundary: Comparing two different `Kind` values where the `ID`s are identical (e.g., `KindCFG:1` vs `KindSymbol:1`) to ensure strict ordering.

71: String + Undefined Kind: A `FuncRef` initialized with an invalid/unexported `Kind` (e.g., `Kind(255)`) must be handled by the `default` case to prevent unhandled switch branches.",239,12266,
analysis/check/fixpoint/summary/dynamic_index_facts.go,q0,"31: Nil/Empty handling + An empty slice `in` passed to `Normalize` might trigger unexpected behavior if the underlying `factmap` implementation doesn't handle zero-length inputs gracefully.

41: Nil/Empty handling + Passing a `nil` slice to `cloneDynamicIndexFacts` returns `nil`, but passing a slice of facts where `fact.Table` is nil would cause a panic during `Clone()`.

48: Nil/Empty handling + `dynamicIndexFactsEqual` with `nil` vs `[]callboundary.DynamicIndexFact{}` may return false depending on whether the lattice implementation treats nil as an empty set or an invalid state.

68: Malformed input + `dynamicIndexKeyOf` assumes `fact.Table` is non-nil; a fact with a nil `Table` pointer will cause a panic when calling `.Key()`.

73: Rare type combinations + `dynamicIndexFactLess` relies on `left.table < right.table`; if the `PathKey` implementation has non-transitive comparison logic or handles complex/cyclic paths, this comparison could fail or panic.",238,11654,
analysis/check/fixpoint/summary/path_invalidation_facts.go,q0,NONE,2,667,
analysis/check/fixpoint/program/internal/projectsummary/project_return_param_aliases.go,q0,"48: Cyclic/Self-referential types: An object literal where an entry's source is the object itself, potentially causing infinite recursion if `active` tracking fails.
58: Recursion/Depth limits: A deeply nested object literal structure that could cause a stack overflow during the recursive descent in `projectReturnSourceParamAliases`.
63: Cyclic/Self-referential types: An object entry that points back to a previously visited `ExprRef` in the same traversal path, testing the `active` map's cycle detection.
73: Malformed input: An object entry with a `Suffix` that contains segments that cannot be converted to a `StaticMemberSuffixKey`.
83: Rare type combinations: An object entry where the `entrySource` is an expression but `pathReader.ExpressionPathRef` returns a path that doesn't match any parameter symbols.
105: Nil/Empty handling: A `sourcePath` that is empty or has a zero symbol, which should be caught by `returnAliasParamIndex` to prevent invalid placeholder generation.
116: Malformed input: A parameter index that is out of bounds or a parameter symbol that resolves to an empty string slot, testing the robustness of the reassignment check.
128: Memory/Overflow: A very large number of segments in `prefix` and `suffix` being concatenated, testing the efficiency and bounds of `appendSegments`.",301,18828,
analysis/check/fixpoint/summary/heap_table_objects.go,q0,"14: Nil/Empty handling + An input map containing only `identity.ID{}` or `Bottom()` objects results in a `nil` return, testing if callers handle `nil` vs `empty map` correctly.
34: Huge unions/intersections + The `joinHeapTableObjects` function performs double normalization on potentially massive maps, which could trigger OOM or extreme latency if the underlying `Join` is not optimized for sparse maps.
40: Huge unions/intersections + The `widenHeapTableObjects` function performs double normalization, which may fail to converge or cause performance death spirals if the widening logic interacts poorly with the normalization of `Bottom`.
14-40: Cyclic/Self-referential types + Since `normalizeHeapTableObjects` calls `heapidentity.CloneObject`, a self-referential table structure in the input could cause infinite recursion/stack overflow during the cloning process.",192,10190,
analysis/check/fixpoint/summary/store_relation_facts.go,q0,NONE,2,158,
analysis/check/fixpoint/summary/snapshot.go,q0,NONE,2,640,
analysis/check/fixpoint/summary/summary_key.go,q0,NONE,2,106,
analysis/check/fixpoint/program/internal/projectsummary/project_return_presence_relations.go,q0,"43: nil/empty handling + A `result` that returns an empty slice from `ReturnPoints()` but satisfies the `returnValueSourceReader` interface.
48: nil/empty handling + A `result` where `ReturnPoints()` returns a point, but `ReturnPresenceRelations(point)` returns an empty slice.
56: nil/empty handling + A `reg` that is `nil`, which should trigger the early return to prevent downstream panics.
57: nil/empty handling + A `result` that implements `returnValueSourceReader` but fails the `hasSources` or `hasValues` type assertions.
60: boundary condition + A `result` where `projectedReturnPresenceArity` returns exactly 1, which should be ignored as relations require at least two indices.
64: malformed input + A `result` where `ReturnPoints()` returns a point that is immediately flagged as unreachable by `projectedReturnPointUnreachable`.
68: malformed input + A `sourceReader` that returns `ok=false` for a specific point, potentially indicating a corrupted analysis state.
78: overflow/huge unions + An extremely large `arity` value that could cause the nested triple-loop to hang or consume excessive memory.
83: rare type combinations + A `triggerPresence` that is `presence.Bottom()`, which might not be explicitly handled by the `TargetPresence` logic.
105: malformed input + A `reader.SourceValueAtBoundary`",320,19056,
analysis/check/fixpoint/program/internal/projectsummary/project_return_slots.go,q0,"24: arity vs declared length mismatch + `arity > len(declared)` where `exit.ReadValue` returns a value that must be joined with a non-existent declaration (effectively joining with `Top` or `Bottom` implicitly).
24: arity vs declared length mismatch + `len(declared) > arity` where the loop terminates before processing all declared values, potentially leaving trailing declared constraints unapplied to the return set.
34: nil/empty handling + `value` is `product.Bottom` but `product.Equal` implementation fails or panics due to registry mismatch.
37: rare type combinations + `product.PresenceOf(joined)` and `product.PresenceOf(declared)` result in a presence state that is invalid or unrepresentable in the registry.
39: malformed input + `declaredKind.IsTop()` returns true, but the underlying `product.Get` returns a value that causes `runtimekind.Join` to panic.
43: rare type combinations + `evidence.Join` results in an overflow or an invalid evidence state when joining highly divergent evidence paths.
46: rare type combinations + `declaredWitness` is neither `Bottom` nor `Top`, but `product.Set` fails because the `joined` value's structure is incompatible with the witness type.
49: malformed input + `declaredOrigin` is neither `Bottom` nor `Top`, but `product.Set` triggers a panic because the `joined` value lacks the necessary internal",320,18512,
analysis/check/fixpoint/summary/effect_delta_facts.go,q0,"31: nil/empty handling + An empty input slice `in` passed to `cloneEffectDeltas` returns `nil`, which might cause panics in callers expecting a non-nil empty slice.
32: nil/empty handling + `cloneEffectDeltas` returns `nil` for `len(in) == 0`, potentially breaking callers that attempt to range over or append to the result without a nil check.
38: malformed input + `effectDeltaMap` defines `Valid` based on `IsPlaceholder()`, meaning a delta with a non-placeholder target is treated as ""invalid"" (effectively zero/bottom) during normalization or joins.
44: rare type combinations + `effectDeltaEqual` relies on `effectdelta.Domain(reg).Equal`, which may fail or panic if `a.Value` and `b.Value` are incompatible types within the registry's lattice.
53: recursion/depth limits + `effectDeltaKeyOf` calls `delta.Target.Key()`, which could trigger infinite recursion or stack overflow if the `PathKey` contains a cyclic reference.
57: overflow/comparison + `effectDeltaLess` uses standard Go comparison on `PathKey`, `site` (string), and `kind` (int); if `PathKey` is a complex struct, the comparison behavior depends on whether it is comparable and how its internal fields are ordered.",299,12526,
analysis/check/internal/sourcebridge/source_value.go,q0,NONE,2,800,
analysis/check/fixpoint/program/method_receiver_context.go,q0,"116: recursion/depth limits + Deeply nested `if/else/do` blocks in `collectTypesAndMetatables` could cause stack overflow if the AST is maliciously deep.
143: recursion/depth limits + Deeply nested `if/else/do` blocks in `collectSetmetatableReceivers` could cause stack overflow if the AST is maliciously deep.
168: nil/empty handling + `stmt.Types[i]` being nil in a local assignment with mismatched type/expression counts.
188: nil/empty handling + `stmt.Exprs` being empty or shorter than `symbols` in a local assignment.
203: nil/empty handling + `stmt.Lhs` or `stmt.Rhs` being empty in an assignment statement.
225: malformed input + `target.Symbol` being 0 in `collectAssignmentSetmetatableReceiver` (though handled, it's a common path for broken ASTs).
233: malformed input + `call.Args` having fewer than 2 elements in `collectSetmetatableReceiver` (handled, but critical for index out of bounds).
243: malformed input + `object.Symbol` being 0 in `collectSetmetatableReceiver` (e.g., `setmetatable(nil, mt)`).
253: malformed input + `meta.Symbol` being 0 in `collectSetmetatableReceiver` (e.g.,",320,18868,
analysis/check/fixpoint/program/param_inference.go,q0,"148 + cyclic/self-referential tables + A table containing itself as a member could cause infinite recursion in `seedReachableHeapFromValue` if `seen` is not correctly managed or if the heap object structure is malformed.
188 + huge/complex unions + `product.Join` on extremely large or deeply nested type unions could cause significant performance degradation or stack exhaustion during the join operation.
218 + missing/nil arguments + If `present` is shorter than `args`, the loop might access out-of-bounds or fail to account for the implicit `nil` passed to the callee.
253 + Top/Bottom edge cases + If `product.Equal` or `product.Join` behaves unexpectedly with `Top` or `Bottom` (e.g., due to custom metatable logic), the parameter inference might become unsound.
288 + singleton table identity + If a table has a singleton identity but its internal structure is being mutated by another thread or process, `seedReachableHeapFromValue` might encounter inconsistent state.
318 + cyclic heap objects + `seedReachableHeapFromID` relies on the `seen` map to prevent infinite loops when traversing `object.Root()` or `object.DynamicIndexFacts()`.
331 + massive dynamic index facts + A table with a massive number of dynamic index facts could cause a stack overflow due to the recursive calls to `seedReachableHeapFromValue`.
343 + malformed static members + If `",320,18914,
analysis/check/fixpoint/query/query.go,q0,"48: ErrRegistryRequired + Nil Registry + Passing a nil `axis.Registry` which will cause a panic in `summaryLattice` or `summary.Normalize` during execution.
53: ErrDuplicateFunction + Duplicate Keys + Providing two different `Body` functions with the same `SummaryKey` to check for collision handling.
56: ErrNilBody + Nil Function Body + Providing a `Function` where `Body` is `nil` to ensure the driver doesn't attempt to call it during the transfer phase.
103: Nil Seed + Empty Seed + Providing a `nil` `d.seed` to ensure the `Initial` function returns a safe empty summary rather than panicking.
108: Malformed Seed + Seed Mismatch + A `seed.Read(key)` that returns `ok=true` but provides a summary that is inconsistent with the `Registry`'s expected types.
116: Error Propagation + Body Panic/Error + A `Body` function that returns a non-nil `error` to ensure the solver halts and returns the error instead of continuing with invalid data.
124: Normalization Overhead + Massive Summary + A `Body` function that returns an extremely large or deeply nested `summary.Summary` to test the performance/stack depth of `summary.Normalize`.
148: Lattice Divergence + Infinite Join + A `Join` operation that produces a summary that never stabilizes, testing if `WidenAt` or `WidenDelay` correctly prevents infinite",320,16203,
analysis/check/fixpoint/summary/escape_event_facts.go,q0,"41: `escapeEventDominates` + Cyclic/Self-referential paths: If `parent.Target` and `child.Target` represent a cycle where the child is a prefix of the parent, the dominance logic might create an infinite loop in the fixpoint engine.

42: `escapeEventDominates` + Identity/Self-dominance: If `parent` and `child` are identical, the function returns `true` (via `pathHasPrefix`), which can cause fixpoint engines to fail to terminate if they expect strict dominance.

43: `escapeEventDominates` + Recursive Subsumption: If `parent.Recursive` is true but `parent.Target` is a deep descendant of `child.Target`, the logic correctly returns `false`, but testing requires highly nested path structures.

46: `escapeEventDominates` + Non-recursive equality: If `parent.Recursive` is false, it requires strict `Equal` equality; a test case with a child that is a prefix but not equal to the parent would exercise this boundary.

50: `pathHasPrefix` + Empty Segment Lists: If `prefix.Segments` is empty (representing a root/global path), the loop is skipped, potentially allowing any `candidate` with matching metadata to be considered a ""prefix.""

51: `pathHasPrefix` + Metadata Mismatch: If `Symbol`, `Root`, or `Version` match but the segment lengths differ, it tests the boundary where a path is a ""logical"" prefix",320,12179,
analysis/diagnostic/assertion.go,q0,NONE,2,483,
analysis/check/fixpoint/summary/param_equality.go,q0,"31: Admit (Self-equality) + An equality where `Left == Right` (e.g., `ParamEquality{1, 1}`) which should be rejected as a non-relation.
32: Admit (Invalid Indices) + Negative parameter indices (e.g., `Left: -1`) which represent malformed or uninitialized registry states.
34: Admit (Canonicalization) + An unsorted pair (e.g., `Left: 5, Right: 2`) to ensure the lattice correctly treats it as `Left: 2, Right: 5`.
54: paramEqualitiesSummaryEqual (Bottom-Bottom Equality) + Two summaries that are both ""Bottom"" (empty/invalid) to ensure they are considered equal rather than unequal.
60: paramEqualitiesSummaryLessOrEq (Bottom-Top Comparison) + A ""Bottom"" summary compared against a non-bottom summary to ensure it correctly returns `true` (Bottom is the least element).
63: paramEqualitiesSummaryLessOrEq (Top-Bottom Comparison) + A non-bottom summary compared against a ""Bottom"" summary to ensure it correctly returns `false`.
71: joinParamEqualities (Bottom-Identity) + Joining a ""Bottom"" summary with a populated summary to ensure the result is exactly the populated summary.
78: paramEqualitiesBottom (Empty Summary) + A summary with zero parameter count and zero equality relations to ensure it is correctly identified as the lattice bottom.",316,11507,
analysis/check/fixpoint/summary/return_presence_relation.go,q0,"31: Self-referential relation + A relation where `TriggerIndex == TargetIndex` is explicitly invalidated, preventing trivial/circular identity implications.
32: Bottom/Top lattice boundaries + Relations involving `IsBottom()` or `IsTop()` are rejected, preventing the lattice from being polluted by non-informative or impossible presence states.
48: Sorting/Ordering stability + A case where `triggerIndex`, `triggerPresence`, `targetIndex`, and `targetPresence` are all identical but in different orders (if they were non-monotonic) to ensure the `Less` function provides a strict total ordering for the `factset`.",132,3104,
analysis/check/fixpoint/summary/channel_select_facts.go,q0,"33: `optionalPlaceholderPath` + Nil/Empty Path: If `path.IsEmpty()` or `path.IsPlaceholder()` is called on a zero-valued or uninitialized `pathdom.Path` struct, it tests the robustness of the path domain's internal state.

38: `channelSelectFactKeyOf` + Empty Select ID: If `fact.Select` is an empty string, the key becomes valid for comparison but might cause collisions or unexpected behavior in the lattice if multiple ""empty"" selects are treated as the same ID.

41: `channelSelectFactKeyOf` + Zero-Value Path Keys: If `fact.Result.Key()` or `fact.Case.Key()` returns a zero-value or a malformed key due to a broken path graph, the `channelSelectFactKey` will be populated with invalid identifiers.

48: `channelSelectFactLess` + Non-Deterministic Ordering: If `left.hasDefault` and `right.hasDefault` are both true (or both false), the function returns `false`, which is correct, but if the `Less` implementation is used in a way that expects a strict weak ordering and the other fields are identical, it tests the stability of the sort.

58: `channelSelectFactLess` + Identical Keys with Different `hasDefault`: If all other fields are identical, the logic `!left.hasDefault && right.hasDefault` ensures a specific order for default cases, which is a boundary condition for how the lattice handles the",320,15634,
analysis/check/fixpoint/summary/normal_return_facts.go,q0,"31 + nil/empty handling + Passing a `NormalReturnFacts` struct where all internal slices/maps are nil instead of empty.
32 + nil/empty handling + Passing a `NormalReturnFacts` struct where some fields are nil and others are non-empty, testing the `normalReturnFactsEmpty` logic.
38 + nil/empty handling + Calling `cloneNormalReturnFacts` with a struct that has nil fields, potentially causing panics in the underlying `clone` functions.
48 + nil/empty handling + `normalReturnFactsEqual` receiving one or both arguments as ""empty"" but non-nil, testing if normalization correctly equates them.
62 + nil/empty handling + `normalReturnFactsLessOrEq` receiving uninitialized/nil facts, testing if normalization prevents nil pointer dereferences during comparison.
77 + nil/empty handling + `joinNormalReturnFacts` receiving two empty/nil fact sets, testing if the join of two empty sets results in a valid empty set.
91 + nil/empty handling + `widenNormalReturnFacts` receiving empty facts, testing if widening an empty set against a populated set behaves correctly.
104 + nil/empty handling + `normalReturnFactsEmpty` checking a struct where all fields are nil, testing if `len(nil)` is handled safely (which it is in Go, but critical for logic).",293,13408,
analysis/domain/constraint/decision/result.go,q0,"49: default case + An integer cast to `Result` that is not 0, 1, or 2 (e.g., `Result(99)`) will return `""?""`.
58: Combine logic + A combination of `Unknown` and `Valid` (or `Unknown` and `Unknown`) to ensure the lattice correctly defaults to `Unknown` rather than a valid/invalid state.",87,2110,
analysis/check/fixpoint/summary/return_condition.go,q0,"46: Nil/Empty handling + `in` slice is nil or empty, potentially causing issues if `Normalize` doesn't handle zero-length slices gracefully.
50: Nil/Empty handling + `in` slice is nil, which returns `nil` instead of an empty slice, potentially causing nil-pointer dereferences in callers expecting a non-nil slice.
56: Malformed input + `refinement.Target` is nil, which would cause a panic during the `Clone()` call.
78: Rare type combinations + `value` is `Bottom` or `Top`, which are filtered out by `usefulReturnConditionValue` and might lead to unexpected empty maps in the lattice.
81: Overflow/Boundary + `ReturnIndex` is a very large integer or negative, which is caught by `Valid` but might indicate upstream corruption.
81: Malformed input + `Target.IsPlaceholder()` is false, which would cause the refinement to be silently dropped from the lattice.",211,5137,
analysis/check/fixpoint/summary/path_facts.go,q0,"21: Valid/Placeholder check + A fact where `Path.IsPlaceholder()` returns true, potentially causing the lattice to treat a non-empty fact as ""invalid"" or ""bottom"" during a join.
22: Clone/Deep Copy + A `Path` object that contains a cyclic reference to itself, which would cause `f.Path.Clone()` to enter an infinite recursion/stack overflow.
41: Valid/Placeholder check + A fact where `Path.IsPlaceholder()` is true, which might cause the intersection logic to incorrectly discard a member that should have been preserved.
42: Clone/Deep Copy + A `Path` object with a self-referential structure, causing a stack overflow during the `Clone()` call.
51: Nil/Empty handling + Passing a `nil` slice to `Normalize`, which tests if the underlying `factmap` implementation handles nil vs empty slice gracefully.
61: Nil/Empty handling + Passing a `nil` slice to `clonePathValueFacts`, which should return `nil` without panicking.
62: Nil/Empty handling + Passing a `nil` slice to `clonePathStaticMemberFacts`, which should return `nil` without panicking.
74: Equality/Lattice comparison + Two slices containing facts with identical `Path` keys but different `Value` pointers, testing if the `Equal` implementation is deep or shallow.
86: Lattice Ordering + Two slices where paths are logically equivalent but have different internal memory layouts, testing the robustness of",320,8206,
analysis/check/fixpoint/summary/proof_facts.go,q0,"33: `admitBranchProof` / Placeholder Path: A proof where `Path.IsPlaceholder()` is true but the `Kind` is an unhandled or default enum value, testing the `default` fallthrough.
36: `admitBranchProof` / Presence Lattice Extremes: A `BranchProofPathPresence` where `Presence` is `Bottom` or `Top`, testing if the lattice correctly rejects non-informative proofs.
40: `admitBranchProof` / Equality/Inequality Constraint Conflict: A `BranchProofPathEqual` where `Other` is already populated, testing the strictness of the canonicalization requirement.
41: `admitBranchProof` / Equality/Inequality Presence Reset: A `BranchProofPathEqual` where `Presence` is non-bottom, testing if the logic correctly forces `Presence` to `Bottom` to maintain the ""Equality"" invariant.
51: `branchProofKeyOf` / Path/Other Key Collision: A scenario where `Path.Key()` or `Other.Key()` returns a value that causes hash collisions or unexpected equality in the `branchProofKey` struct.
59: `branchProofLess` / Total Ordering Stability: A case where `kind`, `path`, `presence`, and `other` are all identical but the underlying `Key()` values differ, testing the stability of the lattice's `Less` function.",296,7103,
analysis/domain/constraint/expr/expr.go,q0,"103: nil/empty handling: Passing a `nil` `Expr` to `VisitExpr` will cause a panic when the type switch attempts to evaluate `e.(type)`.
104: nil/empty handling: If `v.Default` is `nil` and the expression type is not explicitly handled (or is `nil`), the function returns the zero value of `R`, which may lead to silent logic errors in the caller.
110-165: recursion/depth limits: A deeply nested or cyclic `Expr` structure (e.g., `Add(e, e)`) will cause a stack overflow during `VisitExpr` if the visitor implementation is recursive.
110-165: malformed input: If an `Expr` is constructed via manual struct initialization rather than constructors, it may contain `nil` pointers for `Left` or `Right` fields, causing panics during visitation.",199,4785,
analysis/diagnostic/policy.go,q0,"49: nil/empty handling: Passing a `nil` slice to `Apply` returns `nil` instead of an empty slice, which might cause unexpected behavior in callers expecting a non-nil empty slice.
54: malformed input: If `diags` contains duplicate `Code` entries with different properties, the policy applies rules independently to each, potentially leading to inconsistent output sets.
64: rare type combinations: If a `Rule` is constructed manually with both `Disabled: true` and `Enabled: true`, `Enabled()` (line 76) prioritizes `Disabled` due to the `if rule.Disabled` check.
73: malformed input: If `p.Rules` is `nil`, `Apply` and `ApplyOne` will not panic (Go map access on nil is safe), but they will treat all diagnostics as having no rules, effectively defaulting to `defaultEnabled`.
76: rare type combinations: A rule where `HasEnabled` is true but both `Enabled` and `Disabled` are false (manually constructed) will return `false` via the `rule.Enabled` check.",239,5864,
analysis/domain/constraint/expr/expr_compare.go,q0,"27: Invalid ExprRel value + A `ExprRel` cast from an out-of-bounds integer (e.g., `ExprRel(255)`) to ensure the `String()` method returns `?` instead of panicking.
43: Deeply nested expression recursion + An `ExprCompare` where `Left` or `Right` contains a massive tree of nested `ExprCompare` objects to trigger a stack overflow during `Equals`.
43: Cyclic expression reference + An `ExprCompare` where `Left` or `Right` contains a pointer/reference back to the parent `ExprCompare` (if `Expr` is an interface) to trigger infinite recursion in `Equals`.
43: Nil/Zero-value Expr + An `ExprCompare` where `Left` or `Right` is a nil interface value to check if `ExprEquals` handles nil safely without panicking.",190,4797,
analysis/diagnostic/types.go,q0,"23: Severity.String() + A `Severity` value cast from an arbitrary integer (e.g., `Severity(999)`) to ensure it returns ""unknown"" rather than panicking or returning an empty string.
43: Code.String() + An empty `Code` string (`""""`) to verify it defaults to the string ""diagnostic"" as per the logic.
75: Diagnostic.String() + A `Diagnostic` where `Position.Valid()` returns `false` (e.g., zero-valued position) to ensure it gracefully falls back to just the `Message`.
75: Diagnostic.String() + A `Diagnostic` where `Message` contains complex characters or is empty, to ensure `fmt.Sprintf` handles the formatting without error.",164,6205,
analysis/check/fixpoint/summary/summary_lattice.go,q0,"41: Normalize + Nil/Empty handling + If `s` is passed as a zero-value `Summary`, the function must return a zero-value `Summary` without panicking on slice access.
54: Normalize + Nil/Empty handling + If all fields are empty or contain only ""bottom/top"" values, it must return a clean `Summary{}` to prevent accumulation of useless data.
73: Equal + Nil/Empty handling + If `a` and `b` have different lengths for `Returns` or `NormalReturnParams`, `max` is used to prevent out-of-bounds, but the logic relies on `returnAt` to handle the ""missing"" slots as bottom.
103: LessOrEq + Nil/Empty handling + If `a` is bottom, it returns true immediately; if `b` is bottom, it must correctly check if `a` is also bottom to avoid incorrect lattice ordering.
106: LessOrEq + Nil/Empty handling + If `b` is bottom and `a` is not, it must return `false` (bottom is the least element).
135: Join + Nil/Empty handling + If both `a` and `b` are ""bottom"" summaries, it must return a zero-value `Summary{}` rather than a summary with empty slices.
148: Join + Nil/Empty handling + If `a` is bottom, it returns `Normalize(reg, b)`, which could result in an empty summary if",320,7569,
analysis/domain/effect/capability/capability.go,q0,NONE,2,503,
analysis/domain/effect/control/reserved.go,q0,NONE,2,386,
analysis/check/placementplan/plan.go,q0,"118 + cyclic/self-referential types + A graph where `entry.Children` contains a cycle (e.g., A -> B -> A) could cause infinite recursion in `walk` if the `seen` map logic is bypassed or fails.
118 + recursion depth + A very deep tree of `Children` IDs could cause a stack overflow in the recursive `walk` function.
148 + nil pointer + Passing a `nil` `result` to `addResult` triggers a `BlockerMissingExitState` but relies on `result.FunctionResults()` not being called on a nil pointer (handled by the early return).
178 + nil pointer + If `st.HeapTableObjectsSnapshot()` or `st.PlacementsSnapshot()` returns a nil pointer instead of an empty struct, the subsequent field access will panic.
188 + nil pointer + If `reg` is nil, `heapObjectChildren` is called with a nil registry, which is handled, but if `reg` is non-nil but the registry internal state is corrupted, `heapObjectChildren` might panic.
218 + huge unions + If `aggregate.placements` contains many entries for the same ID, `placement.Join` is called repeatedly; if `Join` is computationally expensive or has overflow issues, this becomes a bottleneck.
248 + empty/nil input + If `p.Entries` is empty, `MaxTargetDepth` returns 0, but if `p.Entries` contains",320,7773,
analysis/diagnostic/evidence.go,q0,"28: Invalid TrustKind: An integer cast to `TrustKind` that is not in the defined iota range (e.g., `TrustKind(99)`) to trigger the `default` case.
48: Invalid EvidenceKind: An integer cast to `EvidenceKind` that is not in the defined iota range (e.g., `EvidenceKind(-1)`) to trigger the `default` case.
84: Empty Explanation: Calling `String()` on an `Explanation` with a `nil` or zero-length `evidence` slice.
100: Empty Message: An `Evidence` item where `Message` is `""""`, forcing the fallback string interpolation logic.
103: Invalid Span: An `Evidence` item where `Span.Valid()` returns `false`, testing the branch that omits line/column prefixes.
101: Nil/Zero Span: An `Evidence` item with a zero-valued `Span` (0:0) which might be considered ""valid"" or ""invalid"" depending on `Span` implementation.",229,7488,
analysis/domain/effect/dispatch/reserved.go,q0,NONE,2,410,
analysis/domain/effect/dispatch/labels.go,q0,NONE,2,692,
analysis/domain/effect/mutation/active.go,q0,NONE,2,350,
analysis/check/fixpoint/summary/summary.go,q0,"43-60: Nil/Empty handling + A `Summary` where all slices are `nil` but `NormalReturnFacts` is non-empty (or vice versa) might bypass the early return and trigger unnecessary allocations or incorrect zero-value initialization.

61-63: Shallow copy vs Deep copy + A `product.Value` that contains internal pointers or nested slices, where `copy()` only performs a shallow copy, leading to shared state between the original and cloned `Summary`.

64-66: Shallow copy vs Deep copy + A `product.Value` representing a complex Lua table where the slice copy fails to decouple the underlying data structures, causing mutations in one summary to corrupt the other.

67-69: Shallow copy vs Deep copy + A `ParamMemberCallObligation` containing nested metadata that is not explicitly deep-copied, leading to race conditions or side effects during fixed-point iteration.

70-72: Shallow copy vs Deep copy + A `ParamMemberReturnSlot` that holds references to shared analysis domains, causing ""spooky action at a distance"" when the analysis refines a value.

73-75: Shallow copy vs Deep copy + A `ReturnParamPathAlias` that points to a specific variable path; if the path structure is mutated, the clone will point to the wrong variable.

76-78: Shallow copy vs Deep copy + A `product.Value` in `NormalReturnParams` that is a complex union type, where the slice copy fails to",320,9670,
analysis/domain/constraint/expr/expr_equal.go,q0,"11 + nil/empty handling + Comparing `(nil, nil)` returns true, but `(nil, non-nil)` or `(non-nil, nil)` must be tested to ensure no panic occurs during the first `VisitExpr` call.
78 + recursion/depth limits + A deeply nested `BinOp` tree (e.g., 10,000 levels deep) will cause a stack overflow due to the mutual recursion between `ExprEquals` and `VisitExpr`.
78 + cyclic/self-referential types + An `Expr` where `av.Left == av` (a circular pointer) will cause infinite recursion and a stack overflow.
103 + malformed input + If `e.Variables()` returns a slice containing an empty string or a string with non-printable characters, the `seen` map and subsequent `sort.Strings` must handle it without error.
103 + rare type combinations + If `e.Variables()` returns `nil` instead of an empty slice, the `range` loop must be verified to not panic.",230,6782,
analysis/check/fixpoint/summary/summary_slots.go,q0,"13: Index out of bounds / Empty Returns: Accessing `s.Returns[i]` where `i` is exactly `len(s.Returns)` or the slice is nil.
20: Index out of bounds / Empty NormalReturnParams: Accessing `s.NormalReturnParams[i]` where `i` is exactly `len(s.NormalReturnParams)` or the slice is nil.
26: Index out of bounds / Empty ParamObligations: Accessing `s.ParamObligations[i]` where `i` is exactly `len(s.ParamObligations)` or the slice is nil.
34: Nil Registry: Passing a `nil` registry to `UsefulParamObligation` which might be expected to panic in standard logic but is explicitly handled.
41: Nil Registry: Passing a `nil` registry to `UsefulNormalReturnParam` which might be expected to panic in standard logic but is explicitly handled.
53: Index out of bounds / Mismatched Counts: Accessing `s.NormalReturnParamConditions[i]` when `i` is within the range of `normalReturnParamCount` but exceeds the actual slice length.
64: Trailing Bottom Values: A summary where all `NormalReturnParams` are `product.Bottom`, testing the loop's ability to reduce `paramCount` to zero.
68: Trailing Non-Useful Conditions: A summary where all `NormalReturnParamConditions` are not useful, testing the loop's ability to reduce `",320,10142,
analysis/domain/constraint/expr/expr_arith.go,q0,"48: Recursion/Depth Limits: A deeply nested `BinOp` tree (e.g., 10,000 levels) that triggers a stack overflow during `Substitute` or `Variables`.
54: Recursion/Depth Limits: A deeply nested `BinOp` tree that triggers a stack overflow during `Eval`.
78: Overflow: `OpDiv` with `math.MinInt64 / -1` which results in an integer overflow in Go.
85: Overflow/Edge Case: `OpMod` with `math.MinInt64 % -1` which is mathematically undefined/problematic in many integer implementations.
101: Overflow: `safeAdd` with `math.MaxInt64 + 1` or `math.MinInt64 - 1`.
107: Overflow: `safeSub` with `math.MaxInt64 - (-1)` or `math.MinInt64 + 1`.
113: Overflow: `safeMul` with `math.MaxInt64 * 2` or `math.MinInt64 * 2`.
116: Overflow: `safeMul` with `-1 * math.MinInt64` which cannot be represented as a positive `int64`.
119: Overflow: `safeMul` with `math.MinInt64 * -1` which cannot be represented as a positive `int64`.",314,8389,
analysis/domain/constraint/equality/egraph.go,q0,"138 + recursion/depth limits + `Find` uses naive recursion for path compression which will stack overflow on a very long, non-compressed chain of `parent` pointers.
188 + recursion/depth limits + `propagateCongruence` calls `Union` which calls `propagateCongruence`, potentially causing stack overflow on deep, highly-interconnected congruence chains.
213 + cyclic/self-referential types + `Register` does not check if a path is its own parent, which could lead to infinite loops in `Find` or `propagateCongruence` if the `pathdom.Path` structure is malformed.
248 + malformed input + `Find` calls `makeSet`, which could lead to unexpected memory growth if an adversary provides a massive stream of unique, non-existent keys.
278 + recursion/depth limits + `propagateCongruence` performs a recursive `Union` on matching children, which can trigger an exponential explosion of calls in a dense graph of equalities.
313 + huge unions/intersections + `GetEquivalenceClass` performs an $O(N)$ scan of the entire `parent` map for every call, making it extremely slow for large E-graphs.
331 + huge unions/intersections + `ClassRepresentatives` performs an $O(N)$ scan of the entire `parent` map, which is inefficient for large-scale equality reasoning.",308,9511,
analysis/domain/constraint/expr/expr_ref.go,q0,"24: Nil/Empty Substitution - Passing a `nil` map to `Substitute` will cause a runtime panic.
48: Nil/Empty Substitution - Passing a `nil` map to `Substitute` will cause a runtime panic.
72: Nil/Empty Substitution - Passing a `nil` map to `Substitute` will cause a runtime panic.
96: Nil/Empty Substitution - Passing a `nil` map to `Substitute` will cause a runtime panic.
120: Nil/Empty Substitution - Passing a `nil` map to `Substitute` will cause a runtime panic.
144: Nil/Empty Substitution - Passing a `nil` map to `Substitute` will cause a runtime panic.
168: Nil/Empty Substitution - Passing a `nil` map to `Substitute` will cause a runtime panic.
192: Nil/Empty Substitution - Passing a `nil` map to `Substitute` will cause a runtime panic.
203: Negative Indexing - `paramExprKey` with a negative integer creates a valid-looking but logically nonsensical key like `param[-1]`.
207: Negative Indexing - `retExprKey` with a negative integer creates a valid-looking but logically nonsensical key like `ret[-1]`.
211: Negative Indexing - `paramLenExprKey` with a negative integer creates a valid-looking but logically nonsensical key like `param[-1].len`.
215: Negative Indexing - `",320,7887,
analysis/domain/constraint/expr/expr_minmax.go,q0,"21 + Recursion/Depth Limits + A deeply nested `Min(Min(Min(...)))` structure that triggers a stack overflow during `Substitute`.
23 + Recursion/Depth Limits + A deeply nested `Min(Min(Min(...)))` structure that triggers a stack overflow during `Variables`.
23 + Recursion/Depth Limits + A deeply nested `Min(Min(Min(...)))` structure that triggers a stack overflow during `Eval`.
23 + Cyclic/Self-referential + An `Expr` implementation that returns itself in `Substitute`, causing infinite recursion.
23 + Cyclic/Self-referential + An `Expr` implementation that returns itself in `Variables`, causing infinite recursion.
23 + Cyclic/Self-referential + An `Expr` implementation that returns itself in `Eval`, causing infinite recursion.
31 + Nil/Empty Handling + A `Min` struct where `Left` or `Right` is a `nil` interface, causing a panic during `Eval`.
31 + Nil/Empty Handling + A `Min` struct where `Left` or `Right` is a `nil` interface, causing a panic during `Substitute`.
31 + Nil/Empty Handling + A `Min` struct where `Left` or `Right` is a `nil` interface, causing a panic during `Variables`.
31 + Nil/Empty Handling + A `Min` struct where `Left` or `Right` is a `nil` interface, causing a panic during `String()",320,8511,
analysis/domain/effect/capability/label/label.go,q0,"21: NormalizeLabel/Type Switch: A malformed or custom `effect.Label` implementation that returns a type not explicitly handled in the switch, testing the `default` fallback.
21: NormalizeLabel/Type Switch: A `label` that, when passed to `NormalizeLabel`, returns a nil interface or a type that triggers a panic during type assertion.
85: ReturnTransform/Pointer vs Value: A `returns.ReturnType` that is a pointer to a type not explicitly listed (e.g., a custom struct implementing the interface but not matching the specific pointer/value cases), testing the `default` fallback.
85: ReturnTransform/Pointer vs Value: A `returns.ReturnType` that is a nil pointer of a known type (e.g., `*returns.SameAs(nil)`), testing if the type switch handles nil pointers of concrete types correctly.",186,4962,
analysis/domain/effect/label.go,q0,"31: nil/empty handling + Passing a literal `nil` interface to ensure the function returns `nil` without a panic.
35: rare type combinations + Passing a non-pointer type that implements `Label` (e.g., a struct) to ensure it is returned as-is without attempting `Elem()`.
38: malformed input + Passing a pointer to a type that *does not* implement `Label` to ensure the type assertion `ok` handles the failure gracefully.
34: metatable edge cases + Passing a pointer to a type with a custom `IsNil()` behavior or a proxy object that might behave unexpectedly during `reflect.ValueOf`.",142,3961,
analysis/domain/constraint/expr/expr_simplify.go,q0,"33, 51, 68: Recursion/Depth Limits: A deeply nested tree of `Min`/`Max` nodes (e.g., `Min(Min(Min(...)))`) will cause a stack overflow due to the recursive `Simplify` calls.

68: Overflow/Precision: `BinOp` evaluation of `lc.Value` and `rc.Value` might result in integer overflow or precision loss if the constants are at the extreme limits of the underlying numeric type.

71: Malformed Input/Panic: If `BinOp.Eval(nil)` is called with an operation that is not implemented or expects a specific context/environment that is `nil`, it may trigger a panic.

71: Error Path: The `if ok` check handles cases where `Eval` fails (e.g., division by zero), but it silently falls back to returning the unsimplified `BinOp`, which might hide logic errors in the caller.

81: Rare Type Combinations: The identity simplification `x + 0 = x` only checks if the constant is exactly `0`; it does not handle cases where `lc.Value` might be a float `0.0` vs integer `0` if the type system distinguishes them.

94: Overflow/Edge Case: The `x * 0 = 0` simplification is a ""lossy"" optimization; if `x` is an expression that would normally panic (like a division by zero), this simplification bypasses that error.",318,8115,
analysis/domain/lattice/lattice.go,q0,NONE,2,124,
analysis/domain/effect/iteration/labels.go,q0,"28: nil/empty handling + A slice containing only `nil` pointers to `Iterator` (e.g., `[]effect.Label{&Iterator{}}` where the pointer itself is nil) to test the `normalized != nil` guard.

31: malformed input + A slice containing a non-iterator type that `effect.NormalizeLabel` returns as a type not handled by the `switch` (e.g., a raw string or integer) to ensure the loop continues rather than panicking.

45: rare type combinations + An `other` label that is a pointer to an `Iterator` (`*Iterator`) which might fail the `ok` check in the type assertion if `NormalizeLabel` doesn't dereference it.",158,4682,
analysis/domain/effect/mutation/labels.go,q0,"31: `expr.ExprEquals(m.LengthDelta, o.LengthDelta)` — Deeply nested or cyclic expression trees in `LengthDelta` could cause stack overflow or infinite recursion during equality checks.

31: `m.Target.Index == o.Target.Index` — Comparison of `ParamRef` indices where indices might be negative or represent out-of-bounds parameter references if not validated upstream.

48: `l.Delta == o.Delta` — Integer overflow/underflow if `Delta` is used in arithmetic elsewhere, or extreme values (e.g., `math.MinInt`) that might behave unexpectedly in string formatting.

63: `t.Target.Index == o.Target.Index && t.Value.Index == o.Value.Index` — Comparison of `TableMutator` where `Target` or `Value` refer to the same index (self-referential mutation), potentially masking logic errors in the analysis engine.",205,5129,
analysis/domain/constraint/numeric/numeric.go,q0,"51: overflow + `c.C` is `math.MinInt64` or `math.MaxInt64`, which might cause unexpected behavior in downstream solvers if they assume non-negative bounds or positive offsets.
138: overflow + `c.Offset` is `math.MinInt64`, which could lead to integer underflow when a solver calculates `len(arr) + offset`.
178: overflow + `c.M` is `0`, which would cause a division-by-zero panic in any solver attempting to evaluate the `ModEq` constraint.
178: malformed input + `c.M` is negative, which is mathematically valid but often unhandled in modular arithmetic implementations.
178: malformed input + `c.R` is negative or `abs(c.R) >= abs(c.M)`, violating standard modulo properties.
203: recursion/depth + `c.X` or `c.Array` are extremely deep/complex paths, potentially causing high latency or stack exhaustion during `a.Hash()` or `a.Equal()`.
203: nil/empty + `a` or `b` are ""empty"" paths (if `pathdom.Path` allows it), testing if `IsEmpty()` correctly prevents hash collisions or logic errors.",285,7395,
analysis/domain/constraint/numeric/diff/difference.go,q0,"41 + overflow + `safeAddInt64` with `math.MinInt64` and a large positive `b` could trigger the `a < math.MinInt64-b` check incorrectly if not careful (though Go's constant folding helps, runtime values are tricky).
78 + overflow + `safeAddInt64` with `a = math.MaxInt64` and `b = math.MaxInt64` to ensure clamping to `maxWeight` works.
118 + memory/growth + adding thousands of unique variable names to trigger repeated $O(V^2)$ slice reallocations and copies in `getOrCreateNode`.
148 + overflow + `AddConst` with `c = math.MinInt64` to ensure the `if c == math.MinInt64` guard prevents a panic on `-c`.
163 + overflow + `AddLowerBound` with `c = math.MinInt64` to ensure the guard prevents a panic on `-c`.
185 + empty/nil + calling `solve()` on a graph where `g.names` is empty (though `NewDifferenceGraph` prevents this, a manual struct init might not).
208 + overflow + Floyd-Warshall path relaxation where `g.dist[i][k]` is a large negative number and `g.dist[k][j]` is a large negative number, potentially underflowing `int64`.
235 +",320,9067,
analysis/domain/lattice/lift/product.go,q0,NONE,2,478,
analysis/domain/effect/ownership/labels.go,q0,"47 + Store.Into negative index + A `Store` label where `Into.Index` is a large negative number (e.g., `-2147483648`) to test the fallback string formatting logic.

52 + Store.Equals type mismatch + Comparing a `Store` label against a different type that `effect.NormalizeLabel` might return as a non-nil interface to ensure no panic occurs during type assertion.

66 + Send.FromParam boundary + A `Send` label with `FromParam` set to `math.MaxInt` or a negative value to see if the string representation or subsequent logic breaks.

104 + Opaque.Equals type mismatch + Passing a `nil` interface or a non-label type to `Opaque.Equals` to verify `effect.NormalizeLabel` and the type assertion handle unexpected types gracefully.",186,5584,
analysis/domain/path/address/local.go,q0,NONE,2,861,
analysis/domain/constraint/numeric/modular/modular.go,q0,"41: Modulus <= 0 + Passing a non-positive modulus (e.g., 0 or -1) to `Check` to ensure it returns `false` rather than panicking on division.
79: Modulus <= 0 + Passing a non-positive modulus to `IsConsistent` to ensure it returns `false` rather than panicking.
105: Modulus <= 0 + Passing a non-positive modulus to `CountInRange` to ensure it returns -1 (unknown) or 0 rather than panicking.
116: Modulus <= 0 + Passing a non-positive modulus to `countInRangeImpl` to ensure it returns 0 rather than panicking.
116: upper < lower + Providing an inverted range (e.g., `lower=10, upper=5`) to ensure it returns 0.
121: residue % modulus + Passing a negative residue (e.g., -1) to ensure it is normalized to a positive value before comparison.
128: first % modulus + Passing a negative `lower` bound to ensure the starting point calculation handles Go's negative modulo correctly.
138: upper - first + Providing a massive range (e.g., `lower=-9e18, upper=9e18`) to check for integer overflow during the subtraction or division.
138: (upper-first)/modulus + Providing a very large `modulus` (e.g., `math",320,9171,
analysis/domain/effect/param_ref.go,q0,"14: Negative index boundary + `ref.Index = -1` with `argCount = 0` (should return false, but tests often only check positive indices).
14: Negative index boundary + `ref.Index = -1` with `argCount = 1` (tests the exact boundary of the ""last"" element logic).
23: Out-of-bounds negative + `ref.Index = -100` with `argCount = 1` (tests the wrap-around logic failing to stay within positive bounds).
23: Out-of-bounds positive + `ref.Index = 100` with `argCount = 1` (tests the upper bound check for indices larger than the argument list).
23: Zero-argument edge + `argCount = 0` with any `ref.Index` (tests if the function correctly handles empty argument lists without panicking).",197,5896,
analysis/domain/effect/compose.go,q0,"14 + Tail Name Collision + `r1.Tail.Name` and `r2.Tail.Name` are both empty strings or identical non-nil values, testing the string concatenation logic.
15 + Tail Name Collision + `r1.Tail.Name` and `r2.Tail.Name` are extremely long strings that could cause memory pressure during concatenation.
35 + Nil Label Equality + `NormalizeLabel(existing)` returns `nil`, which could cause a panic if `normalized.Equals(l)` is called without the `nil` check (though the code currently checks `normalized != nil`).
43 + Pure Intersection + `r1` or `r2` are `Pure` (empty/identity) but contain labels, testing if the short-circuit correctly ignores label overlap.
54 + Tail Name Mismatch + `r1.Tail.Name != r2.Tail.Name` where both tails are non-nil, resulting in a `nil` tail (the intersection of two different paths is an empty path).
66 + Subset Pure/Unknown + `r1` is `Pure` but `r2` is `Unknown`, testing the priority of the identity element in subset logic.
78 + Subset Open/Closed + `r1` is `Open` (contains a tail) and `r2` is `Closed` (no tail), testing the boundary of effect containment.
88 + Label Normalization Nil + `NormalizeLabel(label)` returns `nil` for a specific",320,9011,
analysis/domain/effect/postcondition/labels.go,q0,"49 + nil pointer dereference + Passing a `nil` pointer to `Equals` (e.g., `Present{}.Equals((*Present)(nil))`) tests the specific `o != nil` check.
73 + nil pointer dereference + Passing a `nil` pointer to `Equals` (e.g., `Absent{}.Equals((*Absent)(nil))`) tests the specific `o != nil` check.
96 + nil pointer dereference + If `n.Refinement` is `nil`, `refinementString` handles it, but `Equals` relies on `effect.NormalizeLabel` which might return `nil`.
101 + nil pointer dereference + If `n.Refinement` is `nil`, the `Hash` function must avoid calling `n.Refinement.Hash()` to prevent a panic.
106 + nil pointer dereference + If `a` or `b` is `nil`, the function must return `false` rather than panicking on `a.Equals(b)`.
111 + nil pointer dereference + If `refinement` is `nil`, the function must return `""<nil>""` instead of calling `.String()`.",255,6889,
analysis/domain/effect/mutation/transforms.go,q0,"48: nil/empty handling: Comparing `nil` against a non-nil `TypeTransform` to ensure the `a == nil || b == nil` short-circuit prevents panics.
52: rare type combinations: Passing a pointer to a type that is not explicitly handled in the `switch` (e.g., a custom implementation of `TypeTransform`) to trigger the `default: return false` path.
56: rare type combinations: Passing a pointer to `ElementUnion` when the logic expects a value type, testing the `*ElementUnion` case.
60: rare type combinations: Passing a pointer to `ContainerElementUnion` to ensure the dereference logic in `containerElementUnionEquals` doesn't crash.
64: rare type combinations: Passing a pointer to `ToArray` to verify the `*ToArray` case handles the interface-to-pointer conversion correctly.
88: nil/empty handling: Passing a `nil` pointer inside a `normalize` function (e.g., `*ElementUnion(nil)`) to ensure the `if tt != nil` check prevents a nil-pointer dereference.
98: nil/empty handling: Passing a `nil` pointer to `normalizeContainerElementUnion` to ensure the function returns `false` rather than panicking.
108: nil/empty handling: Passing a `nil` pointer to `normalizeToArray` to ensure the function returns `false` rather than panicking.",311,8356,
analysis/domain/effect/returns/reserved.go,q0,"28 + CorrelatedReturn.Equals + Comparing two `CorrelatedReturn` structs where `Indices` are nil vs empty slices (may return true/false depending on desired semantics of ""empty"").
31 + CorrelatedReturn.Equals + Comparing two `CorrelatedReturn` structs with extremely large `Indices` slices to check for slice iteration/memory overhead.
41 + reservedReturnTypeEquals + Passing a nil pointer of a type that is not explicitly handled in the switch (e.g., a nil `ReturnType` interface) to check for panic.
43 + reservedReturnTypeEquals + Passing a nil pointer of a type that *is* handled (e.g., `*DeepElementOf(nil)`) to ensure the `av != nil` check prevents a nil pointer dereference.
73 + deepElementOfEquals + Passing a `ReturnType` that is a pointer to a different type entirely to ensure `normalizeDeepElementOf` handles type mismatch safely.
78 + stringUnpackValueEquals + Passing a `ReturnType` where `Format.Index` is a maximum integer value or negative value.
83 + selectCaseOfParamEquals + Passing a `ReturnType` where `Source.Index` is an invalid or out-of-bounds index.
88 + selectResultOfCasesEquals + Passing a `ReturnType` where `Cases.Index` and `Default.Index` are identical or both zero.
93 + normalizeDeepElementOf + Passing a nil pointer of a type that is not `DeepElementOf` to",320,8121,
analysis/domain/effect/returns/labels.go,q0,"93: nil/pointer handling: Passing a nil pointer of a specific type (e.g., `(*ElementOf)(nil)`) to `returnTypeEquals` to test if the type switch and subsequent normalization handle nil-pointer dereferences.
103: rare type combinations: Passing a type that is not explicitly handled in the `switch` (e.g., a custom implementation of `ReturnType`) to ensure `reservedReturnTypeEquals` handles it without panicking.
113: nil/pointer handling: Passing a pointer to a struct that contains a nil `ParamRef` or an invalid index to `elementOfEquals`.
135: recursion/depth limits: Passing a `TypeProjection` where the `Projection` contains a deeply nested or cyclic path to test `projection.Equal`.
141: nil/pointer handling: Passing a nil pointer to `normalizeElementOf` (e.g., `*ElementOf(nil)`) to ensure the `if rr != nil` check prevents a panic.
149: nil/pointer handling: Passing a nil pointer to `normalizeOptionalElementOf` (e.g., `*OptionalElementOf(nil)`) to ensure the `if rr != nil` check prevents a panic.
157: nil/pointer handling: Passing a nil pointer to `normalizeCallbackReturn` (e.g., `*CallbackReturn(nil)`) to ensure the `if rr != nil` check prevents a panic.
165: nil/pointer handling:",320,8865,
analysis/domain/path/path.go,q0,NONE,2,128,
analysis/domain/effect/row.go,q0,"13: Nil Var pointer + Calling `v.String()` on a nil receiver to ensure it returns `""""` instead of panicking.
65: Nil Label in slice + `r.Hash()` where `NormalizeLabel(label)` returns nil, potentially causing a panic in `labelIdentity`.
85: Nil Label in slice + `r.String()` where `NormalizeLabel(l)` returns nil, testing the `if normalized != nil` guard.
101: Duplicate Labels + `r.With()` where the input `labels` contains elements already present in `r.Labels` (testing the `containsLabelEquals` deduplication).
114: Nil Label in slice + `r.Without()` where the `match` function is called on a `nil` normalized label.
122: Type Mismatch + `r.Equals(nil)` or `r.Equals(""string"")` to ensure the type assertion `other.(Row)` handles non-Row types gracefully.
138: Nil Tail + `r.equalsRow` where one row has a `Tail` and the other has a `nil` Tail, testing the XOR-like logic for tail presence.
148: Nil Label + `labelIdentity(nil)` where the label is nil, testing the specific `""<nil>""` string branch.
149: Pointer vs Value + `labelIdentity` where the label is a pointer to a struct vs the struct itself, testing the `reflect.Elem()` logic.",319,8275,
analysis/domain/lattice/factset/factset.go,q0,"48: `Admit` / `Valid` / `Key` / `EqualFact` / `Less` / `CloneFact` / `Prefer` / `Dominates`: If any of these function pointers are `nil` when called, the program will panic with a nil pointer dereference.

74: `Normalize`: If `Admit` returns a fact that is itself a pointer to a structure that is mutated by a subsequent `Key` or `EqualFact` call, the map keying will break.

103: `Equal`: If `a` and `b` are extremely large, the double call to `Normalize` (which involves sorting and $O(N^2)$ compression) may cause a timeout or CPU exhaustion.

114: `LessOrEq`: If `s.Intersect` is true, but `s.Key` is not a pure function (e.g., it relies on external state), the `keySet` comparison will yield non-deterministic results.

126: `Join` (Intersect): If `a` and `b` are large, the intersection logic creates a new slice and then calls `Normalize` again, potentially leading to $O(N^2)$ or $O(N^3)$ complexity depending on the `Dominates` implementation.

138: `Join` (Union): If `a` or `b` contain self-referential pointers, `s.Clone(a)` or `s.Clone(b)` may trigger infinite recursion",320,8446,
analysis/domain/lattice/factmap/factmap.go,q0,"74: nil/empty handling + Passing a `nil` `m.Domain` will cause a panic when calling `m.Domain.Bottom()`.
84: nil/empty handling + If `m.Valid` is provided but returns `false` for all elements, `Normalize` returns `nil` (correct, but often missed in tests).
88: malformed input + If `m.WithValue` returns a fact that is not ""valid"" or is ""bottom"", it may violate the canonical form invariants.
90: cyclic/self-referential + If `m.WithValue` or `m.collide` returns a fact that contains a pointer to the original `existing` fact, `Normalize` could enter an infinite loop or create a cyclic structure.
105: nil/empty handling + `Clone` returns `nil` for an empty slice, which is fine, but if `m.CloneFact` is expected to handle specific identity logic, it is bypassed for empty inputs.
121: malformed input + `Equal` calls `Normalize` on both inputs; if `Normalize` is not idempotent or has side effects, `Equal` will behave unpredictably.
133: rare type combinations + In `Intersect` mode, if `a` is a subset of `b` but `b` contains keys not in `a`, `LessOrEq` returns `true` (this is the ""must map"" logic, but can be counter-intuitive).
143: mal",320,8702,
analysis/domain/path/address/root.go,q0,"29: nil/empty handling + A `pathdom.Path` where both `Symbol` is `0` and `Root` is `""""` (empty string) tests the fallback logic of `RootOfPath`.
41: malformed input + A `Root` struct manually initialized with `kind: rootSymbol` but `symbol: 0` (bypassing the constructor) to test the guard in `Symbol()`.
46: malformed input + A `Root` struct manually initialized with `kind: rootName` but `name: """"` (bypassing the constructor) to test the guard in `Name()`.
55: rare type combinations + Comparing a `rootSymbol` kind with a `rootName` kind where both have zero-value fields (`symbol: 0`, `name: """"`) to ensure `Equal` doesn't return a false positive.
59: malformed input + A `Root` with `kind: rootInvalid` (the `iota` default) to ensure `isValid()` correctly rejects uninitialized or corrupted states.",227,5797,
analysis/domain/lattice/lift/map.go,q0,"48 + topSentinel + A `topSentinel` map with a non-zero length could theoretically be passed in by a user, but the `isTop` check relies on pointer identity, which might fail if the map is copied or shadowed.
63 + canonicalize + If `elem.Equal` is not reflexive or has side effects, the loop that iterates over `m` to find a `bot` value might behave unpredictably or skip keys.
68 + canonicalize + The `out := make(map[K]V, len(m)-1)` logic is slightly flawed; if multiple keys are `bot`, it may under-allocate or require multiple passes, though it only performs one pass.
94 + Equal + If `a` or `b` are `nil` (representing `Bottom`), the `isTop` check passes, but the subsequent `for k, av := range a` will work fine, while `b[k]` on a `nil` map is safe in Go but logically requires `bot`.
114 + LessOrEq + If `isTop(b)` is true, it returns `true` immediately, but if `isTop(a)` is true and `isTop(b)` is false, it returns `false`, which is correct but relies on `isTop` being perfectly implemented.
131 + Join + If `a` and `b` are both non-empty but `pointwiseLessOrEq` is called, a very large number of keys could",320,9074,
analysis/domain/lattice/lift/must.go,q0,"44 + nil/empty handling + `MustMapValues` with a `nil` map might return a `MustMapLane` where `values` is `nil`, potentially causing panics in callers expecting a non-nil map.
53 + nil/empty handling + `Clone` on a lane with a `nil` map returns a lane with a `nil` map, which is indistinguishable from an empty map in some contexts.
65 + nil/empty handling + `Top` returns a lane with a `nil` map, which may cause panics if the caller immediately attempts to iterate over `Values()` without a nil check.
73 + nil/empty handling + `Equal` comparing a `nil` map (Top) and a non-nil empty map might return `true` or `false` depending on how `finiteMapEqual` handles `nil` vs `len 0`.
78 + nil/empty handling + `LessOrEq` comparing a `nil` map (Top) against a populated map might return `false` incorrectly if `finiteMustMapLessOrEq` doesn't handle `nil` `a` gracefully.
87 + nil/empty handling + `Join` with a `nil` map (Top) and a populated map returns a `nil` map, effectively treating Top as an identity for Join, which is correct but requires careful `nil` handling in `finiteMustMapJoin`.
95 + nil/empty handling + `Widen` with a `nil` map",320,9039,
analysis/domain/path/address/address.go,q0,"41: Empty Key -> Passing an empty string to `StableFromKey` to ensure it returns the zero-value and `false` rather than panicking during parsing.
44: Resolver Path Collision -> A key that is valid for `ParseResolverPath` but should be rejected as a `Stable` address.
47: Ambiguous Encoded Key -> A key that `looksEncodedNamedRootKey` identifies as encoded but fails the actual `parseEncodedNamedRootKey` logic.
58: Invalid Root Identity -> A `Stable` object initialized with a `Root` that fails `isValid()`, testing if `StableKey` and `Path` correctly return zero-values/false.
88: Zero-length Append -> Appending an empty slice to a `Stable` address to ensure it returns a valid copy of itself rather than a new identity.
89: Large Append Allocation -> Appending a massive slice of segments to check for potential memory exhaustion or integer overflow in `make`.
98: Parent of Root -> Calling `Parent()` on an address that has zero segments to ensure it returns `false` instead of a slice bounds error.
99: Parent of Empty Suffix -> Calling `Parent()` on a `Stable` address where `a.suffix.segments` is nil/empty to ensure it handles the slice index safely.
108: Root Mismatch -> Comparing two `Stable` addresses with different `Root` types (Symbol vs Named) to ensure `Equal` doesn't crash on type-specific",320,8694,
analysis/domain/path/address/key.go,q0,"32: nil/empty handling + A `sym` of 0 returns an empty string, which might collide with a valid empty `pathdom.PathKey` if not carefully distinguished.
43: malformed input + A `key` that starts with 's' followed by non-digits or an extremely large integer that overflows `uint64` during parsing.
58: malformed input + A `key` that mimics the `n[len]:[root]` format but contains invalid UTF-8 or control characters in the `root` string.
74: malformed input + A `key` that starts with `n` followed by a length prefix that is mathematically impossible (e.g., `n100:abc` where the string is shorter than 100).
83: malformed input + A `key` that starts with `n` but has no colon or has a colon at an index that causes an out-of-bounds slice panic.
87: malformed input + A `key` that starts with `s` but is followed by a non-digit, potentially causing `looksStableSymbolRootSuffix` to return false incorrectly.
94: malformed input + A `key` that starts with `sym` but has a non-digit at index 3, or a length that causes an out-of-bounds access during the digit loop.
103: malformed input + A `key` that starts with `sym` but uses a delimiter like `@",320,8404,
analysis/domain/path/address/parser.go,q0,"34: nil/empty segments + `cloneSegments` might panic or return a nil slice if `parsed.segments` is nil.
48: version boundary + a version of `0` or `-1` (though `int` is used, the logic specifically checks `<= 0`).
64: segment type mismatch + comparing a `SegmentField` with a `SegmentIndexNumber` (if such a kind exists) to ensure it doesn't falsely equate.
74: segment identity + two different segment instances with the same `Name` but different `Kind` (e.g., Field vs IndexString).
85: symbol/version boundary + `sym = 0` or `version = -2147483648` (min int) to check for invalid identity generation.
94: empty/malformed input + `pathKey` or `contextKey` being an empty string or a string that doesn't follow the `symN@V` pattern.
98: symbol mismatch + `pathKey` and `contextKey` having different `symbol.ID` values but identical segment structures.
111: malformed placeholder + a `parsed.root` that looks like a placeholder (e.g., `$0`) but contains non-numeric characters or exceeds integer bounds.",281,7977,
analysis/domain/path/address/root_suffix.go,q0,"48: Overflow/Malformed Integer: A `sym` prefix followed by a number exceeding `uint64` limits or a non-numeric character immediately after `@`.
63: Overflow/Malformed Integer: A `@` symbol followed by a negative number or a number that overflows the internal `ParsePositiveIntAfterAt` logic.
76: Zero/Empty Value: A `prefix` followed by `0` (e.g., `""s0""`), which is explicitly rejected as an invalid root.
86: Integer Overflow: A very large `length` value in an encoded key (e.g., `""n9999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999999",320,7567,
analysis/domain/path/address/suffix.go,q0,"41: Parent + nil/empty handling + Calling `Parent()` on a zero-value `Suffix` where `segments` is `nil` to ensure it returns `false` rather than panicking.

51: Equal + rare type combinations + Comparing a `Suffix` with a non-nil but empty `segments` slice against a `Suffix` with a `nil` `segments` slice.

63: HasPrefix + boundary conditions + Passing a `prefix` that is identical to `s` (length equality) to ensure the loop executes correctly.

75: RemainderAfterPrefix + boundary conditions + Passing a `prefix` that is exactly equal to `s` to ensure it returns an empty slice and `true` rather than `nil, false`.

101: RelativeStaticMemberSuffixKey + nil/empty handling + Passing a `nil` slice to ensure it returns `"""", false` instead of an empty string key.

110: FieldCanonicalRelativeStaticMemberSuffixKey + error paths + Passing segments that result in `changed == false` to ensure it returns the ""not found"" signal.

117: RelativeStaticMemberSuffixSegments + malformed input + Passing a `pathdom.PathKey` that contains invalid or non-parsable segment characters.

123: cloneSegments + nil/empty handling + Passing a `nil` slice to ensure it returns `nil` rather than an empty allocated slice `[]segment.Segment{}`.",309,7043,
analysis/domain/value/axis/key.go,q0,NONE,2,476,
analysis/domain/placement/placement.go,q0,"79: Invalid Type/Overflow: Passing a `Value` cast from an out-of-bounds `uint8` (e.g., `Value(255)`) to `String()` to trigger the `default` case.
79: Invalid Type/Overflow: Passing a `Value` cast from an out-of-bounds `uint8` (e.g., `Value(255)`) to `Join`, `Meet`, or `LessOrEq` to test behavior of comparison logic on undefined lattice elements.",114,3140,
analysis/domain/path/placeholder.go,q0,"11: Negative/Non-numeric suffix + A string like `""$abc""` or `""$""` where the prefix is correct but the suffix is invalid.
12: Non-dollar prefix + A string like `""123""` or `""a$1""` that lacks the leading `$` required for the canonical form.
18: Empty suffix + A string like `""$""` which satisfies the length check but contains no digits to parse.
23: Integer Overflow + A string like `""$9223372036854775808""` (on 64-bit) that exceeds `math.MaxInt`.
27: Non-digit characters + A string like `""$12a3""` where a non-numeric character is embedded within a numeric sequence.",173,4360,
analysis/domain/path/format.go,q0,"23: nil nameResolver + p.Symbol != 0: If `nameResolver` is nil, it skips the symbol lookup and returns `p.Root`, which might be an empty string or a placeholder, potentially masking a missing symbol.
36: empty path: If both `Root` and `Symbol` are zero/empty, it returns an empty string, which might be indistinguishable from a path that was intended to be a root-only path.
53: SegmentIndexInt overflow: If `seg.Index` is `math.MinInt64`, `strconv.Itoa` handles it, but if the logic assumes non-negative indices, this could represent an invalid Lua table index.
75: empty path: If `IsEmpty()` returns true for a path with a non-zero `Symbol` (depending on implementation), `Key()` returns an empty string, losing identity.
79: version 0 vs unversioned: If `p.Version` is `0`, it is treated as unversioned; if `0` is a valid version in the domain, the key becomes ambiguous.
100: empty path: If `Root` and `Symbol` are both zero, it returns `0`, which might collide with a valid hash of a different empty-state object.
105: Symbol/Version collision: If `p.Symbol` is non-zero but `p.Version` is also non-zero, the hash mixes them; if `p.Version` is extremely large",320,7100,
analysis/domain/path/address/structural_key.go,q0,"41: Nil/Empty segments + `StructuralKeyFromPathKey` returns a valid `local` key with a `nil` segments slice, potentially causing panics in downstream consumers expecting initialized slices.

58: Mismatched address spaces + `HasPrefix` returns `false` when comparing a `local` key to a `stable` key, which is a silent logical divergence rather than an error.

75: Mismatched address spaces + `RemainderAfterPrefix` returns `nil, false` when comparing `local` vs `stable`, which might mask logic errors in callers that assume `HasPrefix` and `RemainderAfterPrefix` are always called on compatible types.

86: Empty suffix + `Append` returns the original key but performs a string comparison `k.PathKey() != """"` which could fail if the original key was valid but its string representation is empty.

90: Large suffix + `Append` performs a manual slice allocation and two `copy` calls that could trigger an OOM if `suffix` is an extremely large slice provided by a malicious or malformed input.

123: Identity transformation + `FieldCanonicalPathKey` returns `"""", false` if the transformation results in a key identical to the input, which might be unexpected if the caller expects a ""canonical"" version regardless of change.

143: Mismatched address spaces + `RebasePathKey` returns `"""", false` if `from` and `to` are different kinds (local vs stable), which is a critical error path",320,8888,
analysis/domain/path/construction.go,q0,"49: Clone + Empty Segments + Nil/Zero Value: Calling `Clone()` on a zero-value `Path` where `Segments` is nil might return a struct that behaves differently than a non-nil empty slice in downstream iterations.

63: Parent + Zero-Value Path: Calling `Parent()` on a `Path{}` (where `Segments` is nil) returns a new `Path{}`, but if the receiver has a `Root` or `Symbol` but no segments, it resets those fields to zero-values, potentially losing the identity of the root.

88: DirectFieldName + Empty String Name: A segment with `Kind: SegmentField` but `Name: """"` will return `false`, which might be an error if the Lua code actually contains an empty string identifier (rare but possible in some AST representations).

104: Append + Empty Receiver: If `p.IsEmpty()` is true, `Append` returns a literal `Path{}`, which effectively discards the `seg` being appended; this means you cannot ""start"" a path by appending to an empty one, making the constructor `NewPath` strictly mandatory.

105: Append + Slice Capacity/Aliasing: The logic `next.Segments = append(next.Segments, p.Segments...)` followed by another `append` is safe, but if `p.Segments` is extremely large, this creates a massive allocation spike for every single step in a deep path.",307,8203,
analysis/domain/path/placeholder_path.go,q0,"20: Negative index handling + Passing a negative integer to `NewPlaceholder` to ensure it returns a zero-value `Path` rather than a malformed `$ -1` string.

31: Symbol/Placeholder invariant + A `Path` with `Symbol == 0` but a `Root` that is not a valid placeholder (e.g., `""foo""`) to test if `IsPlaceholder` correctly fails.

42: Invariant violation + A `Path` with `Root == ""something""` but `Symbol == 0` to trigger the ""resolved path missing Symbol"" error string.

63: Out-of-bounds substitution + A placeholder `$10` being substituted against an `args` slice of length 2 to ensure it returns `false` instead of panicking.

68: Empty argument substitution + A placeholder `$0` being substituted with a `Path{}` (empty) to ensure it returns `false` per the requirement ""or arg path empty"".

72: Segment concatenation/Shallow copy + A placeholder being substituted with an `argPath` that has a large number of `Segments` to check for unexpected behavior during slice appending.",247,5593,
analysis/domain/state/key/value.go,q0,"21: nil/empty handling: An empty `Value("""")` string passed to `ParseSymbolValue` tests the length check and prefix validation.
22: malformed input: A `Value` starting with 's' but followed by non-numeric characters (e.g., `""sabc""`) tests the `ParseUnsignedDecimal` failure path.
25: boundary condition: A `Value` representing a zero ID (e.g., `""s0""`) tests the logic that treats zero as an invalid/empty symbol.
34: overflow: A `Value` representing a number larger than `math.MaxInt` (e.g., `""r9223372036854775808""`) tests the integer overflow protection during conversion to `int`.
34: malformed input: A `Value` starting with 'r' but containing non-numeric characters (e.g., `""r-1""` or `""r_x""`) tests the unsigned parser error path.",221,5817,
analysis/domain/value/axis/evidence/evidence.go,q0,"89: malformed input + An integer cast to `Value` that is outside the defined `iota` range (e.g., `Value(255)`) will trigger the `default` case in `String()`.
98: rare type combinations + `Meet(gradualTop, explicitTop)` returns `bottom`, which is a critical semantic boundary where two ""top-like"" values result in ""unreachable"" evidence.
108: malformed input + `Covers` relies on `Join`; if `v` or `other` are invalid `Value` types (e.g., `Value(10)`), the lattice logic may return mathematically inconsistent results for the `Covers` relation.",153,4299,
analysis/domain/path/identity.go,q0,"16-21: Mixed Symbol/Root identity mismatch; one path has a `Symbol` while the other has an identical `Root` but `Symbol == 0`, testing if the precedence logic correctly rejects the match.
24-26: Nil/Empty Segment slice; comparing a path with `Segments = nil` against one with `Segments = []Segment{}` to ensure length-based equality holds for empty collections.
30-34: Segment field mismatch; testing if a single field difference (e.g., `Index` vs `Name`) in a very long segment list is caught before the loop terminates.
51-55: Symbol/Version tie-breaking; testing two paths with identical `Symbol` values but different `Version` values to ensure the ordering is strictly deterministic.
56-57: Symbol/Root fallback; testing a path with a `Symbol` against a path with a `Root` but no `Symbol` to ensure the `else if` correctly prioritizes the `Root` comparison.
60-62: Segment length tie-breaking; testing two paths with identical `Symbol`/`Root` where one has a massive number of segments and the other has zero.
64-76: Segment field ordering; testing the stability of the sort when `Kind`, `Name`, and `Index` are all identical except for one field in a deep segment list.",297,8463,
analysis/domain/value/axis/escape/escape.go,q0,"104: malformed input + A `Value` cast from an arbitrary `uint8` (e.g., `Value(255)`) will bypass the `bottom/fresh/escaped` constants and trigger the `default` case in `String()`.
100: overflow/boundary + While the lattice is small, `Join` and `Meet` rely on the underlying `uint8` comparison; if a user manually injects a value like `Value(255)`, `Join(Value(255), escaped)` will return `255`, potentially breaking lattice invariants in higher-level analysis.
88: rare type combinations + If `Value.Hash()` is used in a map where the key is a custom type that wraps `Value`, the `internal.MixHash` must ensure no collisions occur between the `escape` salt and the `uint64(v)` value.",194,5884,
analysis/domain/value/axis/reducer.go,q0,"41: Type Mismatch Panic + A `Reader` implementation that returns a type different from the generic `T` requested by `Get` (e.g., `Get[int]` when the underlying map holds a `string`), triggering the explicit panic.

41: Nil Reader + Passing a `nil` interface to `Get`, which should return the zero value and `false` rather than panicking.

41: Key ID Collision + A `Reader` that returns a value for a key ID that exists but is of an incompatible type, testing the strictness of the type assertion.

34: Nil Writer + Passing a `nil` interface to `Set`, which should return silently without attempting to call `SetAny`.

34: Zero-Value/Nil Value + Passing a `nil` value of a pointer/interface type to `Set`, testing if the `Writer` implementation handles nil-valued `any` correctly.",196,4846,
analysis/domain/path/segment/segment.go,q0,"83: Malformed field access + A suffix starting with a dot but containing no name (e.g., `"".""` or `"".[1]""`) which triggers the `end == 1` error path.
95: Malformed string index + A quoted index that is truncated before the closing quote (e.g., `[""key`) or missing the closing bracket (e.g., `[""key""`).
108: Malformed integer index + An integer index containing non-digit characters or invalid symbols (e.g., `[12a3]`).
115: Integer overflow + An integer index value that exceeds the maximum capacity of a signed `int` (e.g., `[9223372036854775808]`), causing `strconv.Atoi` to fail.
131: Malformed escape sequence + A quoted string containing an invalid escape character (e.g., `[""\z""]`) which should trigger the `(s[i] != '\\' && s[i] != '""')` error path.
131: Truncated escape sequence + A quoted string ending with a trailing backslash (e.g., `[""key\""]`) which causes an out-of-bounds read or error.
145: Malformed unquote + A quoted string with a trailing backslash that is not followed by a valid escape character (e.g., `[""\x""]`).
164",320,8431,
analysis/domain/value/axis/registry.go,q0,"53: nil registry + Calling `Freeze()` on a nil pointer triggers a panic.
64: nil registry + Calling `RegisterErased` on a nil pointer returns an error instead of panicking.
67: nil spec + Passing a nil `ErasedSpec` interface to `RegisterErased` returns an error.
70: frozen registry + Attempting to register a new spec after `Freeze()` has been called returns an error.
73: empty spec id + An `ErasedSpec` implementation that returns an empty string for `ID()` returns an error.
76: duplicate spec + Registering two different spec instances that share the same `ID()` string returns an error.
101: nil registry + Calling `Specs()` on a nil pointer returns `nil` rather than panicking.
113: nil registry + Calling `Reducers()` on a nil pointer returns `nil` rather than panicking.
118: nil registry + Calling `SpecsView()` on a nil pointer returns an empty `SpecsView` struct.
124: nil registry + Calling `ReducersView()` on a nil pointer returns an empty `ReducersView` struct.",253,6068,
analysis/domain/value/product/runtime_validation.go,q0,NONE,2,200,
analysis/domain/value/axis/presence/presence.go,q0,"46 + panic/error returns + Passing an `axis.Reader` that does not implement the `Reader` interface (e.g., a custom struct that only implements `axis.Reader` but lacks the `Presence()` method).
55 + panic/error returns + Passing an `axis.Writer` that does not implement the `Writer` interface (e.g., a struct that implements `Reader` but lacks `SetPresence()`).
108 + malformed input + Passing a `Value` cast from an invalid `uint8` (e.g., `Value(5)`) to `Join`, which relies on the assumption that only the four defined constants exist.
124 + malformed input + Passing a `Value` cast from an invalid `uint8` (e.g., `Value(255)`) to `Meet`, which relies on the assumption that only the four defined constants exist.
151 + malformed input + Calling `String()` on a `Value` cast from an invalid `uint8` (e.g., `Value(10)`) to ensure the `default` case handles out-of-bounds enum values.",249,6753,
analysis/domain/value/product/registry.go,q0,"26: nil/empty handling: A `nil` spec passed to `registerProductSparseAxis` triggers `reg.RegisterErased(nil)`, which may cause a panic if the registry implementation expects a non-nil interface.

36: malformed input: A spec that returns `presence.Key.ID()` for its `ID()` method triggers a specific error path to prevent core lane collision.

39: rare type combinations: A spec that returns `true` for `HasMeet()` but has a broken or panicking `Meet()` implementation (not visible here but implied by the validation) would bypass this check but fail during usage.",134,3013,
analysis/domain/value/product/runtime_compare.go,q0,"24: unregistered axis slot + A `slot.key` exists in the product definition but was never registered in the `registryRuntime`, triggering a panic.
27: bottom value intersection + A slot contains a value that is technically not `presence.Bottom()` but is logically equivalent to the axis's `bottomAny` (e.g., a specific NaN or null-equivalent in a custom type).",85,1951,
analysis/domain/value/product/runtime_hash.go,q0,"41: panic: unregistered axis slot + A `slot.key` that exists in the `slots` slice but was never registered in the `registryRuntime`, triggering a runtime panic during hash calculation.",42,1029,
analysis/domain/value/axis/runtimekind/runtimekind.go,q0,"78: invalid tag: Passing a `Tag` value $\ge$ `tagCount` (e.g., `Tag(255)`) to `Singleton` to ensure it returns `Bottom` rather than panicking.
86: invalid tag: Passing a `Tag` value $\ge$ `tagCount` to `Without` to ensure the loop `continues` safely without corrupting the mask.
101: invalid tag: Passing a `Tag` value $\ge$ `tagCount` to `Contains` to ensure it returns `false` instead of performing an out-of-bounds bit shift.
138: invalid tag: Passing a `Tag` value $\ge$ `tagCount` to `tagMask` to trigger the explicit `panic` (verifying the safety boundary).
144: malformed input: Passing a string that does not match any `tagNames` to `ParseTag` to ensure it returns `(0, false)` instead of a valid tag.
151: malformed input: Passing an empty string `""""` to `ParseTag` to ensure it handles the non-match gracefully.",244,6438,
analysis/domain/value/axis/assertion/assertion.go,q0,"81: Malformed input/Bitmask overflow: Passing a `Flag` with bits set outside of `knownFlags` (e.g., `Of(Flag(0xFF))`) to see if `Of` correctly masks them or if `Has` behaves unexpectedly.
104: Nil/Empty handling: Calling `Flags()` on a `Value` where `state` is `concrete` but `flags` is `0` (though `Of` tries to prevent this, manual construction could bypass it).
118: Rare type combinations: `Join(Top(), Bottom())` where the interaction between the ""unreachable"" state and the ""no information"" state might violate lattice properties if not strictly ordered.
131: Rare type combinations: `Meet(Top(), Bottom())` where the identity of `Top` (no info) meets `Bottom` (unreachable) to ensure it returns `Bottom` rather than `Top`.
143: Rare type combinations: `Combine(Top(), Bottom())` where combining ""no info"" with ""unreachable"" must result in ""unreachable"".
164: Malformed input: `Equal(a, b)` where `a` and `b` have different `state` values but identical `normalizedFlags` (e.g., `state: top` vs `state: concrete` with `flags: 0`).
176: Malformed input: `Hash()` on a `Value` with an undefined/invalid `state` (e.",320,8538,
analysis/domain/value/axis/identity/identity.go,q0,"33: Nil/Zero ID handling: Passing `symbol = 0` to `LuaFunction` returns a zeroed `ID`, which might collide with an uninitialized `ID` in other parts of the system.

47: Nil/Zero ID handling: Passing `graphID = 0` or `exprRef = 0` to `LuaTableLiteral` returns a zeroed `ID`, potentially causing identity collisions between different uninitialized table expressions.

56: Overflow/Hash Collision: `id.Index` is a `uint64`; if `internal.MixHash` does not handle large bit distributions well, high-entropy indices could cause hash collisions in the lattice.

79: Nil/Zero ID handling: Calling `ID()` on a `Value` with `state == bottom` or `state == top` returns a zeroed `ID`, which could be misinterpreted as a valid identity if the caller ignores the boolean return.

108: Lattice Logic (Join): If `a.id == b.id` is true for two different `state` values (e.g., one is `top` and one is `singleton`), the logic relies on the `state == top` check occurring first to prevent returning a `singleton` when it should be `top`.

123: Lattice Logic (Meet): If `a.id == b.id` is true for two different `state` values (e.g., one is `bottom` and one is `singleton`), the logic relies on the `",320,7695,
analysis/domain/value/typevalue/normalize.go,q0,NONE,2,92,
analysis/domain/value/axis/spec.go,q0,"48: Nil/Incomplete Spec: Providing a `Spec` where `LessOrEq` is nil but `Equal` and `Join` are present triggers the automatic derivation logic.
58: Nil/Incomplete Spec: Providing a `Spec` where `Widen` is nil triggers the automatic fallback to `Join`.
63: Panic/Validation: Calling `Lattice()` or `Erase()` on a `Spec` with a missing required function (e.g., `Bottom` or `Hash`) triggers a panic via `mustValidate`.
118: Type Mismatch/Panic: Passing a value to `MeetAny` that does not match the underlying type `T` causes a panic in `cast`.
122: Type Mismatch/Panic: Passing a value to `WidenAny` that does not match the underlying type `T` causes a panic in `cast`.
126: Type Mismatch/Panic: Passing a value to `HashAny` that does not match the underlying type `T` causes a panic in `cast`.
131: Type Mismatch/Panic: Passing a value to `cast` that is not of type `T` (e.g., passing a `nil` to a non-nullable `T` or a different struct) triggers a panic.",279,6943,
analysis/domain/value/typevalue/materialize.go,q0,NONE,2,761,
analysis/domain/value/typevalue/literal.go,q0,NONE,2,921,
analysis/domain/value/typevalue/reconstruct.go,q0,NONE,2,390,
analysis/domain/value/axis/typewitness/typewitness.go,q0,"53: nil/empty handling + `Of` receives a `nil` type after `unwrap.Alias` is called, testing the redundant nil check.
54: rare type combinations + `Of` receives a type that satisfies `typ.IsAny` or `typ.IsUnknown` (e.g., a malformed or uninitialized type node).
57: rare type combinations + `Of` receives a `*typ.TypeParam` or `*typ.Generic` which should be treated as `Top` because they lack concrete runtime evidence.
59: recursion/depth limits + `Of` receives a deeply nested `*typ.Instantiated` type where `refinement.ContainsFreeTypeParam` might trigger a stack overflow.
74: nil/empty handling + `Type()` is called on a `concrete` state `Value` where the internal `t` field was manually set to `nil` (bypassing `Of`).
86: cyclic/self-referential types + `Join` uses `typ.SameNodeOrAcyclicEqual` which may hang or panic if the type graph contains a cycle.
89: rare type combinations + `Join` receives two different literal types (e.g., different numeric subtypes) that `typelit.MergeFamilyBases` must reconcile.
113: cyclic/self-referential types + `Equal` uses `typ.SameNodeOrAcyclicEqual` on two types that form a cycle, testing the ""Acyclic""",320,7663,
analysis/domain/value/product/intern.go,q0,"48: `normalizeShape` + Invalid Shape: Passing a `Shape` value that is not `ShapeBottom` or `ShapeTop` (e.g., a zero-value or bit-flipped integer) to trigger the panic.
61: `normalizePresence` + Invalid Presence: Passing a `presence.Value` that does not match any of the four known states to trigger the panic.
73: `Hash` + Nil Value: Passing a `Value` where `v.n == nil` to ensure it correctly defaults to the `Top` hash rather than panicking.
76: `Hash` + Corrupted/Invalid Value: Passing a `Value` with a non-nil `n` that contains an invalid internal state to trigger `rt.validateValue`.
38: `internRuntime` + Hash Collision: Providing two different `shape/p/slots` combinations that produce the same `h` to test the `rt.sameNode` equality fallback.
34: `internRuntime` + Empty/Top Identity: Providing `ShapeTop`, `presence.Top()`, and `len(slots) == 0` to ensure the specialized `Top()` singleton is returned.
28: `internRuntime` + Slot Canonicalization: Providing `slots` that are logically identical but structurally different (e.g., different order or redundant entries) to test `rt.canonicalSlots`.",296,7276,
analysis/domain/value/product/domain.go,q0,"44: nil/empty handling: `a.n` or `b.n` being nil (representing `Bottom`) while other fields are populated could cause unexpected behavior in `equalRuntime`.
58: malformed input: `rt.axes` being empty or containing axes that return incompatible types for `rt.axisValue` during comparison.
85: rare type combinations: `ShapeOf(a)` and `ShapeOf(b)` having different underlying structures that `shapeLessOrEq` might not catch if they share a common interface.
106: malformed input: `rt.axes` containing axes where `spec.spec.LessOrEqAny` panics due to unexpected type assertions on the axis values.
123: nil/empty handling: `a.n` or `b.n` being nil (Bottom) results in `Top()` via `mergeRuntime`, which might be incorrect if the lattice isn't bounded correctly.
134: huge unions/intersections: `rt.canonicalAxes` being extremely large, causing `slots` allocation to spike or `mergeRuntime` to time out.
143: malformed input: `axisMerge` returning a value that `spec.spec.IsTopAny` fails to identify, leading to an invalid `internRuntime` call.
158: nil/empty handling: `a.n` or `b.n` being nil (Bottom) returns the other value, which is correct for Meet, but only if the",320,8077,
analysis/domain/value/variant/internal/discriminant/detector.go,q0,NONE,2,107,
analysis/domain/value/axis/variantorigin/variantorigin.go,q0,"54: nil/empty handling: `Of` with a `nil` or empty `cases` slice returns `Bottom`, which might bypass logic expecting a `concrete` state for valid families.
66: nil/empty handling: `Cases()` returns a copy, but if `v.cases` is `nil` (from a `Bottom` or `Top` value), it returns an empty slice rather than `nil`.
76: huge unions: `Join` performs two `append` operations and then calls `Of`, which performs a `Sort` and `Compact`, leading to $O(N \log N)$ complexity that can be exploited with massive case lists.
88: huge intersections: `Meet` uses a linear scan, but if `a.cases` and `b.cases` are extremely large, the `min` allocation and loop could be a bottleneck.
116: overflow/malformed input: `Hash` uses `uint64(c+1)`, which will overflow to `0` if a `caseIndex` is `math.MaxInt64`, potentially causing hash collisions with empty case sets.
124: recursion/depth limits: `Covers` calls `Join` and `Equal`, which can lead to high memory pressure if called repeatedly in a deep lattice traversal.
127: rare type combinations: `NarrowCase` with `equal=true` on a `top` state returns a `Singleton`, which is a ""jump"" from no information to specific information",320,8935,
analysis/domain/value/variant/internal/discriminant/conflict.go,q0,NONE,2,984,
analysis/domain/value/variant/internal/discriminant/paths.go,q0,NONE,2,509,
analysis/domain/value/variant/internal/discriminant/presence.go,q0,NONE,2,447,
analysis/domain/value/product/runtime.go,q0,"43 + nil registry + Passing a `nil` pointer to `runtimeFor` to trigger the first error path.
46 + unfrozen registry + Passing a registry where `Frozen()` returns `false` to trigger the state validation error.
101 + core lane collision + Registering `presence.Key.ID()` as a sparse axis to trigger the core lane protection error.
113 + nil top type + An axis where `spec.TopAny()` returns `nil` (e.g., a custom type with no default value) to test the fallback to `BottomAny` type reflection.
114 + nil bottom type + An axis where both `TopAny()` and `BottomAny()` return `nil`, potentially causing `reflect.TypeOf(nil)` to return `nil`.
124 + duplicate IDs + A registry containing multiple axes with the same ID, testing the `byID` map collision/overwrite behavior.
131 + empty reducers + A registry with zero reducers to ensure the slice initialization and loop handle zero-length views without panicking.
141 + race condition + Concurrent calls to `runtimeFor` with the same registry to test the double-checked locking pattern and `registryRuntimeCache` mutex.",265,6536,
analysis/domain/value/product/reduce.go,q0,"33 + Non-convergence/Infinite Loop + A set of reducers that oscillate between two states (e.g., A -> B -> A) to trigger the `maxReducerPasses` limit and the subsequent panic.
53 + Presence/Shape Mismatch + A scenario where `nextPresence` is `Top` but `nextShape` is `Bottom`, testing the logic that handles the transition between presence and shape identity.
101 + Unregistered Axis Access + A reducer attempting to `SetAny` using a key that exists in the `values` map but was not registered in the `registryRuntime`.
108 + Top-Any Erasure + A reducer setting a value that matches the axis's `IsTopAny` specification, testing if the key is correctly deleted from the editor's internal map.
114 + Redundant Update Suppression + A reducer setting a value that is already present and equal to the current value, testing if `e.changed` is correctly suppressed to prevent infinite loops.
134 + Partial Bottom Product + A scenario where the `presence` is not `Bottom`, but one specific slot value matches the axis's `bottomAny`, forcing the entire product to `Bottom`.
141 + Empty Slot Set + A product with zero slots (empty `e.values`) to ensure `slots()` returns `nil` rather than an empty slice or panicking.",296,7985,
analysis/domain/value/product/runtime_canonical.go,q0,"21: panic/error returns + Core Lane Collision: A slot containing the `presence.Key.ID()` will trigger a panic, testing if the system correctly distinguishes between core lanes and sparse axes.
25: panic/error returns + Unregistered Axis: A slot with a key that has not been registered in the `registryRuntime` will trigger a panic.
28: nil/empty handling + TopAny Erasure: A slice of slots where every single slot's value satisfies `IsTopAny` will result in an empty `byKey` map and return `nil`.
48: panic/error returns + Core Lane Collision (Check): The `slotsAlreadyCanonical` check will panic if a core lane is passed in, even if the slice is already sorted.
51: panic/error returns + Unregistered Axis (Check): The `slotsAlreadyCanonical` check will panic if an unregistered axis is passed in, even if the slice is already sorted.
54: boundary conditions + TopAny Short-circuit: If a slice contains a mix of valid slots and `IsTopAny` slots, `slotsAlreadyCanonical` returns `false`, forcing a full re-canonicalization.
57: boundary conditions + Lexicographical Order: A slice that is sorted by value but not by key (e.g., `[{b, 1}, {a, 2}]`) will fail the `prev >= slot.key` check.",303,7294,
analysis/domain/value/product/value.go,q0,"46 + Shape invalidity + Passing a `Shape` cast from an arbitrary `uint8` (e.g., `Shape(42)`) to `New` or `WithShape` to test the `String()` default case or logic assumptions.

54 + Nil node + Calling `ShapeOf` or `PresenceOf` on a zero-valued `Value{}` (where `n == nil`) to ensure it defaults to `Top` rather than panicking.

74 + Nil node + Calling `WithShape` on a zero-valued `Value{}` to see if `PresenceOf` correctly handles the nil pointer.

101 + Nil node + Calling `WithPresence` on a zero-valued `Value{}` to see if `ShapeOf` correctly handles the nil pointer.

113 + Core lane collision + Attempting to use `Get` with a key that matches `presence.Key.ID()`, which should trigger a specific panic.

121 + Unregistered axis + Attempting to `Get` an axis that exists in the type system but was never registered in the provided `axis.Registry`.

125 + Type mismatch (Stored vs Registry) + Storing a value of type `int` in a slot but having the `Registry` define that axis as `string`.

128 + Type mismatch (Key vs Registry) + Using a `Key[int]` to access an axis that the `Registry` defines as `string`.

131 + Type mismatch (Stored",320,7201,
analysis/domain/value/typevalue/reconstruct_evidence.go,q0,"28: `kinds.IsBottom() || kinds.IsTop()`: Bottom/Top type collision where a value is simultaneously categorized as both or represents an impossible state.
31: `len(kinds.Tags())`: Empty tag set where a value exists in the registry but has no associated runtime kinds.
43: `default: return nil, false`: Unknown/Custom runtime kind tag that isn't part of the primitive set (e.g., a plugin-defined kind).
47: `unionType(members)`: Massive union of all primitive types that might trigger complex normalization logic or unexpected type identities.
54: `len(types) == 0`: An empty slice passed to `unionType` resulting from a successful `kinds.Tags()` call that returns no elements.
55: `types[0] == nil`: A slice containing a single `nil` type element, testing the distinction between an empty union and a union of nothing.
58: `typenormalize.UnionForEvidence(types...)`: Deeply nested or redundant type slices that could cause stack exhaustion or infinite loops during normalization.",238,5821,
analysis/domain/value/returnpresence/returnpresence.go,q0,"34: Out-of-bounds trigger/target index + A `Point` with a `Presence` slice shorter than the provided `trigger` or `target` integer indices.
34: Missing knowledge/Incomplete facts + A `Point` where `Known[trigger]` or `Known[target]` is `false`, representing an uninitialized or non-deterministic slot.
35: Inconsistent presence across points + A set of points where the `target` presence varies (e.g., `Present` in one, `Absent` in another) despite the `trigger` being met.
35: Non-definite target presence + A scenario where the `target` slot is `Maybe` (neither strictly `Present` nor `Absent`) at a qualifying point.
35: Empty points slice + An empty `points` slice which results in `saw == false`, returning `Bottom, false` without error.
44: Trigger mismatch + A point where the `trigger` slot's presence is strictly incompatible with `triggerPresence` (e.g., `trigger` is `Absent` but `triggerPresence` is `Present`).",240,6890,
analysis/domain/value/typevalue/runtime_kind.go,q0,"44-45: Union member nil-handling + A union containing only `nil` and `unknown` types which might bypass the `seen` flag logic.
48-50: Union member failure + A union where one member is a complex type that resolves to `ok=false`, causing the entire union to fail.
53-54: Union empty/all-nil + A union containing only `nil` members (e.g., `nil | nil`) to test the `seenNil` fallback.
58-61: Instantiated self-reference + An instantiated type where `subst.ExpandInstantiated` returns the same type, potentially causing infinite recursion if not caught.
65-68: Alias self-reference + An alias that points to itself (e.g., `type T = T`), testing the cycle detection in `UnaliasedTarget`.
71-74: Recursive self-reference + A recursive type where `tt.Body` is the type itself, testing the base case for infinite recursion.
77-78: Recursive body nil + A recursive type definition with a `nil` body, which should return `false` rather than panicking.",256,6038,
analysis/domain/value/refinement/refinement.go,q0,"27: recursion/depth limits: A deeply nested `typ.Optional` or `typ.Union` chain that exceeds `typ.DefaultRecursionDepth` to trigger the safety return.
28: recursion/depth limits: A cyclic type definition (e.g., `type T = T`) that would cause a stack overflow if the depth check were missing.
33: recursion/depth limits: A `typ.Union` containing a massive number of members that could cause significant latency or stack pressure during traversal.
35: rare type combinations: A `typ.Union` where one member is `typ.Never` and another is a type that admits false, testing the short-circuit logic.
41: malformed input: An `unwrap.Annotated(t)` that returns a type not explicitly handled in the switch, testing the `subtype.IsSubtype(typ.False, t)` fallback.
75: nil/empty handling: A `valueWitness` or `constraintWitness` that is not `Top`/`Bottom` but returns `ok == false` on `Type()`, testing the early exit.
84: rare type combinations: A case where `subtype.IsSubtype` returns true for both directions (e.g., identical types), testing the `narrower` assignment logic.
88: malformed input: A `product.Meet` call where the registry state is inconsistent, potentially leading to a `Bottom` result that must be handled.
101: cyclic/self",320,7880,
analysis/domain/value/typevalue/presence_projection.go,q0,"28: nil/empty handling + Passing a literal `nil` as the `t` argument to `typeIncludesNil` to check if it correctly returns `false` instead of panicking.
33: recursion/depth limits + A deeply nested `Union` type that exceeds `typ.DefaultRecursionDepth` to ensure it terminates and returns `false` rather than stack overflowing.
33: cyclic/self-referential types + A `Union` type where a member is the `Union` itself, testing if the `depth` check prevents infinite recursion.
33: rare type combinations + A type that is both `IsAny` and contains a `Nil` kind to ensure the short-circuiting logic prioritizes the ""false"" return.
34: malformed input + An `unwrap.NormalizeNil` call that returns `nil` on a non-nil input, testing the subsequent `normalized != nil` safety check.
41: recursion/depth limits + A `Union` type where the `depth` is exactly `typ.DefaultRecursionDepth` to test the boundary of the recursion limit.
44: rare type combinations + A `Union` type containing only `IsNever` or `IsUnknown` members to ensure the loop completes without incorrectly identifying a `Nil`.
46: recursion/depth limits + A `Union` type where the `Nil` is located at the very last index of the `Members` slice, testing full traversal before depth exhaustion.",313,7213,
analysis/domain/value/typevalue/presence.go,q0,"41: Recursion/Depth Limits: An `Instantiated` type that expands to itself or a cycle via `subst.ExpandInstantiated` will cause a stack overflow.
43: Nil/Empty Handling: A `Union` with an empty `Members` slice will bypass the `seenNil` check and return `presence.Present(), true`, which is logically incorrect for an empty set.
44: Malformed Input: A `Union` containing a `nil` member will return `presence.Bottom(), false`, potentially masking a type system inconsistency.
44: Rare Type Combinations: A `Union` containing both `IsAny` and a specific type will return `presence.Bottom(), false`, ignoring the specific type's presence.
46: Nil/Empty Handling: A `Union` where all members are `IsUnknown` or `IsAny` will return `presence.Bottom(), false`.
51: Recursion/Depth Limits: An `Instantiated` type that expands to a type requiring deep re-evaluation via `presenceFromType(expanded)` can trigger deep recursion.
52: Cyclic/Self-referential: If `subst.ExpandInstantiated(tt)` returns the same pointer as `t`, it returns `Bottom`, but if it returns a type that eventually points back to `t`, it will loop.
63: Rare Type Combinations: A type that is neither `Nil`, a `RuntimeKind`, nor an `Interface` (e.g., a custom structural type",320,7317,
analysis/domain/value/typevalue/structural.go,q0,"24: `witness.Type()` returns a type that is structurally identical to the `origin` but has a different internal identity, testing if the cache/narrowing logic handles type-identity vs. structural-equivalence collisions.

26: `cache.NarrowVariantByOrigin` with a `t` that contains a circular reference back to the `origin` family, potentially causing infinite recursion in the cache's narrowing logic.

28: `cache.TypeFromVariantOrigin` returning a type that is `nil` or `Top` despite the `origin` being valid, testing the robustness of the `structuralTypeWithPresence` wrapper.

34: `cache.TypeFromVariantOrigin` called when `origin.Family()` or `origin.Cases()` contains malformed or unexpected values (e.g., empty sets), testing the cache's boundary handling.

43: `opts.OptionalWhenMaybe` is true and `p` is a ""Maybe"" presence type that is neither `Absent` nor `Present` (e.g., a custom/malformed presence state), testing the `default` switch fallthrough.

46: `presence.Equal(p, presence.Absent())` where `p` is a nil or uninitialized `presence.Value`, testing if the equality check panics on nil receivers.",279,6532,
analysis/domain/value/typevalue/cache.go,q0,"36 + Nil Receiver + Calling `FromType` on a nil `*Cache` pointer to trigger the fallback to the package-level function.
48 + Nil Receiver + Calling `FromTypeWithWitness` on a nil `*Cache` pointer to trigger the fallback.
56 + Nil Receiver + Calling `originOfType` on a nil `*Cache` pointer.
64 + Nil Receiver + Calling `NarrowVariantByOrigin` on a nil `*Cache` pointer.
72 + Nil Receiver + Calling `TypeFromVariantOrigin` on a nil `*Cache` pointer.
80 + Nil Receiver + Calling `OriginByPathLiteral` on a nil `*Cache` pointer.
88 + Nil Receiver + Calling `OriginByPathLiteralNot` on a nil `*Cache` pointer.
96 + Stale Cache/Registry Mismatch + A `product.Value` exists in the map but its underlying `axis.Registry` state has changed (e.g., via erasure), forcing a `delete` and re-computation.
96 + Bottom/Top Type Edge Case + A cached value represents a `Bottom` or `Top` type which, by definition, cannot be validated against a registry, forcing a cache miss/re-computation.
104 + Nil Registry + Passing a `nil` registry to `cachedProductActive` which must bypass registry lookups to avoid panics.
107 + Erased Metadata + A registry where the `variantorigin.Key` has been erased",320,8085,
analysis/engine/callpayload/call_outcome.go,q0,NONE,2,470,
analysis/domain/value/variant/internal/discriminant/residual.go,q0,"49 + recursion/depth limits + `mergeKeepsPreciseFieldType` calls `literalErasedResidualsCleanlyMergeable` which can cause a stack overflow if two records contain each other as fields.
49 + cyclic/self-referential types + `typ.SameNodeOrAcyclicEqual` is the only guard against infinite recursion when comparing two records that reference themselves.
54 + nil/empty handling + `ar != nil && br != nil` handles cases where `unwrap.RecordWithAliasPolicy` returns nil, but the logic relies on the caller not having passed nil types.
65 + malformed input + `fieldMergeKind` enters an infinite loop if a `typ.Alias` points to itself (a cycle in the type graph).
65 + malformed input + `fieldMergeKind` will panic if `t.Kind()` is called on a type that is not properly initialized or is a nil pointer after the alias loop.",203,4806,
analysis/domain/value/variant/cache.go,q0,"78: `originPathKey` + overflow + a `suffix` slice with > 4 elements to trigger the `overflow` string serialization.
103: `originByPathLiteral` + slice mutation + a caller modifying the `cases` slice after it has been appended to the cache.
116: `originPathLiteralKey` + nil/empty + a `suffix` slice that is `nil` or has `len == 0` to test the empty `originPathKey` initialization.
133: `NarrowByOrigin` + invalid family + a `familyID` that exists in the cache but has since been invalidated/deactivated in the global registry.
148: `NarrowByOrigin` + mismatch + a `familyID` that is valid but does not match the actual `originFamily` of the provided type `t`.
171: `TypeFromOrigin` + invalid family + a `familyID` that is non-zero but does not exist in the global `loadOriginFamily` registry.
203: `originCaseKey` + large input + a `cases` slice with > 4 elements to trigger the `originCaseOverflowKey` path.
206: `originCaseKey` + duplicate values + a `cases` slice containing duplicate integers to test the `insertOriginCase` deduplication logic.
213: `insertOriginCase` + boundary + a `value` that is smaller than all existing values to test the `i=0",320,6625,
analysis/domain/value/variant/origin_catalog.go,q0,"44: Poisoned ID collision + `storeOriginFamily` + Attempting to re-insert a valid family into an ID that was previously marked as poisoned due to incompatibility.
51: Incompatible family signature + `storeOriginFamily` + Providing a family with the same ID but a different `signature` string to trigger the poisoning logic.
53: Poisoning side-effect + `storeOriginFamily` + A sequence of calls where a valid family is replaced by an incompatible one, then checking if subsequent valid attempts are blocked.
78: Nil/Empty case slice + `cloneOriginCases` + Passing a `nil` slice to ensure it returns `nil` rather than an empty allocated slice, preventing unnecessary allocations.
101: Type mismatch in overlapping indices + `originCasesOverlapCompatible` + Two `TaggedRecord` families sharing the same index but having different `typ.Type` values (e.g., one is a string, one is a table).
118: Index collision in merge + `mergeOriginCases` + Two families providing different `originCase` objects for the same `index`, testing which one ""wins"" the merge (last-write-wins).
126: Massive index spread + `mergeOriginCases` + Providing cases with indices `0` and `math.MaxInt`, testing the efficiency and stability of the `sort.Ints` and slice growth.
143: Non-identical case order + `originCasesEqual` + Two families with identical index/type pairs",320,5013,
analysis/domain/value/variant/origin.go,q0,"41: `len(out) == 0` (Empty Intersection): A narrowing operation where the provided `cases` slice contains indices that exist in the family but result in an empty set of types, testing the transition to `typ.Never`.

58: `familyID == 0` (Zero-ID Boundary): Passing a null/zeroed family ID to `FullFamilyType` to ensure it doesn't attempt to load a non-existent global registry entry.

60: `len(family.cases) == 0` (Empty Family): A family exists in the registry but contains no registered cases, testing the handling of ""ghost"" or uninitialized variant families.

71: `familyID == 0 || len(cases) == 0` (Empty Evidence): Reconstructing a type from an empty set of evidence, which should fail rather than returning a valid empty union.

85: `len(remaining) != 0` (Incomplete Evidence): Providing a `cases` slice containing indices that do not exist in the `family.cases` registry, testing the strictness of the reconstruction.

86: `len(out) == 0` (Zero-Case Reconstruction): Attempting to reconstruct a type from a family that has no matching cases, ensuring it returns `nil` instead of a malformed union.",283,5345,
analysis/domain/value/variant/narrow.go,q0,"44: nil/empty handling: Passing `t == nil`, `suffix == []`, or `lit == nil` returns `nil, false` instead of panicking.
45: cyclic/self-referential: `typ.SameNodeOrAcyclicEqual` prevents infinite loops when a narrowing results in a type structurally identical to the input.
51: recursion/depth limits: A deeply nested `Alias` or `Optional` chain exceeding `typ.DefaultRecursionDepth` returns `nil, false`.
56: cyclic/self-referential: A `Recursive` type where `v.Body == t` is an immediate cycle that must be caught to prevent infinite recursion.
61: recursion/depth limits: An `Instantiated` type that expands into a type requiring further recursion could exceed the depth limit.
67: huge unions: A `Union` with a massive number of members where every member satisfies the predicate, testing the `len(out) == len(v.Members)` short-circuit.
74: rare type combinations: A `Union` where all members are filtered out, resulting in an empty slice and returning the original `t` (potentially masking a logic error).
81: rare type combinations: A `Union` where all members are filtered out, returning `typ.Never` to correctly represent an impossible state.
86: rare type combinations: A non-union type that satisfies the literal predicate, returning `typ.Never` (the ""Not"" case).
9",320,6097,
analysis/engine/callboundary/normal_return_facts.go,q0,"148: EscapeEventEffectSite + Unknown/Invalid Kind + Passing a `EscapeEventKind` cast from an arbitrary `uint8` (e.g., `255`) to see if it defaults to ""opaque"" correctly.
158: EscapeEventFromEffectSite + Malformed Site String + A site string that contains the prefix but has a suffix that isn't `.recursive` or a valid kind (e.g., `""escape-event.garbage""`).
158: EscapeEventFromEffectSite + Prefix Collision + A site string that is exactly the prefix `""escape-event.""` with no following name.
168: escapeEventKindName + Out-of-bounds Enum + Passing a `uint8` value that is not defined in the `const` block to ensure it returns an empty string.
188: escapeEventKindByName + Partial Match/Substring + A name that is a substring of a valid kind (e.g., `""bor""`) to ensure it doesn't trigger a false positive.",222,2774,
analysis/domain/value/variant/internal/discriminant/tags.go,q0,"43: Cyclic/Self-referential types + The `d.active` map check prevents infinite recursion, but a cycle involving an `Alias` or `Recursive` type might return an empty set unexpectedly rather than an error.
58: Cyclic/Self-referential types + A `typ.Recursive` type pointing to itself will trigger the `d.active` check and return an empty `requiredTagSet`, which might be semantically incorrect for certain type analyses.
78: Cyclic/Self-referential types + An `Alias` pointing to itself or a `Recursive` type with a cycle will be caught by the `active` map, potentially masking a malformed type definition.
81: Malformed input + `subst.ExpandInstantiatedChanged` returning `false` for a complex type might lead to an empty tag set, which could be misinterpreted as ""no required tags"" rather than ""expansion failed.""
95: Huge unions/intersections + A `typ.Record` with a massive number of `StaticMembers` or `Fields` could cause significant allocation overhead during the `addPrefixed` recursion.
111: Huge unions/intersections + A `typ.Union` with a very large number of members will perform $O(N \times M)$ work where $M$ is the number of tags, potentially causing a timeout or high CPU usage.
114: Nil/Empty handling + A `typ.Union` with `nil` members or an empty slice is handled, but if",320,6396,
analysis/domain/value/variant/narrow_truthy.go,q0,"41: Recursion/Depth Limit: A deeply nested `typ.Alias` or `typ.Optional` chain that exceeds `typ.DefaultRecursionDepth` to trigger the `false` return.
44: Cyclic/Self-referential: A `typ.Recursive` type where `v.Body == t` (direct self-reference) to ensure it doesn't infinite loop.
45: Nil Body: A `typ.Recursive` type with a `nil` Body to ensure it handles broken structural definitions gracefully.
51: Instantiated Expansion Failure: A `typ.Instantiated` type where `subst.ExpandInstantiatedChanged` returns `false`, testing the fallback to `true`.
54: Huge Union: A `typ.Union` with a massive number of members, where only the very last member is truthy, testing exhaustive iteration.
60: Boolean Literal Edge Case: A `typ.Literal` that is specifically `kind.Boolean` with `Value == false` to ensure it is the only truthy-denied boolean.
62: Nil/Unknown Type: A `default` case where the underlying type is `nil` or an unhandled type that resolves to `kind.Nil`.
81: Recursive Depth Limit: A `typ.Recursive` type that bypasses the `v.Body == t` check but eventually hits the `depth` limit.
86: Instantiated Expansion Failure: A `typ.Instantiated` type that fails to expand, testing",320,6004,
analysis/engine/dynamicindex/dynamicindex.go,q0,"78: nil/empty handling: `CloneMap` returns `nil` for an empty input map, which may cause panics in callers expecting an initialized (but empty) map.
101: admission/lattice edge case: `admissionLessOrEq` treats `AdmissionUnknown` as the ""Top"" element, which could cause unexpected lattice behavior if `AdmissionUnknown` is used in a context where it should be strictly greater than `AdmissionRejected`.
104: admission/lattice edge case: `admissionJoin` returns `AdmissionUnknown` for any mismatch that isn't `Bottom`, potentially masking a conflict between `Admitted` and `Rejected` as a simple ""unknown"" state.",146,2028,
analysis/domain/value/variant/origin_ints.go,q0,"21: nil/empty handling: Passing a `nil` slice to `compactInts` returns `nil` immediately, avoiding a sort on a null pointer.
24: duplicate/boundary values: A slice containing only identical values (e.g., `[5, 5, 5]`) tests the `out[len(out)-1] != value` logic to ensure it collapses to a single element.
33: empty intersection: Passing an empty `cases` slice returns `false` without entering the loop, testing the base case of the intersection logic.
38: constraint mismatch: Passing a `constraint` map that is `nil` will cause a panic when accessing `constraint[c]`, representing an unhandled error path for malformed input.
42: large/unsorted input: `sameIntSet` uses `append([]int(nil), a...)` to create a deep copy, preventing the original input slice from being mutated by the `sort.Ints` call in `compactInts`.
43: disparate lengths: Comparing two slices that are identical in content but differ in frequency (e.g., `[1, 1]` vs `[1]`) tests if the compaction correctly normalizes them before the length check.",268,3941,
analysis/domain/value/variant/origin_family.go,q0,"34: *typ.Recursive (Self-referential): A recursive type where `v.Body == t` to prevent infinite recursion during origin detection.
35: *typ.Recursive (Nil Body): A recursive type with a `nil` body which would otherwise cause a panic during unwrapping.
41: *typ.Instantiated (Expansion Failure): An instantiated type that fails to expand (e.g., due to unbound type variables), returning `false`.
55: *typ.Union (Nil Member Filtering): A union containing `nil` members (e.g., `nil | A | B`) to ensure the discriminant is derived only from the non-nil record variants.
63: *typ.Union (Single Member Union): A union that, after filtering `nil`, has fewer than 2 members, which cannot form a discriminated family.
67: *typ.Union (Open/Map Records): A union containing ""open"" records or records with map components, which are ineligible for closed-set discrimination.
72: *typ.Union (Non-Conflicting Records): A union of records that do not have conflicting tags/presence, meaning they don't form a valid discriminant set.
76: *typ.Union (Hash Collision/Zero): A union where the combined member hashes result in a zero value, requiring `nonZeroHash` to ensure a valid ID.
101: *typ.Record (Empty Tagged Record): A record with no required tags, which cannot be",320,4739,
analysis/domain/value/variant/origin_project.go,q0,"23 + Mismatched Child Families + A variant where different arms project to different `childFamily` IDs, causing an early exit.
35 + Incomplete Case Selection + A `cases` slice that contains indices not present in the `family.cases` definition, causing `len(selected) != 0`.
55 + Nil Type/Suffix + Passing a `nil` `typ.Type` or an empty `suffix` to the literal matcher.
78 + Truthy Sentinel Logic + A `lit` that is a `isTruthySentinel` (e.g., boolean/nil) which bypasses `pathAdmitsLiteral` for a different logic branch.
88 + Empty Intersection + A variant where no arms satisfy the `pathAdmitsLiteral` predicate, resulting in `changed = true` but `len(out) == 0`.
104 + Partial Case Coverage + A `parentCases` slice that does not perfectly match the indices available in the `parentFamily` definition.
116 + Path Projection Failure + A case where the `suffix` path does not exist on a specific arm, testing the `!equal` logic for keeping/dropping arms.
121 + Family Mismatch + A case where the projected `childFamily` does not match the `constraintFamily`, testing the ""keep arm"" fallback.
128 + Empty Intersection/Equality + A case where `casesIntersect` returns a result that is the inverse of the `equal` flag, testing the filtering logic.",320,4656,
analysis/domain/value/variant/path_access.go,q0,"24: recursion/depth limits: A type chain (Alias -> Optional -> Recursive) that exceeds `typ.DefaultRecursionDepth` to trigger the depth guard.
25: nil/empty handling: Passing a `nil` type or an empty `suffix` slice to ensure the function returns `false` rather than panicking.
30: cyclic/self-referential: A `*typ.Recursive` where `v.Body == t` (direct self-reference) to trigger the immediate failure path.
38: malformed input: An `*typ.Instantiated` type where `subst.ExpandInstantiatedChanged` returns `ok=false` due to invalid type parameters.
43: huge unions: A `*typ.Union` with a very large number of members to test slice allocation and the ""all-or-nothing"" failure logic.
46: rare type combinations: A `*typ.Union` containing a member that is itself a `*typ.Union` or a `*typ.Recursive` to test nested lifting.
48: error paths: A `*typ.Union` where one member's path resolution fails, ensuring the entire union resolution returns `false`.
54: nil/empty handling: A `*typ.Record` where `suffix[0]` is accessed, but the logic must ensure `suffix` is not empty (handled by line 25).
58: malformed input: A `*typ.Record` where `r.",320,4951,
analysis/engine/calloutcome/merge.go,q0,"24 + nil/empty handling + Passing `nil` as `primary` or `supplemental` to check if the provider chain breaks or returns the other provider correctly.
48 + duplicate index handling + Two providers returning different types for the same Lua stack index (e.g., one says `string`, one says `number`) to test `product.Meet` logic.
55 + type evidence edge case + A result slot containing a type that is `Any` or `Unknown` to ensure the merge doesn't attempt to `Meet` a specific type with a ""top"" type.
71 + authority override + `primary` having `PostReturnAuthority = true` but `supplemental` having different facts, to ensure `primary`'s authority correctly truncates the merge.
94 + massive union/intersection + Two providers returning massive `Placements` maps with overlapping `identity.ID` keys to test the performance and correctness of `placement.Join`.
114 + nil registry/map handling + A `nil` `reg` combined with empty `left` or `right` maps in `withSupplementalHeapTableObjects` to check for nil pointer dereferences during cloning.
118 + registry/map merge + A scenario where `reg` is provided and both maps have overlapping `identity.ID` keys to test the `Join` logic of the heap domain.
131 + type identity edge case + A value that `typevalue.TypeOf` cannot resolve (returns `ok=false`) to",320,4909,
analysis/engine/calloutcome/authority.go,q0,"46: nil/empty handling + An `outcome` with nil slices for all `NormalReturnFacts` and `Param` fields to ensure no panic occurs during `len()` checks.
58: nil/empty handling + A `results` slice that is `nil` or empty, testing the loop boundary in `outcomeHasAuthoritativeResult`.
64: nil/empty handling + A `reg` that is `nil`, which should trigger the early return to prevent panics in subsequent `product` calls.
65: rare type combinations + A `value` that is exactly `product.Bottom(reg)`, testing if the equality check correctly identifies non-authoritative bottom types.
66: rare type combinations + A `value` that is exactly `product.Top()`, testing if the equality check correctly identifies non-authoritative top types.
68: malformed input + A `value` that lacks the `evidence.Key` metadata, testing if `product.Get` handles missing keys gracefully without panicking.
69: rare type combinations + A `value` where the evidence is explicitly `Top` (e.g., via a failed type assertion), ensuring it doesn't count as authority.
72: rare type combinations + A `value` that has a valid type but that type is `typ.Any` or `typ.Unknown`, testing the distinction between ""known value"" and ""authoritative type.""",302,5295,
analysis/engine/factapply/call_outcome.go,q0,NONE,2,376,
analysis/engine/callproducer/callproducer.go,q0,"24 + Empty Result Targets + A `CallSiteView` that reports a non-zero `ResultTargetCount()` but returns nothing during `ForEachResultTarget` iteration.
34 + Missing Call Site + A `cfg.Point` that exists in the CFG but has no associated call-site data in the `facts` map.
35 + Ineligible Context + A call site with a context like `CallSiteContextStackManipulation` which should be filtered out by `eligibleView`.
58 + Zero-length Slice + A `CallResultTarget` slice that is non-nil but has a length of 0, testing the `len(targets) == 0` branch.
68 + All Targets Invalid + A slice of targets where every single target fails the `strictResultTarget` predicate, resulting in an empty `out` slice.
76 + Zero-count View + A `CallSiteView` where `ResultTargetCount()` is 0, testing the early return of `nil`.
88 + Invalid Local Assignment + A `CallResultTarget` of kind `LocalAssignment` where `TargetSymbol()` is 0 (representing an uninitialized or invalid symbol).
90 + Malformed Ordinary Assignment + An `OrdinaryAssignment` where `TargetPath().Segments` is non-empty (representing a path-based assignment that isn't ""strict"").
92 + Negative Result Index + A `CallResultTarget` of kind `Expression` where `ResultIndex()` is -1 (representing an invalid stack index).",320,5499,
analysis/engine/effectlowering/callable_value.go,q0,"39: nil/empty handling + `config.CalleeValue` or `config.Callable` being nil results in an empty `CallOutcome` instead of a panic.
43: malformed input + `calleeValue` returning a `product.Value` that is not a table/map, causing `product.Get` to potentially fail or return a nil witness.
45: rare type combinations + `witness.Type()` returning `ok=true` but a type that is not a function (e.g., a table that doesn't satisfy the `callable` check).
48: malformed input + `callable(t)` returning a function pointer where `fn.Returns` is nil, which would cause a panic during `len(fn.Returns)` if not for the `len == 0` check.
48: rare type combinations + a generic function (where `len(fn.TypeParams) != 0`) which the provider explicitly ignores, potentially masking valid call sites.
52: nil/empty handling + a function return type being explicitly `nil` in the type definition, which is skipped via the `ret == nil` check.
52: huge unions/intersections + a return type that is a `Top` type (e.g., `any` or `unknown`), which is skipped, potentially leading to an incomplete `CallOutcome`.
56: malformed input + `returnValueFromType` receiving a type that cannot be materialized from the current `ctx.Registry`",320,4989,
analysis/engine/effectlowering/module_load.go,q0,"46: nil/empty handling + A `ModuleLoadOutcomeProviderConfig` where `Exports`, `NameFor`, or `Sources` is nil, which should return an empty `CallOutcome` rather than panicking.
51: malformed input + A `CallSiteView` that reports a call to a function named `""require""` but provides zero arguments or multiple arguments, testing the `len(args) != 1` guard.
55: rare type combinations + An argument source that exists in the `Sources` map but resolves to a non-string type (e.g., a number or table) during `ValueOfSource`.
59: malformed input + A `value` that is a valid `product.Value` but cannot be cast to a string literal via `exactStringLiteral`, such as a table containing a string.
64: nil/empty handling + A `reg` (Registry) being passed as `nil` to `exactStringLiteral`, which would cause a panic if the nil check were missing.
65: rare type combinations + A `product.Value` that exists in the registry but lacks the `typewitness.Key` witness, testing the `ok` check on `product.Get`.
69: malformed input + A `witness.Type()` that returns `ok=true` but provides a type that is not a `*typ.Literal` (e.g., a complex union or function type).
74: rare type combinations + A `typ.Type` that",320,5632,
analysis/engine/factapply/channel_select_fact.go,q0,"41: malformed input + `event.ResultPath()` returns `ok=true` but an empty string, which should trigger a failure to create a fact.
47: malformed input + `event.CasePath()` returns `ok=true` but an empty string, which should trigger a failure to create a fact.
53: rare type combinations + `event.PayloadValue()` returns `ok=true` but the payload is a nil or zero-value type that might affect downstream analysis.",107,1608,
analysis/engine/effectlowering/provider_facts.go,q0,"28: nil/empty handling + `activePathInvalidationTargets` returns `nil` if `sig.Effect.Labels` is empty, but if `NormalizeLabel` returns a nil pointer for a non-empty slice, it might cause issues in callers.
48: nil/empty handling + `activePathInvalidationTargets` handles `*ownership.Store` being `nil` via a check, but if `normalized.Into` itself is a nil pointer, it could panic.
78: rare type combinations + `signatureEscapeEvents` uses a large switch on `effect.NormalizeLabel(label).(type)`; if a new type is added to the domain but not this switch, it silently ignores the effect.
118: overflow/boundary + `signatureEscapeEvents` handles `ownership.Send` by iterating from `start` to `len(args)`; if `start` is calculated as a very large number due to an invalid `FromParam`, the loop might behave unexpectedly.
158: nil/empty handling + `signatureStoreRelations` checks `normalized != nil` for `*ownership.Store`, but if `normalized.Param` or `normalized.Into` are nil, `ResolveParamIndex` might panic depending on its implementation.
174: boundary conditions + `sendStartIndex` handles `argCount <= 0` and negative `fromParam` (Lua-style negative indexing), but if `fromParam` is `math.MinInt64`, `argCount + start` could overflow.",320,5077,
analysis/engine/factapply/applicator.go,q0,"58: `NoNormalReturn` / Nil/Empty handling: If `facts.NoNormalReturn` returns true for a point that is actually a valid return path, the engine might prematurely drop all state, leading to ""lost"" facts.

61: `PathDescendantInvalidation` / Cyclic/Self-referential types: If a path invalidation fact targets a path that is a prefix of itself or part of a cycle, it could trigger infinite recursion in the underlying `applyPathDescendantInvalidation`.

64: `PostconditionRefinements` / Huge unions/intersections: A massive list of refinements at a single point could cause an $O(N^2)$ or worse explosion in type complexity if each refinement triggers a complex union/intersection calculation.

73: `PathAssignment` / Malformed input: If `fact.TargetPath()` returns a path that is logically impossible (e.g., indexing into a non-table type), the applicator must ensure it doesn't panic when attempting to traverse the state.

76: `applyObjectLiteralEntries` / Deeply nested structures: A large object literal with deeply nested paths could cause a stack overflow if the applicator uses naive recursion to apply entries to the state.

86: `BranchRefinements` / Recursion/Depth limits: If a branch refinement targets a path that is being refined by another branch refinement in a loop, it could lead to infinite refinement cycles.

98: `BranchPresenceRelations` / Rare type combinations: If a presence relation",320,5054,
analysis/engine/effectlowering/postconditions.go,q0,"29: Nil Config Fields + Passing a `config.Facts` where `Graph`, `Registry`, `Signatures`, or `NameFor` is nil to ensure the function returns the original facts without panicking.
41: Empty Graph RPO + An empty Reverse Post-Order slice from `config.Graph.RPO()` to ensure the loop terminates gracefully without error.
42: Missing CallSite + A `cfg.Point` that exists in the graph but has no associated call site in `config.Facts` to ensure the loop continues.
46: Name Resolution Failure + A `config.NameFor` call that returns `ok == false` (e.g., due to an unresolved global or a malformed symbol) to ensure the point is skipped.
51: Missing or Non-Never Signature + A signature that is either missing from the registry or has a return type that is not `Never` (e.g., `void` vs `nil` vs `never`) to ensure it doesn't incorrectly flag normal returns.
57: Nil Signature Type + A `signature.Function` where `sig.Type` is `nil` to ensure `signatureHasNoNormalReturn` doesn't trigger a nil pointer dereference.
58: Empty Returns Slice + A signature where `len(sig.Type.Returns) == 0` to ensure the index `[0]` access is guarded.",299,5467,
analysis/engine/factapply/branch_presence.go,q0,"38: `branchEdgeImpliesAbsentFromNonFalseFalsy` + Empty `branchRefinements` slice: If the slice is empty, the function returns `false` immediately, potentially missing a case where a relation should have been triggered by a default state.

48: `branchEdgeImpliesAbsentFromNonFalseFalsy` + `!ctx.Edge.Cond` inversion: If the condition is a complex expression, the logical negation might not map to a valid or existing branch refinement in the list.

64: `branchTriggerCanBeFalse` + `resolvePathValueAtCached` failure: If the path resolution fails (e.g., due to a broken reference or malformed path), it returns `true`, which might lead to overly conservative (pessimistic) analysis.

68: `branchTriggerCanBeFalse` + `product.Get` with `runtimekind.Key`: If the `current.value` is a type that does not support key-based lookup (like a primitive or a malformed product), this may trigger a panic depending on the implementation of `product.Get`.

70: `branchTriggerCanBeFalse` + `IsTop()`/`IsBottom()`: If the type system encounters a ""Top"" (any value) or ""Bottom"" (no value) type due to an error in previous analysis steps, it defaults to `true`, potentially masking logic errors in the engine.

78: `refinementHasPresence` + `refinement.Constraint()`",320,4816,
analysis/engine/factapply/postconditions.go,q0,NONE,2,287,
analysis/engine/factapply/branch_path.go,q0,"148 + `projectPathHeapStaticMemberValue` + A path where `rootValue` is a cyclic table structure, potentially causing infinite recursion in `HeapMemberFromValue`.
163 + `projectPathHeapStaticMemberValue` + A path where `parentValue` is `Bottom`, testing if the fallback to `hasRootProjected` correctly handles missing parent information.
183 + `projectPathDynamicIndexValue` + A path where `snapshot.Facts` contains a massive number of entries, testing the performance/overflow of the `joinMatchingDynamicIndexValues` loop.
206 + `projectPathHeapDynamicIndexValue` + A path where `parentValue` is a non-table type that still has an `identity.Key`, testing the robustness of the `product.Get` and `ID()` calls.
225 + `joinMatchingDynamicIndexValues` + A `facts` map where multiple entries have the same `tableKey` but different `Admission` statuses, testing the filtering logic.
254 + `joinMatchingHeapDynamicIndexValues` + A `facts` map where all entries are `AdmissionRejected`, testing if the function correctly returns `found = false`.
268 + `dynamicIndexFactMayMatchSegment` + A `fact.KeyValue` that is a complex type (e.g., a table) instead of a primitive, testing the `typevalue.TypeOf` error path.
288 + `projectPathStructuralValueCached` + A `rootType`",320,5539,
analysis/engine/effectlowering/provider_operational.go,q0,"46: nil/empty handling + `effects == nil` returns early, but if `out` is passed as a nil pointer (if it were a pointer), it could cause issues; however, here it just returns the original `out`.
66: nil/empty handling + `ctx.Registry == nil` or `e.PathStaticMembers` being empty returns `nil`, preventing panics on `ctx.Registry` access.
78: malformed input + `fact.Type == nil` is explicitly checked to prevent `typevalue.FromType` from panicking on a nil type.
88: malformed input + `presence.Equal` is used, but if `p` contains an undefined/unsupported presence bitmask, `operationalPresenceValue` returns `false` rather than panicking.
138: malformed input + `template.Root == """"` is checked to prevent creating identity-based values with empty string keys.
146: malformed input + `object.ID == """"` is checked to prevent creating heap objects with empty identity keys.
153: malformed input + `member.Value == """"` is checked to prevent creating static members with empty identity keys.
157: malformed input + `heapidentity.StaticMemberSuffixKey(member.Suffix)` returning `ok == false` handles invalid path suffixes.
163: malformed input + `entry.Key == """" && entry.KeyType == nil && entry.Value == """"` is a specific check for",320,5673,
analysis/engine/effectlowering/provider_returns.go,q0,"54: nil/empty handling + `transform` is a nil pointer of a type that satisfies the `returns.ReturnType` interface, potentially causing a panic in the type switch.
64: nil/empty handling + `transform` is a nil pointer of a concrete type (e.g., `*returns.SameAs`), which is explicitly checked but highlights a pattern of fragile interface-to-concrete casting.
114: malformed input + `sig.Type.Params` is accessed via `argIndex` without verifying if `sig.Type` is nil (though checked, the logic relies on `argIndex` being within bounds of a potentially empty slice).
138: malformed input + `sig.Type.Params` is accessed via `argIndex` without verifying if `sig.Type` is nil.
158: malformed input + `sig.Type.Params` is accessed via `argIndex` without verifying if `sig.Type` is nil.
178: malformed input + `sig.Type.Params` is accessed via `argIndex` without verifying if `sig.Type` is nil.
203: nil/empty handling + `sig.Type` is nil, which is checked, but `returnTypeOps.InstantiateGenericCall` is called with a potentially nil `sig.Type` if the check were bypassed.
226: nil/empty handling + `argSources` is empty, returning `nil, false`, which might cause callers to",320,5673,
analysis/engine/effectlowering/provider.go,q0,"49: nil/empty handling + If `config.Signatures` or `config.NameFor` is nil, the provider returns an empty `CallOutcome` instead of panicking.
54: malformed input + If `nameFor` returns a name that exists in the signature registry but is logically invalid for the current `callproducer`, it may lead to unexpected `CallOutcome` states.
59: rare type combinations + If `instantiateSignatureForCall` encounters a signature with complex nested types or circular references, it may trigger infinite recursion or stack overflow.
62: nil/empty handling + If `sig.OperationalEffects` is non-nil but contains an empty set of effects, the logic branches into a complex path of path invalidations and length floors.
78: nil/empty handling + If `sig.Type` is non-nil but `sig.Type.Returns` is an empty slice, the provider returns early with authority checks but no results.
83: malformed input + If `sig.Type.Returns` contains `nil` elements, the loop must handle the transition from `signatureReturnValue` to `returnValueFromType` without panicking.
85: rare type combinations + If `returnValueFromType` is called with a `ret` that is a complex type not representable as a simple value, it may return an unexpected `ok=false`.
88: overflow/recursion + If `sig.OperationalEffects` contains a massive number of effects, `operationalReturnAllocationValue`",320,5751,
analysis/engine/factapply/branch_refinement.go,q0,"82: nil/empty handling + `resolver == nil` check allows the function to return early without panicking if the visibility resolver is missing.
88: nil/empty handling + `arrayPath.Symbol == 0` prevents processing of invalid or uninitialized array paths.
93: nil/empty handling + `pathKey == """"` prevents writing length floors to non-existent or invisible state keys.
124: malformed input + `targetPath.Symbol == 0` handles cases where the refinement target is an uninitialized or invalid symbol.
127: rare type combinations + `falsyAbsentRefinementUnproven` handles the edge case where a value is ""falsy"" but not necessarily `nil` (e.g., `false`).
131: malformed input + `len(targetPath.Segments) == 0` handles refinements targeting the root symbol directly.
132: recursion/depth limits + `rootRefinementInvalidatesDescendants` handles the logic for cascading invalidations when a root type is changed.
141: rare type combinations + `refineProductValue` handles the case where a refinement constraint is empty/nil.
151: nil/empty handling + `targetPath.Symbol == 0` or empty segments prevents processing of invalid paths in descendant refinement.
156: overflow/huge unions + `product.Equal(reg, rootValue, product.Bottom(reg))` handles the case where the value is already known to be",320,4715,
analysis/engine/factapply/call_edges.go,q0,"78: nil graph handling + `graphRPO` returns `nil` if `graph` is nil, which is safe but requires callers to check.
101: negative index + `callOutcomeTargetForResult` returns `false` for `resultIndex < 0`, preventing out-of-bounds access.
116: empty target + `callOutcomeRelatableTarget` returns `false` if `TargetSymbol` is 0 or path is empty, preventing invalid symbol tracking.
133: nil provider/graph + `applyCallOutcomeEdgeFacts` returns early if `outcomeProvider` or `ctx.Graph` is nil.
148: non-branching context + `applyCallReturnConditionRefinements` returns early if the call site isn't a conditional context.
151: disconnected branch + `callOutcomeConditionBranchPoint` returns `false` if the branch point doesn't match the current edge.
163: malformed substitution + `refinement.Target.Substitute(bindings)` returns `ok=false` if the path cannot be resolved with current bindings.
185: non-branching edge + `applyCallReturnPresenceRelations` returns early if the edge is not a branch.
211: invalid relation targets + `callOutcomeTargetForResult` or `callOutcomeRelatableTarget` returns early if indices are invalid or targets are non-assignable.
225: missing assignment points + `callOutcomeResultAssignmentPoint` returns `false`",320,4717,
analysis/engine/factapply/source_read.go,q0,NONE,2,698,
analysis/engine/factapply/call_materialize.go,q0,"34: recursion/depth limits: A self-referential call graph where `materialize` calls itself via `read`, potentially causing a stack overflow if `active[point]` logic is bypassed or mismanaged.
38: cyclic/self-referential types: The `active` map check handles recursion, but if `activeBase[point]` contains a type that refers back to the same `point` during its own materialization, it could lead to infinite logical loops in type resolution.
58: malformed input: If `facts.CallSiteView(ctx.Point)` returns a view with an invalid or unexpected structure, the subsequent loop over `ArgumentSources` might trigger a panic.
64: rare type combinations: `materializeObjectLiteralHeap` is called on arguments; if an argument is a complex type that is simultaneously being materialized, it tests the interaction between heap materialization and call-site state.
71: malformed input: If `outcomeProvider` returns a result with a `result.Index` that is extremely large or out of bounds for the registry, it could cause an index out of range panic.
81: huge unions/intersections: `constrainReturnSlot` uses `product.Meet`, which could trigger an expensive or infinite computation if the union/intersection of return types is massive or recursive.
86: overflow/out of bounds: `target.ResultIndex()` is checked for `< 0`, but a very large positive index could cause a panic in `WriteReturnSlot`.",320,4673,
analysis/engine/factflow/call_producer.go,q0,NONE,2,93,
analysis/engine/factflow/branch_path_evidence.go,q0,NONE,2,979,
analysis/engine/factapply/channel_select_branch.go,q0,"78: nil/empty handling + `resolver == nil` check handles cases where the visibility resolver is uninitialized, preventing a panic.
83: malformed input + `resultKey == """"` or `caseKey == """"` handles cases where the resolver returns empty strings for invalid or out-of-scope paths.
94: rare type combinations + `missingKnownSelect` handles the case where a type claims to be a channel select result but contains a Select ID that doesn't match any known facts, forcing the state to `Bottom`.
111: huge unions + `typeexpr.Union(caseTypes...)` could potentially cause high memory usage or slow performance if a massive number of disparate case types are collected.
138: nil/empty handling + `snapshot.Bottom` check handles cases where the state has already been determined to be impossible/bottom.
153: overflow/edge case + `len(cases) == 0 && !hasDefault` returns `typ.Never`, which is a critical boundary for type narrowing logic.
188: malformed input + `rebasePathKeyToContext` failure handles cases where facts exist for paths that cannot be logically rebased into the current context.
211: recursion/depth limits + `invalidatePathDescendantsAt` (called via `invalidateChannelSelectResultDescendants`) could potentially hit recursion limits if the path tree is extremely deep.",298,3967,
analysis/engine/factapply/channel_select_result.go,q0,"46: nil/empty handling + `events` slice is nil or empty, potentially causing `groups` to be nil and subsequent loop iterations to behave unexpectedly if not for the `range` safety.
50: malformed input + `group.resultIndex` is negative or `group.cases` is empty without a default, testing the guard against invalid channel select metadata.
66: malformed input + `event.SelectID()` returns an empty string, testing if the engine correctly ignores orphaned or unidentifiable select events.
75: rare type combinations + `event.Kind()` is neither `ChannelSelectSelect` nor `ChannelSelectReceive`, testing if the switch handles unexpected event types gracefully.
88: malformed input + `group.cases` contains events with non-monotonic or duplicate indices, testing the stability of the `sort.SliceStable` logic.
103: nil/empty handling + `ctx.Registry` is nil, which would cause a panic in `typeValues.FromTypeWithWitness` if the guard at line 103 is bypassed.
103: nil/empty handling + `cases` is empty and `hasDefault` is false, testing the early exit for select statements that cannot possibly produce a value.
114: malformed input + `resultCases` is empty after filtering invalid payloads, testing the edge case where a select has cases but none are type-valid.
126: rare type combinations + `event.PayloadValue()` and `event.",320,4602,
analysis/engine/factflow/call_result.go,q0,"54: nil/empty handling + Passing a `nil` function to `ForEachValue` to ensure it returns early without a panic.
68: nil/empty handling + Passing an empty map to `copyCallResultValueMap` to ensure it returns `nil` rather than an empty initialized map.
78: nil/empty handling + Passing a `nil` slice to `copyCallResultValueSlice` to ensure it returns `nil` rather than an empty initialized slice.",101,1720,
analysis/engine/factapply/call_outcome_apply.go,q0,"214: cyclic/self-referential types + A recursive `EscapeEvent` with a circular heap structure could cause infinite recursion in `markReachableHeapPlacement`.
263: cyclic/self-referential types + `markReachableHeapPlacement` relies on a `seen` map, but if the `identity.ID` is not correctly unique for cyclic structures, it may loop.
313: malformed input + `resolvePlacementTargetValueAt` could return a `target` with a `nil` or invalid `value` if the registry is in an inconsistent state.
331: huge unions/intersections + `product.Meet` in `mergePlacementIdentityProjection` could explode in complexity if `target.value` and `projected` are massive lattice elements.
368: recursion/depth limits + `markReachableHeapPlacement` performs a depth-first traversal of the heap; a very deep linked list or tree could cause a stack overflow.
388: recursion/depth limits + `markReachableHeapValuePlacement` calls `markReachableHeapPlacement`, creating a recursive loop that depends on the depth of the heap graph.
413: malformed input + `callArgumentPlaceholderBindings` could produce a `bindings` slice with massive gaps if `index` is extremely large, leading to high memory consumption.
435: malformed input + `callBranchProofAt` could return a `BranchProof` with an empty or invalid `Other` key if the `",320,5481,
analysis/engine/factflow/branch_path_relation.go,q0,"48: nil/empty path handling + `leftPath.Clone()` or `rightPath.Clone()` will panic if the input paths are nil.
59: nil/empty path handling + `leftPath.Clone()` or `rightPath.Clone()` will panic if the input paths are nil.
73: nil/empty path handling + `r.leftPath.Clone()` will panic if the relation was initialized with a nil path.
86: nil/empty path handling + `r.leftPath.Clone()` will panic if the relation was initialized with a nil path.
101: nil/empty map handling + returns `nil` instead of an empty map, which might cause nil-pointer dereferences in callers expecting an initialized map.
109: nil/empty slice handling + returns `nil` instead of an empty slice, which might cause issues if the caller performs operations that expect a non-nil slice.",198,3180,
analysis/engine/factflow/expression_refinement.go,q0,NONE,2,93,
analysis/engine/factapply/objects.go,q0,"111: recursion/depth limits: A deeply nested object literal structure that exceeds the stack depth of the `writeObjectLiteralHeap` recursive calls.
118: cyclic/self-referential types: An object literal where an entry's `entrySource.ExprRef` points back to the parent `source.ExprRef`, testing the `active` map cycle detection.
143: malformed input: An object entry with a `Suffix` that contains invalid or non-canonical segments, testing `heapidentity.StaticMemberSuffixKey`.
153: recursion/depth limits: A nested object literal where the `nested` object is itself a circular reference to a previously visited `ExprRef`.
156: rare type combinations: An object literal where the `rootValue` is not actually a product/table type, testing the `product.Get` failure path for `identity.Key`.
178: huge unions/intersections: An `expected` value in `overlayExpectedObjectEntryValue` that represents a massive union of types, testing the performance/stability of `product.Set`.
188: nil/empty handling: An `expected` value that is `Top` or `Bottom` for all keys, testing if the overlay logic correctly skips all `product.Set` operations.
196: malformed input: A `root` path that is empty or a `suffix` with zero segments, testing the boundary condition in `objectEntryTargetPath`.",312,5031,
analysis/engine/factapply/path_fact_apply.go,q0,"48: nil/empty handling + `targetKey == """"` returns `out` immediately, potentially masking cases where a path is invalid due to a malformed/empty path object rather than a missing key.
63: nil/empty handling + `tableKey == """"` returns `out`, which might hide errors where a dynamic index is attempted on a non-existent or invalidly resolved table path.
83: rare type combinations + `product.Get` might fail if the resolved table is not actually a table/product type (e.g., a primitive or nil), returning `ok=false`.
87: malformed input + `heapidentity.BottomObject` check; if the object is ""bottom"" (representing an error or uninitialized state), the write is silently dropped.
92: overflow/huge unions + `dynamicindex.Domain(ctx.Registry).Join(existing, value)` could lead to exponential state explosion if `existing` and `value` are both large unions of many possible keys/values.
118: nil/empty handling + `readKey` or `readValue` being false results in `Bottom` values, which might lead to ""poisoned"" facts if the engine expects a specific type.
153: malformed input + `proof.Kind()` returning an unhandled enum value (though `default` handles it, it returns an empty `BranchProof` which might be swallowed).
161: nil/empty handling + `proof.Presence()` returning `ok=false`",320,4681,
analysis/engine/factflow/facts.go,q0,NONE,2,446,
analysis/engine/factapply/path_presence_implication.go,q0,"41: nil/empty handling + If `ctx.Graph` is non-nil but `ctx.Point` is invalid/zero, the RPO traversal or `callOutcomePresenceRelationCanPublishAt` might trigger an out-of-bounds or nil pointer dereference.
68: cyclic/self-referential + If `triggerAssign` and `targetAssign` are part of a cycle where `callOutcomeLaterPoint` returns the same point, it could cause infinite loops in the calling logic.
108: recursion/depth limits + If `outcome.ReturnPresenceRelations` contains a massive number of relations, the subsequent `publishCallReturnPresenceImplication` calls could lead to stack exhaustion or extreme latency.
138: recursion/depth limits + The `for` loop in `activatePathPresenceImplications` implements a fixed-point iteration that could loop indefinitely if `stateDomain.Equal` fails to detect convergence due to floating-point-like precision issues in product meets.
143: cyclic/self-referential + If `snapshot.Implications` contains implications that trigger each other in a cycle (A implies B, B implies A), the fixed-point loop relies entirely on `stateDomain.Equal` to prevent an infinite loop.
174: malformed input + If `implication.TargetPresence` is a non-concrete/top value that somehow bypasses `presenceIsConcrete`, `product.NewWithPresence` might produce an invalid constraint.
175: rare type combinations +",320,4613,
analysis/engine/factflow/branch_relation.go,q0,"49: Nil/Empty Path Cloning: If `triggerPath` or `targetPath` are nil, `Clone()` will likely trigger a panic during relation creation.
58: Self-Referential Path Cloning: If `triggerPath` and `targetPath` are the same object, `copy()` performs two separate clones which may lead to unexpected memory overhead or identity issues.
75: Nil Map Input: Passing a `nil` map to `copyBranchPresenceRelationMap` returns `nil`, which is safe, but passing a map with `nil` values for keys might cause issues depending on `set.copy()` implementation.
85: Map Key Collision/Overwrite: In `mergeBranchPresenceRelationMap`, if `base` and `added` share the same `cfg.Point`, the logic appends relations, but if `out[point]` was uninitialized (nil), `Relations()` returns nil, potentially creating a set with a nil slice.
89: Massive Relation Explosion: `mergeBranchPresenceRelationMap` performs an $O(N^2)$ style growth by calling `Relations()` (which clones) and then `NewBranchPresenceRelationSet` (which clones again) inside a loop, leading to exponential allocation on large sets.
98: Nil Slice Handling: `copyBranchPresenceRelationSlice` returns `nil` for an empty slice, which is generally safe but can cause ""nil vs empty"" semantic mismatches in callers expecting an initialized slice.",307,3953,
analysis/engine/factflow/path_copy.go,q0,NONE,2,370,
analysis/engine/factflow/refs.go,q0,NONE,2,489,
analysis/engine/factflow/static_member.go,q0,NONE,2,74,
analysis/engine/factflow/call_target.go,q0,"39 + path.Path cloning + A `path.Path` containing a massive or deeply nested segment tree that triggers high memory allocation or stack exhaustion during `Clone()`.
58 + path.Path cloning + A `path.Path` that is nil or contains nil segments, potentially causing a panic during the `Clone()` call within the getter.
78 + path.Path cloning + A `path.Path` that is nil, which would cause a panic when calling `v.target.targetPath.Clone()`.
81 + path.Path keying + A `path.Path` with a highly complex or circular structure that causes the `Key()` method to hang or overflow the stack.
84 + path.Path emptiness + A `path.Path` that is initialized but contains zero segments, testing the boundary of ""empty"" vs ""nil"".
101 + slice nil/empty handling + Passing a `nil` slice or a slice with zero length to ensure it returns `nil` rather than an empty allocated slice.
104 + slice deep copy + A slice containing many `CallResultTarget` objects where each `targetPath` is a massive, complex object, testing the cumulative cost of the `copy()` loop.",261,3577,
analysis/engine/factflow/return.go,q0,NONE,2,395,
analysis/engine/factflow/dynamic_index.go,q0,"49: `tablePath.Clone()` + Deeply nested/recursive path structure that causes stack overflow during cloning.
58: `w.tablePath.Clone()` + Nil `tablePath` pointer causing a panic during the getter call.
71: `w.tablePath.Clone()` + Self-referential path object where `Clone()` enters an infinite loop.
76: `len(in) == 0` + Nil map input (as opposed to empty map) returning `nil` instead of an empty map.
79: `make(map[cfg.Point]DynamicIndexWrite, len(in))` + Extremely large map size causing an out-of-memory (OOM) panic during allocation.
81: `fact.copy()` + Concurrent modification of the `in` map while iterating, leading to a runtime panic.",182,2728,
analysis/engine/factflow/channel_select.go,q0,"78: nil/empty handling + Passing a `nil` `path.Path` into `NewChannelSelect` will cause a panic when calling `.Clone()`.
88: nil/empty handling + Passing a `nil` slice to `NewChannelSelectSet` returns a `nil` slice instead of an empty initialized slice.
103: nil/empty handling + Calling `ResultPath()` when `hasResultPath` is true but `resultPath` is a nil pointer/uninitialized object will panic on `.Clone()`.
110: nil/empty handling + Calling `CasePath()` when `hasCasePath` is true but `casePath` is a nil pointer/uninitialized object will panic on `.Clone()`.
131: nil/empty handling + Calling `copy()` on a `ChannelSelect` where `resultPath` or `casePath` are nil/uninitialized will panic on `.Clone()`.
145: nil/empty handling + Calling `copy()` on a `ChannelSelectSet` with an empty `events` slice returns a `ChannelSelectSet` with a `nil` slice.
148: nil/empty handling + Passing a `nil` map to `copyChannelSelectMap` returns `nil` instead of an empty map.
158: nil/empty handling + Passing a `nil` slice to `copyChannelSelectSlice` returns `nil` instead of an empty slice.",306,4070,
analysis/engine/factflow/call_site.go,q0,"94: nil/empty handling + `NewCallSite` will panic if `config.CalleePath`, `config.ReceiverPath`, or `config.MethodPath` are nil because `.Clone()` is called on them.
114: nil/empty handling + `CalleePath()` will panic if `c.calleePath` is nil.
118: nil/empty handling + `ReceiverPath()` will panic if `c.receiverPath` is nil.
123: nil/empty handling + `MethodPath()` will panic if `c.methodPath` is nil.
163: nil/empty handling + `CalleePath()` will panic if `v.site.calleePath` is nil.
171: nil/empty handling + `ReceiverPath()` will panic if `v.site.receiverPath` is nil.
176: nil/empty handling + `MethodPath()` will panic if `v.site.methodPath` is nil.
203: nil/empty handling + `ForEachResultTarget` will panic if the provided `fn` is nil (though it has a check, the caller might expect an error or no-op).
213: nil/empty handling + `copy()` will panic if any of the path fields are nil.
218: nil/empty handling + `copyCallSiteMap` returns `nil` for an empty map, which might cause a nil-pointer dereference in callers expecting an initialized empty",320,4500,
analysis/engine/factapply/paths.go,q0,"31: Nil Resolver + A `nil` resolver passed to `applyPathAssignment` which should immediately abort without attempting to access `fact` or `sources`.
35: Empty Target Path + A `fact.TargetPath()` that contains zero segments, testing if the engine correctly ignores assignments to ""nothing.""
38: Missing Source Value + A `source` that exists in the fact but cannot be resolved via `sources.ValueOfSource` (e.g., a dangling reference or uninitialized state).
43: Root Origin Invalidation Failure + A path mutation that triggers a failure in `invalidateRootOriginsForPathMutationAt`, testing if the engine reverts to the previous state.
46: Heap Static Member Invalidation Failure + A mutation to a static heap member that fails the invalidation logic, potentially leaving the state inconsistent.
49: Path Subtree Invalidation Failure + A scenario where invalidating a specific path subtree returns `ok=false`, testing the ""all-or-nothing"" atomicity of the assignment.
50: Path Subtree Invalidation Failure + A scenario where invalidating a specific path subtree returns `ok=false`, testing the ""all-or-nothing"" atomicity of the assignment.
53: Path Write Failure + A `writePathAt` call that fails (e.g., due to a read-only property or type mismatch), testing if the assignment is rolled back.
73: Empty Container Path + A `fact.ContainerPath()` that is empty, testing if descendant invalidation correctly",320,5784,
analysis/engine/state/channel_select.go,q0,NONE,2,212,
analysis/engine/factflow/expression_operation.go,q0,"33: Nil/Empty Operand: `NewUnaryExpressionOperation` with an empty string `op` or an invalid `operand` returns a zero-value struct and `false`.
39: Nil/Empty Operands: `NewBinaryExpressionOperation` with an empty string `op` or any invalid `left`/`right` source returns a zero-value struct and `false`.
53: Zero-Value/Invalid Ref: `copyExpressionOperationMap` skips entries where `ref == 0`, which could hide bugs if `0` is a valid (but unintended) reference.
53: Unknown Kind: `copyExpressionOperationMap` skips entries with `ExpressionOperationUnknown`, potentially leading to silent data loss in the resulting map.
51: Empty Input Map: `copyExpressionOperationMap` returns `nil` instead of an empty map when `len(in) == 0`, which may cause nil-pointer dereferences in callers expecting an initialized map.",204,3337,
analysis/engine/factapply/path_state.go,q0,"46: Equivalent Path Explosion + A path with a massive number of aliases could cause an $O(N)$ loop that consumes significant memory/time during state updates.
78: Invalidation Loop Failure + If `invalidate` returns `ok=false` for one alias in a large set, the entire operation aborts, potentially leaving the state partially invalidated (atomicity issue).
108: Root Mutation Symbol Collision + If `localPath.Symbol` is 0 or malformed, it skips invalidation, potentially leaving a stale ""Top"" value in a root variable.
143: Heap Object Identity Loss + If `product.Get` fails to find an identity for a heap object, the static member invalidation silently skips the object, leading to stale heap state.
147: Deeply Nested Static Member + If `localPath.Segments` is extremely deep, the recursive `WithoutStaticMember...` calls could trigger a stack overflow.
163: Cyclic Path Dependencies + If `EquivalentPathKeys` or `PathKeyDescendantInvalidationPrefixes` returns a cycle, `dedupePathKeys` handles it, but the caller might loop infinitely.
188: Target Redundancy/Memory Pressure + A path that is a prefix of many other paths could generate a massive `targets` slice, leading to high allocation overhead.
213: Empty/Nil Key Deduplication + If the input slice contains only empty strings, the function returns an empty slice, which might cause downstream logic to assume no work",320,6583,
analysis/engine/factflow/expression_condition.go,q0,"24: nil/empty handling: Passing `nil` for any of the four slice arguments to `NewExpressionCondition` tests if `copyPostconditionRefinementSlice` handles nil vs empty slices without panicking.

44: nil/empty handling: Calling `RefinementsForValue` on an `ExpressionCondition` where the corresponding slice was initialized as `nil` tests if the return value is a safe `nil` or an empty slice.

50: nil/empty handling: Calling `PathRelationsForValue` on an `ExpressionCondition` where the corresponding slice was initialized as `nil` tests if the return value is a safe `nil` or an empty slice.

65: nil/empty handling: Passing a `nil` map to `copyExpressionConditionMap` tests the explicit `len(in) == 0` check and ensures it returns `nil` rather than an empty initialized map.

65: malformed input: Passing a map with a very large number of elements tests the `make(map..., len(in))` pre-allocation for memory pressure/allocation failure.",230,3696,
analysis/engine/factflow/assignment.go,q0,"38: nil/empty handling + `NewRootAssignment` with a `nil` `targetPath` could cause a panic in `targetPath.Clone()`.
51: nil/empty handling + `NewRootAssignmentWithDeclaredValue` with a `nil` `targetPath` could cause a panic in `targetPath.Clone()`.
60: nil/empty handling + `NewRootAssignmentWithDeclaredContractValue` with a `nil` `targetPath` could cause a panic in `targetPath.Clone()`.
75: nil/empty handling + `TargetPath()` returning a clone of a `nil` `targetPath` will panic.
101: nil/empty handling + `NewPathAssignment` with a `nil` `targetPath` will panic during `targetPath.Clone()`.
106: nil/empty handling + `TargetPath()` returning a clone of a `nil` `targetPath` will panic.
116: nil/empty handling + `NewPathDescendantInvalidation` with a `nil` `containerPath` will panic during `containerPath.Clone()`.
122: nil/empty handling + `ContainerPath()` returning a clone of a `nil` `containerPath` will panic.
131: nil/empty handling + `copy()` on a `PathDescendantInvalidation` with a `nil` `containerPath` will panic.
141: nil/empty handling + `copyRootAssignmentMap` returns `nil`",320,6200,
analysis/engine/factapply/roots.go,q0,"43-45: Nil/Empty handling + A `source` that exists but returns no value via `sources.ValueOfSource`, testing the fallback logic to `declared` values.
51-53: Malformed input + A `targetPath` that has segments but a non-zero `target` symbol, which `rootAssignmentTarget` treats as an invalid root assignment.
63-65: Nil/Empty handling + A `targetPath` where `targetPath.Symbol` is non-zero but `target` is 0, testing the fallback to path-based symbol identification.
83-85: Metatable/Visibility edge case + A `resolver` that is non-nil but `invalidatePathSubtreeAt` returns a state that might be nil or corrupted if the path is invalid.
96-98: Cyclic/Self-referential + A `source.ExprRef` that points to a path identical to the `targetPath`, which should be caught by the `targetKey == sourceKey` check to prevent infinite proof loops.
99-101: Rare type combinations + A `targetKey` or `sourceKey` that is an empty string (e.g., due to a failed visibility lookup), which must not trigger a branch proof.
118-120: Malformed input + A `targetPath` that has segments (e.g., `root.field`), which should be ignored by `applyRootAssignmentNumFloor` as it only applies to",320,6256,
analysis/engine/factflow/branch.go,q0,"84: WithConstraint + huge unions/intersections: If `product.Meet` encounters a massive or deeply nested product tree, this method may trigger a stack overflow or excessive memory consumption during the meet operation.

118: NewBranchLenRefinement + overflow: If `lo` is provided as `math.MinInt64` or a value that interacts poorly with subsequent arithmetic in the engine, it could cause unexpected behavior in length checks.

148: NewBranchRefinementSet + nil/empty handling: Passing a nil slice of `BranchRefinement` to the variadic parameter relies on `copyBranchRefinementSlice` to handle the nil case without panicking.

178: WithLenRefinements + huge unions/intersections: Repeatedly calling `WithLenRefinements` in a loop with large slices can lead to $O(N^2)$ allocation behavior due to the `s.copy()` and `append` pattern.

218: mergeBranchRefinementSetMap + nil/empty handling: If `base` is nil but `add` is populated, the function must correctly initialize the map; if both are nil, it must return nil without error.

228: mergeBranchRefinementSetMap + recursion/depth limits: The nested calls to `Refinements()`, `NewBranchRefinementSet`, and `WithLenRefinements` inside a loop create a high volume of short-lived allocations and deep call stacks when merging large sets of refinements.

231",320,6245,
analysis/engine/factflow/return_presence_relation.go,q0,"68: nil/empty handling: Passing a `nil` map to `copyReturnPresenceRelationMap` returns `nil` instead of an empty map, which might cause unexpected behavior in callers expecting a non-nil initialized map.

68: nil/empty handling: Passing an empty slice to `copyReturnPresenceRelationSlice` returns `nil` instead of an empty slice, potentially breaking callers that use `len()` or range but expect a non-nil slice for serialization/JSON.

78: nil/empty handling: Passing a `nil` slice to `copyReturnPresenceRelationSlice` returns `nil`, which is a subtle distinction from an empty slice `[]ReturnPresenceRelation{}`.",145,2074,
analysis/engine/factquery/root_declaration.go,q0,"38: nil/empty handling: `graph == nil` or `point == 0` or `target == 0` prevents panics when the caller passes uninitialized or zero-value identifiers.
41: malformed input: `dominators == nil` handles cases where the CFG is disconnected or contains an invalid structure that prevents dominator computation.
44: cyclic/self-referential types: `visited` map check prevents an infinite loop if the dominator tree contains a cycle (which would violate CFG properties).
50: malformed input: `len(assignment.TargetPath().Segments) == 0` handles cases where an assignment exists but targets a nested table/field rather than the root symbol itself.
53: rare type combinations: `factflow.RootAssignmentLocalDeclaration` vs `RootAssignmentOrdinaryRootWrite` distinguishes between a variable definition and a value overwrite, which is a critical semantic boundary.
59: overflow/boundary: `parent == cursor` handles the root of the dominator tree to prevent an infinite loop when the traversal reaches the entry point.",230,2950,
analysis/engine/state/dynamic_index_lane.go,q0,"29: nil/empty key handling: A `dynamicindex.Key` with an empty `Table` string (representing a non-existent or nil table access) returns `Bottom` instead of searching the map.

32: Top-value short-circuit: If the lane is already `Top` (representing unknown/any value), it returns `Top` immediately, bypassing key lookups which might be expensive or involve complex lattice logic.

45: Admission/Bottom invariant violation: Passing a `dynamicindex.Fact` where `Admission == AdmissionBottom` to the `with` method triggers a panic, enforcing that the index cannot be updated with ""nothingness.""",139,1905,
analysis/engine/state/effect_delta_lane.go,q0,"28: nil/empty handling + An empty `key.Target` string bypasses the map lookup entirely, potentially masking missing data as a default zero-value.
31: nil/empty handling + If `l.isTop()` is true, it returns `effectdelta.Top()`, which might mask the difference between a ""missing key"" and a ""universal effect"" in lattice analysis.
46: panic/error returns + Passing a `delta.Change` of `effectdelta.ChangeBottom` triggers a panic, which is a critical failure path if the lattice reduction logic produces a bottom value.",128,1830,
analysis/engine/state/heap_table.go,q0,"21: nil/empty handling + Passing a zero-value `identity.ID{}` to bypass the write logic entirely.
24: panic/error returns + Attempting to write to a heap table when the identity lane is at its `top` boundary.
28: rare type combinations + Writing a `Bottom` domain object (representing a null/void state) to an existing ID to trigger a deletion.
29: nil/empty handling + A `without(id)` call where the ID does not exist in the current map, testing the `changed` flag logic.
34: rare type combinations + Writing an object that is logically equivalent to the existing value (via `domain.Equal`) to prevent unnecessary state propagation.",155,1788,
analysis/engine/factflow/object.go,q0,"71: Nil/Empty handling + Passing a `nil` slice to `NewObjectLiteral` results in an empty `entries` slice rather than a `nil` slice, potentially changing behavior in downstream nil-checks.

88: Identity zero-value + An `identity.ID` that is structurally equivalent to the zero-value `identity.ID{}` will return `false` for existence, even if it was explicitly set.

108: Nil/Empty handling + Passing a `nil` slice to `copyObjectEntries` returns `nil`, which is safe, but passing an empty slice `[]ObjectEntry{}` also returns `nil`, losing the distinction between ""no entries"" and ""empty entries.""

116: Nil/Empty handling + Passing a `nil` map to `copyObjectLiteralMap` returns `nil`, which is safe, but it is a common source of ""nil vs empty"" logic errors in callers expecting a non-nil map.

119: Deep Copy/Recursion + `copyObjectLiteralMap` performs a shallow copy of the map structure but relies on `ObjectLiteral.copy()`, which performs a deep copy of the `entries` slice; if `ObjectEntry.suffix` (a `path.Path`) has a circular reference in its internal structure, `e.suffix.Clone()` will cause a stack overflow.",287,4978,
analysis/engine/state/len_floors.go,q0,NONE,2,92,
analysis/engine/factflow/value_source.go,q0,"41: Shape Invariant Conflict: `Expanded=true, Adjusted=true` violates the mutually exclusive rule for value-list transformations.
44: Shape Invariant Conflict: `Expanded=true, Final=false` violates the requirement that expanded lists must be terminal.
47: Shape Invariant Conflict: `OpenTail=true` with `Expanded=false` or `Final=false` violates the requirement for tail-end open lists.
125: Kind/Flag Mismatch (Unknown): `ValueSourceUnknown` with a non-zero `ExprRef` or `CallPoint` should fail validation.
127: Kind/Flag Mismatch (Expression): `ValueSourceExpression` with a `CallPoint` or zero `ExprRef` should fail validation.
129: Kind/Flag Mismatch (Call): `ValueSourceCall` with a zero `CallPoint` or negative `ResultIndex` should fail validation.
131: Kind/Flag Mismatch (Vararg): `ValueSourceVararg` with a non-zero `CallPoint` should fail validation.
133: Kind/Flag Mismatch (Nil): `ValueSourceNil` with any shape flags (e.g., `Final=true`) or non-zero indices should fail validation.
134: Kind/Flag Mismatch (Nil): `ValueSourceNil` with `HasExpr=true` should fail validation.
154: Boolean Flag Desync: `HasExpr` set to `true`",320,4148,
analysis/engine/state/lenbound/lenbound.go,q0,NONE,2,132,
analysis/engine/state/effect_delta.go,q0,"16: `key.Target == """"` + An empty string target bypasses all delta logic, potentially masking logic errors where a caller expects a state change that never occurs.
19: `s.effectDeltas.top` + Attempting to write to a state that has reached its maximum stack/lane depth, triggering a panic.
22: `delta.Change == effectdelta.ChangeBottom` + Attempting to ""delete"" a key that does not exist in the current state, testing the `without` method's ability to handle non-existent keys without side effects.
30: `s.effectDeltas.read(key) == delta` + A ""no-op"" write where the new delta is identical to the existing one, testing if the engine correctly avoids unnecessary state allocations/cloning.",175,2743,
analysis/engine/state/frozen_table.go,q0,"41: identity.ID zero-value check + Passing a zero-value `identity.ID{}` (where Kind, Site, and Index are all 0) to `isFrozen` to ensure it doesn't falsely report a table as frozen.

47: identity.ID zero-value check + Passing a zero-value `identity.ID{}` to `freeze` to ensure it returns `changed=false` and doesn't pollute the lattice with a null identity.

68: Identity comparison/ordering + Providing two `identity.ID` values where `Kind` is identical but `Site` or `Index` are at their maximum integer boundaries to test the stability of the `identityIDLess` sort.

68: Identity comparison/ordering + Providing two `identity.ID` values where `Kind` is different but the `Kind` values are at the extreme ends of their type range.",191,2757,
analysis/engine/sourcevalue/source_floor.go,q0,"47 + Cyclic Recursion + An expression that references itself (e.g., `x = x + 1`) to trigger the `active` map check and prevent infinite recursion.
85 + Overflow + An operation where the result of `left + (-c)` exceeds `math.MinInt64` (e.g., `MinInt64 - 1`).
104 + Overflow + An operation where `floor + c` exceeds `math.MaxInt64` (e.g., `MaxInt64 + 1`).
115 + Overflow + An operation where `floor + c` is less than `math.MinInt64` (e.g., `MinInt64 - 1`).
118 + Integer Overflow + A subtraction operation where the operands are valid but their mathematical difference cannot be represented by an `int64`.
36 + Nil Resolver + Passing a `nil` resolver to ensure the function returns `0, false` instead of panicking during `RootOrVisibleKeyAt`.
36 + Malformed Source + A `ValueSource` where `Kind` is `ValueSourceExpression` but `HasExpr` is `false`.
100 + Non-Integer Constant + A binary operation where one operand is a valid expression but its value is a float or string, causing `exactIntegerSource` to fail.",293,4089,
analysis/engine/state/dynamic_index.go,q0,"41: `key.Table == """"` + An empty string key that bypasses the write logic entirely, potentially masking a logic error where a fact was intended to be cleared/updated.
44: `s.dynamicIndex.top` + A state where the dynamic index has reached its maximum capacity/depth, triggering a panic during a write attempt.
48: `domain.Equal(fact, domain.Bottom())` + An adversarial ""Bottom"" fact (representing an empty set or contradiction) used to trigger the deletion/removal logic of an existing key.
49: `s.dynamicIndex.without(key)` + A scenario where the key being ""removed"" does not actually exist in the index, testing the `changed` flag logic.
53: `domain.Equal(s.dynamicIndex.read(reg, key), fact)` + A redundant write where the new fact is identical to the existing one, testing if the engine avoids unnecessary state cloning/reachability recalculations.
56: `s.reachable()` + A massive or highly interconnected state where calculating the ""reachable"" set after a fact update causes an exponential explosion in memory or time.",245,3401,
analysis/engine/state/channelselectfact/channelselectfact.go,q0,"89: nil/empty handling + Adding a fact to a `nil` map (via `l.facts == nil`) tests if the `Add` method correctly initializes the underlying map when transitioning from a `Top` (empty) state.

104: nil/empty handling + `Has` on a `bottom` lane ensures that a lane explicitly marked as `bottom` does not panic when checking for a fact, even if the map is nil.

113: nil/empty handling + `Snapshot` on a `bottom` lane ensures that a `bottom` state returns a specific `Bottom: true` flag rather than an empty `Top: true` snapshot.

123: nil/empty handling + `factsFromSet` with a `nil` map ensures that an empty or uninitialized fact set returns a `nil` slice rather than a slice with length 0, preventing potential nil-pointer dereferences in callers.

133: malformed input + `factLess` stability + if two `Fact` structs have identical fields except for `HasDefault`, the sorting logic must handle the boolean comparison without panicking or creating unstable sort orders.

143: nil/empty handling + `cloneSet` with a `nil` map ensures that cloning an empty/uninitialized set returns `nil` instead of an empty initialized map, preserving the ""empty"" semantic.",291,3661,
analysis/engine/state/num_floors.go,q0,NONE,2,406,
analysis/engine/state/numbound/numbound.go,q0,NONE,2,599,
analysis/engine/state/pathevidence/lane.go,q0,NONE,2,119,
analysis/engine/sourcevalue/path_read.go,q0,"41 + Nil/Empty Path Key + `resolver.KeyAt` returns an empty string, which should be handled gracefully without attempting to read from the state.
46 + Bottom Value Identity + `in.ReadPathKey` returns a value that is logically `Bottom`, requiring the function to return `false` rather than a ""valid"" bottom value.
50 + Canonical Path Fallback + A path key that is not canonical but its canonical counterpart is `Bottom`, testing the double-check logic.
65 + Identity Mismatch + A heap object where the `identity.Key` exists but the ID is invalid or does not match the expected object ID.
71 + Root/Self-Reference Loop + A heap object where `object.Root()` returns a different ID than the object itself, testing the cycle/identity check.
75 + Meet-to-Bottom + A case where the intersection (meet) of the root and the value results in `Bottom`, indicating the value is not a valid subset of the root.
81 + Canonical Suffix Fallback + A suffix that is not a direct member but has a canonical relative static member key that exists.
87 + Presence/Absence Join + A value that is logically `Absent` but has a member that is `Present`, testing the presence-join logic for gradual typing.
94 + Absent Presence + A value that is explicitly marked as `Absent` (nil-like), which should immediately disqualify it from being a table.
97 + Top/Bottom",320,4709,
analysis/engine/factflow/postcondition.go,q0,"53: Nil/Empty handling + Passing a `nil` slice to `NewPostconditionRefinementSet` might result in an empty slice or nil depending on `copyPostconditionRefinementSlice` implementation.
84: Nil/Empty handling + `Refinements()` on a `PostconditionRefinementSet` with a `nil` internal slice returns `nil` instead of an empty slice.
118: Nil/Empty handling + `copyPostconditionRefinementMap` returns `nil` when input is an empty map, which might cause unexpected `nil` vs `empty` behavior in callers.
127: Nil/Empty handling + `copyPostconditionPathRelationMap` returns `nil` when input is an empty map.
145: Nil/Empty handling + `mergePostconditionMap` with a `nil` `base` map and a non-empty `add` map relies on `copyMap(nil)` returning `nil` and the subsequent `out == nil` check.
156: Nil/Empty handling + `mergePostconditionMap` with a non-empty `base` map and a `nil` `add` map returns a copy of `base` without error.
164: Nil/Empty handling + `mergePostconditionMap` with a `nil` `base` map and a `nil` `add` map returns `nil`.
168: Nil/Empty handling + `mergePostconditionMap` with a non-empty `base` map and",320,5823,
analysis/engine/solve/solver.go,q0,"148 + `s.order[c] = len(s.order)` + A `Cell` type that is a slice or map (non-comparable) would cause a runtime panic during assignment or map keying.
178 + `s.cur[c] = initial(c)` + `Initial` returning a value that violates the lattice's `Bottom` property or is `nil` when the lattice expects a concrete type.
201 + `v := s.domain.Bottom()` + `Bottom()` returning a value that is not ""equal"" to itself according to `Lattice.Equal`.
233 + `insertAt := sort.Search(...)` + A `Cell` type with a custom `Less` logic (if implemented via interface) that is inconsistent with its `comparable` equality.
253 + `next = s.domain.Join(prev, v)` + A `Join` implementation that is not monotone, causing the solver to oscillate or fail to converge.
256 + `if s.widenAt(d) && s.visits[d] > 0` + `WidenAt` returning `true` for a cell that is not part of a cycle, potentially causing premature loss of precision.
257 + `next = s.domain.Widen(prev, next)` + A `Widen` implementation that does not satisfy the Ascending Chain Condition (ACC), causing an infinite loop.
262 + `next =",320,5366,
analysis/engine/state/pathevidence/snapshot.go,q0,NONE,2,402,
analysis/engine/state/pathevidence/static_member.go,q0,NONE,2,388,
analysis/engine/sourcevalue/source_values.go,q0,"148: recursion/depth limits + A cyclic `ExpressionRefinement` where `refinement.Source()` points back to the same `ExprRef`, causing a stack overflow.
188: cyclic/self-referential types + An `ExpressionOperation` where the `Left` or `Right` operand is the same `ExprRef` as the operation itself, causing infinite recursion.
218: cyclic/self-referential types + A `ValueSourceExpression` that points to an `ExprRef` which is itself an operation containing that same `ExprRef` as a child.
233: malformed input + A `ValueSourceCall` with a negative `ResultIndex` (though checked, it tests the error path of the `read` function's return).
278: recursion/depth limits + A deeply nested `ObjectLiteral` where the `resolve` callback triggers a chain of `ValueOfSource` calls that exceeds stack limits.
313: recursion/depth limits + A `WithExpressionRefinements` wrapper where the `refinements` map contains a cycle of refinements, leading to infinite recursion in `valueOfSource`.
328: panic/error returns + Passing a `nil` `Registry` to `WithExpressionRefinements`, which triggers a panic.
335: recursion/depth limits + A `WithExpressionRefinements` wrapper where the `base` provider is also a `expressionRefinementSourceValues` that forms a cycle.",316,5528,
analysis/engine/state/snapshot.go,q0,NONE,2,425,
analysis/engine/state/effectdelta/effectdelta.go,q0,"58: nil/empty handling + Passing a `nil` registry to `Domain` which propagates to `product.Domain(reg)` and potentially causes a panic in the underlying lattice implementation.
71: nil/empty handling + `Equal` check where one operand is `ChangeBottom` and the other is a non-bottom value, testing the short-circuit logic for the lattice's least element.
81: rare type combinations + `LessOrEq` where `a.Change` is not `Bottom` but `b.Change` is `Bottom`, which should return `false` according to the logic.
94: rare type combinations + `Join` where both `a` and `b` are non-bottom but have conflicting `Before`/`After` values that result in a `Top` value in the product domain.
106: recursion/depth limits + `Widen` being called on deeply nested product values where the `valueDomain.Widen` might trigger infinite recursion if the product domain isn't properly bounded.
118: nil/empty handling + Passing a `nil` registry to `Bottom`, which is explicitly handled but tests if the returned `Value` is safely usable by callers.
131: malformed input + `changeLessOrEq` with an undefined `Change` value (e.g., `Change(255)`) which might bypass the explicit equality/bottom/unknown checks.
135: malformed input + `changeJoin` with an",320,5083,
analysis/engine/state/len_floor_lane.go,q0,"41: empty pathKey + An empty string `""""` passed as a `pathKey` which might bypass logic if the caller assumes non-empty keys are always valid.
42: bottom lane + A lane where `Bottom()` is true, which should return `false` even if the key exists in an underlying map.
45: non-positive floor + A `lenbound.Floor` where `Lo` is exactly `0` or negative, which should be treated as ""not found"" or ""invalid"" for length bounds.
51: non-positive write value + A `write` call with `lo <= 0`, which should be rejected as an invalid length bound.
53: redundant write + A `write` call where the new `lo` is greater than or equal to the `existing.Lo`, which should result in no change (idempotency check).
57: nil map clone + A scenario where `mapedit.Clone` returns `nil` (e.g., on an empty/bottom lane), requiring the manual initialization of the map.
58: map initialization + A case where the cloned map is `nil`, testing the fallback to `make(map..., 1)`.",259,3458,
analysis/engine/state/value_lane.go,q0,NONE,2,81,
analysis/engine/state/placement_lane.go,q0,"34: nil/empty handling + An `identity.ID` initialized to its zero value (empty string/struct) triggers a fallback to `placement.Bottom` instead of a map lookup.
37: boundary condition + If `isTop()` is true, the function returns `placement.Unknown` regardless of whether the specific `id` exists in the underlying map.
51: panic/error returns + Passing `placement.Bottom` to the `with` method triggers an immediate panic, which is an adversarial way to break state construction.",114,1855,
analysis/engine/state/heap_table_lane.go,q0,"34: nil/empty identity ID + An attempt to read from the lane using a zero-value `identity.ID{}` which should return `BottomObject` rather than panicking or looking up in the map.

40: top/bottom boundary + A `read` operation on a lane marked as `top` (representing unknown/all possible tables) which must return `TopObject` instead of attempting to access a nil `l.values` map.

43: missing key/bottom boundary + A `read` operation for an ID that exists in the lattice domain but is missing from this specific lane's map, requiring a fallback to `BottomObject`.

54: top/bottom boundary + A `hasFinite` check on a `top` lane which must return `false` even if the ID is theoretically valid in the global domain.

62: map mutation/immutability + A `without` call where the ID is not present in the map, which must return the original lane and `false` to prevent unnecessary state propagation.

69: nil map/initialization + A `with` call on a lane where `l.values` is `nil` (possible if `top` was true and then transitioned), requiring the creation of a new map to avoid a panic.

72: deep copy/aliasing + A `with` call where the `object` being inserted is a complex table; if `heapidentity.CloneObject` fails to perform a deep copy, subsequent mutations to the original object",320,4833,
analysis/engine/state/heapidentity/heapidentity.go,q0,"148 + `withoutStaticMembersMatching` + `prefix` is an empty slice or contains segments that don't exist in any keys, potentially causing a no-op that returns the original object instead of a clone.
153 + `withoutStaticMembersMatching` + `address.RelativeStaticMemberSuffixSegments(key)` returns `ok=false` + a malformed or root-level `PathKey` that bypasses the prefix check entirely.
168 + `withoutStaticMembersMatching` + `len(out.staticMembers) == 0` + an edge case where all members are deleted, ensuring the map is nil-ified to prevent memory leaks or inconsistent state.
185 + `ObjectDomain.Equal` + `a.bottom` and `b.bottom` are both true + verifying that two different ""Bottom"" objects (with different roots) are considered equal.
203 + `ObjectDomain.LessOrEq` + `a.bottom` is true and `b.bottom` is false + ensuring the lattice property that Bottom is less than everything else holds.
216 + `ObjectDomain.Join` + `a.bottom` and `b.bottom` are both false but `a.root` and `b.root` are `Top` + checking if joining two ""Top-ish"" objects behaves correctly.
231 + `ObjectDomain.Widen` + `prev.bottom` and `next.bottom` are both false + checking if widening two non",320,4595,
analysis/engine/state/placement.go,q0,"14: `id == (identity.ID{})` + An empty/zero-value identity ID that should be ignored rather than processed or stored.
17: `s.placement.isTop()` + Attempting to write to a ""top"" lane which triggers a panic (testing the invariant that the top lane is immutable/read-only).
21: `value == placement.Bottom` + Deleting a placement by setting it to `Bottom`, specifically testing the case where the ID doesn't exist or is already `Bottom` (the `!changed` path).
21: `value == placement.Bottom` + A ""deletion"" that triggers a full state recalculation via `s.reachable()`, testing if the state remains consistent after a removal.
28: `placement.Equal(...)` + A write operation where the new value is logically equivalent to the existing value, testing the short-circuit to prevent unnecessary state cloning/reachability updates.",203,2518,
analysis/engine/state/map_lane.go,q0,"48 + nil/empty handling + `l.values` being nil while `l.bottom` is false (if `mustSetLane` was manually constructed) would cause `len(in)` to panic in `sortedSetValues`.

78 + nil/empty handling + `l.values` being nil while `l.bottom` is false would cause `mapedit.Clone(l.values)` to potentially return nil, leading to a panic on the subsequent `values[v] = struct{}{}`.

101 + malformed input + `elem.Top()` returning a value that, when passed to `wrap`, results in a non-nil but logically ""empty"" or ""invalid"" state that `Equal` might not catch.

115 + huge unions/intersections + `domain.Top()` returning a massive, non-finite map that causes an OOM when `asMap` is called.

124 + nil/empty handling + `l.top` being true while `l.values` is nil (valid) vs `l.top` being false while `l.values` is nil (invalid/panic).

134 + nil/empty handling + `l.top` being true and `l.values` being nil, which is the intended ""Top"" state, but `without` or `with` might not be called on Top states by callers, leading to unexpected behavior if they are.

143 + nil/empty handling + `l.top` being true",320,4195,
analysis/internal/hash/hash.go,q0,NONE,2,374,
analysis/internal/mapedit/mapedit.go,q0,NONE,2,77,
analysis/engine/state/pathevidence/branch_proof.go,q0,"48: Cyclic/Self-referential types: A chain of `BranchProofPathEqual` proofs where `A == B`, `B == C`, and `C == A` could cause an infinite loop in the BFS queue if `seen` logic is bypassed or if `rebaseEquivalentPathKey` returns the same key.

53: Malformed input: If `equivalentPathKeysForProof` returns a slice containing the original `pathKey` itself, the `seen` map prevents a loop, but the `out` slice will contain the input key, which might violate caller expectations of ""equivalent"" meaning ""different but equal.""

78: Nil pointer/Panic: If `matches` is passed as `nil` to `deleteBranchProofsMatching`, the code handles it via `branchProofMatchesPath`, but if `branchProofMatchesPath` were called directly by a caller without that check, it would panic.

103: Nil pointer/Panic: If `matches` is `nil`, `branchProofMatchesPath` returns `false`, but if the logic inside `matches` is expected to be called for all proofs, it might mask a logic error where a user expected a filter to be applied.

125: Huge unions/Memory exhaustion: A very large map of `BranchProof` combined with a high-frequency call to `branchProofsFromSet` will cause massive allocations of `BranchProof` slices and trigger heavy GC pressure during the `sort.Slice` operation.",311,4090,
analysis/engine/state/path_evidence.go,q0,"46: nil/empty handling + `pathaddr.LocalKeyFromPathKey` returning `ok=false` for a malformed or non-local `pathKey` (e.g., a global or invalidly constructed key).
56: nil/empty handling + `WritePathKey` receiving a `product.Bottom(reg)` value, which triggers a deletion of the finite entry.
67: nil/empty handling + `UpdatePathKey` receiving a `fn` that transforms a valid value into `product.Bottom(reg)`, testing the removal logic.
79: recursion/depth limits + `InvalidatePathKeySubtree` being called on a deeply nested or cyclic structural path-key that might cause stack overflow in the underlying `pathEvidence`.
90: recursion/depth limits + `InvalidatePathKeyDescendants` being called on a path-key that has a massive number of descendants, testing the efficiency/depth of the invalidation traversal.
113: malformed input + `ReadPathStaticMember` being called with a `pathKey` that does not resolve to a valid local key.
119: nil/empty handling + `WritePathStaticMember` being called with a `product.Value` that is nil or represents an uninitialized state.
141: malformed input + `AddIndexInRangeProof` being called with empty string `indexKey` or `arrayKey`, testing the early-exit guard.
149: malformed input + `HasIndex",320,4385,
analysis/engine/state/num_floor_lane.go,q0,"44: empty pathKey + `read` returns `0, false` even if the underlying lane contains a value for an empty string key.
54: non-existent pathKey + `read` returns `0, false` without triggering a panic on a potentially nil `l.lane.Values()`.
59: empty pathKey + `write` returns the original lane without modification, ignoring the update.
62: redundant write + `write` returns `false` if the new `lo` is not strictly less than the existing `Lo`, testing the lattice monotonicity.
67: nil map clone + `write` handles a `nil` return from `mapedit.Clone` by initializing a new map, preventing a panic.
76: empty pathKey + `clear` returns the original lane without modification.
77: bottom lane + `clear` returns the original lane immediately, avoiding unnecessary map operations on a ""bottom"" state.
80: non-existent pathKey + `clear` returns `false` if the key is missing, preventing accidental ""no-op"" writes that look like successes.
83: total depletion + `clear` sets the internal map to `nil` when the last key is removed, testing the transition to a potential ""bottom"" state.",274,4878,
analysis/engine/state/internal/floor/floor.go,q0,"33: `Widen` monotonicity violation + If `ops.Lo(next)` is significantly larger than `ops.Lo(prev)` but still within `int64` range, it triggers `TopLo` to prevent infinite ascending chains.
35: `Widen` non-monotonicity + If `ops.Lo(next)` is actually *smaller* than `ops.Lo(prev)`, the function returns a `minInt64` instead of `TopLo`, potentially breaking the lattice property if the caller assumes `Widen` only handles increases.
37: `Join` / `Widen` integer overflow + If `ops.Lo` returns `math.MinInt64` or `math.MaxInt64`, the comparison logic in `minInt64` and the `>` check in `Widen` must be verified against boundary behavior.
19: `Equal` identity + If `ops.Lo(a)` and `ops.Lo(b)` return values that are mathematically equal but represent different semantic states (e.g., different `T` wrappers), the lattice treats them as identical.
22: `LessOrEq` inversion + The implementation uses `ops.Lo(a) >= ops.Lo(b)`, meaning a *larger* `Lo` value is ""less than"" (more specific) in the lattice order; a test case with inverted `Lo` logic would break the entire convergence engine.
25: `Join` bottom-heavy + If `ops.",320,5596,
analysis/engine/state/pathevidence/domain.go,q0,"33 + Bottom/Top identity + A `Join` or `LessOrEq` operation where one operand is the `Bottom` lane (all `true` flags) and the other is `Top` (all `false` flags) to ensure the lattice doesn't collapse or panic.
41 + Equal/LessOrEq short-circuiting + Comparing two `Lane` objects where one has `refinementsBottom: true` and the other has a populated `refinements` map, testing if the `refinementLane` helper correctly handles the transition from the ""Bottom"" sentinel to actual data.
51 + Join/Widen component divergence + A `Join` where `refinements` and `staticMembers` result in different ""Bottom"" states (one is bottom, one is not), testing if `fromLanes` correctly reconstructs the `Lane` struct's boolean flags.
78 + Nil/Empty Map handling + Calling `refinementLane` on a `Lane` where `refinementsBottom` is false but the underlying `l.refinements` map is nil or empty.
98 + Massive Union/Intersection + A `Join` operation involving two `Lane` objects with extremely large `refinements` or `staticMembers` maps to check for memory exhaustion or performance degradation in the underlying `product.Domain`.
106 + Bottom state reconstruction + A `fromLanes` call where all input lanes are `Bottom` to ensure the resulting `Lane` has all `...",320,4402,
analysis/ir/dominance/doc.go,q0,NONE,2,101,
analysis/ir/dominance/dominators.go,q0,NONE,2,144,
analysis/ir/dominance/info.go,q0,NONE,2,382,
analysis/engine/state/store_relation.go,q0,"41: nil/empty handling: A `StoreRelation` with an empty string for `Source` or `Into` (representing a null/invalid path) is silently ignored by `has` and `add`.
47: nil/empty handling: An attempt to `add` a relation with an empty `Source` or `Into` returns the original lane without error, potentially masking logic errors in the caller.
68: malformed input: If `s.storeRelations.snapshot` returns a `nil` slice for `relations` due to an internal lattice error, the snapshot will contain a `nil` slice rather than an empty one.
81: rare type combinations: The `storeRelationLess` comparison relies on string comparison of `PathKey`; if `PathKey` contains non-standard UTF-8 or control characters, the lattice ordering might behave unexpectedly during merges.",185,2911,
analysis/ir/ssa/ssa.go,q0,NONE,2,183,
analysis/engine/state/pathevidence/refinement.go,q0,"21: Empty pathKey + existing refinements: Verifies that an empty string key is ignored even if it would otherwise trigger a state change or deletion.
31: Writing Bottom to a non-bottom lane: Tests the transition where a specific refinement is deleted, potentially triggering a change in the lane's reachability status.
33: Deleting a key that doesn't exist: Tests the logic path where `deletePathValueEntry` is called for a key not present in the map, ensuring no false ""changed"" signals.
41: Writing an identical value to an existing key: Tests the short-circuit logic that prevents unnecessary cloning and reachability recalculations when the value is idempotent.
46: Nil map initialization: Tests the edge case where `l.refinements` is nil (not just empty), ensuring the lane can recover from a zero-value state.
55: UpdatePathKey with a mutating/panicking function: Tests the stability of the read-modify-write cycle if `fn` returns a value that triggers complex domain logic or panics.
66: Deleting the last remaining key: Tests the transition where the map becomes empty, specifically checking if the function correctly returns `nil` to signal an empty refinement set.",264,4658,
analysis/lua/bind/bind.go,q0,NONE,2,88,
analysis/lua/bind/name_normalization.go,q0,NONE,2,232,
analysis/lua/bind/result.go,q0,NONE,2,140,
analysis/engine/state/pathevidence/invalidation.go,q0,"84 + cyclic/self-referential proofs + A `BranchProofPathEqual` where `Path == Other` could cause infinite loops in the BFS queue if `seen` logic is bypassed or if `addSubtreeAliases` doesn't handle identity.
103 + recursion/depth limits + A chain of `BranchProofPathEqual` aliases (e.g., `a=b, b=c, c=d...`) could lead to massive memory consumption or extremely long execution times in the `queue` processing.
135 + cyclic/self-referential types + `expandDescendantInvalidationPrefixes` uses two separate queues; if `addDescendantAliases` and `addSubtreeAliases` create a cycle between `descSeen` and `subtreeSeen`, the loop may not terminate.
168 + malformed input + `addSubtreeAliases` relies on `pathaddr.RebasePathKey`; if that function returns `ok` for a nonsensical or non-hierarchical rebase, it could corrupt the invalidation set.
213 + nil/empty handling + `sortedPathKeySet` returns `nil` for empty maps, which is fine, but if callers expect an empty slice `[]pathdom.PathKey{}`, it might cause nil-pointer dereferences in downstream logic.
248 + malformed input + `pathKeyInSubtree` returns `false` if `parsePathKeyPair` fails, but if the ""malformed"" key is actually a valid path that just doesn",320,5640,
analysis/lua/bind/result_copy.go,q0,NONE,2,389,
analysis/internal/recursion/recursion.go,q0,"14: depth > maxDepth + 1: If `Enter` is called when `depth` is already exactly `maxDepth + 1`, it returns `false`, but if the caller ignores the boolean and uses the returned `Guard`, they may bypass the limit on the subsequent call.
18: maxDepth = -1: A negative `maxDepth` will cause `Enter` to immediately return `false` on the first call, potentially breaking logic that expects at least one level of recursion.
18: maxDepth = math.MaxInt: If `maxDepth` is set to the maximum possible integer, `next.depth++` will overflow to a negative number, causing the guard to allow infinite recursion.",151,2087,
analysis/lua/bind/walk.go,q0,NONE,2,392,
analysis/engine/state/pathevidence/path_presence_implication.go,q0,"28: nil/empty handling + `clonePathPresenceImplicationSet` returning `nil` when the input map is empty, potentially causing issues if the caller expects a non-nil map for subsequent operations.

38: nil/empty handling + `validPathPresenceImplication` returning `false` if `Trigger` or `Target` are empty strings, which might be valid path keys in some malformed AST scenarios.

44: malformed input + `pathPresenceImplicationPresenceValid` returning `false` if the `presence.Value` is a non-standard/custom type that doesn't equate to `Present` or `Absent`.

65: nil/empty handling + `deletePathPresenceImplicationsMatching` returning the original `in` map if `len(in) == 0`, which might lead to unexpected side effects if the caller expects a new copy.

79: nil/empty handling + `pathPresenceImplicationMatchesPath` returning `false` if the `matches` function is `nil`, which could mask logic errors in the caller.

93: nil/empty handling + `pathPresenceImplicationsFromSet` returning `nil` when the input map is empty, which might cause panics in callers attempting to iterate or access index 0.

103: rare type combinations + `pathPresenceImplicationLess` relying on `.String()` comparison, which could lead to unstable or incorrect sorting if two different `presence.Value` types produce the same string representation.",316,6269,
analysis/engine/transfer/transfer.go,q0,"48: Nil/Empty handling: If `graph.RPO()` returns a nil slice, the `len(cells) != 0` check handles it, but if the graph is disconnected, the solver may never reach certain points.
68: Nil/Empty handling: If `config.Entry` is provided but points to a value not present in the graph's RPO, `validateConfig` will panic.
81: Nil/Empty handling: If `config.Initial` is provided but returns `ok=false` for the entry point, the logic falls back to `config.EntryState`, which might be `nil` or `Bottom` depending on the domain.
85: Nil/Empty handling: If `config.Initial` returns a state that is not compatible with the `domain` (e.g., a non-bottom state for a point that should be bottom), the solver may fail to converge.
101: Malformed input: If `graph.Successors(point)` returns a successor that is not part of the original `cells` list, the solver's internal state management may become inconsistent.
103: Malformed input: If `graph.IsBranch(point)` returns true but `graph.EdgeCond` returns inconsistent values for different successors, the `hasCond` logic may produce incorrect edge transfers.
133: Panic/Error returns: If `config.Graph` is nil, the function panics immediately.
136: Panic/Error returns:",320,4657,
analysis/engine/visibility/resolver.go,q0,"71: Nil Receiver/Source: Calling `KeyAt` on a nil `*Resolver` or a resolver with a nil `source` returns an empty string instead of panicking.
78: Zero-Version Visibility: A symbol that exists but has an SSA version of `0` (uninitialized or non-versioned) returns an empty string.
88: Nil Resolver in Helper: Passing a `nil` `PathKeyResolver` to `RootOrVisibleKeyAt` returns an empty string rather than panicking.
89: Empty Segment List: A path with a valid `Symbol` but zero `Segments` bypasses the resolver and returns the raw `path.Key()`.
100: Non-Positive Version: Passing a `version <= 0` to `KeyForVersion` returns an empty string, even if the symbol is valid.
103: Cache Miss/Empty Segments: A path with zero segments that is not in the `root` cache triggers a string-based key generation via `VersionedRootString`.
108: Single Segment Cache Collision: A path with exactly one segment uses a `singleSegment` struct for caching, which could be stressed by many unique segments for the same symbol/version.
114: Failed Segment Resolution: A single-segment path where `pathaddr.LocalKeyForVersion` fails (e.g., due to an invalid segment type) returns an empty string.
119: Nil Receiver Fallback: If `r` is `",320,4313,
analysis/internal/keycodec/codec.go,q0,"14: value = 0 + The loop logic `i--` followed by `value == 0` break might produce unexpected results or index out of bounds if the logic assumes `value` is always > 0.
35: leading zeros + A string like `""0123""` which is technically a valid number but rejected to prevent ambiguity/octal confusion.
44: uint64 overflow + A string of 20 digits (e.g., `""18446744073709551616""`) that exceeds `math.MaxUint64`.
55: index out of bounds + An index `i` exactly equal to `len(s)` or a negative index.
65: signed int overflow + A valid `uint64` that is larger than `math.MaxInt64` (e.g., `""9223372036854775808""`), which would overflow a signed `int`.
65: zero value rejection + The string `""0""` being rejected by the `n == 0` check, which might be an error if zero is a valid ID.",259,3796,
analysis/lua/cfgbuild/builder_symbols.go,q0,NONE,2,240,
analysis/engine/visibility/flow.go,q0,"41: nil/empty handling + `config.Graph` being nil returns an empty table instead of panicking.
44: nil/empty handling + `graph.RPO()` returning an empty slice returns an empty table.
101: malformed input + `def.Symbol == 0` (null symbol) is silently dropped during normalization.
104: malformed input + `def.Point` not existing in the RPO index is silently dropped.
133: cyclic/recursion + `len(knownPreds) == 0` handles entry points or disconnected components by returning nil.
153: rare type combinations + `root == """"` logic assumes the first version encountered has a non-empty root, which might fail if the first version is a zero-value.
163: overflow + `next[sym]++` for a symbol that is redefined in a tight loop or massive CFG could theoretically overflow `int`.
164: recursion/depth limits + `phis[key]` lookup and insertion in a loop with `changed = true` could lead to infinite loops if the CFG contains cycles that prevent convergence.",246,4694,
analysis/engine/visibility/visibility.go,q0,"44: nil/empty handling: Calling `VisibleVersion` on a nil `*Table` pointer to ensure it returns a zero `ssa.Version` instead of panicking.
45: malformed input: Passing `sym == 0` (the zero value for `symbol.ID`) to `VisibleVersion` to ensure it treats it as an invalid lookup.
51: malformed input: Passing `sym == 0` or a `version.ID <= 0` to `set` to ensure invalid versions are not persisted in the map.
54: nil/empty handling: Calling `set` on a `Table` where the internal `visible` map has not been initialized (lazy initialization check).
73: nil/empty handling: Calling `Define` on a nil `*Builder` pointer to ensure it returns a zero `ssa.Version` instead of panicking.
74: malformed input: Passing `sym == 0` to `Define` to ensure it returns a zero `ssa.Version`.
88: nil/empty handling: Calling `SetVisible` on a nil `*Builder` pointer to ensure it returns silently instead of panicking.
89: malformed input: Passing `sym == 0` or a `version.ID <= 0` to `SetVisible` to ensure invalid versions are ignored.
92: nil/empty handling: Calling `SetVisible` on a `Builder` where the `visible` map is uninitialized.
97:",320,4460,
analysis/ir/dominance/helpers.go,q0,"49: validPoint + Out-of-bounds index + A `cfg.Point` value that is negative or exceeds `graphSize` (e.g., a corrupted or uninitialized index).

53: buildRPONumbers + Partial RPO coverage + An `rpo` slice containing points that are not present in the `graphSize` range, testing the `-1` default assignment.

53: buildRPONumbers + Disjoint RPO + An `rpo` slice that is missing certain points within the `graphSize`, ensuring they remain `-1`.

63: sortByRPO + Out-of-order/Invalid points + A `points` slice containing indices that do not exist in the `rpoNum` mapping, testing the fallback to `-1` comparison.

63: sortByRPO + Duplicate RPO values + Multiple points having the same RPO index, forcing the tie-breaker `int(a) - int(b)` to execute.

63: sortByRPO + Integer Overflow + Extremely large `cfg.Point` values where `int(a) - int(b)` could theoretically overflow if `cfg.Point` is a 64-bit type and the difference exceeds `int` limits.",263,3478,
analysis/engine/state/state.go,q0,"54: nil/empty handling + `ReadSymbolValue` with `sym == 0` returns `product.Bottom` instead of panicking, which might mask logic errors in symbol resolution.
66: panic/error returns + `WriteValue` panics if `s.values.top` is true, which is a critical failure if the solver incorrectly attempts to write to a ""Top"" state.
78: nil/empty handling + `UpdateValue` passes `s.ReadValue` (which can return `Bottom`) into `fn`, which might cause `fn` to panic if it expects a concrete type.
138: recursion/depth limits + `Join` and `Widen` perform deep structural joins across 11 different lanes; a highly complex state with massive maps could cause significant latency or stack pressure.
138: huge unions/intersections + `Join` and `Widen` create entirely new `State` objects by re-mapping every single lane, which is an $O(N)$ operation where $N$ is the number of tracked facts.
178: malformed input + `fromLanes` assumes all passed maps are valid and non-nil; if a lane's internal map is nil due to a bug in a sub-domain, `valueLaneFromMap` or similar may panic.
194: recursion/depth limits + `reachable()` performs a cascading reachability check across multiple sub-lanes; if sub-lanes have cyclic dependencies, this",320,5952,
analysis/ir/dominance/postdominance.go,q0,"61: recursion/depth limits: A deeply nested, linear CFG (e.g., a long chain of `if` statements) could cause a stack overflow in the recursive `visit` function.
65: malformed input: A `cfg.Point` value that is within the `graphSize` range but refers to a non-existent or logically disconnected node in the underlying graph.
118: nil/empty handling: Passing a `nil` graph to `ComputePostDominators` to ensure it returns initialized (non-nil) maps rather than panicking.
125: nil/empty handling: Passing a `nil` graph to `ComputeImmediatePostDominators` to ensure it returns an empty map rather than panicking.",158,3310,
analysis/lua/cfgfacts/assignments.go,q0,NONE,2,394,
analysis/lua/cfgfacts/copy.go,q0,NONE,2,120,
analysis/lua/cfgfacts/control.go,q0,NONE,2,468,
analysis/lua/cfgfacts/doc.go,q0,NONE,2,119,
analysis/lua/cfgfacts/declarations.go,q0,NONE,2,162,
analysis/ir/cfg/cfg.go,q0,"148: nil receiver + `Entry()` returns 0 instead of panicking, which might mask a nil pointer bug in the caller.
161: nil receiver + `ID()` returns 0, potentially causing collisions if 0 is a valid ID.
178: `AddNode` + `ensureAdjacencyLen` + `n=0` (though unlikely via `New`) could lead to slice bounds issues if logic assumes `len(nodes) >= 2`.
188: `AddEdge` + `fromIdx < 0` (impossible via `Point` type but possible via cast) or `fromIdx >= len(c.nodes)` panics.
192: `AddEdge` + `toIdx >= len(c.nodes)` panics if a user tries to link to a non-existent node.
201: `AddEdge` + `succs == nil` handles the first edge to a node by initializing a slice, but doesn't check if `fromIdx` is within `succs` bounds.
218: `Node` + `p >= len(c.nodes)` returns `nil` instead of panicking, which may cause a nil-pointer dereference in the caller.
248: `Predecessors` + `p >= len(c.preds)` returns `nil` via `edgesReadOnly`, which is safe but requires the caller to check for nil.
278: `IsJoin` + `",320,4595,
analysis/ir/dominance/immediate.go,q0,"22 + Nil Graph + Passing a `nil` graph pointer to `computeImmediateDominatorData` to ensure it returns an empty struct rather than panicking.
27 + Invalid Entry + A graph where `g.Entry()` returns a `cfg.Point` index that is out of bounds or negative.
36 + Self-Referential Entry + A graph where the entry point's ID is equal to the graph size, potentially causing an out-of-bounds on `idomByPoint[int(entry)]`.
41 + Infinite Loop (Cyclic IDom) + A malformed graph where `idomByPoint` contains a cycle (e.g., A -> B -> A), causing the `intersect` function to loop forever.
44 + Divergent Fingers + A graph where `rpoNum` values are inconsistent with the `idomByPoint` hierarchy, causing the `intersect` pointers to never meet.
63 + Unreachable Block + A block that is part of the RPO but has no predecessors, testing if the algorithm correctly skips it without breaking the `changed` loop.
68 + Invalid Predecessor + A block whose `predecessorsOf` returns a `cfg.Point` that is out of bounds or invalid, testing the `validPoint` guard.
88 + IDom Oscillation + A graph structure that causes `newIDom` to flip-flop between two values, testing the convergence of the `changed` flag.
114 + Nil Receiver",320,4381,
analysis/lua/moduleidentity/projection.go,q0,NONE,2,568,
analysis/ir/dominance/frontier.go,q0,"43 + markEpoch overflow + A massive graph where `markEpoch` wraps around to 0, potentially causing the `runnerMark` check to fail and creating infinite loops or incorrect frontiers.
65 + self-referential idom + A node where `idomByPoint[runnerIdx] == runner` (a self-loop in the dominator tree), which could cause an infinite loop if the `dom == runner` check were missing or bypassed.
68 + missing idom in path + A graph where a node's dominator chain is broken (a node has no `idom` entry), which could cause the runner to traverse invalid memory or skip frontier updates.
23 + invalid point in RPO + An RPO slice containing a `cfg.Point` value that is out of bounds for the graph size, testing the robustness of the `validPoint` guard.
31 + insufficient predecessors + A graph where all nodes have only 0 or 1 predecessor, resulting in an empty frontier despite complex dominance relationships.
88 + idom map mismatch + An `idom` map containing points that are not present in the graph `g`, testing the bounds-checking during the conversion to `idomByPoint`.
101 + empty frontier filtering + A graph where every node's dominance frontier is empty, ensuring the function returns an empty map rather than a map full of empty slices.",296,5964,
analysis/lua/bind/symbols.go,q0,"138: nil/empty handling + `ResolvesToGlobal` returns `false` if `name` is an empty string, which might mask logic errors in the caller.
158: nil/empty handling + `GlobalSymbol` returns `(0, false)` if `name` is empty, which is correct but requires callers to check the boolean.
178: nil/empty handling + `LocalSymbolAt` returns `(0, false)` if `index` is negative, preventing a panic but potentially hiding out-of-bounds logic in the compiler.
188: complexity/performance + `LocalOrigin` performs an $O(N \times M)$ nested loop over all local symbols, which could be a DoS vector if a file has thousands of locals.
204: malformed input + `SymbolTypeAnnotation` assumes `stmt.Types` and `stmt.localSymbols` are perfectly synchronized; a mismatch in length/index could cause a panic.
238: nil/empty handling + `define` silently returns if `name` is empty or `id` is 0, which could lead to ""lost"" symbols that are hard to debug.
248: recursion/depth limits + `lookup` iterates through `b.scopes` and `b.deferred`; extremely deep nesting or a massive number of deferred scopes could cause performance degradation.
268: malformed input + `bindLValue` handles `nil` expressions via a switch case, but if the AST",320,4700,
analysis/lua/cfgbuild/builder_edges.go,q0,"21: Nil Statement Handling + Passing a `nil` `ast.Stmt` to `appendNodeForStmt` which bypasses `recordStmtPoint` logic but still creates a graph node.
38: Nil Statement Handling + Passing a `nil` `ast.Stmt` to `recordStmtPoint` which is explicitly checked but could lead to ""ghost"" nodes in the CFG with no associated AST.
45: Empty Pending Gotos + Calling `takePendingGotos` with a label that exists in the map but has an empty slice, or a label that doesn't exist at all.
54: Dead Code Path + Calling `materializePendingCond` when `state.live` is false, ensuring no `Noop` node is created for unreachable code.
75: Out-of-Bounds Edge Index + Providing an `edgeStart` that is negative or significantly larger than `len(edges)` to test the clamping logic.
76: Edge Index Boundary + Providing `edgeStart == len(edges)` to ensure the loop is skipped gracefully without a panic.
78: Non-Existent Edge Search + Searching for an edge with a specific `From` and `Cond` that does not exist in the graph, testing the `0, false` return path.",271,3693,
analysis/lua/semantics/branch_condition.go,q0,NONE,2,655,
analysis/lua/cfgbuild/cfgbuild.go,q0,"41: nil/empty handling: `fn` is nil but `bindings` is non-nil, testing if the builder gracefully skips statement processing without panicking.
54: nil/empty handling: `stmts` is an empty slice, testing if the builder correctly connects the Entry to the Exit with no intermediate nodes.
73: unsupported analysis surface: A function containing a Lua feature not supported by the `bind.Result` (e.g., `pcall` or `load`), triggering the `unsupported` flag and returning `nil`.
84: malformed input/unsupported: A chunk containing an unsupported control flow structure that sets `b.unsupported` to true.
101: metatable/type edge cases: `pendingGotos` or `breakTargets` being empty or containing invalid points if the AST is partially malformed.
111: recursion/depth limits: `buildStmts` (called on 45 and 81) being passed deeply nested AST structures that could cause stack overflow during CFG traversal.",225,3441,
analysis/lua/bind/functions.go,q0,"67: nil/empty handling + Calling `FunctionSymbol` with a nil `Result` receiver will return `0, false` instead of panicking.
77: nil/empty handling + Calling `FunctionBySymbol` with a nil `Result` receiver or a `0` symbol returns `nil, false`.
87: nil/empty handling + Calling `Functions` with a nil `Result` receiver returns `nil` safely.
93: nil/empty handling + Calling `NestedFunctions` with a nil `Result` receiver returns `nil` safely.
101: nil/empty handling + Calling `FunctionOrigins` with a nil `Result` receiver or an empty `functions` map returns `nil`.
117: nil/empty handling + Calling `FunctionOrigin` with a nil `Result` receiver or a missing `fn` returns an empty struct and `false`.
126: nil/empty handling + Calling `ParentFunction` with a missing `fn` returns `nil, false`.
134: nil/empty handling + Calling `DeclaringFunction` with a nil `Result` receiver or `0` symbol returns `nil, false`.
142: nil/empty handling + Calling `DirectCaptures` with a nil `Result` receiver or a missing `fn` returns `nil`.
150: nil/empty handling + Calling `ParamSymbols` with a nil `Result` receiver or a missing `fn` returns `nil`.
158: nil",320,6078,
analysis/lua/bind/types.go,q0,"211: recursion/depth limits: A deeply nested `OptionalTypeExpr` or `ArrayTypeExpr` (e.g., `[[[[...]]]]`) could cause a stack overflow in `bindTypeExpr`.
215: recursion/depth limits: A deeply nested `UnionTypeExpr` or `IntersectionTypeExpr` could cause a stack overflow in `bindTypeExpr`.
223: recursion/depth limits: A deeply nested `MapTypeExpr` or `RecordTypeExpr` could cause a stack overflow in `bindTypeExpr`.
231: recursion/depth limits: A deeply nested `FunctionTypeExpr` (recursive function types) could cause a stack overflow in `bindTypeExpr`.
241: recursion/depth limits: A deeply nested `GenericTypeExpr` (e.g., `List<List<List<...>>>`) could cause a stack overflow in `bindTypeExpr`.
246: recursion/depth limits: A deeply nested `MetaTypeExpr` could cause a stack overflow in `bindTypeExpr`.
251: recursion/depth limits: A deeply nested `ConditionalTypeExpr` could cause a stack overflow in `bindTypeExpr`.
257: cyclic/self-referential: A `TypeRefExpr` that points to a type currently being bound (e.g., `type T = T`) might cause infinite recursion if the binder doesn't track ""in-progress"" types.
264: malformed input: A `TypeRefExpr`",320,5832,
analysis/lua/cfgbuild/loopfacts.go,q0,"23 + Recursion Depth + Deeply nested `DoBlockStmt` or `IfStmt` structures causing a stack overflow during symbol collection.
43 + Malformed/Complex For-Loop + A `NumberForStmt` where `NumForSymbol` returns a symbol that is also declared as a local within the same block.
53 + Rare Type Combination + A `GenericForStmt` where the iterator variables are simultaneously being modified in the body via `AssignStmt`.
66 + Malformed Input + An `AssignStmt` where `Lhs` contains non-`IdentExpr` types (e.g., table indexing `t[i] = 1`) which are silently skipped by the `!ok` check.
76 + Self-Referential/Cyclic Logic + A `FuncDefStmt` that redefines a symbol already present in the `excluded` set, testing the boundary of local vs. outer scope.
95 + Nil/Zero Value Handling + A `FuncDefTargetSymbol` or `SymbolOf` returning a `0` (nil) `symbol.ID`, which must be caught to prevent polluting the output.
105 + Redundancy/Overflow + A massive loop body that writes to the same variable thousands of times, testing the efficiency and correctness of the `seen` map deduplication.",280,4135,
analysis/lua/cfgbuild/builder_statements.go,q0,"41: nil/empty handling + A `nil` statement in the `stmts` slice could cause a panic if the loop doesn't check for it (though `buildStmt` handles it, the caller `buildStmts` does not).
53: malformed input + A `FuncCallStmt` where `stmt.Expr` is not a `FuncCallExpr` but is also not `nil` (e.g., a custom AST node type) might bypass the `nil` check but fail the type assertion.
78: recursion/depth limits + Deeply nested `DoBlockStmt` or `IfStmt` could cause a stack overflow during the recursive `buildStmts` calls.
103: cyclic/self-referential + A `FuncDefStmt` where the function name is part of its own body's expression tree could cause infinite recursion in symbol resolution or CFG construction.
118: cyclic/self-referential + A `LabelStmt` that is immediately followed by a `GotoStmt` to itself, creating a zero-length loop.
133: cyclic/self-referential + A `GotoStmt` that targets a label defined *after* the goto, which is then resolved via `pendingGotos` to create a backward edge.
154: recursion/depth limits + A `WhileStmt` containing a `WhileStmt` containing a `WhileStmt`... (deeply nested loops).
178: recursion/depth limits + A `Repeat",320,4498,
analysis/lua/branchcond/branch_condition.go,q0,"148 + recursion/depth limits + A deeply nested logical expression like `a and b or c and d ...` could cause a stack overflow in `polarityChecks`.
186 + recursion/depth limits + A deeply nested logical expression like `not (not (not (not ...)))` could cause a stack overflow in `polarityChecks`.
213 + nil/empty handling + `pathexpr.Resolve` returning an empty path for a valid-looking expression might cause downstream logic to treat a valid variable as a non-existent one.
248 + malformed input + `expr.Lhs` or `expr.Rhs` being `nil` (if the AST is partially constructed) would cause a panic in `nilComparisonPath`.
311 + malformed input + `call.Args[0]` being `nil` in `typeCallSubjectPath` would cause a panic if the AST is malformed.
323 + malformed input + `call.Args` being empty or having more than one element in `typeCallShape` is handled, but `call.TypeArgs` being non-empty is a rare edge case for Lua-like ASTs.
343 + overflow + `numparse.ParseIntegerLiteral` might return a value that exceeds `int64` limits, though `LenFloor` is `int64`.
356 + overflow + `c + 1` in `lengthFloorForRelop` can overflow `int64` if `",320,5531,
analysis/lua/semantics/facts.go,q0,NONE,2,423,
analysis/lua/callorder/callorder.go,q0,"74: recursion/depth limits: A deeply nested `ast.TableExpr` or `ast.AttrGetExpr` could cause a stack overflow during `collectExpr`.
86: nil/empty handling: An `ast.TableExpr` with a `nil` field in the `Fields` slice is handled, but a `nil` `expr.Fields` slice itself might panic depending on the AST implementation.
103: malformed input: A `ast.LogicalOpExpr` where `Lhs` or `Rhs` is `nil` would cause a panic in `containsCall` or `collectExpr`.
118: malformed input: A `ast.RelationalOpExpr` where `Lhs` or `Rhs` is `nil` would cause a panic in `collectExpr`.
135: malformed input: A `ast.UnaryMinusOpExpr` (or other unary ops) where `expr.Expr` is `nil` would cause a panic.
146: recursion/depth limits: A `ast.FuncCallExpr` with a deeply nested `Receiver` or `Func` could cause a stack overflow.
154: malformed input: A `ast.FuncCallExpr` where `Args` contains a `nil` element would cause a panic in `collectExpr`.
173: recursion/depth limits: A deeply nested `ast.TableExpr` in `containsCall` could cause a stack overflow.
184: malformed input",320,5694,
analysis/lua/cfgbuild/expr_support.go,q0,"138: recursion/depth limits + A deeply nested `ast.TableExpr` or `ast.LogicalOpExpr` could cause a stack overflow in the recursive `appendValueExprCalls` calls.
148: recursion/depth limits + A deeply nested `ast.AttrGetExpr` (e.g., `a.b.c.d...`) could cause a stack overflow in the recursive `appendValueExprCalls` calls.
165: recursion/depth limits + A deeply nested `ast.TableExpr` could cause a stack overflow in the recursive `appendValueExprCalls` calls.
215: recursion/depth limits + A deeply nested `ast.LogicalOpExpr` could cause a stack overflow in the recursive `appendValueExprCalls` calls.
223: recursion/depth limits + A deeply nested `ast.RelationalOpExpr` could cause a stack overflow in the recursive `appendValueExprCalls` calls.
226: recursion/depth limits + A deeply nested `ast.StringConcatOpExpr` could cause a stack overflow in the recursive `appendValueExprCalls` calls.
228: recursion/depth limits + A deeply nested `ast.ArithmeticOpExpr` could cause a stack overflow in the recursive `appendValueExprCalls` calls.
230: recursion/depth limits + A deeply nested `ast.UnaryMinusOpExpr` could cause a stack overflow in the recursive `appendValueExprCalls` calls.
253: recursion/depth limits",320,4760,
analysis/lua/semantics/result_index.go,q0,NONE,2,409,
analysis/lua/pathexpr/object_literal.go,q0,"38: nil/empty handling: A `table` with `Fields` containing only `nil` elements (e.g., `{nil, nil}`) to test if `lastTableFieldIndex` and the loop handle sparse/nil slices without panicking.
48: recursion/depth limits: A deeply nested table constructor (e.g., `{{{{...}}}}`) to check for stack overflow during the recursive `objectEntries` calls.
51: cyclic/self-referential types: A table where a field's value is a reference to the parent table (if the AST allows such construction) to test for infinite recursion.
63: malformed input: A `field.Value` that is a `TableExpr` but contains a `nil` `Key` or `Value` to see if `ObjectLiteralTable` or `objectEntrySuffix` triggers a nil pointer dereference.
68: rare type combinations: A table field where `field.Key` is a complex expression (like a function call or another table) to see if `ResolveTableFieldSuffix` handles non-literal keys.
73: metatable edge cases: A table field where `field.Value` is an expression that triggers a complex `sourceprovenance.ValueShape` lookup, testing if the ""expanded"" logic correctly identifies non-terminating shapes.
78: nil/empty handling: A table where all fields are `nil`, resulting in `lastTableFieldIndex` returning `-1`, testing if the loop in `objectEntries`",320,4490,
analysis/lua/pathexpr/table_field.go,q0,"46: nil field handling + Passing a `nil` pointer for the `field` argument to ensure the function returns the zero-value and `false` without panicking.
49: nil arrayIndex + Passing a `nil` pointer for `arrayIndex` when `field.Key` is `nil` to ensure it doesn't attempt to dereference and increment.
51: arrayIndex increment + Passing a pointer to a zero-value `int` to verify the 1-based Lua indexing logic correctly increments the caller's state.
61: empty string dot-notation + A `StringExpr` with an empty string value (`""""`) using `ast.AttrKeyDot` which should be rejected as an invalid field name.
68: unsupported key syntax + A `StringExpr` using a syntax other than `Dot` or `Index` (e.g., a malformed AST node) to ensure it returns `false`.
76: non-index number syntax + A `NumberExpr` used with `ast.AttrKeyDot` (e.g., `t.1`) which is syntactically invalid in Lua and should be rejected.
79: non-integer decimal + A `NumberExpr` containing a float (e.g., `t[1.5]`) which should fail the `parseNonNegativeDecimalInt` check.
79: negative number index + A `NumberExpr` containing a negative value (e.g., `t[-1]`) which should be",320,4219,
analysis/lua/semantics/sem.go,q0,NONE,2,92,
analysis/lua/pathexpr/path.go,q0,"56: recursion/depth limits: A deeply nested `ast.AttrGetExpr` (e.g., `a.b.c.d...`) could cause a stack overflow in the recursive `resolveMutationContainer` calls.
73: nil/empty handling: A `name.Receiver` that is non-nil but contains an empty path (via `IsEmpty()`) returns `false`, testing the boundary of ""valid but empty"" paths.
86: nil/empty handling: If `bindings.SymbolOf(expr)` returns a valid ID but `bindings.Name(id)` returns an empty string, it falls back to `expr.Value`, testing the edge case of unmapped symbols.
101: malformed input: An `ast.AttrKeyDot` with an empty string value (`obj.`) is explicitly rejected, testing the boundary of valid Lua identifiers.
111: overflow: `parseNonNegativeDecimalInt` handles strings representing integers larger than `math.MaxInt`, testing the overflow protection logic.
111: malformed input: A string containing non-digit characters (e.g., `""123a""`) or leading zeros that might be interpreted incorrectly by the manual parser.
125: malformed input: An empty string passed to `isIdentName` returns `false`, testing the boundary of identifier validation.",289,4519,
analysis/lua/cfgfacts/metadata.go,q0,"31, nil/uninitialized map, calling `SetAssignment` on a zero-valued `Metadata` struct without prior initialization.
41, nil/uninitialized map, calling `Loop` on a zero-valued `Metadata` struct (returns `false` correctly, but tests should verify no panic).
48, nil/uninitialized map, calling `SetLoop` on a zero-valued `Metadata` struct.
58, nil/uninitialized map, calling `SetTypeDefinition` on a zero-valued `Metadata` struct.
68, nil/uninitialized map, calling `SetFunctionDefinition` on a zero-valued `Metadata` struct.
77, deep copy/pointer aliasing, calling `SetFunctionDefinition` with a fact whose `TargetPath` is shared across multiple calls.
81, nil/uninitialized map, calling `NumericFor` on a zero-valued `Metadata` struct.
87, nil/uninitialized map, calling `SetNumericFor` on a zero-valued `Metadata` struct.
93, nil/uninitialized map, calling `GenericFor` on a zero-valued `Metadata` struct.
103, deep copy/pointer aliasing, calling `SetGenericFor` with a fact whose internal pointers are mutated externally after insertion.
113, nil/uninitialized map, calling `SetLabel` on a zero-valued `Metadata` struct.
123, nil/uninitialized map, calling `ShortCircuitGuard`",320,5204,
analysis/lua/channelruntime/channel_runtime.go,q0,"31: Nil/Malformed Call: A `call` with a `nil` `Func` field or a `bindings` object that returns `ok=false` for a valid-looking symbol.
34: Ambiguous Symbol: A `callee` that resolves to a symbol with `Symbol == 0` (uninitialized/null symbol) despite having a matching field name.
34: Shadowed Global: A `callee` that matches `select` but belongs to a local table or a non-global symbol kind.
44: Empty Path Resolution: A `call.Receiver` that resolves to a valid symbol but an empty `pathdom.Path` (e.g., a naked variable used as a receiver).
45: Type Mismatch/Projection Failure: A `channelPath` that resolves to a valid symbol but whose type projection via `pathType` fails due to an invalid segment index.
56: Nil/Empty Receiver: A `call` where the `Receiver` is a valid AST node but resolves to an empty path, testing the `IsEmpty()` boundary.
65: Cyclic/Deep Projection: `luatypeprojection.ApplySegments` being called with a path containing a massive number of segments or segments that point to self-referential table structures.
65: Missing Annotation: A `p.Symbol` that exists in `bindings` but has no associated `SymbolTypeAnnotation`, testing the `ok` return path.
66: Type Resolution Failure: A `current` type that",320,5204,
analysis/lua/semantics/extract_assign.go,q0,"24: ErrPointMismatch + A mismatch between the number of CFG points provided and the sum of function calls and assignment targets (e.g., a partial CFG trace).
33: ErrPointMismatch + A local assignment where the number of expressions/calls does not align with the number of declared names (e.g., `local a, b = func()`).
53: Symbol ID 0 + A local variable that is declared but not yet bound to a symbol in the `bindings` map (e.g., shadowing or uninitialized scope).
74: ErrPointMismatch + A global/upvalue assignment where the number of CFG points does not match the sum of calls and LHS targets.
96: Path Resolution Failure + A complex LHS target (like `a[b[c]]`) where `pathexpr.Resolve` returns `hasPath = false` due to non-deterministic or malformed indexing.
97: Container Path Failure + A mutation target (like `a.b.c = 1`) where the container path cannot be resolved, testing the boundary of nested table access.",236,3019,
analysis/lua/semantics/channel_select.go,q0,"41: Nil/Empty Field Handling: A table field where `field.Value` is nil would cause a panic in `channelSelectCase` if not caught by the loop's nil check.
48: Malformed Table Structure: A table field with a `nil` key (e.g., `{[nil] = ...}`) would cause `ast.KeyName(field.Key)` to potentially panic depending on the `ast` package implementation.
56: Empty Case List: A `select` call with a table containing only a `default` key and no actual channel cases returns `false`, which is a subtle semantic edge case for ""empty"" selects.
66: Malformed Receiver: A receive case where the receiver is a complex expression that resolves to an empty path (e.g., `select({ [chan()] = ... })`) returns `false`.
67: Cyclic/Self-Referential Path: If `pathexpr.Resolve` encounters a circular reference in the bindings, it may trigger a stack overflow or return an error that this function treats as a simple `false`.",232,3547,
analysis/lua/sourcevalue/iterator.go,q0,NONE,2,99,
analysis/lua/semantics/extract_call_return.go,q0,"24: ErrPointMismatch + A `FuncCallStmt` where the AST expression is a complex chain (e.g., `a().b().c()`) but the provided CFG points only cover a subset of the calls.
27: ErrPointMismatch + A `FuncCallStmt` where the number of identified call sites in the expression does not match the number of control-flow points provided.
41: ErrPointMismatch + A `ReturnStmt` where the number of return values (including the implicit return of the statement itself) does not match the length of the provided points slice.
46: CallContextReturnSource + A return statement containing a single function call that is actually the sole expression in the list (e.g., `return func() end`).
51: ReturnFact/Sources + A return statement with multiple expressions where some are calls and others are literals, testing the ability to map specific return values to specific CFG points.",200,2892,
analysis/lua/semantics/result_copy.go,q0,"51: copyOrdinaryAssignmentFact + Nil/Empty Path: An assignment where `Path` or `ContainerPath` is nil would cause a panic during `.Clone()`.
61: copyCallFact + Nil/Empty Path: A call where `CalleePath`, `ReceiverPath`, or `MethodPath` is nil would cause a panic during `.Clone()`.
83: copyObjectEntries + Nil Suffix: An object entry with a nil `Suffix` would cause a panic during `.Clone()`.
101: copyChannelSelectCases + Nil ChannelPath: A channel select case with a nil `ChannelPath` would cause a panic during `.Clone()`.
118: copyResultTarget + Nil Path: A call result target with a nil `Path` would cause a panic during `.Clone()`.",172,2575,
analysis/lua/semantics/extract_branch.go,q0,"21: ErrPointMismatch + A condition containing a function call that is not reflected in the provided `points` slice, causing a mismatch between expected and actual control flow points.
24: ErrPointMismatch + A condition with exactly $N$ calls where the `points` slice contains $N+1$ elements, but the logic fails to account for the branch target point itself.
28: CallContextCondition + A condition that is itself a predicate call (e.g., `if is_valid(x) then`), testing if the resolver correctly identifies the condition as the primary driver rather than a side-effect call.
33: BranchConditionFact + A condition that evaluates to a constant or a non-call expression, testing if `conditionValueSource` handles the absence of a `resolver` or `calls` gracefully.
37: Type Switch + A statement that is technically a branch type but is not explicitly handled in the `switch` (e.g., a custom AST node implementing `ast.Stmt`), potentially leading to a `fact` with nil fields.
46: exprCalls + A deeply nested or recursive expression tree in the `condition` that might cause a stack overflow during the expression traversal.",255,3850,
analysis/lua/transferfacts/assert_postconditions.go,q0,NONE,2,438,
analysis/lua/semantics/call_fact.go,q0,"21: nil/empty handling + `stmt` being nil or `stmt.Names` being empty results in an early return of `nil` instead of an empty slice.
28: nil/empty handling + `bindings` being nil causes `bindings.LocalSymbolAt` to be skipped, potentially leaving `target.Symbol` as 0 even if a local exists.
43: nil/empty handling + `stmt` being nil or `stmt.Lhs` being empty results in an early return of `nil`.
52: malformed input + `pathexpr.Resolve` returning a path with `Symbol == 0` (invalid/unresolved symbol) results in `HasSymbol` being false.
78: nil/empty handling + `call.Args` or `call.TypeArgs` being nil/empty might cause issues in `copyExprs` or `copyTypeExprs` if they don't handle nil slices.
101: nil/empty handling + `context` being an undefined/unhandled `CallContextKind` returns `false, false, false` for all flags.
105: nil/empty handling + `call` being nil returns empty paths and `false` for all booleans.
115: malformed input + `call.Method` being an empty string while `call.Receiver` is present results in a partial return where `hasMethodPath` is false.
131: nil/empty handling + `call.Receiver` being",320,4761,
analysis/lua/semantics/result_metadata.go,q0,"6: nil/empty handling: Calling `TypeDefinition` on a nil `Result` pointer to ensure the receiver check prevents a panic.
13: nil/empty handling: Calling `FunctionDefinition` on a nil `Result` pointer to ensure the receiver check prevents a panic.
20: nil/empty handling: Calling `NumericFor` on a nil `Result` pointer to ensure the receiver check prevents a panic.
27: nil/empty handling: Calling `GenericFor` on a nil `Result` pointer to ensure the receiver check prevents a panic.
34: nil/empty handling: Calling `Label` on a nil `Result` pointer to ensure the receiver check prevents a panic.
41: nil/empty handling: Calling `Goto` on a nil `Result` pointer to ensure the receiver check prevents a panic.",177,2269,
analysis/lua/semantics/extract.go,q0,"14: nil/empty handling + Passing a `nil` `built` pointer or a `built` object with a `nil` `Graph` to trigger `ErrNoCFG`.
25: nil/empty handling + Passing a `nil` `fn` pointer to `ExtractFunction` which bypasses statement extraction but still attempts to process guards.
45: malformed input + A `ShortCircuitGuard` that exists in `Meta` but has a `nil` `Condition` field.
46: malformed input + A `ShortCircuitGuard` that exists in `Meta` but returns `ok == false` for a specific point.
49: cyclic/self-referential + A `point` that is already present in `r.branches`, testing the idempotency of the guard extraction.
61: recursion/depth limits + A deeply nested `ast.DoBlockStmt` (e.g., 1000+ levels) that could cause a stack overflow during `extractStmts`.
76: nil/empty handling + An `ast.Stmt` that is explicitly `nil` within a slice of statements.
93: recursion/depth limits + An `ast.IfStmt` where both `Then` and `Else` branches contain deeply nested recursive structures.
97: recursion/depth limits + An `ast.WhileStmt` containing a deeply nested block of statements.
101: recursion/depth limits + An `ast.RepeatStmt` where the body is a",320,4318,
analysis/lua/transferfacts/branch_refinement_edges.go,q0,NONE,2,222,
analysis/lua/transferfacts/branch_path_relations.go,q0,NONE,2,222,
analysis/lua/sourceprovenance/expr_nil.go,q0,"11: nil handling + Passing a literal `nil` as the `expr` argument to the function.
15: nil-able type check + Passing a `reflect.Chan` or `reflect.Map` that is initialized but contains no data (is `nil`).
15: nil-able type check + Passing a `reflect.Slice` that is `nil` vs an empty slice `[]T{}` (which is not `nil`).
15: nil-able type check + Passing a `reflect.Interface` that is a `nil` interface vs an interface containing a `nil` concrete pointer.
15: nil-able type check + Passing a `reflect.Pointer` that points to a zero-value vs a `nil` pointer.
15: nil-able type check + Passing a `reflect.UnsafePointer` that is `nil`.",186,2573,
analysis/lua/semantics/object_literal.go,q0,"24: recursion/depth limits: A deeply nested object literal (e.g., `{a={b={c={...}}}}`) could trigger a stack overflow during the recursive calls to `r.extractObjectLiteral`.
38: nil/empty handling: An object literal with a `nil` field value (e.g., `{a = nil}`) might cause issues if `entry.Value` is expected to be a valid `ast.Expr`.
46: recursion/depth limits: An object literal containing a self-referential structure (if the AST allows it) would cause infinite recursion in the `table.Fields` loop.
54: recursion/depth limits: A function call with a massive number of arguments could cause a stack overflow via `r.extractObjectLiterals(expr.Args, resolver)`.
58: malformed input: A `CastExpr` that satisfies `ProofInner` but contains a malformed or nil `Expr` field would cause a panic.
61: malformed input: A `NonNilAssertExpr` wrapping a `nil` expression would cause a panic when passed back into `r.extractObjectLiteral`.",244,4122,
analysis/lua/semantics/result.go,q0,"38: nil receiver + Calling `Function()` on a nil `*Result` pointer.
44: nil receiver + Calling `LocalAssignment()` on a nil `*Result` pointer.
53: nil receiver + Calling `OrdinaryAssignment()` on a nil `*Result` pointer.
62: nil receiver + Calling `Call()` on a nil `*Result` pointer.
71: nil receiver + Calling `Return()` on a nil `*Result` pointer.
80: nil receiver or nil expression + Calling `ObjectLiteral()` with a nil `ast.Expr` argument.
88: nil receiver + Calling `ChannelSelect()` on a nil `*Result` pointer.
89: missing fact + Calling `ChannelSelect()` on a point that exists in `calls` but lacks the `HasChannelSelect` flag.
97: nil receiver + Calling `ChannelSelects()` on a nil `*Result` pointer.
111: empty map + Calling `ChannelSelects()` when `r.calls` is initialized but contains no entries.
113: massive map + Calling `ChannelSelects()` on a `Result` with millions of `calls` to test slice allocation and sort performance.
124: nil receiver + Calling `BranchCondition()` on a nil `*Result` pointer.",283,3807,
analysis/lua/transferfacts/calls.go,q0,NONE,2,402,
analysis/lua/transferfacts/expr_refs.go,q0,NONE,2,370,
analysis/lua/sourceprovenance/producer.go,q0,"49: Recursion/Depth Limits: An infinitely nested chain of `CastExpr` or `NonNilAssertExpr` will cause `AssertionInner` to enter an infinite loop or stack overflow.
78: Recursion/Depth Limits: An infinitely nested chain of `CastExpr` or `NonNilAssertExpr` will cause `AssertionInner` to enter an infinite loop or stack overflow.
88: Recursion/Depth Limits: An infinitely nested chain of `CastExpr` or `NonNilAssertExpr` will cause `ProofInner` to enter an infinite loop or stack overflow.
88: Cyclic/Self-referential Types: A `CastExpr` that points back to itself via its `Expr` field will cause `ProofInner` to loop infinitely.
88: Malformed Input: A `CastExpr` where `wrapped.Type` is nil will cause a panic when passed to `castTargetIsAny`.
98: Nil/Empty Handling: A `PrimitiveTypeExpr` with a `Name` that is an empty string or contains non-standard characters might bypass logic intended for specific types.",234,3652,
analysis/lua/semantics/value_list.go,q0,"31 + Mismatched slice lengths + A scenario where `len(calls) > len(points)` causes the loop to terminate early via `break`, potentially leaving the map incomplete.
31 + Mismatched slice lengths + A scenario where `len(calls) < len(points)` results in a map that only contains a subset of the available points.
63 + Mismatched slice lengths + A scenario where the number of detected function calls is greater than the number of available CFG points, causing silent truncation of call-to-point mappings.
101 + Nil resolver/Empty maps + If `callPoints` and `exprPoints` are both empty, the function returns `nil`, which might cause a panic in the caller if they don't check for a nil `CallPointResolver`.
104 + Nil resolver/Empty maps + A call to the returned anonymous function with a `nil` call or index when the underlying maps were empty.
108 + Out-of-bounds index + A `call.index` that is negative or exceeds `len(exprs)`, which is handled by the check but tests the boundary of the `exprs` slice.
109 + Identity mismatch + A scenario where `sourceprovenance.Call(exprs[call.index])` returns a different call object than `call.call` despite being at the same index.",294,3928,
analysis/lua/semantics/extract_metadata.go,q0,"24: ErrPointMismatch + A CFG where a type definition is associated with multiple control flow points (e.g., via a complex jump or duplicate entry).
42: ErrPointMismatch + An interface definition that spans multiple points in the CFG, potentially due to malformed AST/CFG mapping.
55: Nil Bindings + Passing `nil` for `bindings` to see if `bindings.FuncDefTargetSymbol(stmt)` is safely bypassed or causes a panic.
58: Empty Path + A function definition where `pathexpr.ResolveFuncName` returns a path that is technically non-nil but contains no segments.
71: Nil Function Body + A function definition where `stmt.Func` is `nil`, testing if the assignment fact creation panics when accessing `stmt.Func`.
76: Nil Container Path + A function definition where `targetPath.Parent()` returns an empty/nil path, testing `container.IsEmpty()` safety.
88: Nil Statement + Passing a `nil` `stmt` to `functionDefinitionTargetExpr`, which would cause a panic on `stmt.Name` access.
89: Method Function + A function definition where `stmt.Name.Method` is non-empty, testing if the logic correctly returns `nil` for method-based assignments.
105: ErrPointMismatch + A numeric for loop where the CFG provides 1 or 3 points instead of exactly 2 (Init and Check).
113: Zero Symbol ID +",320,5468,
analysis/lua/transferfacts/branch_root_refinements.go,q0,"16 + Nil/Empty Segments + A refinement where `TargetPath().Segments` is nil or an empty slice, testing if the stable sort correctly prioritizes ""root"" (empty path) refinements.
33 + Nil/Empty Presence + A refinement where `TrueValue()` or `FalseValue()` returns a zero-value/nil presence, testing if the presence check prevents panics.
45 + Unknown Check Kind + A `branchcond.Check` with a `Kind` that is neither `CheckTruthy` nor `CheckFalsy` (e.g., a custom or uninitialized enum value), testing the `default: return nil` path.
61 + Boolean Inversion + A `CheckFalsy` condition where the actual boolean `cond` is `true`, testing if the logic correctly maps the ""falsy"" check to the ""true"" literal refinement.",186,2903,
analysis/lua/transferfacts/branch_value_constraints.go,q0,"24: Malformed type name + `runtimekind.ParseTag` returns `ok=false` if `typeName` is an empty string or a non-tag string (e.g., `""not-a-tag""`).
33: Nil tag edge case + `tag == runtimekind.Nil` triggers a specific branch that forces `presence.Absent()` instead of the standard `presence.Present()` logic.
35: Scalar type witness + `scalarTypeForTag` returns `nil, false` for non-primitive tags (like `Table` or `Function`), which must be handled by the caller to avoid nil pointer dereferences.
48: Complementary set logic + `runtimekind.Top().Without(tag)` creates a complex union/subtraction that might result in an empty set or an unexpected type if `tag` is `Top`.
50: Nil complement + `tag == runtimekind.Nil` in the unmatched path forces `presence.Present()`, creating a logical inversion of the matched path.
63: Presence mismatch + `product.NewWithPresence` with `product.ShapeTop` and `presence.Absent()` might create a constraint that is logically impossible to satisfy in certain lattice implementations.",258,3233,
analysis/lua/transferfacts/call_targets.go,q0,"23: nil/empty handling: An empty `targets` slice returns `nil`, which might cause a panic in callers expecting a non-nil empty slice.

34: malformed input: A `target.Kind` that matches none of the switch cases results in `CallResultTargetUnknown`, which may lead to silent failures in downstream analysis.

41: rare type combinations: If `target.HasPath` is false but `target.HasSymbol` is true, the logic falls through to specific assignment-based path reconstruction, which might produce incorrect paths for non-assignment target kinds.

43: malformed input: A `CallResultTargetLocalAssignment` with `HasSymbol` true but an empty/invalid `target.Name` will create a path with an empty segment.

45: malformed input: A `CallResultTargetOrdinaryAssignment` with `HasSymbol` true will create a path with an empty string segment, potentially breaking path equality checks.",203,2824,
analysis/lua/sourcevalue/object_literal.go,q0,"36 + `luatypeprojection.ConstructorPathFromSegments` + A malformed or extremely deep segment path that fails to resolve to a valid constructor path.
43 + `luatypeprojection.ExpectedRecordField` + An entry that exists in the `expected` type but is missing from the actual `lit.Entries()`, testing the ""filling in"" logic.
54 + `ObjectLiteralEntryHasUntrustedTopOrigin` + A value that is technically a valid type but carries a ""Gradual Top"" evidence, testing if it's correctly skipped/ignored.
62 + `luatypeprojection.AdoptExpectedFieldType` + A type mismatch where the actual value `t` is incompatible with the `expected` field type, testing the ""adoption"" failure path.
71 + `builder.Build()` + A massive number of entries that might cause an internal overflow or memory exhaustion during the final type construction.
74 + `seenUntrustedTop` fallback + A case where all entries are ""untrusted top"" types, testing if the function correctly falls back to an empty record instead of the expected type.
93 + `len(segs) != 1` + A field name that is actually a nested path (e.g., `a.b.c`), testing if the dot-field resolver correctly ignores non-shallow fields.
106 + `ObjectLiteralEntryHasUntrustedTopOrigin` + A value that is `Any` or `Unknown` but carries an untrusted origin, testing the",320,4602,
analysis/lua/transferfacts/branches.go,q0,"21: Nil/Empty Path - A `BranchConditionFact` where `target.IsEmpty()` is true, testing if the engine correctly aborts before attempting to refine a non-existent path.
41: Falsy/Empty Segment - A `CheckFalsy` condition where `target.Segments` is empty, testing if `hasTrue` is correctly set to `false` to avoid invalid refinements.
68: Non-Ge Length Guard - A `branchLenRefinements` call with a `CheckLen` (not `Ge`) kind, testing if the function correctly ignores non-inequality length checks.
78: Complex Truthy Condition - A `CheckNone` condition containing a deeply nested or complex logical expression in `fact.Condition`, testing if `branchcond.TruthyChecks` correctly extracts all sub-checks.
86: Zero/Negative Length Floor - A `CheckLenGe` where `check.LenFloor <= 0`, testing if the lowerer correctly ignores non-positive length constraints that don't provide useful refinement.
93: Mixed Truthy/Falsy Condition - A `CheckNone` condition containing both truthy and falsy sub-conditions, testing if the refinement list correctly aggregates both edges.
97: Refinement Ordering - A complex condition where refinements are returned out of order, testing if `orderRootRefinementsBeforeDescendants` prevents invalid state propagation in the engine.",302,3859,
analysis/lua/transferfacts/assertions.go,q0,"22: recursion/depth limits: A deeply nested chain of `CastExpr` or `NonNilAssertExpr` (e.g., `cast(cast(cast(x)))`) could cause a stack overflow due to the recursive call to `addAssertionRefinementsForSource`.

28: nil/empty handling: If `outerSource.Expr` is valid but `innerExpr` is `nil`, the function returns early, potentially masking a malformed AST where an assertion exists but its operand is missing.

31: malformed input: If `input.ExpressionRefinements` is nil, it is initialized, but if `outerSource.ExprRef` is an empty or zero-value key, it may lead to silent data loss in the refinement map.

33: recursion/depth limits: The recursive call `l.addAssertionRefinementsForSource(input, inner)` uses a modified `inner` source, which could lead to infinite recursion if the AST contains a cycle where an expression is its own child.

43: rare type combinations: If `value.Has(assertion.AnyClaim)` is true but `l.valueFromType(typ.Any)` returns a nil or unexpected product value, `product.Set` might panic or create an invalid state.

47: malformed input: If `typ` is a complex type expression that is neither `*ast.PrimitiveTypeExpr` nor a known type, `assertion.Type()` is returned as a fallback, which might be an overly broad",320,4287,
analysis/lua/sourceprovenance/shape.go,q0,"21: `targetCount` mismatch + `len(exprs)` mismatch: Providing a `targetCount` significantly larger than `len(exprs)` to trigger the `NewNilSource` logic in `assignmentSource`.
48: `openTailFinal` true + `canExpandFinal` true: A single-element expression list where the expression is a function call that returns multiple values, testing the `openTail` expansion logic.
68: `targetIndex` > `finalExprIndex` + `finalExpands` true: An assignment where the number of targets exceeds the number of expressions, but the last expression is a multi-return function (e.g., `a, b, c = func() return 1, 2 end`).
68: `targetIndex` > `finalExprIndex` + `finalExpands` false: An assignment where targets exceed expressions and the last expression is a scalar (e.g., `a, b = 1`), testing the fallback to `NewNilSource`.
76: `missingAssertionInner(expr)` true: An expression that lacks type assertions or metadata, forcing the resolver to return an `UnknownSource` instead of a specific type.
81: `resolver` returns `ok=false` or `point=0`: A function call where the `CallPointResolver` cannot map the AST node to a CFG point, testing the fallback to `UnknownSource`.
81: `resolver` returns `ok=true` but `point=0`:",320,5693,
analysis/lua/sourceprovenance/source.go,q0,"48: Shape validation logic: `Expanded=true, Adjusted=true` returns false, testing the mutual exclusivity of Lua stack adjustment modes.
49: Shape validation logic: `Expanded=true, Final=false` returns false, testing the invariant that expanded lists must be terminal.
50: Shape validation logic: `OpenTail=true, Expanded=false` returns false, testing the requirement that open tails must be part of an expanded list.
118: `SourceUnknown` strictness: A `SourceUnknown` with any non-zero shape flag or a non-nil `Expr` must return `false`.
121: `SourceUnknown` index boundary: A `SourceUnknown` with `ExprIndex` or `ResultIndex` set to `0` (instead of `NoSourceIndex`) must return `false`.
127: `SourceExpression` nil expression: An `ast.Expr` that is a nil pointer must cause `Valid()` to return `false`.
128: `SourceExpression` call point mismatch: A `SourceExpression` where `HasCallPoint` is true but `CallPoint` is 0 (or vice versa) must return `false`.
130: `SourceCall` result index boundary: A `SourceCall` with `ResultIndex = -1` (NoSourceIndex) must return `false`.
131: `SourceCall` call point mismatch: A `SourceCall` where `HasCallPoint` is false but `CallPoint`",320,5651,
analysis/lua/transferfacts/branch_guard_proofs.go,q0,"44 + target.IsEmpty() + A `Check` with an empty path could cause downstream logic to process invalid/null paths.
78 + CheckPathEqual/Not + `other.IsEmpty()`: If the comparison path is empty, the evidence is dropped, potentially masking equality/inequality logic.
103 + CheckIndexInRange + `other.IsEmpty()`: An empty index path results in no evidence, which might fail to catch out-of-bounds or range-check logic.
121 + frozenTableBranchEvidence + `fact.Condition` is nil + A nil `ast.Expr` passed into the recursive descent could cause a panic in `PredicateCall`.
128 + frozenTableBranchEvidenceForCondition + `call` is nil + If `PredicateCall` returns a nil pointer for a call, `l.tableFrozenCallPath(call)` will panic.
135 + frozenTableBranchEvidenceForCondition + `targetPath` is empty + If a frozen table call resolves to an empty path, the evidence is silently dropped.
143 + frozenTableBranchEvidenceForCompoundCondition + `ast.UnaryNotOpExpr` recursion + Deeply nested `not(not(not(...)))` expressions could cause a stack overflow.
144 + frozenTableBranchEvidenceForCompoundCondition + `ast.LogicalOpExpr` recursion + Deeply nested `and(or(and(...)))` expressions could cause a stack overflow.
161 + tableFrozenCallPath + `call.Receiver`",320,4349,
analysis/lua/sourcevalue/expression_operation.go,q0,"43: Nil/Empty Operand Handling + A `product.Value` that exists in the registry but has no associated type or evidence (returning `nil, false`).
48: Malformed/Unsupported Operation + A `factflow.ExpressionOperation` with a `Kind` that is neither Unary nor Binary (e.g., a custom or corrupted enum value).
53: Nil/Empty Operand Handling + A binary operation where the `left` operand is valid but the `right` operand is a ""ghost"" value with no type information.
65: Rare Type Combinations + A value that is not a standard type but satisfies `topOriginEvidence` via `IsGradualTop` or `IsExplicitTop`.
71: Malformed Input + A binary operation where the operator is not `""and""` or `""or""`, forcing a fallback to a failed `topOriginOperationValue`.
75: Nil/Empty Operand Handling + A case where `operationTopOrigin` fails to find evidence for both operands, returning a zero-value `product.Value`.
85: Rare Type Combinations + A case where `leftOK` and `rightOK` are both true but their evidence values are non-equal, testing the fallback to `evidence.Top()`.
88: Rare Type Combinations + A case where only one operand has evidence, testing the ""inheritance"" of evidence from a single side.
95: Nil/Empty Operand Handling + A `product.Value` that is technically non-nil but contains",320,5433,
analysis/lua/transferfacts/assignments.go,q0,"88: recursion/depth limits: A deeply nested `typ.Alias` chain that exceeds `typ.DefaultRecursionDepth` to ensure it returns `false` rather than stack overflowing.
94: cyclic/self-referential types: A `typ.Recursive` type where `v.Body == t` (direct self-reference) to ensure the recursion terminates immediately.
118: recursion/depth limits: A deeply nested `typ.Alias` chain in `recordWithCallableFieldDepth` that exceeds `typ.DefaultRecursionDepth`.
122: cyclic/self-referential types: A `typ.Recursive` type where `v.Body == t` to ensure the recursion terminates.
158: malformed input: An `ast.AttrGetExpr` where `attr.Key` is `nil`, potentially causing a panic in `dynamicIndexKeySource`.
163: malformed input: A `sourceprovenance.NewSourceShape` call that returns `ok == false`, triggering a hard `panic`.
165: malformed input: A `sourceprovenance.NewExpressionSource` call that returns `ok == false`, testing the fallback to `UnknownValueSource`.
188: nil/empty handling: A `typ.Type` that is `nil` passed to `declaredValue`, ensuring it returns a zero-value `product.Value` instead of panicking.",302,4870,
analysis/lua/transferfacts/values_exprref.go,q0,NONE,2,436,
analysis/lua/transferfacts/values_expression.go,q0,NONE,2,598,
analysis/lua/transferfacts/fact_set_append.go,q0,"14: nil/empty handling + Passing a `nil` map as `out` will cause a panic when attempting to access `out[point]`.
14: nil/empty handling + Passing a `point` that does not exist in the `out` map relies on the `Refinements()` method of a zero-value set to handle uninitialized map entries gracefully.
23: nil/empty handling + Passing a `point` that does not exist in the `out` map will cause `prior.LenRefinements()` to be called on a zero-value/nil set.
31: nil/empty handling + Passing a `point` that does not exist in the `out` map relies on `out[point].Evidence()` to return an empty slice rather than panicking.
39: nil/empty handling + Passing a `point` that does not exist in the `out` map relies on `out[point].Relations()` to return an empty slice rather than panicking.
47: nil/empty handling + Passing a `point` that does not exist in the `out` map relies on `out[point].Values()` to return an empty slice rather than panicking.",255,3611,
analysis/lua/transferfacts/lower.go,q0,"34: nil Config.Registry + A panic occurs if the registry is not provided, which is a critical dependency for value generation.
37: nil result or graph + Returns an empty facts object instead of erroring, which might mask upstream failures in the analysis pipeline.
41: nil Config.TypeResolver + Falls back to creating a new resolver using `config.Bindings`, which might lead to inconsistent type resolution if the bindings are incomplete.
138: nil call or zero-value point + Skips entries in the map, potentially losing call-site information if the AST and CFG are out of sync.
153: nil graph or result + Returns nil, which could cause a nil pointer dereference in the caller if they expect a valid `factflow.Facts` object.
157: nil fact.Call + Skips the point, which could happen if a call fact exists but the underlying AST expression is missing or malformed.
188: nil l or nil call or empty callPoints + Returns a zero-value point, which might be interpreted as a valid CFG point in some contexts, leading to incorrect fact mapping.",243,3861,
analysis/lua/transferfacts/path_static_value.go,q0,"28: nil/empty handling + A `lowerer` instance with a `nil` registry or a `target.Symbol` of `0` (the null symbol) will trigger an early exit.
33: malformed input + A `target.Segments` slice containing invalid indices or paths that do not exist within the type structure will cause `ApplySegments` to fail.
38: rare type combinations + A type that satisfies `IsOptionalLike` but contains a `nil` internal representation (e.g., a malformed union or a custom type wrapper) will trigger the `present == nil` check.",130,2389,
analysis/lua/transferfacts/type_cast_calls.go,q0,"41: nil/empty handling + `argPath.IsEmpty()` returns false if the type cast is applied to a path that resolves to an empty sequence or an unresolvable index.
46: nil/empty handling + `l.bindings == nil` returns false if the expression is an identifier but the symbol table is uninitialized.
48: malformed input + `l.bindings.TypeValueRef(ident)` returns false if the identifier exists in the AST but has no corresponding declaration in the binding map.
51: rare type combinations + `l.resolveDecl(decl)` may return a complex/cyclic type that causes downstream issues if the type witness is not a primitive.",144,2209,
analysis/lua/transferfacts/channel_selects.go,q0,"41: nil/empty handling + A `nil` receiver `l` passed to `channelSelects` would cause a panic when accessing `l.bindings`.
44: nil/empty handling + A `nil` `fact.Call` would cause a panic when passed to `channelSelectFactFromCall`.
51: nil/empty handling + A `nil` `l` or `l.bindings` would cause a panic when accessing them in `channelSelectFactFromCall`.
63: malformed input + A `CallResultTarget` with a `Kind` that is not a local or ordinary assignment (e.g., a global or temporary) is silently dropped.
75: malformed input + A `FuncCallExpr` with zero arguments or where the first argument is not a `TableExpr` returns `false` without error.
83: malformed input + A table containing `nil` fields (e.g., `{ [1] = nil, default = ... }`) is skipped, potentially missing the `default` key if it were nil.
94: malformed input + A table where all fields are `nil` or none are valid `ReceiveCaseSyntax` returns `false`, even if a `default` key was present.
101: malformed input + A `ReceiveCaseSyntax` where `pathexpr.Resolve` fails (e.g., due to a broken path or missing symbol) causes the entire `select` to be discarded.
125:",320,5364,
analysis/lua/transferfacts/objects.go,q0,"31: recursion/depth limits: A deeply nested `typ.Alias` or `typ.Recursive` type that exceeds `typ.DefaultRecursionDepth` to trigger the depth-limit exit.
35: recursion/depth limits: A `typ.Recursive` type where `v.Body == t` (immediate self-reference) to test the cycle-detection guard.
45: recursion/depth limits: A `typ.Instantiated` type that expands to itself (`expanded == t`) to test the expansion cycle-detection guard.
55: recursion/depth limits: A `typ.Union` containing a member that triggers a cycle or exceeds depth, testing if the loop correctly continues to other members.
75: recursion/depth limits: A `typ.Union` containing a member that triggers a cycle or exceeds depth, testing if the loop correctly continues to other members.
103: recursion/depth limits: A `typ.Recursive` type where `v.Body == nil` to ensure the nil-pointer guard prevents a panic.
108: recursion/depth limits: A `typ.Instantiated` type where `subst.ExpandInstantiated(v)` returns `nil` to ensure the nil-pointer guard prevents a panic.
116: recursion/depth limits: A `typ.Union` where `v.Members` is empty or contains `nil` elements, testing the robustness of the member iteration.
135: recursion/depth limits: A `typ.Recursive",320,4701,
analysis/lua/transferfacts/path_literal_narrowing.go,q0,"24: target.Segments empty/nil + A path with a valid symbol but zero segments (e.g., a direct variable access) might bypass refinement logic unexpectedly.
30: rootType missing from l.symbolTypes + A path referencing a symbol that was pruned or is unknown to the current analysis context.
32: cyclic/self-referential types + `variant.NarrowByPathLiteral` returning a type that is structurally identical to `rootType` but causes infinite recursion in subsequent passes.
33: cyclic/self-referential types + `typ.SameNodeOrAcyclicEqual` failing to catch a complex recursive type loop, leading to an infinite refinement loop.
43: target.Segments empty/nil + A branch condition on a zero-length path (e.g., a global check) that should be ignored but might trigger logic errors.
53: missing symbol + A check on a symbol that exists in the AST but has no associated type information in the `lowerer`.
56: malformed/unresolvable path + `narrowAnchor` failing to find a valid anchor in a deeply nested structure where intermediate fields are not unions.
60: huge unions/intersections + `variant.NarrowByPathLiteral` or `NarrowByPathLiteralNot` returning both `hasMatched` and `hasUnmatched` for a massive union, causing combinatorial explosion in refinement.
83: recursion/depth limits + `narrowAnchor` iterating through a very deep `target.Segments` slice where every",320,4943,
analysis/lua/typecall/shared.go,q0,NONE,2,87,
analysis/lua/typeoperator/arithmetic.go,q0,NONE,2,116,
analysis/lua/typeoperator/comparison.go,q0,NONE,2,431,
analysis/lua/typeoperator/concat.go,q0,NONE,2,70,
analysis/lua/typeannotation/annotation.go,q0,"21: Empty slice input + An empty `exprs` slice returns `nil, true`, which might cause nil-pointer dereferences in callers expecting a non-nil slice if they only check the boolean.
25: Malformed annotation + An annotation with an empty string `Name` (e.g., `@(arg)`) triggers a failure path.
25: Argument count overflow + An annotation with multiple arguments (e.g., `@(arg1, arg2)`) triggers a failure path.
43: Number parsing ambiguity + A numeric string that is neither a valid integer nor a valid float (e.g., a malformed scientific notation or a number with invalid characters) triggers the `false` return path.
47: Unsupported expression type + A complex AST expression (e.g., a function call or a table constructor) passed as an annotation argument triggers the `default` failure path.",192,3000,
analysis/lua/typeoperator/metamethod.go,q0,NONE,2,138,
analysis/lua/transferfacts/type_refs.go,q0,"21: nil/empty handling: Passing a slice containing a `nil` element (e.g., `[]any{nil}`) will cause `typeRef` to return `0, false`, which is silently ignored by the blank identifier in line 16, potentially resulting in a zero-valued `TypeRef` in the output slice.

16: error paths: The use of the blank identifier `_` ignores the boolean success flag from `typeRef`, meaning if a type cannot be resolved, the resulting `out` slice will contain a default `0` value, which may cause downstream logic errors in the factflow engine.

13: nil/empty handling: While `len(types) == 0` is handled, a `nil` slice input is also handled by the same check, but the function returns `nil` instead of an empty slice, which might affect callers expecting a non-nil slice for iteration.

26: malformed input: If `typ` is a non-comparable type (like a slice or a map) passed as an `any`, the line `l.types[typ]` will trigger a runtime panic because Go maps cannot use non-comparable types as keys.",255,4109,
analysis/lua/transferfacts/path_presence_narrowing.go,q0,"23: Nil/Empty Path - A `target.Path` with a zeroed `Symbol` or empty `Segments` slice should return early without attempting to look up types.
28: Missing Symbol - A `target.Symbol` that exists in the path but is missing from the `l.symbolTypes` map.
32: Cyclic/Self-Referential Type - A `rootType` that is equal to its own `narrowed` version (e.g., a recursive table definition) to prevent infinite refinement loops.
38: Nil/Empty Suffix - A `suffix` of segments that is empty or a `t` that is nil, triggering the base case of the recursion.
38: Recursion Depth Limit - A deeply nested type structure (e.g., 100+ nested `Optional` or `Alias` types) that exceeds `typ.DefaultRecursionDepth`.
41: Nil/Nil-wrapped Type - A type that, after `unwrap.NormalizeNil`, results in a nil value, testing the robustness of the type switch.
44: Deeply Nested Alias - An `Alias` type that points to another `Alias`, requiring multiple recursive steps to find the concrete type.
46: Deeply Nested Optional - An `Optional` type wrapping another `Optional` (e.g., `Optional<Optional<T>>`) requiring recursive unwrapping.
48: Massive Union - A `Union` type with a very large number of `Members` to test the",320,5276,
analysis/lua/transferfacts/symbol_types.go,q0,"23: nil/empty handling: `bindings`, `graph`, or `result` are nil, potentially causing a panic in callers expecting a map.
28: malformed input: `id` is 0 or `expr` is nil, which could lead to corrupting the symbol map with zero-value keys.
30: error returns: `resolver.Type(expr)` returns `ok=false`, representing a type resolution failure that silently skips the symbol.
41: nil/empty handling: `result.LocalAssignment(point)` returns `ok=false` or `HasSymbol=false`, testing the robustness of the RPO traversal.
51: malformed input: `fact.Symbol == 0` or `fact.Type != nil` or `fact.Expr == nil`, testing the logic for un-annotated locals with invalid or already-typed metadata.
54: cyclic/self-referential: `out[fact.Symbol]` is already present, preventing infinite loops if the RPO contains circular dependencies in type inference.
65: malformed input: `pathexpr.Resolve` returns `ok=false` or a `Symbol == 0`, testing access chains that point to non-existent or invalid roots.
69: nil/empty handling: `rootType` is nil, testing the case where a symbol exists in the map but has no associated type information.
73: boundary condition: `len(resolved.Segments) == 0`, testing the edge case where",320,4598,
analysis/lua/typecall/callable.go,q0,"23: Recursion/Depth Limits: A deeply nested `Optional` or `Alias` chain that hits the `stopDepth` threshold.
33: Cyclic/Self-referential Types: An `Instantiated` type that expands to itself, triggering the `expanded == t` safety check.
41: Nil/Empty Handling: A `Record` type that is technically non-nil but lacks the `__call` metamethod.
48: Nil/Empty Handling: A `Union` type containing only `nil` or empty members.
52: Nil/Empty Handling: A `nil` pointer passed as an `Intersection` type.
53: Rare Type Combinations: An `Intersection` where the first member is a non-callable and the second is a `Function`.
61: Recursion/Depth Limits: A `Function` witness wrapped in a deep `Union` or `Intersection` tree.
64: Nil/Empty Handling: A `nil` pointer passed as an `Intersection` type within a witness search.
65: Rare Type Combinations: An `Intersection` where the first member is a non-callable and the second is a valid `Function` witness.",255,3350,
analysis/lua/transferfacts/type_is_active_in.go,q0,"44-45: Fixed-point convergence/Infinite loop: A graph with a cycle where `typeIsRelationKilledAt` toggles the `out` state prevents the `changed` flag from ever stabilizing.
58: Nil/Empty input: If `input.RootAssignments` is nil, the function returns `false` immediately, but if `input.PathAssignments` is nil, `typeIsRelationKilledAt` might panic depending on the `ok` check implementation.
78: Malformed input/Empty slice: If `result.BranchCondition(branch)` returns a fact with a nil `Path`, `fact.Check.Path.Equal(target)` will panic.
103: Nil/Empty refinement: If `fact.ValueForEdge(cond)` returns a nil `refinement`, the subsequent call to `refinement.Constraint()` will panic.
104: Nil/Empty constraint: If `refinement.Constraint()` returns a nil `value`, the subsequent call to `product.PresenceOf(value)` will panic.
123: Graph topology/Disconnected components: If `graph.Predecessors(point)` returns a slice containing the `point` itself (self-loop) and it is the only predecessor, `allPredecessorsActive` may create a logic deadlock in the fixed-point iteration.
129: RPO/Ordering mismatch: If `graph.RPO()` returns a different order than what `laterPoint` expects, the ""later""",320,4551,
analysis/lua/typeoperator/unary.go,q0,NONE,2,387,
analysis/lua/transferfacts/values_source.go,q0,"36 + nil result + A `source.OpenTail` call where `result` is nil, testing the guard against a nil pointer dereference.
43 + missing call fact + A `SourceCall` that claims to be an open tail but the `result.Call` lookup fails, testing the fallback to the original source.
47 + non-callable fact + A `SourceCall` where the retrieved fact is a primitive (e.g., a number) rather than a function, testing the type-check guard.
52 + index overflow + A `SourceCall` at the maximum possible integer value for `TargetIndex` or `ResultIndex`, testing if incrementing for the `errorSource` causes an overflow.
65 + invalid shape + A `source.Final/Expanded/Adjusted/OpenTail` combination that violates the internal invariants of `NewValueSourceShape`, triggering a panic.
78 + unknown kind + A `source.Kind` value that is not explicitly handled in the switch (e.g., a zero-value or corrupted enum), triggering a panic.
84 + invalid value source + A `factflow.New...ValueSource` constructor that returns `ok=false` due to internal state inconsistency, triggering a panic.",266,4112,
analysis/lua/transferfacts/return_presence.go,q0,"14: nil/empty handling: If `result.Function()` returns a non-nil function with a `nil` `ReturnTypes` slice, the code may panic or behave unexpectedly depending on the AST implementation.
38: malformed input: If `fn.ReturnTypes[0]` is a `*ast.TupleTypeExpr` but its `Elements` slice contains `nil` entries, `declaredReturnTypes` will return a slice containing `nil` which may cause panics in downstream consumers.
68: malformed input: If `arity` is calculated as a very large number (e.g., via a corrupted `input.Returns` or `declaredReturnTypes`), the nested triple loop in `inferReturnSourcePresenceRelations` will cause an $O(N^2)$ complexity explosion.
88: malformed input: If `arity` is large, `make([]presence.Value, arity)` and `make([]bool, arity)` can trigger an out-of-memory panic.
105: malformed input: If `arity` is larger than the number of available `sources`, the loop terminates safely, but if `arity` is smaller than `len(sources)`, it silently ignores trailing return values.
116: malformed input: If `source.ResultIndex` is a very large integer (beyond slice bounds of the call results), it relies on the `result.Index() != source.ResultIndex` check, but if `input.CallResultValues` is malformed,",320,5401,
analysis/lua/typeresolve/binding_lookup.go,q0,NONE,2,257,
analysis/lua/transferfacts/type_is_recognition.go,q0,"41: nil/empty handling + `call.Args[0]` access will panic if `call.Args` is an empty slice despite `TypeIsCall` returning true.
41: malformed input + `argPath.IsEmpty()` check assumes `pathexpr.Resolve` returns a valid path object even on failure.
51: nil/empty handling + `input`, `graph`, or `result` being nil is handled, but `graph.RPO()` returning an empty slice is a silent no-op.
65: rare type combinations + `callResultTargetPath(fact, 1)` assumes the error path is always at index 1, which may fail for non-standard Lua call signatures.
69: rare type combinations + `callResultTargetPath(fact, 0)` assumes the value path is always at index 0.
81: malformed input + `typeIsSuccessEdges` might return an empty slice if the CFG is disconnected, leading to silent skips.
84: rare type combinations + `appendBranchPresenceRelations` creates mutual exclusivity between `errPath` and `valuePath` which might be invalid if both can be present.
118: malformed input + `branchFact.Stmt != fact.SourceStmt` might fail if the AST comparison logic doesn't handle pointer equality vs structural equality for identical statements.
148: rare type combinations + `l.typeIsCall(fact)` returning `nil` type might cause `l.typeWitnessValue(",320,5187,
analysis/lua/typeannotation/typeannotation.go,q0,"31: recursion/depth limits: A deeply nested `OptionalTypeExpr` or `UnionTypeExpr` could cause a stack overflow during recursive `Type` calls.
43: recursion/depth limits: A deeply nested `UnionTypeExpr` or `IntersectionTypeExpr` could cause a stack overflow during recursive `Type` calls.
54: recursion/depth limits: A deeply nested `MetaTypeExpr` could cause a stack overflow during recursive `Type` calls.
55: recursion/depth limits: A deeply nested `TupleTypeExpr` could cause a stack overflow during recursive `Type` calls.
73: malformed input: A `PrimitiveTypeExpr` with an empty name or invalid characters might bypass `BuiltinPrimitiveType` and create a broken `ref`.
83: malformed input: An `ArrayTypeExpr` where `Element` is nil (if the AST allows it) would cause a panic in `Type(expr.Element, resolver)`.
101: malformed input: A `MapTypeExpr` where `Key` or `Value` is nil would cause a panic in `Type`.
111: malformed input: A `RecordTypeExpr` with an empty `Fields` slice or nil field types would cause a panic or empty record.
128: recursion/depth limits: A `FunctionTypeExpr` with deeply nested `TypeParams` or `Returns` could cause a stack overflow.
129: recursion/depth limits: A `FunctionTypeExpr",320,4444,
analysis/lua/typecall/callable_return.go,q0,"26: Recursion/Depth Limits: A deeply nested `Optional` or `Union` chain that exceeds the implicit stack limit or the `stopDepth` threshold.
33: Huge Unions: A `Union` containing thousands of members, where only the very last member is a valid callable, testing slice allocation and loop performance.
39: Huge Unions: A `Union` where every member is a valid callable, testing the `normalize.UnionForEvidence` complexity and memory pressure.
45: Intersection/Empty Path: An `Intersection` where all members are non-callables, ensuring it correctly returns `nil, false` rather than a partial result.
49: Cyclic/Self-referential: An `Instantiated` type that expands to itself (e.g., `type T = T`), testing the `expanded == t` guard against infinite recursion.
54: Rare Type Combinations: A type that is both `Any` and a `Function` (if the type system allows such overlap), testing the priority of the `switch` vs `IsAny`.
65: Nil/Malformed Input: A `Function` type where `fn.Returns[0]` is explicitly `nil` despite `len(fn.Returns) > 0`.
71: Metatable Edge Case: A `Record` where the `Metatable` is present but `__call` is a non-callable type (e.g., a string or number), testing the recursive descent.
75: Metatable Edge Case",320,4626,
analysis/lua/typecall/distribute.go,q0,"24: Nil/Empty Union: A union containing only `nil` or `never` types results in `MemberCallMissing` rather than `MemberCallOK`.
38: Empty Union: A union with zero members returns `(nil, false)`, testing the boundary of empty collection handling.
49: Nil Intersection: Passing a `nil` intersection pointer returns `(nil, false)` instead of panicking.
58: Empty Intersection Result: An intersection where all members fail the query returns `(nil, false)`, testing the ""empty meet"" edge case.
73: Empty Union: A union with zero members returns `(nil, false)`, testing the boundary of empty collection handling.
86: Witness Mismatch: A union where two arms return different function signatures (e.g., `(int) -> int` vs `(string) -> int`) returns `false`.
87: Nil Witness: A union where all members are `nil` types results in `witness != nil` being false, returning `(nil, false)`.
98: Empty Intersection Result: An intersection where no members satisfy the query returns `MemberCallMissing`.
104: Single Member Intersection: An intersection that results in exactly one valid member bypasses `IntersectionForMeet` to return the raw type.",276,4722,
analysis/lua/typecall/generic_call.go,q0,"146 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
188 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
201 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
213 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
223 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
233 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
243 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
253 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
263 + recursion/depth limits + `instantiatedArgumentAssignable` returns `true` on depth exhaustion, potentially masking type mismatches in deeply nested recursive types.
273 + recursion",320,4321,
analysis/lua/typecall/metamethod.go,q0,"23: recursion/depth limits: A type with a depth exceeding `stopDepth` threshold will trigger the early exit.
24: recursion/depth limits: A type with a depth exceeding `stopDepth` threshold will trigger the early exit.
34: recursion/depth limits: An `Optional` type wrapping a deeply nested structure could hit the depth limit.
36: recursion/depth limits: An `Alias` pointing to another `Alias` could cause infinite recursion if not for the depth check.
38: cyclic/self-referential: An `Instantiated` type that expands back to itself (e.g., `T = List<T>`) could cause infinite recursion.
40: cyclic/self-referential: An `Instantiated` type that expands back to itself (e.g., `T = List<T>`) could cause infinite recursion.
47: nil/empty handling: A `Record` with a `nil` metatable field will return `nil, false` instead of panicking.
48: nil/empty handling: A `Record` with a `nil` metatable field will return `nil, false` instead of panicking.
49: malformed input: A `Record` where the metatable is ""unconstrained"" (e.g., a raw `any` or `table`) will fail to resolve the field.
53: huge unions: A `Union` containing thousands of members will cause significant latency/stack pressure during the `type",320,4021,
analysis/lua/typeprojection/constructor_path.go,q0,"21: nil/empty handling: An empty `segs` slice returns `false`, testing if the caller correctly handles the absence of a path.
35: malformed input: A `SegmentField` with an empty string name (e.g., `""""`) returns `false`, testing protection against invalid Lua field identifiers.
40: malformed input: A `SegmentIndexString` with an empty string name (e.g., `""""`) returns `false`, testing protection against empty string keys in table indices.
45: rare type combinations: A `Segment` with an unknown `Kind` (e.g., a custom or unhandled segment type) returns `false`, testing the robustness of the switch default case.",155,2087,
analysis/lua/typeoperator/numeric_literals.go,q0,"41: `strconv.ParseFloat` + A string containing only whitespace or non-numeric characters that `strings.TrimSpace` doesn't catch as an error but `ParseFloat` fails on.
42: `math.IsNaN` + A string literal representing `""NaN""` which is a valid float but should be rejected as a numeric literal.
42: `math.IsInf` + A string literal representing `""Inf""` or `""Infinity""` which should be rejected as a numeric literal.
25: `lit.Value.(string)` + A `typ.Literal` where `Base` is `kind.String` but the underlying `Value` interface holds a non-string type (type mismatch/malformed internal state).
11: `operatorSurface(t, 0)` + A deeply nested or cyclic type structure that could cause a stack overflow if `operatorSurface` does not implement depth limiting.",194,2891,
analysis/lua/typecall/member_call.go,q0,"71: recursion/depth limits + A deeply nested `typ.Alias` or `typ.Optional` chain that hits `stopDepth` and returns `MemberCallMissing` instead of a more descriptive error.
76: nil/empty handling + An empty `typ.Union` or `typ.Intersection` passed to `MemberCall` which might trigger unexpected behavior in `memberCallDistributeUnion`.
84: recursion/depth limits + A self-referential `typ.Instantiated` type where `subst.ExpandInstantiated(v) == t` to test the infinite loop prevention.
103: recursion/depth limits + A self-referential `typ.Instantiated` type where `subst.ExpandInstantiated(v) == t` to test the infinite loop prevention.
125: recursion/depth limits + A self-referential `typ.Instantiated` type where `subst.ExpandInstantiated(v) == t` to test the infinite loop prevention.
145: nil/empty handling + A `typ.Union` containing only `typ.Never` types, testing if `checked` remains false and returns `false` for `callableValue`.
147: rare type combinations + A `typ.Union` where one member is a `typ.Function` and another is a `typ.Nil`, testing the strictness of the ""all members must be callable"" rule.
149: rare type combinations + A `typ.Intersection` where one",320,5424,
analysis/lua/valueexpr/classify.go,q0,"28: NumberExpr/Malformed Input: A string that passes initial checks but fails both `ParseIntegerLiteral` and `ParseFloatLiteral` (e.g., ""NaN"", ""inf"", or a number exceeding float64 precision).
33: NumberExpr/Overflow: An extremely large integer string that might overflow the internal representation used by `numparse.ParseIntegerLiteral`.
43: AssertionInner/Nil/Empty Handling: An expression that is not an assertion but is passed to `AssertionInner`, testing if the unwrapping logic handles non-assertion types gracefully.
44: RuntimeKind/Nil/Empty Handling: An expression that evaluates to `nil` or is an empty/zero-value `ast.Expr` to see if `AssertionInner` panics.
45: RuntimeKind/Rare Type Combinations: A boolean expression wrapped in multiple layers of assertions/casts to test the depth of the `AssertionInner` unwrapping.",199,2845,
analysis/module/manifest/effect_wire.go,q0,NONE,2,95,
analysis/module/manifest/effect_codec_projection.go,q0,"34: unknown projection step kind: A wire payload containing an undefined or corrupted string in `step.Kind` (e.g., `""field_extra""`) that bypasses standard enum logic.

38: decodeType error propagation: A `step.Type` that contains a deeply nested or cyclic type structure that causes `decodeType` to fail or exhaust resources.

42: unknown projection step kind: A wire payload where `step.Kind` is an empty string or a null-equivalent, testing the robustness of the string-based switch.",116,1555,
analysis/lua/typeoperator/operand_predicates.go,q0,"14: nil/empty handling: `operatorSurface(t, 0)` returning `nil` for a non-nil input type (e.g., a type with a broken or uninitialized surface).
24: rare type combinations: `subtype.IsSubtype` being called with a type that has a circular reference to `typ.Integer`.
34: malformed input: `isNumericStringLiteral(t)` receiving a `typ.Literal` where `lit.Value` is not a `string` (type assertion failure).
41: malformed input: `lit.Value` being a `float64` that is `NaN` or `Inf`, testing the robustness of `isIntegralFloat`.
43: malformed input: `numericStringLiteral(lit)` receiving a string that represents a number larger than `float64` precision can hold.
53: rare type combinations: `isStringLike(t)` being called on a type that is a subtype of `String` but has a custom metatable that interferes with `IsSubtype`.
63: recursion/depth limits: `typetable.IsBuiltinTopMarker(t)` being called on a deeply nested or cyclic table type that might trigger stack overflow.",266,4029,
analysis/lua/typeoperator/logical.go,q0,"14: recursion/depth limits: A deeply nested `Union` or `Optional` structure that hits the `stopDepth` threshold, testing if the system fails gracefully rather than stack overflowing.
26: cyclic/self-referential types: A `Union` member that contains a reference back to the parent `Union`, testing if `logicalLeftVariants` enters an infinite loop.
31: huge unions/intersections: A `Union` with thousands of members, testing the slice allocation and memory pressure during the `append(out, variants...)` loop.
48: malformed input: A `typ.Never` or `typ.Any` type passed as the `left` operand, testing if the short-circuit logic in `logicalVariantResult` correctly bypasses further evaluation.
78: rare type combinations: A `typ.Literal` where `Base` is `kind.Boolean` but `Value` is not a `bool` (e.g., a string or nil), testing the `!ok` error path in `truthinessOf`.
88: nil/empty handling: A `nil` type passed to `truthinessOf`, testing if the `t == nil` check prevents a panic when calling `t.Kind()`.
94: rare type combinations: A `typ.Optional` type being evaluated for truthiness, which should fall into the `truthUnknown` case via the `switch t.Kind()` block.",306,4667,
analysis/module/importlookup/source.go,q0,"14: nil/empty handling: An empty string `path` is passed, which should immediately return `nil, false` without iterating.
17: nil/empty handling: A `Manifest` entry in the slice is `nil`, which would cause a panic if the `m == nil` check were missing.
17: malformed input: A `Manifest` has a `Path` that matches but an `Export` that is `nil`, which should be skipped to avoid returning a nil type.
16: boundary condition: The `Manifests` slice is empty, which should result in the loop being skipped entirely and returning `nil, false`.",139,2715,
analysis/lua/typeprojection/apply.go,q0,"41: Metatable/Payload extraction + A `typ.Meta` type where `meta.Of` is nil or points to a type that fails the generic constraint.
44: Generic constraint violation + A `typ.Generic` where the payload is a valid type but fails the `subtype.IsSubtype` check against the constraint.
47: Generic instantiation + A `typ.Generic` with `len(TypeParams) != 1` (violating the hardcoded assumption in the logic).
54: StepGenericArg index out of bounds + A `typ.Instantiated` type where `step.Index` is exactly `len(inst.TypeArgs)`.
54: StepGenericArg nil element + A `typ.Instantiated` type where `inst.TypeArgs[step.Index]` is explicitly `nil`.
54: StepGenericArg negative index + A `projection.StepGenericArg` with a negative `Index` value.
65: Empty projection steps + An empty `p.Steps` slice which should return the original `source` type.
81: Empty segments + An empty `segments` slice which should return the original `source` type.
84: Segment type mismatch + A `segment.SegmentIndexInt` being applied to a type that only supports string-based field access.
96: Invalid segment kind + A `segment.Segment` with an undefined `Kind` (e.g., 0 or an unhandled enum value).",317,4726,
analysis/lua/typeoperator/operator.go,q0,"24: recursion/depth limits + A deeply nested `typ.Alias` or `typ.Instantiated` type that exceeds `typ.DefaultRecursionDepth` to trigger the early exit.
43: recursion/depth limits + A `typ.Union` containing members that themselves contain unions, creating a tree depth that exceeds the limit during `binaryOpDepth` recursion.
54: cyclic/self-referential types + A `typ.Alias` where `UnaliasedTarget()` returns the alias itself, potentially causing an infinite loop if `stopDepth` logic is bypassed.
55: cyclic/self-referential types + A `typ.Instantiated` type where `subst.ExpandInstantiated` returns the same type, testing the identity check.
63: huge unions/intersections + A `typ.Union` with thousands of members, testing the memory/time complexity of `binaryLeftUnion` and the subsequent `normalizeOperatorResults`.
74: nil/empty handling + A `typ.Union` with an empty `Members` slice, which should return `nil, false` via `operatorOverUnion`.
103: metatable edge cases + A type that triggers `unaryMetamethodReturn` with a custom `__len` metamethod that returns a non-integer type.
115: metatable edge cases + A type where `unaryMetamethodName` returns a valid name but `unaryMetamethodReturn` returns `found=false`, testing the fallback logic.",320,5081,
analysis/module/manifest/type_wire.go,q0,NONE,2,410,
analysis/lua/typeresolve/resolver.go,q0,"48: nil receiver + `Type()` call: Calling `Type` on a nil `Resolver` should fallback to `typeannotation.Type` rather than panicking.
56: nil receiver + `Decl()` call: Calling `Decl` on a nil `Resolver` should return `nil, false` instead of panicking.
71: empty path + `ResolveTypeRef()`: Passing an empty slice `[]string{}` to `ResolveTypeRef` might bypass the `len(path) == 1` check and hit the external resolver with an empty path.
79: empty name + `currentBinding()`: A type reference with an empty string name `""""` should be handled gracefully by the binding lookup.
86: empty current stack + `currentBinding()`: Calling `currentBinding` when `r.current` is empty (no lexical scope) should return `false` rather than an index out of bounds.
91: zero ID + `resolveDecl()`: A `bind.TypeDecl` with `ID == 0` (uninitialized/malformed) should be treated as an invalid reference.
96: cyclic alias + `activeAliasRef()`: An alias that refers to itself via a type parameter (e.g., `type T<P> = T`) to see if it triggers infinite recursion or correctly returns a placeholder.
100: cyclic alias + `activeAliasRef()`: An alias that refers to itself via a non-parameterized path to ensure `activeRec`",320,4947,
analysis/lua/typeprojection/object_literal_expected.go,q0,"26: Nil/Empty handling: Passing a `rec` with a `nil` field entry or a field where `field.Type` is `nil` to trigger the early exit.
46: Nil/Empty handling: Passing `inferred` as `nil` to ensure the function doesn't panic when checking `IsAny` or `IsUnknown`.
47: Rare type combinations: Passing `typ.Any` or `typ.Unknown` for both `declared` and `inferred` to ensure the ""widening"" logic doesn't accidentally return a specific type when it should fail.
48: Rare type combinations: Passing a type that is `IsFreshAssignable` but not a `subtype` to verify the widening logic correctly returns the `declared` type.
56: Recursion/Depth limits: Passing a `typ.Union` with a depth exceeding `typ.DefaultRecursionDepth` to trigger the depth-limit exit.
57: Malformed input: Passing a type that is `Annotated` but whose underlying value is not a `*typ.Union` to test the type assertion failure.
66: Huge unions: Passing a `typ.Union` with multiple valid record arms to ensure the function returns `nil, false` (ambiguity) instead of the first match.
78: Malformed input: Passing a record where a field's type is `Annotated` but the underlying type is not a `*typ.Literal`.
82: Rare type combinations:",320,4951,
analysis/lua/typeresolve/type_name_walk.go,q0,"14: nil/empty handling: An `expr` that is explicitly `nil` (e.g., a malformed AST node) returns `true` silently, potentially masking a bug where a type was expected but missing.
23: recursion/depth limits: A deeply nested `OptionalTypeExpr` (e.g., `[[[[[int]]]]]`) could trigger a stack overflow.
25: huge unions/intersections: A `UnionTypeExpr` or `IntersectionTypeExpr` containing thousands of elements could cause significant latency or stack exhaustion via `WalkTypeNameExprs`.
30: recursion/depth limits: A `MapTypeExpr` where the `Key` or `Value` is itself a deeply nested map.
33: recursion/depth limits: A `RecordTypeExpr` with a massive number of fields or deeply nested field types.
38: recursion/depth limits: A `FunctionTypeExpr` with a massive number of parameters or a deeply nested `Variadic` type.
46: recursion/depth limits: A `GenericTypeExpr` where the `Base` is a generic type that contains the current type in its `Args`, creating an infinite recursion loop.
52: recursion/depth limits: A `MetaTypeExpr` wrapping itself or another type in a circular reference.
58: recursion/depth limits: A `ConditionalTypeExpr` where the `Check`, `Extends`, `Then`, and `Else` branches form a complex recursive tree.
66: nil/",320,4829,
analysis/module/manifest/effect_codec_support.go,q0,"21: nil/empty handling: A `nil` pointer passed to `decodeParamRef` returns a zero-value `ParamRef`, which might be indistinguishable from a valid index `0` depending on domain logic.

34: malformed input: `decodeIteratorKind` receives an empty string or a string with unexpected whitespace/casing (e.g., `""Indexed ""`), triggering an error path.

45: malformed input: `effectLabelWireKey` uses `json.Marshal` on `w`; if `w` contains types that cannot be marshaled (though unlikely for a wire struct), it silently falls back to `w.Kind`, potentially losing data integrity.",145,2924,
analysis/lua/visibilityfacts/visibility.go,q0,"24: nil graph handling + A `nil` graph passed to `Resolver` returns a default resolver instead of panicking.
34: zero-value symbol handling + A `fact.TargetSymbol()` returning `0` (null symbol) is explicitly ignored to prevent invalid definitions.
54: zero-value symbol handling + `event.ResultPath()` or `event.CasePath()` returning a symbol of `0` is ignored to prevent polluting definitions with null IDs.
65: missing symbol kind + A symbol exists in `needed` but `bindings.Kind(sym)` returns `ok=false`, forcing it to be seeded at entry.
83: empty path segments + A `pathdom.Path` with a non-zero symbol but zero segments is ignored to prevent invalid path resolution.
93: invalid container path + A `PathDescendantInvalidation` fact containing a malformed or empty container path.
96: nil/empty callee path + A `CallSite` where `CalleePath()` returns a path with a symbol of `0`.
97: nil/empty receiver path + A `CallSite` where `ReceiverPath()` returns a path with a symbol of `0`.
99: nil/empty method path + A `CallSite` where `MethodPath()` returns a path with a symbol of `0`.
102: non-expression argument source + An argument source where `source.Kind != factflow.ValueSourceExpression` is skipped, preventing crashes on direct",320,5471,
analysis/symbol/symbol.go,q0,NONE,2,403,
analysis/test/value/standard/registry.go,q0,NONE,2,97,
analysis/module/manifest/effect_codec_refinement.go,q0,"24 + nil pointer dereference + A pointer to a `postcondition.Present` that is itself `nil` (testing the redundant nil check inside the type switch).
29 + nil pointer dereference + A pointer to a `postcondition.Absent` that is itself `nil`.
41 + malformed input + A `decodeEffectRefinement` call with a `w.Kind` that is an empty string or an invalid UTF-8 sequence.
53 + recursion/depth limits + A `mutation.ElementUnion` where the `Source` is a deeply nested or self-referential `ParamRef` (testing stack exhaustion in `encodeParamRef`).
56 + recursion/depth limits + A `*mutation.ElementUnion` that points to itself, causing infinite recursion in `encodeEffectTransform`.
61 + recursion/depth limits + A `mutation.ContainerElementUnion` where `Container` and `Value` are identical or cyclic `ParamRef` objects.
64 + recursion/depth limits + A `*mutation.ContainerElementUnion` that triggers infinite recursion via the pointer dereference.
68 + recursion/depth limits + A `mutation.ToArray` where the `Element` is a cyclic `ParamRef`.
71 + recursion/depth limits + A `*mutation.ToArray` that triggers infinite recursion via the pointer dereference.
74 + malformed input + A `transform` type that implements the interface but is an unexported or unexpected concrete type.
88 + malformed",320,4737,
analysis/module/manifest/effect_codec.go,q0,"33: nil/empty handling + `row.Pure()` returning true for a row with non-nil labels or tail could lead to unexpected data loss in the manifest.
38: nil/empty handling + `row.Labels` being nil (not just empty) might cause `len(row.Labels)` to behave fine but `make` to allocate a zero-capacity slice.
44: malformed input + `validateManifestEffectLabel` returning an error during encoding prevents the creation of a manifest for an ""unsafe"" effect.
51: nil/empty handling + `row.Tail` being nil is handled, but if `row.Tail.Name` were to trigger a panic (unlikely in Go, but relevant if `Tail` was an interface), it would crash.
58: nil/empty handling + `w == nil` returns `effect.Empty`, which is a specific sentinel value that must be handled correctly by callers.
64: malformed input + `decodeEffectLabel` returning an error mid-loop causes the entire row decoding to fail, potentially losing valid labels.
67: malformed input + `validateManifestEffectLabel` failing on a decoded label prevents the reconstruction of a previously valid manifest.
81: malformed input + `label` being an unknown type in the type switch returns an error, preventing the encoding of custom/unsupported Lua effects.
84: recursion/depth limits + `encodeIteratorKind` or `encodeEffectTransform` could potentially trigger deep recursion if the",320,4991,
analysis/type/access/access.go,q0,NONE,2,375,
analysis/module/manifest/effect_codec_returns.go,q0,"24, 26, 29, 31, 34, 36, 39, 41, 44, 46: Recursion/Stack Overflow: A pointer-based type (e.g., `*returns.SameAs`) that points to itself or forms a cycle will cause a stack overflow during encoding.
50: Malformed Input/Error Path: A `TypeProjection` containing a deeply nested or invalid `Projection.Steps` slice that triggers an error in `encodeProjectionSteps`.
58: Nil Handling: Passing a `nil` wire object to `decodeEffectReturn` to ensure it returns `(nil, nil)` without panicking.
61, 63, 68, 70, 73, 75, 78, 80: Unsupported/Inactive Types: Providing a `Kind` that is explicitly handled as ""inactive"" (like `selectCaseOfParam`) to ensure it correctly triggers the `inactiveReturnTransform` error path.
83: Malformed Input/Error Path: A `TypeProjection` wire with a corrupted or invalid `Projection` slice that causes `decodeProjectionSteps` to return an error.
87: Malformed Input/Error Path: A wire with an unrecognized `Kind` string (e.g., `""returns.maliciousInjection""`) to verify the default error case.
92: Rare Type Combination/Error Path: A `ReturnType` that is not nil but",320,5497,
analysis/module/signaturelookup/source.go,q0,"28: nil/empty handling: An empty string `name` bypasses all manifest and stdlib lookups immediately.
31: nil/empty handling: A `Manifests` slice containing `nil` pointers will be skipped without erroring.
51: nil/empty handling: A `Manifests` slice containing `nil` pointers will be skipped during the merge process.
54: rare type combinations: If `m.FunctionSignatures` contains a signature that is itself a pointer or contains pointers, `sig.Clone()` must handle deep vs shallow copying to prevent side effects in the merged map.",129,2539,
analysis/module/manifest/effect_codec_expr.go,q0,"14 + recursion/depth limits + A deeply nested `BinOp` tree (e.g., 1000+ levels) that could trigger a stack overflow during the recursive `encodeExpr` calls.
48 + nil/empty handling + A pointer to an interface that is itself nil (e.g., `var e expr.Expr = (*expr.Var)(nil)`), testing the `value.IsNil()` check.
49 + malformed input + A pointer to a non-interface type that satisfies the `expr.Expr` interface via a custom type, testing the `reflect` logic.
68 + malformed input + A `BinOp` where one side is a valid expression but the other is a nil pointer, testing the `encodeRequiredExprPair` error path.
88 + malformed input + A `Min` or `Max` expression where one child is missing, testing the requirement enforcement.
101 + malformed input + A `BinOp` where the `Left` or `Right` field is a nil `*exprWire`, testing the `decodeRequiredExpr` error path.
133 + malformed input + A `Min` or `Max` wire where one child is nil, testing the requirement enforcement during decoding.
153 + malformed input + A `BinOp` wire containing an invalid/unrecognized `Op` string, testing the `decodeExprOp` error path.
165 + malformed input + A `BinOp` wire",320,5797,
analysis/module/manifest/operational_effects_codec.go,q0,"213: nil/empty handling + `encodeOperationalEffects` returns `nil, nil` if the input is empty, which might cause callers to skip necessary initialization logic.
253: nil/empty handling + `decodeOperationalEffects` returns an empty struct instead of an error when `w` is nil, potentially masking malformed JSON.
318: malformed input + `decodeReturnAllocationTemplate` fails if the `Root` ID is not present in the `Objects` list, preventing ""root-only"" templates.
323: cyclic/self-referential + `validateReturnAllocationTemplateRefs` checks for existence but does not detect infinite recursion in object references.
345: malformed input + `decodeAllocationObjectTemplate` fails if `StaticMembers` use a suffix that cannot be parsed by `segment.ParseFormattedSegments`.
383: malformed input + `decodePlaceholderPath` fails if `Param` is negative, which is a valid integer in JSON but invalid for the domain.
403: malformed input + `decodePresence` returns `presence.Bottom()` and an error for unknown strings, which might be ignored if the caller only checks the error.
418: malformed input + `decodeEscapeKind` returns `signature.EscapeNone` and an error for unknown strings, which could lead to silent logic errors if the error is swallowed.",295,4494,
analysis/module/manifest/manifest.go,q0,"41 + nil receiver + Calling `DefineType` on a nil `*Manifest` pointer.
48 + nil receiver + Calling `DefineFunctionSignature` on a nil `*Manifest` pointer.
55 + nil receiver + Calling `SetExport` on a nil `*Manifest` pointer.
66 + nil receiver + Calling `Encode` on a nil `*Manifest` pointer.
78 + cyclic/self-referential types + `m.Export` containing a type that references itself, potentially causing infinite recursion in `encodeType`.
93 + cyclic/self-referential types + `m.Types` containing a type that references itself, potentially causing infinite recursion in `encodeType`.
111 + cyclic/self-referential types + `m.FunctionSignatures` containing a type that references itself, potentially causing infinite recursion in `encodeType`.
124 + malformed input + `Decode` receiving a JSON byte slice that is valid JSON but contains invalid schema types (e.g., a string where a `typeWire` object is expected).
125 + malformed input + `Decode` receiving a JSON byte slice that is valid JSON but contains a `types` array with missing `name` fields.
135 + malformed input + `Decode` receiving a JSON byte slice where `export` is a valid type but fails internal `decodeType` logic (e.g., invalid type constraints).
142 + malformed input + `Decode` receiving a JSON",320,4760,
analysis/module/manifest/type_codec.go,q0,"14 + cyclic/self-referential types + A `typ.Annotated` or `typ.Optional` containing itself would cause a stack overflow during encoding.
31 + huge unions/intersections + A `typ.Union` with thousands of members could cause excessive memory allocation or stack exhaustion in `encodeTypeList`.
58 + huge unions/intersections + A `typ.Map` with extremely complex nested `Key` and `Value` types could trigger deep recursion limits.
88 + cyclic/self-referential types + An `typ.Alias` pointing to itself (if not caught by the type system earlier) would cause infinite recursion in `encodeType`.
101 + cyclic/self-referential types + An `typ.Instantiated` type where the `Generic` payload is a recursive structure would cause a stack overflow.
118 + malformed input + A `typ.Recursive` type being passed to `encodeType` triggers a specific error path that is often missed by standard happy-path tests.
135 + malformed input + A `typeWire` with a `Kind` that is an empty string or contains non-printable characters.
158 + malformed input + A `typeWire` with a `Kind` of ""union"" but a `nil` `Members` slice.
178 + malformed input + A `typeWire` with a `Kind` of ""instantiated"" where the `Generic` field is a `typ.Literal` instead of",320,4749,
analysis/type/access/index_map.go,q0,NONE,2,99,
analysis/module/signature/function.go,q0,"29: nil/empty handling: Comparing two `Function` structs where both `Type` and `OperationalEffects` are `nil` (tests if the logic correctly identifies them as equal rather than panicking).

31: nil/empty handling: Comparing a `Function` with a `nil` `Type` against one with a non-nil `Type` (tests the XOR-style nil check).

34: rare type combinations: Comparing two `Function` structs where `Type` is non-nil but `typ.TypeEquals` returns false due to complex nested type mismatches.

37: nil/empty handling: Comparing two `Function` structs where both `OperationalEffects` are `nil`.

39: nil/empty handling: Comparing a `Function` with a `nil` `OperationalEffects` against one with a non-nil `OperationalEffects`.

47: nil/empty handling: Calling `String()` on a `Function` where `Type` is `nil` (tests the `<nil>` fallback).

49: rare type combinations: Calling `String()` on a `Function` where `Type` is non-nil but `Effect.Pure()` is true (tests if it suppresses the effect string).

52: nil/empty handling: Calling `String()` on a `Function` where `Type` is non-nil and `Effect.Pure()` is false (tests the `! %s` formatting).

56: nil/empty handling: Passing `nil` to `cloneOperationalEffects",320,4485,
analysis/module/signature/operational_effects.go,q0,"118 + nil/empty handling + `cloneReturnPresenceRelations` returns `nil` instead of an empty slice `[]ReturnPresenceRelation{}`, which may cause unexpected behavior in callers expecting a non-nil slice.
126 + nil/empty handling + `clonePathPresenceRefinements` returns `nil` instead of an empty slice, potentially breaking callers that use `len()` or range without checking for nil.
134 + nil/empty handling + `clonePathStaticMemberFacts` returns `nil` instead of an empty slice.
142 + nil/empty handling + `clonePathInvalidations` returns `nil` instead of an empty slice.
150 + nil/empty handling + `cloneFrozenTables` returns `nil` instead of an empty slice.
158 + nil/empty handling + `cloneEscapeEvents` returns `nil` instead of an empty slice.
166 + nil/empty handling + `cloneStoreRelations` returns `nil` instead of an empty slice.
174 + nil/empty handling + `cloneReturnAllocationTemplates` returns `nil` instead of an empty slice.
188 + nil/empty handling + `cloneAllocationObjectTemplates` returns `nil` instead of an empty slice.
201 + nil/empty handling + `cloneAllocationStaticMemberTemplates` returns `nil` instead of an empty slice.
210 + nil/empty handling + `cloneAllocationDynamicEntryTemplates` returns `nil` instead of an empty",320,4164,
analysis/module/typelookup/source.go,q0,"28: len(path) < 2 + An empty slice `[]string{}` or a slice with a single empty string `[]string{""""}` that bypasses the `len == 1` check but fails the logic.
32: modulePath construction + A path where `len(path) == 2` results in an empty `modulePath` string (e.g., `path := []string{"""", ""Type""}`), testing the `modulePath == """"` guard in `Lookup`.
38: modulePath/name empty + Passing `""""` as `modulePath` or `name` to ensure the function returns `false` rather than attempting a map lookup.
40: Manifest override order + Multiple manifests containing the same `modulePath` and `name`, where the last one in the slice must be the one returned.
41: Nil manifest + A `Manifests` slice containing `nil` elements (e.g., `[]*manifest.Manifest{m1, nil, m2}`) to ensure the loop doesn't panic.
42: Path mismatch + A manifest where `m.Path` is an empty string or contains special characters/dots that might conflict with the `strings.Join` logic.
43: Nil Types map + A manifest where the `Types` map is explicitly `nil` rather than just empty.
45: Nil Type value + A manifest where the map entry exists but the value `m.Types[name]` is `nil`.
6",320,3981,
analysis/module/manifest/type_codec_primitives.go,q0,"46 + malformed input + A `typ.Type` with a `Kind` not covered by the switch (e.g., a custom implementation) will trigger an error return.
58 + malformed input + A `typ.Literal` where `Base` is `kind.Boolean` but `Value` is a `string` will trigger a type assertion error.
59 + malformed input + A `typ.Literal` where `Base` is `kind.Integer` but `Value` is a `float64` will trigger a type assertion error.
64 + malformed input + A `typ.Literal` where `Base` is `kind.Number` but `Value` is an `int64` will trigger a type assertion error.
69 + malformed input + A `typ.Literal` where `Base` is `kind.String` but `Value` is `nil` will trigger a type assertion error.
83 + malformed input + A `typeWire` with `Base: ""boolean""` but `Bool: nil` will trigger a ""missing value"" error.
87 + malformed input + A `typeWire` with `Base: ""integer""` but `Int: nil` will trigger a ""missing value"" error.
91 + malformed input + A `typeWire` with `Base: ""number""` but `Number: nil` will trigger a ""missing value"" error.
95 + malformed input + A `typeWire` with",320,5659,
analysis/module/manifest/type_codec_structured.go,q0,"46 + nil/empty handling + A function with zero parameters, zero returns, and zero type parameters might trigger unexpected behavior in the `typ.Func()` builder.
74 + malformed input + A `typeWire` with `Kind: ""function""` but missing `Params` or `Returns` fields (nil slices) could cause panics in the builder.
95 + malformed input + An interface wire where `Methods` contains a type that is not a `*typ.Function` (e.g., a primitive) will trigger the error path.
114 + recursion/depth limits + A generic type whose `Body` is itself (self-referential) will cause infinite recursion in `encodeType` or `decodeType`.
131 + malformed input + A `typeWire` with `Kind: ""typeparam""` but an empty `TypeParams` slice will trigger the error path.
138 + nil/empty handling + A `TypeParam` with a `nil` constraint will trigger the error path in `encodeTypeParamWire`.
153 + malformed input + An annotation wire with `Kind: ""string""` but a `nil` `String` pointer will trigger the error path.
171 + malformed input + An annotation wire with `Kind: ""bool""` but a `nil` `Bool` pointer will trigger the error path.
176 + malformed input + An annotation wire with `Kind: ""int""` but a `nil` `Int`",320,5217,
analysis/module/signaturelookup/internal/stdlib/registry.go,q0,"148 + `setmetatable` + TypeParam circularity: A type parameter `T` that is defined as a table containing itself could cause infinite recursion during type normalization or cloning.
178 + `table.remove` + Array/Table mismatch: Passing a non-array type (like a dictionary/hash map) to a function expecting `NewArray(elem)` might trigger edge cases in the type checker's array-to-table conversion.
213 + `table.create` + Record/Table overflow: Creating a table with extremely large `narray` or `nhash` values might test the limits of the `typetable.NewRecord` implementation.
225 + `table.freeze` + Deeply nested frozen tables: Freezing a table that contains itself or has a massive depth of nested tables could trigger stack overflow during ownership tracking.
288 + `string.format` + Variadic type mismatch: Providing a massive number of arguments or arguments that don't match the `formatstring` pattern to test the robustness of the variadic type inference.
355 + `math.max/min` + Variadic overflow: Passing an extremely large number of `typ.Number` arguments to test the limits of the variadic parameter handling.
410 + `coroutine.resume` + Callback return complexity: Using a callback that returns a highly complex, self-referential, or deeply nested type to test the `returns.CallbackReturn` transformation.
465 +",320,5298,
analysis/type/access/index_tuple.go,q0,"21: nil/empty handling: A tuple with zero elements (`len(tup.Elements) == 0`) where a non-literal integer key is provided, testing if the function correctly returns an empty `fieldResult` instead of an empty union.
24: nil/empty handling: A tuple containing only `nil` elements being indexed by a non-literal integer, testing if the normalization correctly collapses `nil` elements into `typ.Unknown`.
32: boundary conditions: An `index` value of exactly `0` or exactly `len(tup.Elements) + 1`, testing the off-by-one boundary of the 1-based Lua indexing logic.
35: nil/empty handling: A tuple where the specific element at `index-1` is `nil`, testing if it is correctly promoted to `typ.Unknown` rather than returning a nil type.",189,2142,
analysis/type/access/index_composite.go,q0,"14: `in == nil` + An intersection type with a nil pointer (not just an empty slice) to ensure the function doesn't panic when traversing a broken AST.
15: `len(u.Members) == 0` + A union type that exists but contains no members, testing the ""empty set"" logic for type resolution.
22: `distributeIntersection` closure + A member within an intersection that returns `(nil, false)` to ensure the intersection logic correctly handles ""missing"" keys across all branches.
27: `indexDepth(...).materialize()` + A deeply nested type structure where `indexDepth` returns a complex type that might fail during the `materialize()` call (e.g., a type with circular references).",162,2991,
analysis/type/access/distribute.go,q0,"41: `distributeIntersection` + Empty intersection members: If `members` is empty, it returns an empty `fieldResult{}`, which might be interpreted as a type error rather than a ""bottom"" type in some type systems.

18: `distributeUnion` + All members missing fields: If every member in a union is missing the queried field and `missingFieldReadsNilDepth` returns true, it returns `typ.Nil`, which tests the boundary between ""field doesn't exist"" and ""field is nil"".

14: `distributeUnion` + Depth-induced stack overflow: If `query` is a recursive function that doesn't respect the `depth` increment, the `depth+1` passed here will eventually cause a stack overflow in the host Go process.

31: `distributeUnion` + Massive Union Explosion: If `out` contains thousands of distinct types, `normalize.UnionForEvidence(out...)` may trigger an exponential complexity blowup or memory exhaustion during normalization.

26: `distributeUnion` + Nilable vs Non-Nilable mismatch: A case where one member is a non-nilable type and another is a nilable type, testing if the `nilable` flag correctly propagates to the final union.

16: `distributeUnion` + Total failure via non-nilable member: A case where one member is a strict type (not nilable) that lacks the field, which must cause the entire union access to fail (`ok: false`) immediately",320,4164,
analysis/type/access/field_projection.go,q0,"31: nil receiver in interface method lookup: passing a `nil` `receiver` to `subst.Self` might cause a panic if the substitution logic assumes a non-nil type.
43: nil record lookup: passing a `nil` `*typ.Record` to `fieldInRecord` returns an empty `fieldResult`, which might mask logic errors in the caller.
52: map component with string key: a record that is both a fixed structure and a map where the key is a complex type that *could* contain the string.
56: open table field access: accessing a field on an `Open` table (e.g., `t.anything`) returns `typ.Unknown`, which may lead to type-checking bypasses.
62: nil value in map: a map where the `value` type is explicitly `nil`, which should resolve to `typ.Nil` rather than an error.
67: empty union: a `typ.Union` with an empty `Members` slice, which should return an empty `fieldResult` rather than panicking.
68: recursion depth: `fieldInUnion` passes `depth` to `distributeUnion`, which is likely intended to prevent infinite loops in cyclic type definitions.
75: nil intersection: passing a `nil` `*typ.Intersection` returns an empty `fieldResult`, potentially causing silent failures in type refinement.
76: intersection member exhaustion: an intersection with no members, which might represent a `bottom",320,4139,
analysis/type/access/field_descent.go,q0,"15: recursion/depth limits: A deeply nested type structure (e.g., a recursive `Union` or `Intersection`) that hits `typ.DefaultRecursionDepth` to ensure the function terminates rather than stack overflowing.
17: special type short-circuit: A type that is `Any`, `Unknown`, or `Never` passed as the root type, testing if the descent is bypassed immediately.
22: special type short-circuit: A type wrapped inside a wrapper (like an `Annotated` type) that is `Any` or `Unknown`, testing if the short-circuit works inside the descent loop.
24: rare type combinations: An `Annotated` type that wraps a `nil` type, testing if `unwrap.Annotated` returns a type that causes the `switch` to hit `default`.
25: malformed input: A `*typ.Record` where the internal field map is nil or the requested `name` is an empty string.
27: rare type combinations: A `*typ.Interface` where the interface definition is empty or the type `t` passed is not the actual interface type.
29: rare type combinations: A `*typ.Map` where the `Key` type is a complex type (like a `Union`) rather than a primitive, testing key-lookup logic.
31: rare type combinations: A `*typ.ReadonlyMap` where the map is empty, testing if the descent correctly returns an empty `fieldResult`.",320,4140,
analysis/test/laws/lattice/laws.go,q0,"48: nil/empty handling + If `s.Sample` contains `nil` elements, `s.fmt(x)` or `s.Domain.Equal` may panic depending on the domain implementation.
78: nil/empty handling + If `s.Domain.Bottom()` or `s.Domain.Top()` returns `nil`, `s.Domain.LessOrEq(bot, x)` will likely panic.
103: nil/empty handling + If `s.Domain.Equal` or `s.Domain.LessOrEq` are provided as `nil` (despite `domainValid` check), the suite will panic.
114: nil/empty handling + If `s.Format` is `nil` and `x` is a type that `fmt.Sprintf(""%+v"")` cannot handle (e.g., certain pointer-to-pointer or complex interface types), it may produce unhelpful output or panic.
138: recursion/depth limits + If `s.Domain.Equal` or `s.Domain.LessOrEq` are implemented via deep recursion on nested structures, the $O(N^2)$ or $O(N^3)$ loops in the suite will trigger a stack overflow.
148: cyclic/self-referential + If `s.Sample` contains a self-referential structure (e.g., a map or slice containing itself), `s.fmt(x)` will enter an infinite loop/stack overflow.
164",320,5181,
analysis/type/format/format_union.go,q0,NONE,2,487,
analysis/type/ambient/types.go,q0,"40: nil/empty handling + `inst.TypeArgs[0] == nil` handles a generic instantiation where the type argument is explicitly passed as a null/nil type.
40: malformed input + `inst.Generic == nil` handles a corrupted or manually constructed `Instantiated` type where the underlying generic definition is missing.
40: rare type combinations + `len(inst.TypeArgs) != 1` handles cases where a type is incorrectly treated as a `Channel` but has zero or multiple type arguments (violating the `Channel<T>` contract).
40: malformed input + `!ok` handles cases where a type is passed to `ChannelPayloadType` that is not an `Instantiated` type (e.g., a primitive or a raw interface).",169,2344,
analysis/type/access/index_record.go,q0,"14: nil record handling + Passing a `nil` pointer for `r` to ensure the function returns an empty `fieldResult` instead of panicking.
21: map component type compatibility + A `key` that is a complex type (e.g., a table or function) being used to index a record with a `MapComponent` to test `MapComponentKeyAdmitsType`.
24: open table fallback + An `Open` record (Lua table with no fixed fields) being indexed with a completely unknown/garbage type to ensure it returns `typ.Unknown`.
34: static string index vs map component + A string key that exists both as a static field and a map component to verify priority/correctness.
37: map component string key + A string key that is not a static field but is valid within the `MapKey` type constraints.
44: static int index vs map component + An integer key that exists both as a static index and a map component to verify priority/correctness.
47: map component int key + An integer key that is not a static index but is valid within the `MapKey` type constraints.
50: open table fallback + An `Open` record being indexed with an integer key to ensure it returns `typ.Unknown`.",275,3300,
analysis/type/access/index.go,q0,"33: recursion/depth limits + `stopDepth` check prevents stack overflow when a type definition contains a cycle (e.g., `type T = T[1]`).
34: SpecialAccessType + `SpecialAccessType` bypasses standard indexing logic for types that implement custom access rules (like metatables or proxies).
41: nil/empty handling + `missingFieldReadsNilDepth` handles the Lua-specific ""missing key returns nil"" behavior for `RuntimeIndex` mode.
48: recursion/depth limits + `descendAccessWrappers` handles deeply nested type wrappers (like `Annotated<Annotated<T>>`) that could exceed stack limits.
52: metatable edge cases + `SpecialAccessType` inside the wrapper loop handles cases where a wrapper itself triggers a special access rule.
54: rare type combinations + `unwrap.Annotated(t)` handles types that are wrapped in metadata/annotations which must be stripped before type inspection.
55-66: huge unions/intersections + the switch statement handles complex structural types where the key might exist in only one branch of a `Union` or `Intersection`.",246,4232,
analysis/type/access/index_array.go,q0,"21: nil element handling + An array with a `nil` `Element` field (representing an untyped/empty array) to ensure it defaults to `typ.Unknown` rather than panicking.
31: recursion depth limit + A deeply nested or cyclic type structure that triggers `stopDepth` to prevent a stack overflow during runtime key validation.
43: float-to-int precision edge case + A `typ.Literal` with a `kind.Number` value of `math.MaxFloat64` or a non-integer float (e.g., `1.5`) to ensure it correctly returns `false`.
48: massive union member explosion + A `typ.Union` containing thousands of members, or a union of unions, to test the performance and depth of the recursive search.
50: recursive union member validation + A `typ.Union` where only the very last member is a valid `Integer` type, testing the exhaustive search path.
54: unexpected unwrap type + An `Annotated` type that does not match any expected case (e.g., a custom wrapper or a malformed type) to test the `default` fallback logic.",252,4286,
analysis/type/internal/nodeid/nodeid.go,q0,NONE,2,380,
analysis/type/access/wrappers.go,q0,"21: recursion/depth limits: A type structure that exactly hits the `stopDepth` threshold to ensure the function returns the zero value rather than recursing infinitely.
26: nil/empty handling: An `*typ.Optional` where `v.Inner` is `nil`, testing if the recursion handles a null pointer gracefully.
30: cyclic/self-referential types: A `*typ.Recursive` where `v.Body` points back to `t`, testing the identity check to prevent infinite loops.
30: nil/empty handling: A `*typ.Recursive` where `v.Body` is explicitly `nil`, testing the guard against nil pointer dereference.
34: cyclic/self-referential types: An `*typ.Instantiated` type that expands to itself via `subst.ExpandInstantiated`, testing the identity check to prevent infinite loops.
34: nil/empty handling: An `*typ.Instantiated` type where `subst.ExpandInstantiated` returns `nil`, testing the guard against nil pointer dereference.",230,3114,
analysis/type/access/missing_field.go,q0,"14: `*typ.Union` with empty members + An empty union type (e.g., `type T = {}` in some type systems) that could cause a false positive or immediate return.
21: `*typ.Intersection` with empty members + An empty intersection type that might bypass the loop and return `false` incorrectly.
21: `*typ.Intersection` with members that all return `false` + A complex intersection where every member is a primitive, testing the ""all or nothing"" logic of intersection field presence.
11: `descendAccessWrappers` recursion + A deeply nested type structure (e.g., `Map<Map<Map<...>>>`) designed to hit the `stopDepth` limit or cause a stack overflow.
12: `unwrap.Annotated(t)` + A type wrapped in multiple layers of metadata/annotations that might obscure the underlying `typ.Union` or `typ.Intersection`.
13: `switch v := unwrap.Annotated(t).(type)` + A type that is technically a `Record` or `Map` but is wrapped in a way that `unwrap.Annotated` returns a type not explicitly handled in the switch (triggering `default`).",263,3543,
analysis/type/format/format_container.go,q0,"20: MaxTupleElems boundary + A tuple with exactly `MaxTupleElems + 1` elements to ensure the ellipsis `...` is triggered correctly.
21: Large Tuple/Memory + A tuple containing millions of elements to test if the loop or `minInt` causes an integer overflow or excessive allocation.
22: Nil/Empty Element + A tuple where `t.Elements[i]` is `nil` to see if `f.formatType` panics on a null pointer.
23: Recursion Depth + A deeply nested tuple (e.g., `((((...))))`) that exceeds the `guard` limit to ensure the recursion depth is incremented and checked.
24: Cyclic Reference + A tuple that contains itself as an element (e.g., `t.Elements[0] = t`) to test if the `guard` prevents an infinite loop/stack overflow.",191,2422,
analysis/type/access/index_key.go,q0,"21: Nil/Empty Union Handling: A `typ.Union` with a `nil` pointer or an empty `Members` slice could cause a panic or return an incorrect empty `fieldResult` if not caught.
23: Recursive Depth/Stack Overflow: A self-referential `typ.Union` (where a member is the union itself) will cause infinite recursion in `indexByKeyVariants` via `indexKeyUnion`.
31: Runtime Nil-Coalescing: In `indexRuntime` mode, a key lookup that fails on a specific union member must correctly transition to `typ.Nil` without failing the entire union branch.
41: Union Member Exhaustion: A union where every single member fails the `project` lookup (e.g., all members are tables missing the key) must correctly return an empty `fieldResult` rather than a partial one.
43: Runtime Nil-Coalescing in Unions: If `mode == indexRuntime` and `missingNil` is true, a single failing member in a union should not invalidate the whole union, but instead contribute to the `nilable` status.
48: Empty Result Union: A union where all members result in `res.ok == false` but `nilable == true` must return `typ.Nil` instead of an empty `fieldResult`.
53: Union Normalization Overflow: A massive union with thousands of members could cause `normalize.UnionForEvidence` to hit recursion limits or memory exhaustion during type simplification.
61:",320,5149,
analysis/type/format/format_function.go,q0,"20 + MaxTypeParams truncation + A function with 1,000 type parameters where `MaxTypeParams` is small, testing if the `...` ellipsis correctly signals truncation without crashing.
33 + MaxParams truncation + A function with massive parameter lists where `MaxParams` is reached, ensuring the ellipsis is appended correctly to prevent incomplete signature representation.
43 + Variadic/Params overlap + A function that is both variadic and has a large number of parameters, testing the comma placement between the last named parameter and the `...` type.
51 + Single vs Multiple Returns + A function returning a single complex type versus a tuple, testing if the parentheses are correctly omitted for single returns to match standard Lua/TS syntax.
55 + MaxReturns truncation + A function returning a massive tuple of types where `MaxReturns` is exceeded, testing if the return signature correctly truncates with `...` inside the parentheses.",195,2866,
analysis/type/format/format.go,q0,"48: nil/empty handling + Passing a `nil` `typ.Type` (if the interface allows) to `Type()` which would cause a panic in `f.formatType`.
58: overflow/boundary + A `MaxBytes` value of `0` or a negative value which might bypass truncation logic or cause unexpected behavior in `write`.
63: truncation/boundary + A type that is exactly `MaxBytes` long, testing if the `...` suffix logic correctly handles the edge where no bytes remain.
73: truncation/boundary + A string `s` that is exactly equal to `remaining`, testing the boundary of the `len(s) > remaining` condition.
75: truncation/boundary + A string `s` that is much larger than `remaining`, testing if the slice `s[:remaining]` correctly prevents `MaxBytes` overflow.
85: overflow/boundary + A `MaxBytes` value of `1` or `2`, testing if the `f.bytes+3 <= f.opts.MaxBytes` check in `string()` correctly prevents appending `...` when it would exceed the limit.",243,3599,
analysis/type/kind/kind.go,q0,"48-75: Out-of-bounds Kind values + An integer cast to `Kind` that is negative or exceeds the `kindNames` array length (e.g., `Kind(-1)` or `Kind(999)`).
78-86: Invalid/Zero-value string mapping + A `Kind` value that points to an empty string in the `kindNames` array (due to the `_` blank identifiers used in the const block).
88-95: Out-of-bounds `String()` return + A `Kind` value that is technically within the array bounds but represents a skipped index, or a value that bypasses the `if` check via overflow.",150,2215,
analysis/type/channelselect/schema.go,q0,"54: nil/empty handling + `cases` slice is empty and `hasDefault` is false, returning `(nil, false)` instead of panicking.
68: nil/empty handling + `cases` contains only the `DefaultCaseIndex`, which is filtered out, potentially leading to an empty `caseTypes` slice.
84: malformed input + `caseMarkerType` creates a record with specific internal fields that might be shadowed or corrupted by user-defined metatables if not handled by `unwrap`.
101: rare type combinations + `caseType` is a `Union` instead of a `Record`, causing `resultChannelFieldType` to return `false` gracefully.
114: recursion/depth limits + `ResultHasSelectID` calls `unwrap.Annotations`, which could trigger infinite recursion if the type tree is cyclic.
133: malformed input + `ResultWithoutCase` is called with an `index` that matches a member but results in an empty `kept` slice, returning `typ.Never`.
145: malformed input + `resultChannelFieldType` receives a type that is an `Alias` or has `Annotations`, testing the robustness of the `unwrap` chain.
163: malformed input + `ResultCaseTypeFromValue` is called with a `resultType` that is a `Union` containing non-record members.
176: malformed input + `caseMarker` receives a `typ.Type` that is",320,4799,
analysis/type/inspect/visit.go,q0,"37: unwrapTransparent: A type that is wrapped in multiple layers of ""transparent"" wrappers (e.g., an Alias of an Alias of a Ref) could cause unexpected behavior if the unwrapping logic is not idempotent or fails to terminate.

39-105: Nil handler fallback: A `Visitor` where all specific type handlers are `nil` but `Default` is provided, testing if the dispatcher correctly falls through to the catch-all for every single type branch.

107-109: Zero-value return: A `Visitor` where both the specific handler and `Default` are `nil`, testing if the function safely returns the zero value of a generic type `R` without panicking.

101: Recursive type handling: A `typ.Recursive` type that points to itself or forms a cycle, testing if the visitor (which is just a dispatcher) triggers a stack overflow in the *caller* implementation.",200,2856,
analysis/type/subst/expand.go,q0,NONE,2,476,
analysis/type/annotation/annotation.go,q0,"111: Float64 NaN comparison + A payload containing `math.NaN()` will return `false` when compared to itself via `Equal`, potentially breaking map lookups or set logic.
111: Float64 bit-level equality + Two different NaN bit patterns (e.g., signaling vs quiet NaN) will be treated as unequal despite being the same ""type"" of value.
131: Hash collision/stability + Using `math.Float64bits` ensures NaN bit patterns are hashed, but if the user expects `NaN == NaN` to hold for identity, the hash/equal contract is strictly bitwise.
131: Hash collision/stability + A payload with `math.Inf(1)` and `math.Inf(-1)` must be distinct in both `Equal` and `Hash`.
135: Hash stability + A payload with an empty string `""""` vs a payload with a string containing only whitespace or control characters.
135: Hash stability + A payload with a very large string that might trigger different behavior in the underlying `hash.FnvString` implementation.
141: Integer overflow/sign extension + An `int` payload containing `math.MinInt` on a 64-bit system being cast to `uint64` for hashing.
144: Integer overflow/sign extension + An `int64` payload containing `math.MinInt64` being cast to `uint64` for hashing.",317,5341,
analysis/type/inspect/scan.go,q0,"11: nil predicate + passing a `nil` function as the `pred` argument to `containsMatching`.
41: identity tracking + a custom/unregistered type implementation that returns `false` for `trackConcreteNodeIdentity` but is structurally recursive.
65: nil type + passing a literal `nil` as the `t typ.Type` argument.
68: transparent unwrap nil + a type that `unwrapTransparent` returns `nil` for (e.g., an empty `Alias` or `Ref` pointing to nothing).
71: cyclic/self-referential + a type structure where `scan.Enter(t)` returns `false` due to a cycle, potentially masking a `pred` match if the cycle occurs before the match.
74: deep recursion + a deeply nested type (e.g., 10,000 nested `Optional` types) that triggers a stack overflow in the recursive `contains` calls.
77: child traversal + a `WalkChildren` implementation that returns `true` prematurely or fails to visit all branches in a complex `Union`/`Intersection`.",239,3691,
analysis/type/format/format_record.go,q0,"20: MaxRecordFields limit + A record with 10,000 fields where `MaxRecordFields` is set to a small value, testing the `...` ellipsis truncation logic.
34: Empty Fields/StaticMembers + A record where `len(r.Fields) == 0` and `len(r.StaticMembers) == 0`, testing if the first static member correctly avoids a leading comma.
46: MapComponent with no Fields/StaticMembers + A record that only contains a MapComponent, testing if the comma logic correctly handles the transition from an empty field list.
53: Open Record with no Fields/Map + A record where `Open == true` but `Fields` and `MapComponent` are empty, testing if it produces `{...}` instead of `{, ...}`.
73: StaticMemberStringIndex Escaping + A static member name containing a single double-quote (e.g., `foo""bar`), testing if the `strings.ReplaceAll` correctly prevents malformed output.
77: StaticMemberIntIndex Overflow + A static member with `Index` set to `math.MaxInt64`, testing if `strconv.FormatInt` handles the extreme boundary of the integer type.
79: Unknown StaticMemberKind + A static member with a `Kind` not covered by the switch, testing if the `[unknown]` fallback prevents a silent failure or panic.",302,4130,
analysis/type/subst/expand_memo.go,q0,NONE,2,445,
analysis/type/format/format_recursive.go,q0,"14: nil handling + A `typ.Type` interface that is explicitly `nil` (not just a nil pointer to a struct, but a nil interface value).
20: recursion/depth limits + A deeply nested type (e.g., `Array<Array<Array<...>>>`) that exactly hits `MaxDepth`.
25: recursion/depth limits + A massive type tree that exceeds `MaxNodes` mid-traversal to trigger the `truncated` flag.
78: malformed input + An `Alias` type where both `Name` and `Target` are nil/empty, testing the fallback to `""alias""`.
84: recursion/depth limits + A `TypeParam` with a `Constraint` that is itself a deeply nested or cyclic type.
91: huge unions/intersections + A `Generic` type with a `TypeParams` slice length exceeding `MaxTypeParams`.
104: huge unions/intersections + An `Instantiated` type with a `TypeArgs` slice length exceeding `MaxTypeParams`.
118: malformed input + An `Interface` type where `Name` is an empty string, testing the fallback to `""interface{...}""`.
127: cyclic/self-referential + A `Meta` type where `m.Of` points back to the `Meta` type itself, testing the `recursion.Guard`.
145: cyclic/self-referential + A `visitWithGuard` call where `guard",320,4366,
analysis/type/projection/projection.go,q0,"44: nil/empty handling + An empty `Projection` with zero steps returns an empty string, which might be indistinguishable from a failed projection in some calling contexts.

54: recursion/depth limits + `Equal` relies on `stepEqual`, which calls `typ.TypeEquals`; if types are cyclic/self-referential, this will cause a stack overflow.

85: malformed input + A `Step` initialized with an undefined `StepKind` (e.g., `StepKind(255)`) will fall through to the `""unknown""` case in `String()`.

94: rare type combinations + `stepEqual` compares `a.Type` and `b.Type` via `typ.TypeEquals`; if one type is a complex union and the other is a simple primitive, the performance or correctness depends entirely on the external `typ` package's ability to handle asymmetric complexity.",194,2424,
analysis/type/inspect/walk.go,q0,"33: nil receiver + Calling `Enter` on a nil `*Scanner` pointer.
40: cyclic/self-referential + A type that contains itself in `seen.Contains` to trigger early exit.
44: recursion/depth limits + A type structure that exceeds `maxEnters` to test budget exhaustion.
54: nil/empty handling + Passing a `nil` `visit` function to `WalkChildren`.
54: nil/empty handling + Passing a `nil` `typ.Type` to `WalkChildren`.
58: malformed input + An `Annotated` type where `Inner` is `nil` or points to itself.
75: huge unions/intersections + A `Union` or `Intersection` with thousands of members to test short-circuiting.
81: huge unions/intersections + A `Tuple` with a massive number of elements.
84: rare type combinations + A `Function` where `TypeParams` are `nil` or contain `nil` constraints.
101: rare type combinations + A `Record` where `Fields`, `StaticMembers`, or `Metatable` are `nil`.
106: rare type combinations + A `Record` that has a `MapComponent` but `MapKey` or `MapValue` are `nil`.
114: rare type combinations + A `Generic` type where `TypeParams` are `nil`.
119: rare type combinations + An `",320,4330,
analysis/type/normalize/intersection.go,q0,"26: recursion/depth limits: An intersection containing itself (e.g., `A ∩ (B ∩ A)`) would cause a stack overflow in `addMember`.
33: cyclic/self-referential types: A type that references itself via a placeholder or recursive structure could cause infinite recursion in `addMember`.
41: huge unions/intersections: An extremely deep or wide tree of nested intersections could exhaust the stack during the recursive `addMember` calls.
54: nil/empty handling: Passing only `nil` or `typ.Any` values to `IntersectionForMeet` results in an empty `flat` slice, potentially leading to unexpected `MaterializeIntersection` behavior.
61: rare type combinations: An intersection of a `Never` type and a `Nil` type where `Never` is processed first (testing the short-circuit logic).
73: malformed input: A `typ.Intersection` member that is not actually of type `*typ.Intersection` (if type assertions are bypassed) would cause a panic.
88: metatable/complex edge cases: A type that uses a `Placeholder` kind (via `IsPlaceholder`) might bypass standard nil-checks in a way that masks logic errors in `intersectionMemberAcceptsNil`.
101: rare type combinations: A `Union` containing a `Nil` type nested inside another `Union` or `Intersection` to test the depth of the `inspect.Visit` visitor.",315,3954,
analysis/type/inspect/seen.go,q0,"36 + nil/empty handling + Calling `Contains` on a nil receiver or with a nil type argument.
51 + nil/empty handling + Calling `Remember` on a nil receiver or with a nil type argument.
55 + boundary condition + Transitioning from `small` array to `entries` map when `smallLen` is exactly 8.
57 + boundary condition + Transitioning from `small` array to `entries` map when `smallLen` is 0 (empty set).
58 + boundary condition + The ""migration"" loop where `small` entries are moved to `entries` during the first map allocation.
91 + nil/empty handling + Calling `Contains` on a nil receiver or with a nil type argument.
92 + rare type combinations + Passing a type that returns `false` for the custom `track` predicate.
101 + nil/empty handling + Calling `Remember` on a nil receiver or with a nil type argument.
102 + rare type combinations + Passing a type that returns `false` for the custom `track` predicate.
104 + boundary condition + Transitioning from `small` array to `entries` map when `smallLen` is exactly 8.
107 + boundary condition + The ""migration"" loop where `small` entries are moved to `entries` during the first map allocation.
109 + boundary condition + The ""migration"" loop where `small` entries are moved to `entries` during the",320,4902,
analysis/type/literal/literal.go,q0,"21: Cyclic/Self-referential Alias: An `Alias` where `Target` points to itself, testing the `next == t` escape hatch.
22: Nil Alias Target: An `Alias` where `Target` is `nil`, testing the `next == nil` safety check.
24: Recursion Depth Limit: A chain of `Alias` types exceeding `typ.DefaultRecursionDepth`, testing the loop termination.
28: Terminal Alias: A type that is still an `Alias` after the loop finishes, ensuring it doesn't incorrectly return a literal.
37: Nil Literal: Passing a `nil` pointer to `PrimitiveBase`, testing the immediate nil return.
38: Non-primitive Literal: A `Literal` with a `Base` kind not in the switch (e.g., a custom kind), testing the `default: return nil` path.
51: Nil Annotated Inner: An `Annotated` type where `Inner` is `nil`, testing the `ann.Inner == nil` check.
52: Cyclic Annotated: An `Annotated` type where `Inner` points back to the `Annotated` wrapper itself.
56: Recursion Depth Overflow: A deeply nested structure that hits `typ.DefaultRecursionDepth` exactly, testing the `depth > ...` boundary.
59: Nil Type: Passing a `nil` `typ.Type` to `familyBase`, testing the `t == nil` check.
64:",320,4799,
analysis/type/normalize/union.go,q0,"48: recursion/depth limits: A deeply nested `typ.Union` or `typ.Optional` chain that triggers a stack overflow in `addMember`.
48: cyclic/self-referential types: A `typ.Union` member that contains itself, causing infinite recursion in `addMember`.
54: malformed input: A `typ.Union` where `unwrapped.(*typ.Union)` is called on a type that claims to be a `kind.Union` but is actually a different concrete type.
58: rare type combinations: A `typ.Union` containing only `kind.Never` types, which should result in an empty `flat` slice.
60: rare type combinations: A `typ.Union` containing only `kind.Unknown` and `kind.Nil` types, testing the `Optional(typ.Unknown)` logic.
71: rare type combinations: A `typ.Union` where all non-nil members are `kind.Never`, resulting in a return of `typ.Nil`.
74: rare type combinations: A `typ.Union` where `MaterializeUnion` returns a single non-union type (e.g., `String`), testing the `MaterializeOptional` conversion.
81: recursion/depth limits: A `typ.Union` that, when combined with `typ.Nil`, creates a new union that triggers a recursive loop in `UnionForEvidence`.
91: nil/empty handling: Passing `nil` as the `",320,4446,
analysis/type/subtype/guard.go,q0,NONE,2,369,
analysis/type/subtype/subtype.go,q0,NONE,2,81,
analysis/type/refinement/annotation.go,q0,"85 + cyclic/self-referential types + An infinite loop of `Alias -> Alias -> Alias` or `Record -> Field -> Record` that relies on `subst.ExpandInstantiatedChanged` to potentially trigger a stack overflow if the expansion logic doesn't detect the cycle.

95 + malformed/nil union members + A `typ.Union` where `Members` contains a `nil` pointer, which could cause a panic in `IsClosedUnionAnnotation` if the `member == nil` check were missing or if `typ.AbsentOrUnknown` doesn't handle nil.

123 + nil/empty union members + A `typ.Union` with `len(u.Members) == 0` or all members being `nil`, testing if the pruning logic returns the original type or panics.

128 + nil/empty union members + A `typ.Union` where all members are ""dominated"" (pruned), testing if the function correctly falls back to returning the original `t` instead of an empty union.

138 + nil callback + Passing `nil` as the `normalizeUnion` function when `len(keep) > 1`, which would cause a panic if the code reached that branch.

146 + infinite recursion + A deeply nested or cyclic `typ.Alias` chain that exceeds the `recursion.Guard` depth, testing if the function gracefully returns `false` instead of crashing.

155 + cyclic/self-referential types + A `typ",320,4163,
analysis/type/subst/expand_walk.go,q0,"31-33: Self-referential Annotated types + An `Annotated` type where `Inner` points to itself (`ann.Inner == t`) would cause an infinite loop if this check were missing.

37-39: Recursion depth limit + A deeply nested type structure that triggers `guard.Enter() == false` to ensure the expansion halts gracefully rather than panicking.

42-45: Self-referential/Cyclic types + An `Annotated` type that wraps itself or forms a cycle (e.g., `A -> B -> A`) to test the unrolling logic.

48-49: Memoization cycle/Self-reference + A type that is its own expansion, testing if `memo[key] = orig` prevents a stack overflow before `expandInstantiatedCore` is called.

75: Default case/Unknown type + A custom or malformed type implementation that doesn't match any known `typ.Type` case, ensuring it returns the original type instead of panicking.",220,2970,
analysis/type/projection/container.go,q0,"28: recursion/depth limits: A type chain of `Annotated -> Alias -> Optional -> Recursive` exceeding `typ.DefaultRecursionDepth` to trigger the depth guard.
32: nil/empty handling: A `nil` type passed to `descendContainerDepth` via `ElementOf` or `KeyOf`.
41: cyclic/self-referential: A `typ.Recursive` type where `tt.Body == t` to test the immediate cycle detection.
45: cyclic/self-referential: A `typ.Instantiated` type where `subst.ExpandInstantiated(tt) == t` to test expansion-induced cycles.
73: nil/empty handling: A `typ.Map` where the `Key` is a `nil` type or a `typ.Nil` kind.
77: nil/empty handling: A `typ.ReadonlyMap` where the `Key` is a `nil` type.
81: nil/empty handling: A `typ.Record` that has a map component but the `MapKey` is `nil`.
87: huge unions/intersections: A `typ.Union` containing only `nil` members or members that resolve to `nil`, resulting in an empty `members` slice.
88: huge unions/intersections: A `typ.Union` where one member's `KeyOf` returns `ok=false`, causing the entire union projection to fail.
111: nil",320,5123,
analysis/type/table/constructor.go,q0,NONE,2,99,
analysis/type/table/construct.go,q0,NONE,2,192,
analysis/type/subst/expand_function.go,q0,"4: recursion/depth limits + A function containing a parameter that is a recursive call to itself, testing if `isRecursiveInstantiated` and `guard` prevent infinite stack overflow.
11: nil/empty handling + A function with zero parameters (`len(v.Params) == 0`) to ensure the loop and `params == nil` logic don't trigger nil pointer dereferences.
26: nil/empty handling + A function with zero return values (`len(v.Returns) == 0`) to ensure the return slice logic handles empty slices gracefully.
36: malformed input + A function where `v.Variadic` is non-nil but points to a type that triggers a deep recursion cycle during expansion.
44: nil/empty handling + A function where `v.Params` is non-nil but `params` remains `nil` due to no changes, testing the `paramsSrc` assignment logic.
47: nil/empty handling + A function where `v.Returns` is non-nil but `returns` remains `nil` due to no changes, testing the `returnsSrc` assignment logic.
51: rare type combinations + A function with `TypeParams` that are themselves complex types, testing if `RebuildFunction` correctly preserves generic constraints during expansion.",276,3778,
analysis/type/table/doc.go,q0,NONE,2,73,
analysis/type/table/constructor_sort.go,q0,NONE,2,73,
analysis/type/subst/expand_record.go,q0,"46: Metatable recursion/depth limits: A record whose metatable is itself (or a deep chain of records) to test if `expandInstantiatedGuardMode` correctly triggers the `guard` to prevent stack overflow.

53: Map component recursion: A record where `MapKey` or `MapValue` is a type that expands into a different version of itself, testing the `HasMapComponent` logic and potential infinite expansion.

68: Structural identity/Short-circuit: A record with deeply nested but structurally identical types where `mode == expandModeStructural` is used, testing if the function correctly returns `orig` without unnecessary allocations.

88: Interface method type mismatch: An interface method whose type expands from a complex type (like a union) into a simple `*typ.Function`, testing the type assertion `newType.(*typ.Function)` and the fallback `fn = m.Type`.

97: Interface structural identity: An interface where all methods expand to their existing types, testing if the function correctly returns `orig` instead of a new interface instance.",226,3393,
analysis/type/subst/expand_generic.go,q0,"14: nil/empty handling + A `v.Generic` with a non-nil `TypeArgs` but a `nil` `Body` (or vice versa) to test if the function prematurely returns the original type instead of erroring.
15: malformed input + An `Instantiated` type where `len(v.TypeArgs)` does not match `len(v.Generic.TypeParams)`, testing if the substitution logic correctly aborts.
24: cyclic/self-referential types + A type that is not an `Instantiated` type but contains a pointer to itself via a different type wrapper, testing if `inspect.ContainsRecursive` is bypassed.
31: cyclic/self-referential types + A `Generic` body that contains an `Instantiated` type pointing back to the exact same `Generic` pointer, testing the detection of direct infinite recursion.
31: recursion/depth limits + A `Generic` body that contains a deeply nested chain of `Instantiated` types that eventually point back to the same `Generic`, testing the `found` flag logic in `transform.Rewrite`.",236,3868,
analysis/type/table/metatable.go,q0,NONE,2,101,
analysis/type/subst/expand_collections.go,q0,"21 + huge unions/intersections + A union containing thousands of identical or near-identical types that might trigger massive slice reallocations or O(n^2) comparison logic in `MapMembers`.

28 + huge unions/intersections + An intersection of a massive number of types that could lead to stack exhaustion during the recursive `expandInstantiatedGuardMode` calls.

38 + recursion/depth limits + An array whose element type is itself an array of the same type, testing if the `guard` correctly prevents infinite expansion.

48 + rare type combinations + A map where the key is a complex union and the value is a complex intersection, testing the interaction between `expandModeTablePolicy` and nested expansion.

59 + rare type combinations + A readonly map where the key/value expansion results in a type that is structurally identical but a different instance, testing the `key == v.Key` identity check.

69 + recursion/depth limits + A tuple with a very large number of elements, testing the depth of the `MapMembers` recursion and potential stack overflow.

77 + cyclic/self-referential types + An alias that points to itself (e.g., `type T = T`), testing if `expandInstantiatedGuardMode` and the `memo` map prevent an infinite loop.",277,4363,
analysis/type/subst/self_ref.go,q0,"23: nil/empty handling: If `method` is nil, the function returns nil without attempting to access fields, preventing a panic.
24: nil/empty handling: If `receiverType` is nil, the function returns the original method, preventing a substitution that would introduce a nil type into the tree.
31: malformed input: If `method.Params` is empty (a function with no arguments), `selfParamRef` returns false instead of panicking on index 0.
34: rare type combinations: If the first parameter is not a `*typ.Ref` (e.g., it's a primitive or a table), the substitution is skipped.
35: malformed input: If the `Ref` has an empty `Name` or `Module`, it is treated as an invalid/unresolvable reference and skipped.
38: cyclic/self-referential types: `transform.Rewrite` is called with a replacement; if `receiverType` contains a reference back to the same `selfRef`, this could trigger infinite recursion depending on the `transform` implementation.",235,3576,
analysis/type/refinement/free_type_param.go,q0,"14: nil/empty handling + `s.key(t)` will panic if `s` is nil when called via a nil receiver.
48: nil/empty handling + `s.enter(t)` returns `true` if `s` is nil, potentially causing a panic in the caller if it expects a valid state.
78: cyclic/self-referential + `s.containsRecursive(t)` relies on `nodeid.Pointer(t)` which may fail or return 0 for certain types of self-referential structures that aren't pointer-based.
101: cyclic/self-referential + `s.containsRecursiveScan` uses `inspect.WalkChildren` which could enter an infinite loop if the type graph is cyclic and `s.recursiveSeen` fails to catch it.
110: malformed input + `unwrap.Annotations(t)` returning `nil` is handled, but if it returns a type that is not a valid `typ.Type` implementation, `t.Kind()` or type assertions will panic.
113: cyclic/self-referential + `*typ.Recursive` is a hardcoded ""true"" which might bypass the `seen` logic and cause issues in complex nested recursive types.
148: overflow/memory + `s.recursiveMemoSmall` is a fixed-size array; if the type graph is extremely deep but not cyclic, the memoization logic transitions to a map, which could be a memory exhaustion vector.",320,5328,
analysis/type/table/record_normalization.go,q0,NONE,2,85,
analysis/type/subst/self.go,q0,"14: nil/empty handling + Passing `nil` for either `t` or `selfType` to ensure no panic occurs during early return.
48: recursion/depth limits + A deeply nested type structure that exceeds `selfSubstitutionNodeBudget` to trigger the `s.scanner.Enter(t)` failure path.
54: cyclic/self-referential types + A type that contains itself via an `Alias` or `Array` to test if the scanner's `Seen` set prevents infinite recursion.
58: cyclic/self-referential types + A `typ.Recursive` type which should be explicitly ignored by the scanner to prevent infinite loops.
61: rare type combinations + A `typ.Union` where the first member is a massive recursive tree and the second member is the only one containing `Self`.
81: huge unions/intersections + A `typ.Union` with thousands of members to check for performance degradation or stack exhaustion during iteration.
94: huge unions/intersections + An `typ.Intersection` with a massive number of members.
101: rare type combinations + A `typ.Map` where the `Key` is a complex type and the `Value` is the one containing `Self`.
113: rare type combinations + A `typ.Tuple` with a very large number of elements.
121: rare type combinations + A `typ.Function` with a massive number of parameters and a variadic argument containing `Self`.",320,4355,
analysis/type/subst/params.go,q0,"34: nil/empty handling + `substituteParams` returns `nil` immediately if `t` is `nil`, which might bypass expected transformation logic in complex nested structures.
44: malformed input + `Params` returns the original type `t` if `len(params) != len(args)`, potentially masking a logic error where mismatched generic counts should be an error.
45: nil/empty handling + `Params` returns `t` if `len(params) == 0`, which might be problematic if the caller expects a new type instance.
50: nil/empty handling + `Params` skips substitution if `p` or `args[i]` is `nil`, which could lead to silent ""half-substituted"" types if a type parameter is mapped to `nil`.
71: recursion/depth limits + `substituteParams` calls `substituteFunctionParams` which calls `substituteParams`, creating a deep recursion path that could stack overflow on highly nested function types.
75: rare type combinations + `substituteParams` handles `*typ.Function` explicitly, but if a type is a custom implementation of `typ.Type` that contains a function, it might fall through to `transform.Rewrite` and behave inconsistently.
84: recursion/depth limits + `substituteFunctionParams` calls `substituteParams` on parameters, variadic types, and return types, creating a massive tree traversal for deeply nested generic functions.
101: recursion/depth limits +",320,4735,
analysis/type/table/table_key.go,q0,NONE,2,374,
analysis/type/subtype/functions.go,q0,"23: Recursion/Depth Limits: A function that returns itself or a function containing itself, causing `c.check` to stack overflow via `depth+1`.
28: Nil/Empty Handling: A function with a `nil` Variadic type but a non-zero number of parameters, potentially causing a panic if `sub.Variadic` is accessed without a nil check.
33: Nil/Empty Handling: A function where `sub.Params[i].Type` is `nil`, which is skipped by the `subT == nil` check, potentially masking a type mismatch.
45: Cyclic/Self-referential Types: A function where the `Variadic` type is the function type itself, creating an infinite recursion loop in `c.check`.
51: Rare Type Combinations: A function where `sub.Returns` is shorter than `super.Returns`, testing if `typ.Nil` is correctly treated as a subtype of all super-return types.
58: Malformed Input: A `typ.Function` where `Params` contains elements that are `nil`, which would cause a panic at `p.Optional` in `minRequiredArgs`.",251,3767,
analysis/type/subtype/containers.go,q0,"13: Cyclic Map Keys/Values: A map where the key is a map containing the original map, causing infinite recursion in `check(sub.Key, super.Key, ...)` or `check(super.Key, sub.Key, ...)`.
13: Cyclic Map Values: A map where the value is a map containing the original map, causing infinite recursion in `check(sub.Value, super.Value, ...)`.
23: Nil Pointer Dereference: Passing a `nil` `sub` or `super` to `checkReadonlyMap` (though handled, it tests the safety of the `sub.Value` access).
31: Mismatched Tuple Lengths: A subtype tuple with more or fewer elements than the supertype, testing the strictness of positional requirements.
32: Deeply Nested Tuples: A tuple containing a tuple containing a tuple, testing if `depth+1` eventually triggers a stack overflow or a controlled recursion limit.
41: Nil Interface Comparison: Passing `nil` for `sub` or `super` to `checkInterface`, testing the early exit before method iteration.
44: Empty Interface Identity: Two different empty interfaces (no methods) that must have identical names to be considered subtypes.
46: Method Name Collision/Mismatch: An interface where `sub` has extra methods not present in `super` (should pass) vs `super` having methods not in `sub` (should fail).
49: Contravariant/Covariant Method Arguments:",320,4417,
analysis/type/table/field_shape.go,q0,"21: nil input to `EntryValueShape` + Passing a literal `nil` instead of a `typ.Type` object to see if the function returns `(nil, false)` without panicking.

23: `splitNilableFieldType` returning `optional=true` + A type that is strictly `nil` (e.g., a type representing only `nil`), which should result in `typ.Never` as the inner type.

27: `PresentReadonlyEntryValue` with an optional type + Passing a type that is `optional=true` to ensure it correctly strips the optionality and returns only the payload.

28: `PresentReadonlyEntryValue` with a `nil` input + Passing a `nil` type to ensure it returns `nil` rather than panicking when attempting to extract the payload.",178,2369,
analysis/type/subtype/core.go,q0,"24: recursion/depth limits: A deeply nested chain of `Annotated` or `Alias` types that exceeds the stack limit before `stopDepthPair` triggers.
38: cyclic/self-referential: A `Recursive` type where `r.Body == r` but `needsCycleGuard` is bypassed or misconfigured, causing infinite recursion.
58: cyclic/self-referential: A `Recursive` type where `r.Body` is a different node that eventually points back to `r`, testing the `inProgress` map logic.
83: malformed input: An `Instantiated` type where `subInst.Generic` is non-nil but contains a type that causes `subst.ExpandInstantiated` to return a pointer to itself.
104: huge unions/intersections: A `Union` or `Intersection` type with thousands of members, testing the performance and potential stack exhaustion of the loop.
133: rare type combinations: An `Optional` type where the `Inner` type is itself an `Optional` or a `Union` containing `Nil`, testing nested unwrapping logic.
155: malformed input: A `Record` type with zero fields being compared against a `Map` or `Array`, testing the ""empty record is a top-type"" edge case.
189: rare type combinations: A `TypeParam` whose `Constraint` is a complex `Intersection` or `Union` that contains the `TypeParam` itself.",320,4442,
analysis/type/subtype/table.go,q0,"14: recursion/depth limits: A self-referential Record where `sub.MapKey` or `sub.MapValue` points back to the same Record, potentially causing a stack overflow in `c.check`.
38: metatable edge cases: A Record with a non-nil `Metatable` being checked against a `ReadonlyMap`, forcing a fallback to `typ.String` keys and `typ.Unknown` values.
54: nil/empty handling: A `sub.MapKey` or `sub.MapValue` being `nil` inside a Record that otherwise passes `sub == nil` checks.
68: nil/empty handling: A `sub` Map with zero elements being checked against a `super` Record that has required fields.
78: rare type combinations: A Record field that is `Optional` but the `sub.Value` is a strict type, testing the `unwrap.IsOptionalLike` logic.
88: overflow/logic error: A mismatch where `c.check(expected, sub.Value)` fails but `c.canWidenTo` is invoked, testing complex widening logic for mutable vs immutable types.
103: recursion/depth limits: A Tuple containing a massive number of elements, each being a complex type, testing depth incrementing.
125: malformed input: A `StaticMember` with a `Kind` that is neither `StaticMemberStringIndex` nor `StaticMemberIntIndex`, returning `nil, false`.",320,4356,
analysis/type/subtype/records.go,q0,"41: recursion/depth limits: A record containing a field that is a record of the same type, causing infinite recursion if `depth` is not checked by the caller.
42: recursion/depth limits: A record containing a field that is a record of the same type, causing infinite recursion if `depth` is not checked by the caller.
56: recursion/depth limits: A record containing a field that is a record of the same type, causing infinite recursion if `depth` is not checked by the caller.
57: recursion/depth limits: A record containing a field that is a record of the same type, causing infinite recursion if `depth` is not checked by the caller.
71: recursion/depth limits: A record containing a field that is a record of the same type, causing infinite recursion if `depth` is not checked by the caller.
72: recursion/depth limits: A record containing a field that is a record of the same type, causing infinite recursion if `depth` is not checked by the caller.
74: recursion/depth limits: A record containing a field that is a record of the same type, causing infinite recursion if `depth` is not checked by the caller.
75: recursion/depth limits: A record containing a field that is a record of the same type, causing infinite recursion if `depth` is not checked by the caller.
85: metatable edge cases: A subtype with a specific metatable being checked against a supertype with an """,320,5089,
analysis/type/subtype/widen.go,q0,"4-5: recursion/depth limits + Infinite recursion via self-referential types if `stopDepthPair` fails to catch a `typ.Recursive` type pointing to itself.
46: huge unions/intersections + A `typ.Union` containing a massive number of members that all require deep recursive widening checks.
58: huge unions/intersections + A `typ.Union` where every member is a complex type, forcing an $O(N \times M)$ widening check.
84: malformed input + A `typ.Literal` with a `Base` kind that doesn't match its actual underlying type structure (e.g., `kind.String` but no string value).
108: rare type combinations + An `Array` widening to a `Map` where the `Map.Key` is not an `Integer`, potentially violating the logic at line 109.
113: cyclic/self-referential + A `typ.Recursive` type where `rec.Body` is a pointer back to the same `rec` object, testing the `rec.Body != rec` guard.
121: recursion/depth limits + A `typ.Tuple` containing deeply nested `typ.Tuple` or `typ.Function` types that exceed the stack limit.
135: recursion/depth limits + A `typ.Function` where the return types are deeply nested or recursive, causing stack overflow during widening.
153: nil/empty handling + A",320,4613,
analysis/type/typ/equality_cycle.go,q0,NONE,2,106,
analysis/type/table/key_domain.go,q0,"41: *typ.Alias (Recursion/Depth Limits): An alias that points to itself or forms a cycle will cause a stack overflow.
44: *typ.Union (Empty Union): A union with zero members returns `false` immediately, which might bypass expected match logic for ""empty"" types.
51: *typ.Intersection (Policy Mismatch): If `intersections` is not `All`, the intersection type itself is passed to `match`, which may fail if `match` only expects leaf types.
53: *typ.Intersection (Empty Intersection): An intersection with zero members returns `true` (vacuous truth), which is mathematically correct but often logically incorrect for type checking.
54: *typ.Intersection (Cyclic Intersection): An intersection containing a member that refers back to the intersection itself will cause a stack overflow.
60: default (Nil/Unknown Type): If `match` is not prepared to handle a `nil` type passed via the `default` case, it may trigger a panic.
66: alias == nil (Nil Pointer): Passing a nil pointer to `keyDomainAliasTargetFor` returns `nil`, which is then passed back into `keyDomainAny` at line 41, causing a recursive loop of `nil` cases.",273,4008,
analysis/type/typ/equality_identity.go,q0,NONE,2,76,
analysis/type/typ/equality_prefilter.go,q0,NONE,2,75,
analysis/type/typ/equals.go,q0,NONE,2,102,
analysis/type/table/key_admission.go,q0,"23 + Cyclic Union/Intersection + A `typ.Union` containing itself as a member will cause a stack overflow due to infinite recursion.
28 + Empty Intersection + An intersection with zero members returns `true`, which might incorrectly validate a key against an ""empty"" set of constraints.
34 + Empty Union + A union with zero members returns `false`, which might be unexpected if the union was intended to represent a bottom type.
40 + Cyclic Alias + An `typ.Alias` that points to itself (or a cycle of aliases) will cause a stack overflow.
51 + Any/Unknown Domain + If `keyDomain` is `Any` but `key` is a specific `Literal`, the logic relies on `typ.IsAny` which may fail if the domain is a complex type structure.
63 + Malformed Literal Value + A `typ.Literal` where `Base` is `kind.String` but `Value` is an `int` will return `false` via the `ok` check, potentially masking type errors.
67 + Malformed Literal Value + A `typ.Literal` where `Base` is `kind.Integer` but `Value` is a `float64` will return `false` via the `ok` check.
118 + Type Mismatch in Literal + A `typ.Literal` where `Base` is `kind.Number` but `Value` is an `int` will fail the `float64` type assertion.
13",320,4222,
analysis/type/typ/flags_aggregates.go,q0,NONE,2,207,
analysis/type/table/record_tail.go,q0,"39: kind.Number to int64 conversion + A `typ.Literal` with a `kind.Number` base where the `float64` value is a very large integer (e.g., `2^53 + 1`) that loses precision, potentially causing a false positive/negative when compared to `index`.

41: kind.Number broad match + A `typ.Type` that is a union/intersection containing a `kind.Number` but no `kind.Integer`, testing if the logic correctly treats all numbers as potential integer keys.

48: mapComponentKeyMayContainAny recursion + A `typ.Type` that is a deeply nested recursive alias or a cyclic type structure that could cause `keyDomainAny` to stack overflow or loop infinitely.

49: aliasPolicy edge case + A `typ.Type` that forms a cycle through its aliases (e.g., `type A = B; type B = A`), testing if `keyDomainTraversal` handles circularity without panicking.",217,3292,
analysis/type/typ/flags_cached_open_recursive.go,q0,NONE,2,385,
analysis/type/table/constructor_node.go,q0,"31: nil/empty handling: Passing a `nil` path or an empty `path` slice to `insert` to check for immediate false returns.
31: nil/empty handling: Passing a `nil` `typ.Type` to `insert` to ensure it doesn't attempt to assign a null type to a node.
41: sealed/type collision: Attempting to `insert` into a node that is already `sealed` via a deep path, which should prevent further modifications.
41: sealed/type collision: Attempting to `insert` a non-sealed type into a node that was previously marked `sealed` by a different path.
41: sealed/type collision: Using `sealed=true` on a leaf node to trigger the mass-nulling of `fields`, `stringIndexes`, and `intIndexes`.
55: malformed input: Providing a `ConstructorField` or `ConstructorStringIndex` with an empty string `""""` as a name.
68: malformed input: Providing a `ConstructorIntIndex` with a negative integer index.
88: recursion/depth limits: A deeply nested `path` that could cause a stack overflow during the recursive `insert` calls.
88: recursion/depth limits: A deeply nested tree structure that could cause a stack overflow during the recursive `build` calls.
93: cyclic/self-referential: A circular reference in the `typ.Type` tree where `build()` calls itself infinitely.",320,4895,
analysis/type/table/record_builder.go,q0,"39: MapComponent + Nil/Zero Value Types: Passing a `nil` or uninitialized `typ.Type` as a key or value might cause a panic in `NormalizeKey` or downstream during type comparison.

48: Field + Duplicate Names: Adding multiple fields with the exact same name (e.g., `Field(""a"", T1)`, `Field(""a"", T2)`) creates an ambiguous record that may break equality checks or lookups.

53: Field + Empty Name: Passing an empty string `""""` as a field name, which is valid in Lua but often causes issues in serialization or code generation.

68: AnnotatedField + Nil Annotations: Passing a `nil` slice for `annotations` instead of an empty slice, testing if the `len(annotations) > 0` check safely handles nil.

78: StaticIntIndex + Integer Overflow/Boundaries: Using `math.MinInt64` or `math.MaxInt64` as an index to see if the type system or subsequent hash maps handle extreme integer boundaries.

83: AddStaticMember + Malformed StaticMember: Passing a `typ.StaticMember` with an invalid `Kind` (e.g., an undefined constant) to see if the builder validates the member's integrity.

88: Build + Cyclic/Self-Referential Types: Building a record where one of the fields is a `typ.Type` that points back to the record being built, testing for infinite recursion in `Re",320,4157,
analysis/type/table/nil_projection.go,q0,"24: Recursion/Depth Limits: A deeply nested `Annotated` or `Alias` chain that triggers a stack overflow during the recursive `withoutNil` calls.
34: Cyclic/Self-referential Types: An `Alias` that points to itself (e.g., `type T = T`), causing infinite recursion in `withoutNil`.
43: Nil/Empty Handling: An `Optional` type where `v.Inner` is explicitly `nil`, testing the transition to `typ.Never`.
53: Huge Unions: A `Union` containing thousands of members, testing the performance and memory pressure of the `members` slice allocation and loop.
65: Rare Type Combinations: A `Union` where every single member is `nil`, testing if the function correctly returns `typ.Never` instead of an empty slice.
75: Malformed Input: A `Union` where `u.Members` contains `nil` pointers, testing the robustness of the `unwrap.NormalizeNil` and loop logic.
81: Nil/Empty Handling: A `Union` that, after removing all `nil` members, results in an empty `members` slice, testing the `len(members) == 0` boundary.
84: Nil/Empty Handling: A `Union` that contains exactly one non-nil member, testing the simplification logic from a `Union` to a single type.",299,4852,
analysis/type/table/top_marker.go,q0,"28: `*typ.Recursive` + A self-referential type where `v.Body == v` (infinite recursion) is explicitly checked to prevent a stack overflow.
28: `*typ.Recursive` + A deeply nested recursive type (e.g., `type T = { val: T }` repeated 10,000 times) that could cause a stack overflow during the `IsLike(v.Body)` call.
31: `*typ.Intersection` + An intersection type containing a `nil` or non-table type that might bypass the `IsLike` logic if the intersection implementation is malformed.
32: `default` + A type that is not in the switch but is not `nil` (e.g., a custom struct or primitive) which relies on `IsBuiltinTopMarker` to safely return `false` rather than panicking.
14: `unwrap.Alias(t)` + An alias chain that points to `nil` or a non-existent type, testing if `unwrap` handles broken type graphs without panicking.",233,3838,
analysis/type/table/optional_payload.go,q0,"23: Nil/Empty input handling: If `items` is `nil`, the function returns `nil` without entering the loop, which might bypass expected slice initialization logic in callers.
24: Nil/Empty input handling: If `items` is an empty slice `[]T{}`, the loop is skipped and `out == nil` is true, returning the empty slice (correct, but often untested).
28: Identity/No-op boundary: If `sameShape` always returns `true` (e.g., all fields are non-optional), the function returns the original `items` slice via the `out == nil` check at line 38, testing the ""no-change"" path.
31: Slice mutation/Side-effect: The line `out = append(out, items[:i]...)` creates a new slice that shares the underlying array of `items` up to index `i`, which could lead to unexpected side effects if `items` is mutated later.
34: Large slice/Memory pressure: If `items` is extremely large and every element requires a split, `out = make([]T, 0, len(items))` could trigger an OOM or significant allocation latency.
44: Nilable Type Recursion: If `splitNilableFieldType` (external) handles cyclic types via pointers, this function is safe, but if it doesn't, a self-referential `field.Type` will cause a stack overflow here.
58: Nilable",320,5153,
analysis/type/typ/guard.go,q0,NONE,2,384,
analysis/type/transform/rewrite.go,q0,"48: nil/empty handling + A `nil` type passed as the root `t` should return `nil` immediately without panicking.
63: recursion/depth limits + A deeply nested type structure that exceeds `maxDepth` should trigger the `guard.Enter()` failure and return the original type rather than stack overflowing.
66: cyclic/self-referential + A type that contains itself (e.g., a `Recursive` type pointing to itself) could cause infinite recursion if the `memo` or `guard` logic fails.
78: nil/empty handling + An `Optional` type where `Inner` is `nil` should be handled as a terminal node to avoid dereferencing a nil pointer.
86: huge unions/intersections + A `Union` or `Intersection` with thousands of members could cause significant latency or memory pressure during the `MapMembers` transformation.
103: huge unions/intersections + A `Map` with extremely large key/value types could trigger high memory usage during the reconstruction of the new map.
138: malformed input + An `Instantiated` type with a `TypeArgs` slice that contains `nil` elements or mismatched lengths.
149: malformed input + An `Interface` where a method's `Type` is not a `*typ.Function` but the code attempts to cast it, potentially leading to unexpected type behavior.
178: cyclic/self-referential + An `Alias` type that",320,4135,
analysis/type/typ/annotated.go,q0,"41: nil inner type + annotation presence: Passing `nil` as `inner` with a non-empty `annotations` slice to ensure it defaults to `Unknown` rather than panicking during `inner.Hash()`.
63: nil inner type + Kind() call: Calling `Kind()` on an `Annotated` type where `Inner` is `nil` to verify it returns `kind.Unknown` instead of a nil pointer dereference.
73: annotation argument type mismatch: Providing an annotation argument that satisfies none of the `AsString`, `AsFloat64`, `AsInt64`, `AsInt`, or `AsBool` checks to trigger the `...` fallback in `String()`.
73: annotation argument with complex/recursive value: An annotation argument whose `AsString()` method triggers deep recursion or a panic during the `String()` construction.
101: type mismatch in Equals: Comparing an `Annotated` type against a non-pointer `Type` implementation to ensure the type assertion `other.(*Annotated)` handles the `!ok` path gracefully.
104: deep structural inequality: Comparing two `Annotated` types with identical annotations but deeply nested, structurally different `Inner` types to test the recursion of `a.Inner.Equals`.
114: nil inner type in unwrap: Passing an `Annotated` type with a `nil` `Inner` field to `unwrapAnnotated` to ensure it returns `Unknown` instead of a nil `Type`.
12",320,4065,
analysis/type/typ/members.go,q0,NONE,2,72,
analysis/type/typ/dedup.go,q0,"23 + Hash Collision + Two distinct complex types that produce the same `uint64` hash but are not structurally equal, forcing the `sameUnionMember` fallback.
43 + Slice Length Mismatch + Passing `types` and `hashes` with different lengths to trigger the early return and prevent out-of-bounds panics.
53 + Nil Type + Passing a `nil` Type to `unionMemberHash` to ensure it returns a safe `0` rather than panicking on a method call.
63 + Cyclic/Recursive Type + Passing two different recursive types (e.g., `type A = {val: A}` and `type B = {val: B}`) where `sameNodeOrAcyclicEqual` fails and `knownContainsRecursive` must prevent infinite recursion.
66 + Recursive Type Inequality + Comparing a recursive type against a non-recursive type where the non-recursive type is a structural subset of the recursive one.",200,2886,
analysis/type/typ/equality_unwrap.go,q0,"12: recursion/depth limits: A chain of `Alias` types that exceeds the `guard` limit, testing if the function returns `nil` instead of panicking.
13: recursion/depth limits: A circular reference of `Alias` types (e.g., `A -> B -> A`) that would cause an infinite loop if the `guard` failed.
17: nil/empty handling: A `Type` that is a non-nil pointer/interface but contains a nil underlying value, testing if `NormalizeNil` correctly collapses it.
28: malformed input: A `Type` implementation that satisfies the interface but returns `true` for `IsNil()` on a non-pointer kind, potentially causing `reflect` panics.
32: rare type combinations: An `Alias` type where `UnaliasedTarget()` returns a type that is itself a `nil` pointer, testing the loop's re-entry into `NormalizeNil`.",205,2580,
analysis/type/transform/rewrite_nodes.go,q0,"24 + recursion/depth limits + A function with deeply nested type parameters or return types that might exceed the `guard` limit or stack depth.
48 + nil/empty handling + A function with `nil` `TypeParams` or `Params` (though likely handled by the loop, it tests the `if typeParams == nil` logic).
78 + nil/empty handling + A function with `nil` `Returns` or `Params` slices.
88 + nil/empty handling + A function with a `nil` `Variadic` type.
118 + metatable edge cases + A metatable that points to a type that itself requires rewriting, potentially causing infinite recursion if not memoized.
126 + recursion/depth limits + A type parameter with a constraint that is a deeply nested or recursive type.
145 + recursion/depth limits + A generic type where the `Body` contains a type parameter that maps back to a type requiring a rewrite, testing the `replacements` closure.
168 + cyclic/self-referential + A recursive type where the `Body` contains a reference to itself, testing if `selfAware` correctly identifies the cycle via `IsRecursiveRef`.
178 + nil/empty handling + A recursive type with a `nil` `Body` (should return `orig` immediately).
193 + metatable edge cases + A record where the `Metatable` is a type that undergoes a transformation, testing the interaction between record and meta",320,4889,
analysis/type/typ/children.go,q0,"13: nil visit function + Passing a `nil` function as the `visit` argument to ensure it returns `false` rather than panicking.
14: nil type + Passing a `nil` `Type` object to ensure it returns `false` rather than panicking.
18: transparent wrapper resulting in nil + A type that, when passed through `unwrapTransparentWrappers`, returns `nil` (e.g., a wrapper around a null pointer).
28: nil union member + A `Union` or `Intersection` containing `nil` elements in its `Members` slice.
30: nil intersection member + A `Union` or `Intersection` containing `nil` elements in its `Members` slice.
34: nil array element + An `Array` type where the `Element` field is `nil`.
36: nil map key/value + A `Map` or `ReadonlyMap` where either the `Key` or `Value` is `nil`.
40: nil tuple element + A `Tuple` containing `nil` entries within its `Elements` slice.
43: nil generic constraint + A `Function` with a `TypeParam` whose `Constraint` is `nil`.
47: nil function parameter + A `Function` where one of the `Params` has a `nil` `Type`.
51: nil variadic type + A `Function` where the `Variadic` field is `nil`.
54: nil function return +",320,4388,
analysis/type/typ/container.go,q0,"26 + nil/empty handling + Passing `nil` to `NewArray` triggers a fallback to `Unknown`, which must be verified to prevent downstream nil pointer dereferences in `knownContains...` functions.

68 + nil/empty handling + `mapString` handles `key` or `value` being `nil` by defaulting to `""unknown""`, which could mask logic errors where a type was expected but not provided.

103 + nil/empty handling + `RebuildMap` handles `nil` keys/values by converting them to `Unknown`, which is a silent recovery path that might hide malformed type trees.

141 + nil/empty handling + `RebuildReadonlyMap` performs the same silent `nil` to `Unknown` conversion as `RebuildMap`.

175 + recursion/depth limits + `NewTuple` iterates over a slice of `Type`; if a tuple contains a self-referential type, the `e.Hash()` or `knownContains...` calls could trigger infinite recursion/stack overflow.

185 + recursion/depth limits + The loop in `NewTuple` calls `e.Hash()` and several `knownContains...` functions; if `e` is a cyclic type, this will cause a stack overflow during construction.

204 + recursion/depth limits + `t.strCache.get` calls `e.String()` in a loop; if the tuple elements form a cycle, this will cause a stack overflow during stringification.",318,4344,
analysis/type/typ/predicates.go,q0,NONE,2,376,
analysis/type/typ/primitive_names.go,q0,NONE,2,391,
analysis/type/typ/record_member_flags.go,q0,NONE,2,92,
analysis/type/typ/flags_field_typeparam.go,q0,"41: nil/empty handling: An empty `params` slice returns `false` immediately, which is correct but often untested in edge-case suites.
42: nil/empty handling: A `params` slice containing only `nil` pointers (e.g., `[]*TypeParam{nil}`) will bypass the `p != nil` check and return `false`.
51: nil/empty handling: An empty `params` slice returns `true` via `len(params) > 0`, which is a boundary condition for empty type parameter lists.
55: nil/empty handling: A `params` slice containing only `nil` pointers will bypass the `p != nil` check and return `false`.
59: nil/empty handling: A `params` slice containing only `nil` pointers will bypass the `p != nil` check and return `false`.
63: nil/empty handling: A `params` slice containing only `nil` pointers will bypass the `p != nil` check and return `false`.",222,3269,
analysis/type/typ/flags_cached.go,q0,"14 + Cyclic/Self-referential Recursive + A `Recursive` node that points to itself or forms a cycle, testing if `ensureContainsFlags` triggers an infinite loop or stack overflow.
21 + Instantiated Identity + An `Instantiated` type that is passed into itself, testing if the hardcoded `true` for `containsInstantiated` correctly bypasses deep traversal.
23 + TypeParam Identity + A `TypeParam` that is passed into itself, testing if the hardcoded `true` for `containsTypeParam` correctly bypasses deep traversal.
48 + Nil/Unknown Type + A type implementation that is not in the switch (e.g., a custom struct implementing `Type`) to ensure it defaults to `false` rather than panicking.
54 + Nil/Annotated Nil + A type that is `nil` or becomes `nil` after `unwrapAnnotatedOrNil`, testing the early return logic in `knownContainsAny`.
57 + IsAny Edge Case + A type that satisfies `IsAny(t)` but is not a standard composite type, testing the short-circuit before `cachedContainsFlags`.
67 + Nil/Annotated Nil + A type that is `nil` or becomes `nil` after `unwrapAnnotatedOrNil`, testing the early return logic in `knownContainsNever`.
70 + IsNever Edge Case + A type that satisfies `IsNever(t)` but is not a standard composite type, testing the short-circuit before `cachedContainsFlags`.",320,4052,
analysis/type/typ/flags_dynamic.go,q0,"31: recursion/depth limits + A type tree that is exactly `DefaultRecursionDepth + 1` deep to ensure the boundary condition triggers the `false` return.
31: nil/empty handling + Passing a `nil` `known` function to ensure the guard prevents a panic during the `known(t)` call.
31: nil/empty handling + Passing a `nil` `t` to ensure the function returns `false` rather than panicking on `unwrapAnnotated`.
38: cyclic/self-referential types + A type that is a `*Recursive` pointer to itself, which should be caught by the specific type check to prevent infinite loops.
41: cyclic/self-referential types + A complex cyclic graph of non-`*Recursive` types that relies on the `seen` map to prevent stack overflow.
41: nil/empty handling + Passing a `nil` `seen` map to ensure the function initializes it rather than panicking on `seen[t]`.
45: malformed input + A type that satisfies `knownContainsOpenRecursive` but contains a child that is `nil`, testing the `next` closure's robustness.",254,3687,
analysis/type/typ/recursive.go,q0,NONE,2,648,
analysis/type/typ/equals_recursive.go,q0,"44 + Cyclic/Self-referential types + A type that contains itself (e.g., `type T = T`) to test if the `seen` map correctly prevents infinite recursion via coinductive equality.
53 + Metatable edge cases + A Record where one has a metatable and the other has a metatable that is itself a recursive type or a cycle.
68 + Huge Unions/Intersections + Two Union types with thousands of members to test for stack overflow or performance degradation during deep comparison.
83 + Huge Unions/Intersections + Two Intersection types with massive member lists to check for linear complexity issues.
96 + Huge Unions/Intersections + Two Tuple types with extremely large element counts.
118 + Metatable edge cases + A Record where the `Metatable` field is a complex, deeply nested type that differs only at the very bottom of the tree.
138 + Rare type combinations + A Function where the `Variadic` type is a complex recursive type, testing the interaction between function signatures and cycle detection.
158 + Cyclic/Self-referential types + Two different `Recursive` types that point to each other's bodies, testing if the `seen` map handles cross-type cycles.
166 + Malformed input/Panic + A `Generic` type where `Body` is nil on one side but the other side is a non-nil type that triggers a nil pointer dereference in `typeEqualsGuard`.",311,4753,
analysis/type/typ/function.go,q0,"48: `ReserveParams` + Nil Receiver + Calling `ReserveParams` on a nil `*FunctionBuilder` pointer.
50: `ReserveParams` + Negative/Zero Capacity + Passing a negative `n` to trigger the `n <= 1` guard.
64: `TypeParamRef` + Nil Parameter + Passing a `nil` `*TypeParam` to ensure it doesn't append a nil entry to the slice.
101: `Build` + Nil Builder + Calling `Build()` on a nil `*FunctionBuilder` (will panic).
113: `String` + Recursive Type + A function type where a parameter or return type is the function itself (infinite recursion in `tp.String()` or `p.Type.String()`).
113: `String` + Cyclic Type Graph + A type parameter that refers back to the function via a complex union/intersection, causing stack overflow during stringification.
133: `String` + Nil Variadic + A function where `f.Variadic` is nil but the logic attempts to call `.String()` on it (though guarded by `f.Variadic != nil`, it's a critical path).
133: `String` + Nil Return Type + A function where one of the `Returns` slice elements is `nil`, causing a panic during `r.String()`.
133: `String` + Empty Name + A parameter with an empty string `Name` to test the `p.",320,4041,
analysis/type/typ/literal.go,q0,"49: LiteralNumber + NaN/Inf: Passing `math.NaN()` or `math.Inf()` to `LiteralNumber` may result in non-deterministic equality or unexpected string formatting.
58: LiteralString + Empty/Unicode: Passing an empty string `""""` or high-surrogate Unicode characters to test `hash.FnvString` and `strconv.Quote` consistency.
75: String + Nil/Type Mismatch: If `l.Value` is manually initialized to `nil` or a type not matching `l.Base`, the type assertions (e.g., `l.Value.(bool)`) will cause a panic.
88: Equals + Nil/Type Mismatch: If `other` is passed as `nil`, the call `other.Kind()` will panic; if `other` is a different type but shares a `kind.Literal` kind, the type assertion `other.(*Literal)` will panic.
88: Equals + Floating Point Precision: Comparing two `LiteralNumber` types with values that are mathematically equal but bitwise different (e.g., `0.0` vs `-0.0`) may return `false` due to `l.Value == ol.Value`.",258,3343,
analysis/type/typ/flags_cached_recursive.go,q0,"7: nil/empty handling + Passing a literal `nil` or a type that `unwrapAnnotatedOrNil` resolves to `nil` to ensure no panic occurs.
10: cyclic/self-referential types + A `*Recursive` type that points to itself, testing if the flag correctly identifies the cycle without re-traversing.
12: recursion/depth limits + A deeply nested `*Union` or `*Intersection` where the `containsRecursive` flag is set at the bottom of a 1000+ level tree.
13: rare type combinations + A `*Union` where one branch is a simple type and the other is a `*Recursive` type, testing the pre-computed flag accuracy.
29: metatable edge cases + A `*Meta` type where the `containsRecursive` flag is set, testing if metadata-driven recursion is correctly flagged.
31: rare type combinations + A `*Generic` type where the underlying template is recursive but the instantiation is not (or vice versa).
33: rare type combinations + An `*Instantiated` type where the template is recursive, testing if the cached flag correctly propagates to the instance.
35: rare type combinations + An `*Interface` that extends a recursive type, testing if the flag is correctly inherited/cached.
37: malformed input + A type that satisfies the interface but falls into `default`, ensuring the function returns `false` rather than panicking.",314,4918,
analysis/type/typ/instantiated.go,q0,"38: Nil/Empty TypeArgs + An empty `args` slice passed to `Instantiate` which might result in a zero-length `TypeArgs` and potentially uninitialized hash properties.
41: Hash Collision/Ordering + Passing `TypeArgs` in different orders (e.g., `Map<int, string>` vs `Map<string, int>`) to see if `MixHash` correctly distinguishes them.
42-58: Property Propagation + Passing a `Type` that is itself an `Instantiated` type to check if `containsInstantiated` or `containsRecursive` flags propagate correctly through nested layers.
58: Recursive Type Propagation + Passing a type that refers back to the generic being instantiated (e.g., `List<List<T>>`) to see if `containsRecursive` is correctly flagged.
76-88: String Cache/Recursion + A self-referential or deeply nested `TypeArg` that causes `a.String()` to trigger infinite recursion or stack overflow during `sb.WriteString`.
89: String Cache/Nil Generic + A `Generic` pointer that is `nil` causing a panic when accessing `i.Generic.Name`.",254,3966,
analysis/type/typ/generic.go,q0,"48-51: Self-referential/Cyclic Hash: A generic whose `Body` contains a reference back to itself, testing if `body.Hash()` triggers infinite recursion during `NewGeneric`.
65-67: Nil/Empty Body: A generic with a `nil` body (forward reference) to ensure the hash calculation doesn't panic and correctly omits the body.
74-75: Nil Receiver/Body: Calling `SetBody` on a `nil` generic or passing a `nil` body to ensure the guard clauses prevent panics.
78-88: Re-hashing/State Mutation: Calling `SetBody` on a generic that already has a body, testing if the identity (hash) is incorrectly mutated after stabilization.
89-94: Property Accumulation: A generic where `containsRecursive` or `containsTypeParam` is `false` initially but becomes `true` after `SetBody`, testing the bitwise-OR logic for property updates.
101-112: String Cache/Recursion: A generic with a highly nested or self-referential `Body` that could cause a stack overflow during `String()` execution.
114-116: Type Identity: Comparing a `Generic` to a non-generic `Type` via `Equals` to ensure the underlying `typeEquals` handles kind mismatches without panicking.",301,4392,
analysis/type/typ/rebuild_function.go,q0,"48: nil return entry + A `returns` slice containing a `nil` element will trigger a panic, testing the ""normalize before building"" invariant.
54: empty/nil variadic + A `variadic` type that is `nil` (representing a non-variadic function) must be handled without calling `.Hash()` to avoid a nil pointer dereference.
61: empty/nil slices + Passing empty slices for `typeParams`, `params`, or `returns` tests if the hashing loop and slice copying logic handle zero-length inputs gracefully.
66-76: complex type composition + Functions containing deeply nested or cyclic types (e.g., a function returning a table that contains the function itself) test if the `known...` predicate functions trigger infinite recursion during hash/property calculation.
66-76: type parameter interaction + A function where `typeParams` are themselves complex types (like unions of other type parameters) tests the interaction between generic identity and structural hashing.",211,2847,
analysis/type/typ/recursive_type_children.go,q0,NONE,2,367,
analysis/type/typ/union.go,q0,NONE,2,102,
analysis/type/typ/type.go,q0,NONE,2,435,
analysis/type/typ/typeparam.go,q0,NONE,2,482,
analysis/type/typ/nominal.go,q0,"48: Defensive copy/Mutation: Passing a slice of `Method` where the underlying array is modified by another goroutine or function after `NewInterface` is called.
56-72: Recursive/Cyclic Type Detection: An interface method whose `m.Type` is the interface itself (or a type containing it), testing if `knownContainsRecursive` correctly breaks infinite loops during hash/flag calculation.
56-72: Deeply Nested/Complex Types: A method signature with extremely deep nesting (e.g., `f: ((((int) -> int) -> int) -> int) -> int`) to check for stack overflow during `m.Type.Hash()` or flag checks.
85-101: String Cache/Recursion: An interface that is part of a cyclic type definition, which could cause `i.Type.String()` to enter an infinite loop if the `strCache` or `String()` implementation doesn't handle cycles.
115-126: Structural Equality/Deep Recursion: Comparing two interfaces that have identical but deeply nested/recursive method signatures, testing for stack exhaustion in `m.Type.Equals`.
115-126: Nil/Malformed Type: An `other` type that is not nil but has a `Kind()` that returns `kind.Interface` while the underlying pointer is nil (if such a state is possible via unsafe casting), causing a panic on `other.(*Interface)`.",311,4060,
analysis/type/typ/record_static_member.go,q0,"48: `staticMembersSorted` + Empty or single-element slice: An empty `[]StaticMember` or a slice with one element bypasses the loop entirely, which is a boundary condition for sorting logic.

73: `writeStaticMemberKey` + String containing escaped quotes: A `member.Name` containing `\""` or `\\` tests if the `strings.ReplaceAll` correctly handles nested escaping to prevent malformed key representations.

73: `writeStaticMemberKey` + Invalid `StaticMemberKind`: A `StaticMember` initialized with a `Kind` value of `0` or `3` (out of enum range) triggers the `[unknown]` fallback path.

73: `writeStaticMemberKey` + Integer overflow/extreme values: A `member.Index` of `math.MinInt64` or `math.MaxInt64` tests if `strconv.FormatInt` handles the full range of 64-bit signed integers without unexpected formatting.",212,3238,
analysis/type/typ/intersection.go,q0,"44: nil/empty handling: Passing a slice containing only `nil` elements to `MaterializeIntersection` to ensure it collapses to `Any`.
44: duplicate/identity handling: Passing a slice where multiple elements are the same pointer or have identical hashes to test `deduplicateTypesWithHashes`.
53: single cardinality collapse: Passing a slice with exactly one non-nil type to ensure it returns the type directly rather than an `Intersection` wrapper.
62: hash/member mismatch: Passing a slice where `len(memberHashes) != len(members)` to trigger the fallback re-hashing loop.
74: huge unions/intersections: Passing a slice with thousands of members to test `hash.MixHash` performance and potential integer overflow in the hash accumulation.
75: rare type combinations: Passing a mix of `Any`, `Never`, and `TypeParam` to verify the boolean flag logic (`containsAny`, `containsNever`, etc.) correctly identifies the ""strongest"" property.
88: cyclic/self-referential types: Passing an `Intersection` that contains itself as a member to see if `knownContainsRecursive` or the hashing logic enters an infinite loop.
98: metatable/interface edge cases: Passing a custom type implementation that panics during `knownContains...` checks or `unionMemberHash`.
107: string cache/recursion: Calling `String()` on an intersection that contains a member whose `String()` method triggers a circular call back to",320,4791,
analysis/type/typ/optional.go,q0,"39: nil/empty handling: Passing a literal `nil` (not a `kind.Nil` type, but a Go `nil` pointer) to `newRawOptionalNode` to ensure it returns the `Nil` singleton rather than panicking.
43: redundant optional wrapping: Passing an existing `Optional` type to `newRawOptionalNode` to verify the flattening logic prevents `Optional<Optional<T>>` nesting.
51: hash collision/stability: Passing a type with a `0` hash or a type that triggers a hash collision in `hash.MixHash` to ensure the `Optional` hash remains unique.
55: containsAny/Never edge cases: Passing a `kind.Any` or `kind.Never` as the `inner` type to verify the boolean flags (`containsAny`, `containsNever`) are correctly propagated.
58: recursion/depth limits: Passing a deeply nested recursive type to `newRawOptionalNode` to check if `knownContainsRecursive` triggers a stack overflow during flag computation.
59: cyclic/self-referential types: Passing a type that contains itself (e.g., `type T = T?`) to ensure `containsOpenRecursive` is correctly identified without infinite recursion.
66: string cache/concurrency: Calling `String()` on the same `Optional` instance from multiple goroutines simultaneously to test the `strCache` thread-safety.
67: malformed/nil inner: Calling `String()` on an `Optional` where",320,4466,
analysis/type/typ/rebuild_record.go,q0,"71-74: Nil/Asymmetric Map Types + The case where `mapKey` is nil but `mapValue` is provided (or vice versa), forcing a fallback to `Unknown` to maintain hash consistency.

83-85: Nil Field Types + A field with a `nil` Type pointer, which would cause a panic during hashing if not caught and converted to `Unknown`.

93-95: Nil Static Member Types + A static member with a `nil` Type pointer, which would cause a panic during hashing if not caught and converted to `Unknown`.

101-106: Asymmetric Map Types + A record where only one of `mapKey` or `mapValue` is non-nil, testing the logic that prevents ""half-defined"" map structures from having inconsistent hashes.

110-113: Recursive/Cyclic Hashing + A field whose `f.Type.Hash()` triggers a deep traversal of a self-referential type, potentially causing a stack overflow.

121-131: Complex Static Member Hashing + A static member with a `StaticMemberIntIndex` kind, testing if the hash correctly incorporates the integer index rather than just the name.

135-137: Metatable Recursion + A metatable that is a reference to the record itself, testing if `metatable.Hash()` handles the cycle.

145-148: Map Key/Value Recursion + A `mapKey` or `mapValue",320,4164,
analysis/type/typ/record.go,q0,"78: nil/empty handling + `f.Type.String()` will panic if a field exists but its `Type` is `nil` (the code checks `f.Type != nil` but the `String()` method itself might be called on a nil receiver in other parts of the system).

101: recursion/depth limits + `r.MapKey.String()` and `r.MapValue.String()` will cause a stack overflow if the record is self-referential (e.g., a record where the map value is the record itself).

111: recursion/depth limits + `r.Open` logic combined with `String()` could lead to infinite recursion if an ""Open"" record contains a type that eventually points back to itself.

135: nil/empty handling + `r.GetField` will panic if `r` is `nil` because it attempts to access `r.sorted` before checking for nil.

156: nil/empty handling + `r.GetStaticMember` handles `r == nil`, but `GetStaticStringIndex` and `GetStaticIntIndex` call it via a receiver that might be nil, which is safe here, but `r.StaticMembers` access in `GetStaticMember` is the only guard.

165: malformed input + `compareStaticMemberKey` (called by `GetStaticMember`) is an external dependency, but if `r.StaticMembers` is not sorted, `sort.Search` will return incorrect",320,3996,
analysis/type/typ/unwrap_internal.go,q0,"14: cyclic/self-referential types: An `Annotated` type where `ann.Inner == t` creates an infinite loop if not for the explicit check on line 14.
14: nil inner type: An `Annotated` type where `ann.Inner` is `nil` would cause a panic on the next iteration if not for the check on line 14.
13: infinite recursion/depth: A deeply nested chain of `Annotated` types (e.g., 10,000+ layers) that tests the stack/loop limits of the `for` construct.",133,1532,
analysis/type/typ/recursive_hash_scratch.go,q0,"41-43: Transition from array to map (visited) + A recursive type structure with exactly 9 levels of nesting to trigger the migration from `visited` array to `visitedMap`.
63-65: Transition from array to map (memo) + A type structure with exactly 17 unique sub-types to trigger the migration from `memo` array to `memoMap`.
25-30: Array-based lookup + A cyclic type where the cycle is detected within the first 8 elements of the stack.
33-40: Array-to-map promotion (visited) + A deep recursion that hits the 9th element, forcing a costly migration of existing pointers into a new map.
54-58: Array-based lookup (memo) + A complex type tree where the same type appears frequently but is not yet in the small memo array.
59-62: Array-based update (memo) + A type that already exists in the small memo array but requires updating its hash value.
66-71: Array-to-map promotion (memo) + A type tree that exceeds 16 unique types, forcing a migration of all previously cached hashes into a map.",260,3055,
analysis/type/typ/recursive_graph_closed.go,q0,"23: nil/empty handling + A `Type` interface that is non-nil but returns `nil` from `unwrapAnnotatedOrNil` (e.g., a broken annotation wrapper).
33: cyclic/self-referential + A `*Recursive` type where `n.Body` is the same `*Recursive` instance, testing the `seen` map's ability to break infinite loops.
34: malformed input + A `*Recursive` type where `Body` is explicitly `nil`, which should return `false` rather than panicking or returning `true`.
43: rare type combinations + A custom `Type` implementation that returns a `nil` child during `recursiveTypeChildrenAll` iteration.
53: metatable edge cases + A `Type` whose `Kind()` or `typePointer()` implementation triggers a panic or returns a zeroed `uintptr` due to internal state corruption.
57: overflow/pointer collision + Two different `Type` instances that share the same `uintptr` (via `typePointer`) but have different `Kind` values, testing the memoization key uniqueness.",241,3726,
analysis/type/typ/recursive_hash_deps.go,q0,"23: nil input + `recursiveHashDeps(nil)` returns `(nil, true)`, which might be unexpected if the caller expects an empty slice instead of nil.
31: nil Body + A `Recursive` type where `Body == nil` triggers a `false` return, representing a broken/incomplete type definition.
45: nil Type + `collectRecursiveHashDepsInTypeMemo` receiving a nil interface value (not just a nil pointer) returns `true`.
48: nil after unwrap + An `Annotated` type wrapping a `nil` type returns `true`, potentially masking a malformed type tree.
56: nil type case + A type that is explicitly the `nil` type (if supported by the type system) returns `true`.
58: Direct recursion + A `Recursive` type that points directly to itself (or a cycle) is handled by the `seen` map to prevent infinite recursion.
60: Deeply nested/Cyclic types + A complex graph of unions/intersections/recursive types that creates a cycle will exercise the `memo` and `seen` logic to prevent stack overflow.",242,3397,
analysis/type/typ/recursive_contains.go,q0,"14: Nil/Empty Body -> A `Recursive` type with a `nil` Body field, testing if the flags reset correctly without panicking.
25: Nil/Empty Body -> A `Recursive` type with a `nil` Body field, testing if the closed flag resets correctly without panicking.
36: Cyclic/Self-referential -> A `Recursive` type that contains itself directly or via a loop, testing if `containsFlagsComputing` prevents infinite recursion/stack overflow.
38: Cyclic/Self-referential -> A `Recursive` type where the `seen` map is re-initialized for every flag check, testing if the cycle detection is robust across different flag computations.
40: Cyclic/Self-referential -> A `Recursive` type where the `seen` map is re-initialized for every flag check, testing if the cycle detection is robust across different flag computations.
42: Cyclic/Self-referential -> A `Recursive` type where the `seen` map is re-initialized for every flag check, testing if the cycle detection is robust across different flag computations.
44: Cyclic/Self-referential -> A `Recursive` type where the `seen` map is re-initialized for every flag check, testing if the cycle detection is robust across different flag computations.
52: Cyclic/Self-referential -> A `Recursive` type that forms a complex graph cycle, testing if `recursiveContainsGraphClosed` correctly identifies a closed loop using the `map[*Recursive]bool`.",320,4023,
analysis/type/typ/recursive_hash.go,q0,"14: nil/empty handling + Passing a `nil` Type interface to trigger the early return.
15: nil/empty handling + Passing a non-nil Type that returns `nil` from `unwrapAliasForEquals`.
16: recursion/depth limits + A deeply nested type structure that exceeds stack limits during `hashBodyWithVisitedMemo`.
17: cyclic/self-referential types + A type that contains itself via an alias or open recursive placeholder to trigger the `knownContainsOpenRecursive` path.
23: cyclic/self-referential types + A `Recursive` type where `r.hash` is non-zero but `recursiveHashDepsValid` is false, forcing a re-computation.
28: cyclic/self-referential types + A `Recursive` type that forms a complex cycle (e.g., A -> B -> A) to test the `hashWithVisitedMemo` cycle detection.
30: malformed input + A `Recursive` type where `recursiveHashDeps(r)` returns a dependency list that is inconsistent with the actual structure.
31: overflow + A massive, complex type tree where the cumulative hash calculation overflows `uint64`.",254,3610,
analysis/type/typ/recursive_hash_traversal.go,q0,"21 + cyclic/self-referential + A `Recursive` type where `rec.Body` points back to `rec` itself to test the sentinel hash path.
24 + cyclic/self-referential + A `Recursive` type that points to a different `Recursive` type which then points back to the first, testing mutual recursion.
45 + infinite recursion + An `Alias` type where `UnaliasedTarget()` returns the `Alias` itself, creating an infinite loop in `hashBodyWithVisitedMemo`.
73 + huge unions/intersections + A `Union` or `Intersection` with thousands of members to test hash accumulation and potential performance/overflow issues.
114 + metatable edge cases + A `Record` where `Metatable` is a `Recursive` type that contains the original `Record`, testing deep metatable recursion.
117 + metatable edge cases + A `Record` where `Metatable` is a `Meta` type pointing back to the `Record` itself.
120 + rare type combinations + A `Record` that is also a `Map` component (via `HasMapComponent`) with complex nested types in both key and value.
131 + recursion/depth limits + A `Function` with a massive number of `TypeParams` or `Params` to stress the stack depth of the recursive calls.
137 + recursion/depth limits + A `Function` with a `Variadic` type that is itself a deeply nested `Function`",320,3464,
analysis/type/typeexpr/typeexpr.go,q0,"41: recursion/depth limits: A deeply nested `Union` of `Union` types (e.g., `Union(Union(Union(...)))`) could cause a stack overflow in the recursive `addMember` function.
41: cyclic/self-referential: A `Union` containing itself as a member (e.g., `T = Union(T)`) will cause infinite recursion in `addMember`.
78: malformed input: An `Intersection` type that does not satisfy the `*typ.Intersection` type assertion but claims `kind.Intersection` could lead to unexpected behavior in `addMember`.
105: malformed input: An `Annotated` type where `Inner` is `nil` returns `typ.Unknown`, which might break downstream logic expecting a valid type.
101: nil/empty handling: Passing a `nil` type to `unwrapAnnotatedOrNil` is handled, but if `unwrapAnnotated` is called on a type that is not `nil` but has a `nil` internal state, it may behave inconsistently.",232,2886,
analysis/type/unwrap/unwrap.go,q0,"41: nil/empty handling + An `Annotated` type where `Inner` is `nil` returns `typ.Unknown` instead of `nil`.
53: recursion/depth limits + An `Annotated` chain exceeding `typ.DefaultRecursionDepth` returns a partially unwrapped type.
56: cyclic/self-referential + An `Annotated` type where `Inner == t` breaks the loop to prevent infinite recursion.
71: cyclic/self-referential + An `Alias` where `UnaliasedTarget() == t` breaks the loop to prevent infinite recursion.
71: nil/empty handling + An `Alias` where `UnaliasedTarget()` returns `nil` returns `nil` instead of the alias itself.
84: cyclic/self-referential + An `Optional` type where `Inner == t` breaks the loop to prevent infinite recursion.
84: nil/empty handling + An `Optional` type where `Inner` is `nil` returns `nil` (which may be indistinguishable from a failed unwrap).
116: cyclic/self-referential + An `Alias` chain where `next == t` returns `nil` instead of the current type.
116: nil/empty handling + An `Alias` chain where `next == nil` returns `nil`.
121: malformed input + A `RecordWithAliasPolicy` call where the final unwrapped type is an `Alias` instead of a `",320,2895,
analysis/type/typ/union_normalize.go,q0,"41: nil/empty handling: Passing a slice containing only `nil` elements results in `filterNilTypes` returning an empty slice, which must correctly trigger the `Never` return on line 55.
55: empty cardinality collapse: A union of zero members (after filtering) must return the `Never` type to represent an impossible state.
58: single cardinality collapse: A union of one member must return that member directly to avoid unnecessary nesting/wrapping.
60: hash/length mismatch: A slice of members where `len(memberHashes) != len(members)` (e.g., due to a partial deduplication error elsewhere) triggers a re-computation of all hashes.
81: huge unions/overflow: A massive slice of members could cause `hash.MixHash` to overflow or result in a hash collision if the mixing function isn't robust.
83-101: rare type combinations: A union containing a mix of `Any`, `Never`, `TypeParam`, and `Recursive` types must correctly set all boolean flags via the short-circuiting `!containsX` checks.
106: cyclic/self-referential types: If `membersCopy` contains a type that points back to the union being constructed, the flag-checking loop (lines 81-101) could trigger infinite recursion if `knownContains...` functions are not depth-limited.
116: malformed/unwrapped types: `unwrapAnnotated(t)`",320,3844,
analysis/type/typ/ref.go,q0,"48: Equals + Type Mismatch + A `Ref` compared against a non-`Ref` type that might trigger a panic if the type assertion `other.(*Ref)` is not guarded (though here it is guarded by `Kind()`, the `other` could be `nil`).

78: Hash + Nil Receiver + Calling `.Hash()` on a `nil` `*Alias` pointer.

83: UnaliasedTarget + Nil Receiver + Calling `.UnaliasedTarget()` on a `nil` `*Alias` pointer.

84: UnaliasedTarget + Nil Field + An `Alias` where `unaliased` is explicitly set to `nil` (e.g., via manual construction) to see if it falls back to `Target`.

101: flattenAliasTarget + Infinite Recursion + A circular alias chain (e.g., `type A = B; type B = A`) that relies on `DefaultRecursionDepth` to prevent a stack overflow.

105: flattenAliasTarget + Self-Referential Type + An alias where `Target` is the alias itself (`type A = A`), testing the `next == current` escape hatch.

106: flattenAliasTarget + Nil Target + An alias pointing to a `nil` type, testing the `next == nil` escape hatch.

107: flattenAliasTarget + Depth Limit + A chain of aliases exactly `DefaultRecursionDepth + 1` long to ensure it returns the last valid type rather than pan",320,4149,
