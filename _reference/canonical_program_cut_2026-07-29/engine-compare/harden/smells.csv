path,query_id,response,completion_tokens,latency_ms,error
analysis/check/body/object_literal_sourcevalue.go,q0,NONE,2,168,
analysis/check/body/module_type_alias.go,q0,NONE,2,1018,
analysis/check/body/expression_operation.go,q0,NONE,2,1305,
analysis/check/body/result_parameter_readmodel.go,q0,NONE,2,1363,
analysis/check/body/api.go,q0,NONE,2,1362,
analysis/check/body/entry_contextual_function.go,q0,NONE,2,1363,
analysis/check/body/return_type_values.go,q0,NONE,2,853,
analysis/check/body/visibility.go,q0,NONE,2,1416,
analysis/check/body/signature_return_type_ops.go,q0,NONE,2,1417,
analysis/check/diagnostics/assignment_call_owner.go,q0,NONE,2,1249,
analysis/check/body/generic_for_transfer.go,q0,144: primitive obsession + replace `string(key)` with a typed key or segment type to avoid repeated parsing.,26,4707,
analysis/check/body/result_accessors.go,q0,117: primitive obsession (passing `name string` where a module alias or identifier type would be clearer) + use a domain-specific type for module names.,35,6638,
analysis/check/body/config.go,q0,48: leaky layering (importing `signaturelookup` to access stdlib logic) + move stdlib extraction logic to the `signaturelookup` package or a shared domain layer.,38,6685,
analysis/check/body/internal/readexpr/readexpr.go,q0,"44: a god function doing too much: `Project` handles exact path resolution, structural evidence projection, heap identity projection, and parent inheritance; split into specialized strategy functions.",37,6685,
analysis/check/body/entry_state.go,q0,"24: primitive obsession (the `parameterEntryState` function takes 9 parameters; group related parameters like `reg`, `typeValues`, `bindings`, and `typeResolver` into a `Context` or `Environment` struct)",49,7589,
analysis/check/body/read_boundary.go,q0,149: primitive obsession + Use a typed constant or enum for `declaration.Source.Kind` instead of comparing against `0`.,29,7661,
analysis/check/diagnostics/assignment_projection.go,q0,240: primitive obsession (passing `int` for depth) + use a dedicated `RecursionDepth` type or constant to clarify intent.,31,3222,
analysis/check/body/call_identity.go,q0,78: primitive obsession - The `stableCalleeName` function manually reconstructs a string representation of a path using a `strings.Builder` and a switch on segment kinds; this logic should be encapsulated within a `path.Path` method or a dedicated `String()` method on a path type to unify the intent.,67,8946,
analysis/check/checktest/checktest.go,q0,"131: god function doing too much + Split `checkSource` into smaller functions for parsing, running the program, and error/diagnostic processing.",32,7628,
analysis/check/body/read_boundary_state.go,q0,"48: duplicated intent that a small type would unify: The `nodeOutputAt` function manually checks for `nil` on `r.registry`, `graph`, and `r.boundaryXfer`. These dependencies should be encapsulated in a validated `Context` or `Engine` type to avoid repetitive nil-checks and ensure the `Result` is in a valid state before use.",79,9273,
analysis/check/body/function_value_types.go,q0,"61: boolean-trap parameter (hasCurrent) + Wrap `hasCurrent` and `current` into a single `StateContext` struct or use a pointer/nil check to represent presence.

75: god function doing too much (functionContextEntryHolds) + Split the function into smaller predicates: `checkPathRefinements`, `checkStaticMembers`, and `checkHeapObjects`.",82,9365,
analysis/check/checktest/internal/precheck/precheck.go,q0,"103: god function doing too much + Split `scanStmt` into smaller, specialized handlers for different AST node categories (e.g., `scanAssignment`, `scanControlFlow`, `scanFunctionDef`).",45,8082,
analysis/check/diagnostics/assignment.go,q0,76: a god function doing too much: `localAssignment` contains a massive chain of `if !ok` type-inference probes; extract each probe into a strategy pattern or a registry of type-inference providers.,46,8013,
analysis/check/diagnostics/call_param_obligation.go,q0,"111: a god function doing too much - The `call` method handles receiver offset logic, obligation iteration, type resolution, mismatch detection, and diagnostic evidence construction. Split into smaller methods for type resolution and diagnostic generation.",47,4881,
analysis/check/diagnostics/diagnostics.go,q0,"61: a god function doing too much: split `produceWithResolver` into smaller, specialized sub-passes or a visitor pattern to handle the growing list of diagnostic checks.",37,4052,
analysis/check/body/result_return_readmodel.go,q0,"63: god function doing too much: `openTailReturnPresenceRelations` handles fact retrieval, call site lookup, state management, context construction, and complex relation transformation; extract the relation transformation loop into a dedicated method.

83: awkward abstraction: `openTailReturnCallSources` returns a triple `(cfg.Point, map[int]int, bool)` where the boolean is a ""comma ok"" idiom for a complex state; return a small struct containing these three fields to clarify intent.",104,11713,
analysis/check/diagnostics/dead_assignment.go,q0,148+ god function doing too much: `collectDeadAssignmentReads` contains a massive switch/if-else block traversing every possible AST node type; extract the expression traversal logic into a dedicated visitor or specialized functions.,46,4652,
analysis/check/diagnostics/member_call.go,q0,87 + duplicated intent + `typedSignatureStructuralDiagnostic` and `call` share almost identical logic for resolving receiver types and narrowing; unify the common logic into a single helper function.,38,2798,
analysis/check/diagnostics/evidence.go,q0,21: primitive obsession (passing `want typ.Type` and `value` as raw types) + wrap `typ.Type` and the value identifier in domain-specific types to clarify their role in evidence generation.,46,3987,
analysis/check/body/import_outcomes.go,q0,"31: god function doing too much - The anonymous function returned by `calleeValueProvider` handles multiple distinct resolution strategies (empty path, path method, projection, prefix, and root projection); extract each strategy into named helper functions.
54: duplicated intent that a small type would unify - The `(product.Value, bool, bool)` return signature in `readablePrefixCalleeValue` uses a boolean to represent ""authoritative"" status; encapsulate this into a small `Resolution` struct with `Value`, `Found`, and `IsAuthoritative` fields.",118,13075,
analysis/check/diagnostics/channel_select_exhaustiveness.go,q0,"118 + primitive obsession + Use a dedicated type for `handled` and `missing` sets instead of `[]string` to avoid manual uniqueness checks.
138 + duplicated intent + The `appendUniqueString` function and its usage in `channelSelectChainDiagnostic` could be replaced by a single `Set` type or a map to unify the logic of collecting unique names.",80,6551,
analysis/check/diagnostics/unused_local.go,q0,NONE,2,505,
analysis/check/diagnostics/member_read.go,q0,63: awkward abstraction + The `walkDepth` function uses a manual `depth` integer and a large switch statement to traverse the AST; consider using a dedicated `ast.Visitor` pattern to decouple traversal logic from the diagnostic logic.,49,2991,
analysis/check/diagnostics/guard_env.go,q0,214: primitive obsession (passing `typeName string` where a `typ.Type` or a specific type enum would be safer) + use a typed constant or `typ.Type` instead of raw strings.,45,4510,
analysis/check/diagnostics/numeric_for.go,q0,"48: primitive obsession: the `role` string parameter should be a custom type (e.g., `OperandRole`) to prevent arbitrary string passing and unify the error message construction.",39,2306,
analysis/check/diagnostics/internal/readmodel/readmodel.go,q0,213 + duplicated intent that a small type would unify + `scalarRuntimeKindType` and `runtimeKindType` share nearly identical logic for mapping `runtimekind` tags to `typ.Type` members; unify the tag-to-type mapping into a single lookup or helper.,60,4508,
analysis/check/exportmanifest/effect_boundary.go,q0,NONE,2,649,
analysis/check/exportmanifest/product_type.go,q0,NONE,2,472,
analysis/check/diagnostics/assignment_diagnostic.go,q0,"44 + duplicated intent + create a helper function to construct a `diagnostic.Diagnostic` from spans and evidence to unify the repetitive mapping of `Position`, `Span`, and `Labels`.
78 + duplicated intent + create a helper function to construct a `diagnostic.Diagnostic` from spans and evidence to unify the repetitive mapping of `Position`, `Span`, and `Labels`.
113 + duplicated intent + create a helper function to construct a `diagnostic.Diagnostic` from spans and evidence to unify the repetitive mapping of `Position`, `Span`, and `Labels`.",118,11021,
analysis/check/exportmanifest/type_defs.go,q0,NONE,2,95,
analysis/check/diagnostics/runtime_guard.go,q0,"14: primitive obsession - Replace `name string` with a custom type or an enum to avoid string parsing.
38: primitive obsession - Replace `fact.Check.TypeName` (string) with a typed identifier.",47,2560,
analysis/check/fixpoint/program/internal/projectsummary/project.go,q0,NONE,2,293,
analysis/check/exportmanifest/source_type.go,q0,24: a god function doing too much - Split the `sourceType` function into smaller handlers based on the `source.Kind` logic.,31,1043,
analysis/check/fixpoint/program/internal/projectsummary/project_heap_table_objects.go,q0,NONE,2,403,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_reachability.go,q0,NONE,2,256,
analysis/check/diagnostics/direct_call.go,q0,"168 + god function + `directFunctionCall` is performing too many distinct tasks (arity validation, contract instantiation, and complex argument type checking); split into `validateArity`, `instantiateContract`, and `checkArguments`.

218 + god function + `directFunctionCall` contains a large loop with deeply nested conditional logic for argument type resolution; extract the argument type resolution logic into a separate function.

288 + duplicated intent + `lowerDirectFunctionContract` and `lowerDirectFunctionType` both perform similar logic for converting AST function structures into internal contract representations; unify the mapping logic.",127,8421,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_return_equalities.go,q0,NONE,2,223,
analysis/check/diagnostics/value_reference.go,q0,114: primitive obsession + replace the `string` name parameter with a `TypeName` type to clarify intent and allow for future validation logic.,31,3230,
analysis/check/exportmanifest/export.go,q0,21: primitive obsession - Replace `path string` with a dedicated `Path` or `ModulePath` type to clarify its purpose and prevent accidental misuse of arbitrary strings.,36,3294,
analysis/check/diagnostics/expression_type.go,q0,"31: boolean-trap parameter (flow and witnessRefine are passed via multiple constructors) + Use a configuration struct or functional options to initialize `expressionTyper`.
114: primitive obsession (op is a string) + Use a typed constant/enum for operators (e.g., `type Operator string`).
189: primitive obsession (op is a string) + Use a typed constant/enum for operators.
201: primitive obsession (op is a string) + Use a typed constant/enum for operators.",113,8508,
analysis/check/body/run.go,q0,"87: god function doing too much: `prepare` handles module identity, type resolution, fact lowering, visibility, expression value providers, and source value construction; split into smaller setup steps.
138: god function doing too much: `Solve` manages configuration defaults, entry state calculation, node transfer construction, and the execution of the transfer engine; extract state preparation to a separate method.
184: god function doing too much: `callOutcomeProvider` handles signature argument type factories, supplemental call outcomes, and signature-based effect lowering; decompose into specific provider builders.
238: god function doing too much: `returnPresenceRelationsForPath` performs name resolution via multiple sources and manually maps internal relations; encapsulate name resolution and relation mapping into dedicated types.",161,16240,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_return_conditions.go,q0,54: boolean-trap parameter (the `normalCond bool` in `normalReturnParamCondition` is a naked boolean that obscures whether it represents a truthy or falsy state; replace with a domain-specific type or enum),49,2157,
analysis/check/fixpoint/program/internal/projectsummary/project_return_presence_relations.go,q0,NONE,2,622,
analysis/check/diagnostics/trust.go,q0,"28 + boolean-trap parameter + replace `read boundaryValueReader` with a struct or specific function types if the logic diverges, or use a functional options pattern if more behaviors are added.

118 + duplicated intent + `explicitTopLikeExpressionType` and `explicitTopLikeCastType` share logic that could be unified by a single type-resolution function that handles both raw expressions and specific AST nodes.",86,5457,
analysis/check/diagnostics/return_contract.go,q0,"178: god function doing too much + Split `directCallResultContract` into smaller functions handling specific cases (definition-based, signature-based, and callable-type-based).
218: god function doing too much + Split `directCallArgsCompatible` into smaller functions for arity checking, generic instantiation, and individual argument validation.
243: god function doing too much + Split `instantiateDirectFunctionContract` into separate logic for generic vs non-generic contracts.
253: god function doing too much + Split `directCallArgumentTypes` into smaller functions for different source types (literal, flow, boundary, etc.).",138,7134,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_return_facts.go,q0,"22: a god function doing too much: split the large `projectNormalReturnFacts` into smaller functions that handle specific snapshot types (e.g., `projectRefinements`, `projectDynamicIndex`, etc.).",46,3088,
analysis/check/fixpoint/program/internal/projectsummary/project_param_obligations.go,q0,188: god function doing too much: `addArithmeticObligations` is a massive switch statement traversing an AST; extract specific visitor methods for each AST node type.,36,3185,
analysis/check/fixpoint/program/internal/projectsummary/project_normal_return_params.go,q0,"39: primitive obsession - Use a specific type for `slot` instead of `string` to avoid manual parsing/validation in multiple functions.
65: primitive obsession - Use a specific type for `slot` instead of `string` to avoid manual parsing/validation in multiple functions.",60,3894,
analysis/check/fixpoint/program/internal/projectsummary/project_return_param_aliases.go,q0,48 + boolean-trap parameter + Replace the `active map[factflow.ExprRef]bool` parameter with a dedicated `visited` set type or a context object to avoid passing a mutable map that requires manual `defer delete` management.,51,4094,
analysis/check/fixpoint/ref/ref.go,q0,NONE,2,1586,
analysis/check/fixpoint/program/method_receiver_context.go,q0,148 + duplicated intent that a small type would unify + The `collectTypesAndMetatables` and `collectSetmetatableReceivers` methods both implement nearly identical AST traversal logic; unify the traversal pattern using a visitor or a shared iterator.,53,3971,
analysis/check/fixpoint/program/param_inference.go,q0,148 + awkward abstraction + `seedReachableHeapFromID` and `seedReachableHeapFromValue` have overlapping responsibilities; unify them into a single recursive function that handles both IDs and Values to reduce branching logic.,48,3825,
analysis/check/diagnostics/type_reference.go,q0,"46 + god function doing too much + Split `Produce` into smaller methods for traversing function signatures vs. traversing the CFG graph.
78 + awkward abstraction + `emitExpr` is a closure passed through multiple layers; consider making it a method on a collector struct to avoid passing the function pointer.
101 + duplicated intent that a small type would unify + `seenRefs` and `seenPrimitives` are two separate maps used for the same purpose; unify them into a single `seen` map or a `visited` tracker struct.",114,9360,
analysis/check/fixpoint/program/internal/projectsummary/project_return_condition_refinements.go,q0,"39: boolean-trap parameter (returnValue bool) -> Replace with a custom type like `RefinementType` (e.g., `ReturnValue` vs `ParameterValue`) to clarify the meaning of `true`/`false` at the call site.",53,5388,
analysis/check/fixpoint/summary/escape_event_facts.go,q0,NONE,2,328,
analysis/check/fixpoint/summary/frozen_table_facts.go,q0,NONE,2,252,
analysis/check/exportmanifest/object_type.go,q0,"146 + awkward abstraction + `directMemberSegment` uses slice comparison logic that could be encapsulated in a `pathdom.Path` or `segment.Segment` method to avoid manual index looping.

249 + duplicated intent + `objectEntriesType` repeats the logic of iterating and adding members which is partially duplicated in `objectLiteralType` and `addObjectLiteralEntries`; a unified way to convert various entry types into `objectMemberMaps` would clean this up.",98,8773,
analysis/check/exportmanifest/function_signatures.go,q0,"210: primitive obsession (passing `signatureName string` to `operationalReturnAllocationTemplates` and `allocationTemplateForReturn` when it is only used for string formatting) + create a `SignatureName` type or pass the components needed for formatting.

235: primitive obsession (passing `signatureName string` to `allocationTemplateForReturn`) + create a `SignatureName` type.

251: primitive obsession (passing `signatureName string` to `allocationTemplateProjector`) + create a `SignatureName` type.

263: primitive obsession (using `fmt.Sprintf` with a long string of concatenated primitives to create a `signature.AllocationTemplateID`) + encapsulate the template ID generation logic into a type that understands the structure of a signature name.",163,8872,
analysis/check/fixpoint/summary/param_condition.go,q0,NONE,2,555,
analysis/check/fixpoint/program/internal/projectsummary/project_return_slots.go,q0,"25: primitive obsession (arity int) + use a custom type like `Arity` or ensure it's treated as a domain concept.
36: god function doing too much (joinDeclaredReturnValue) + encapsulate the logic for joining specific product attributes (presence, kind, evidence, witness, origin) into a single `product.Join(reg, v1, v2)` method.",83,5444,
analysis/check/fixpoint/summary/effect_delta_facts.go,q0,33: duplicated intent that a small type would unify: The manual loop in `cloneEffectDeltas` duplicates the logic of `effectDeltaMap`'s `CloneFact`. Use the map's normalization or a dedicated collection type to handle cloning.,54,2021,
analysis/check/fixpoint/query/query.go,q0,"43 + duplicated intent that a small type would unify + The `Config` and `Driver` structs share identical fields (`reg`, `functions`, `seed`, `widenAt`, `widenDelay`, `Stats`); unify these into a single `Options` or `Settings` type.",61,4652,
analysis/check/fixpoint/summary/proof_facts.go,q0,NONE,2,494,
analysis/check/fixpoint/summary/path_invalidation_facts.go,q0,NONE,2,692,
analysis/check/fixpoint/summary/return_condition.go,q0,NONE,2,319,
analysis/check/fixpoint/summary/snapshot.go,q0,NONE,2,152,
analysis/check/fixpoint/summary/channel_select_facts.go,q0,"20: primitive obsession (selectID is a string) + create a `SelectID` type to wrap the string.
55: primitive obsession (selectID is a string) + use the `SelectID` type.
65: primitive obsession (selectID is a string) + use the `SelectID` type.",70,4178,
analysis/check/diagnostics/condition_reachability.go,q0,"67 + duplicated intent that a small type would unify + The repeated `fmt.Sprintf` pattern for ""no invalidating assignment..."" should be a method on `redundantConditionProof` or a shared constant/helper.
103 + duplicated intent that a small type would unify + The repeated `fmt.Sprintf` pattern for ""no invalidating assignment..."" should be a method on `redundantConditionProof` or a shared constant/helper.
125 + duplicated intent that a small type would unify + The repeated `fmt.Sprintf` pattern for ""no invalidating assignment..."" should be a method on `redundantConditionProof` or a shared constant/helper.
141 + duplicated intent that a small type would unify + The repeated `fmt.Sprintf` pattern for ""no invalidating assignment..."" should be a method on `redundantConditionProof` or a shared constant/helper.
157 + duplicated intent that a small type would unify + The repeated `fmt.Sprintf` pattern for ""no invalidating assignment..."" should be a method on `redundantConditionProof` or a shared constant/helper.
173 + duplicated intent that a small type would unify + The repeated `fmt.Sprintf` pattern for ""no invalidating assignment..."" should be a method on `redundantConditionProof` or a shared constant/helper.
189 + duplicated intent that a small type would unify + The repeated `fmt.Sprintf",300,17819,
analysis/check/fixpoint/summary/param_obligation.go,q0,31: primitive obsession (using `int` for parameter indices) + create a `ParamIndex` type to distinguish indices from other integers.,30,2009,
analysis/check/fixpoint/summary/summary_key.go,q0,NONE,2,679,
analysis/check/internal/sourcebridge/source_value.go,q0,NONE,2,289,
analysis/check/fixpoint/program/internal/callresult/provider.go,q0,"310 + god function doing too much + split `outcomeFromSummary` into smaller functions for mapping different obligation/relation types.
355 + god function doing too much + split `paramMemberReturnPresenceRelations` into smaller functions for finding slots and resolving paths.
438 + god function doing too much + split `dropDescendantFactsBelowReturns` into smaller functions for each fact type filter.
568 + god function doing too much + split `outcomeFromSummary` into smaller functions for mapping different obligation/relation types.
738 + god function doing too much + split `cloneNormalReturnFacts` into smaller functions for each fact type clone.",141,10370,
analysis/check/fixpoint/summary/param_equality.go,q0,"13: primitive obsession: replace `Left` and `Right` ints with a `ParamID` type.
58: primitive obsession: replace `reg *axis.Registry` with a domain-specific type if it is only used to resolve parameter counts.",55,3129,
analysis/check/fixpoint/summary/normal_return_facts.go,q0,"8-76: duplicated intent that a small type would unify. Move the logic for normalizing, cloning, joining, widening, and equality of `NormalReturnFacts` into methods on the `callboundary.NormalReturnFacts` type itself to encapsulate the field-by-field operations.",59,3236,
analysis/check/fixpoint/summary/store_relation_facts.go,q0,NONE,2,1830,
analysis/check/fixpoint/summary/heap_table_objects.go,q0,"31: duplicated intent that a small type would unify: Create a `HeapTableObjects` type (e.g., `type HeapTableObjects map[identity.ID]heapidentity.TableObject`) to encapsulate the map and provide methods for `Equal`, `LessOrEq`, `Join`, and `Widen`, removing the repetitive `normalizeHeapTableObjects` calls.",78,4180,
analysis/check/fixpoint/summary/return_presence_relation.go,q0,41: duplicated intent that a small type would unify: The `ReturnPresenceRelation` and `returnPresenceRelationKey` share identical fields; use the struct itself as the key or define a single `RelationKey` type to avoid manual mapping.,51,2469,
analysis/diagnostic/evidence.go,q0,NONE,2,1675,
analysis/diagnostic/types.go,q0,NONE,2,1077,
analysis/domain/constraint/decision/result.go,q0,NONE,2,787,
analysis/check/placementplan/plan.go,q0,"148 + god function + split `plan()` into smaller methods for ID collection, ordering, and entry construction.",25,2004,
analysis/check/fixpoint/summary/param_obligation_lattice.go,q0,"21: primitive obsession: `o.Member != """"` (and similar checks on lines 44, 65) — use a dedicated `MemberName` type or a non-empty string type to encapsulate validation.
21: duplicated intent: the `Less` and `Valid` logic for these lattice definitions is highly repetitive — create a generic helper or a common interface for lattice components to unify the comparison and validation logic.",90,4839,
analysis/diagnostic/policy.go,q0,"13-18: awkward abstraction; replace the multiple boolean flags (`Disabled`, `Enabled`, `HasEnabled`) with a single optional type or a state enum to represent the rule's configuration.",42,2035,
analysis/domain/constraint/expr/expr_compare.go,q0,NONE,2,855,
analysis/check/fixpoint/summary/dynamic_index_facts.go,q0,"43: duplicated intent that a small type would unify: `cloneDynamicIndexFacts` manually clones the table, which is already handled by the `CloneFact` logic in `dynamicIndexMap`. Use the map's normalization or a unified cloning method.
58: primitive obsession: `f.Site != """"` uses a raw string check; use a dedicated `Site` type or a `IsSet()` method to clarify intent.",90,6814,
analysis/check/fixpoint/summary/summary.go,q0,31: a god function doing too much: The `Clone` method is manually managing the deep copy of over a dozen distinct fields; encapsulate the cloning logic of individual field groups into their own methods or types.,44,4365,
analysis/domain/constraint/expr/expr.go,q0,133 + awkward abstraction + The `VisitExpr` function contains repetitive type-switch logic for both value and pointer receivers; use a helper or unify the interface implementation to avoid the manual duplication of every case.,44,2414,
analysis/check/diagnostics/resolver.go,q0,"46 + god function + The `newResultResolver` function handles too many responsibilities: initializing the struct, populating maps from the result graph, and performing initial type resolution logic. Split the graph traversal/population into a separate `populate` or `load` method.

118 + leaky layering + `ResolveTypeRef` directly accesses `r.result.RequireAliasModulePath` and `r.moduleRefs.ResolveTypeRef`. The resolver is reaching into the `body.Result` and `typeannotation.Resolver` internals to perform path manipulation. Move the module path rewriting logic into the `body` or `typeannotation` packages.

148 + leaky layering + `TypeRefResolved` calls `r.result.TypeRef(ref)`. The resolver is responsible for type resolution, but it is delegating the core logic of ""is this type ref valid"" back to the `body` layer.

213 + leaky layering + `declVisible` performs a type assertion `r.parent.(*resultResolver)`. This creates a circular dependency/tight coupling where the parent must be known to be the same concrete type to traverse the hierarchy. Use an interface method on `typeannotation.Resolver` to handle visibility/parent traversal.

238 + leaky layering + `currentBinding` calls `typeresolve.BindingInExpr`. The resolver is reaching into the `typeresolve` package to perform AST-to-binding",300,17842,
analysis/check/diagnostics/object_literal_mismatch.go,q0,"46: boolean-trap parameter + Replace `(..., bool)` with a named type or a specific result struct to clarify what the boolean represents (e.g., `isClosedRecord`).
58: boolean-trap parameter + Replace `(..., bool)` with a named type or a specific result struct to clarify what the boolean represents (e.g., `isAdmissible`).
71: boolean-trap parameter + Replace `(..., bool)` with a named type or a specific result struct to clarify what the boolean represents (e.g., `isMissing`).
88: boolean-trap parameter + Replace `(..., bool)` with a named type or a specific result struct to clarify what the boolean represents (e.g., `hasMismatch`).
104: boolean-trap parameter + Replace `(..., bool)` with a named type or a specific result struct to clarify what the boolean represents (e.g., `isMismatch`).
118: boolean-trap parameter + Replace `(..., bool)` with a named type or a specific result struct to clarify what the boolean represents (e.g., `isMismatch`).
123: boolean-trap parameter + Replace `(..., bool)` with a named type or a specific result struct to clarify what the boolean represents (e.g., `isMismatch`).",278,18012,
analysis/check/fixpoint/summary/path_facts.go,q0,"56, 66, 74, 82, 86, 90, 94, 98, 102, 106, 110, 114: duplicated intent that a small type would unify. The repetitive boilerplate for `PathValueFact` and `PathStaticMemberFact` (Normalize, Clone, Equal, LessOrEq, Join, Widen) should be unified by a generic helper or a shared interface/wrapper to reduce manual implementation of identical logic.",114,7162,
analysis/domain/constraint/expr/expr_equal.go,q0,85: duplicated intent that a small type would unify: The `collectVars` function uses a `map[string]bool` to track uniqueness; replace with a `Set` type or a dedicated `VariableSet` type to unify the collection and sorting logic.,55,3170,
analysis/domain/constraint/equality/egraph.go,q0,143 + primitive obsession + Use a dedicated `SegmentKey` type instead of `string` for the `children` map and `parentInfo` to avoid repeated string formatting/parsing.,40,3937,
analysis/diagnostic/assertion.go,q0,"38: awkward abstraction: `FormatAssertionClaims` uses an empty `Span{}` to satisfy `AssertionEvidence`, suggesting the evidence generation is too tightly coupled to span metadata for a simple formatting task. Split the logic into a version that doesn't require a span or pass a nil/dummy span explicitly via a more specialized internal function.",70,5151,
analysis/domain/constraint/expr/expr_arith.go,q0,71: primitive obsession: replace `map[string]int64` with an `Environment` type to encapsulate variable lookups.,29,3532,
analysis/domain/effect/capability/capability.go,q0,31 + primitive obsession + Create a `Family` type instead of using raw strings for the `Family` field in `Descriptor`.,28,1453,
analysis/domain/effect/iteration/labels.go,q0,NONE,2,474,
analysis/domain/effect/label.go,q0,NONE,2,196,
analysis/domain/effect/mutation/active.go,q0,NONE,2,237,
analysis/domain/effect/control/reserved.go,q0,NONE,2,960,
analysis/domain/effect/dispatch/labels.go,q0,NONE,2,940,
analysis/domain/effect/capability/label/label.go,q0,"78 + duplicated intent + use a type switch or interface method on `returns.ReturnType` to handle both pointer and value receivers, removing the redundant case pairs.",34,2012,
analysis/domain/effect/mutation/labels.go,q0,NONE,2,702,
analysis/domain/effect/param_ref.go,q0,NONE,2,659,
analysis/domain/constraint/expr/expr_simplify.go,q0,"43 + duplicated intent + create a helper function to extract `(Const, bool)` from an `Expr` to unify the repetitive type assertion logic used in `Min`, `Max`, and `BinOp`.",44,4167,
analysis/domain/constraint/numeric/numeric.go,q0,144 + duplicated intent that a small type would unify + The `Hash` and `Equals` methods for every struct repeat the same pattern; a base struct or a helper could unify the logic for comparing paths and constants.,47,3075,
analysis/domain/constraint/expr/expr_ref.go,q0,"41, primitive obsession: `Of string` uses a raw string to represent a variable name; use a `Var` type or a dedicated `Identifier` type.
138, duplicated intent: `ParamLen` and `RetLen` are manual shorthands for `Len(Param)` and `Len(Ret)`; remove them and use the composition of existing types to unify logic.",81,5238,
analysis/check/fixpoint/program/program.go,q0,"241 + god function doing too much + `collectCallContextKeys` handles prepass solving, enclosed function collection, parameter inference, and multiple iterations of body solving; split into distinct stages for prepass, inference, and context collection.

385 + god function doing too much + `collectCallContextKeysFromResult` iterates over the graph, identifies call sites, performs signature instantiation, manages context key generation, and handles parameter observation; split into a graph iterator and a specialized site processor.

433 + god function doing too much + `collectSignatureCallbackContextKeys` performs signature instantiation, type-checking, symbol resolution, and context state management; extract the type instantiation and seed generation into helper functions.

538 + god function doing too much + `functionValueTypesFromSummaries` iterates over multiple different map types (identities, paths, and contexts) to build a complex result; split into `functionValueTypesByIdentity`, `functionValueTypesByPath`, and `functionValueTypesByContext`.",210,14029,
analysis/domain/lattice/lattice.go,q0,NONE,2,564,
analysis/domain/lattice/factmap/factmap.go,q0,44: boolean-trap parameter: Replace `Intersect bool` and `KeepBottom bool` with a `MapType` or `Config` struct/enum to clarify meaning at the call site.,41,2363,
analysis/domain/constraint/expr/expr_minmax.go,q0,"21 + duplicated intent + create a generic `BinaryExpr` type or a helper to handle `Substitute` and `Variables` for two-operand nodes.
48 + duplicated intent + create a generic `BinaryExpr` type or a helper to handle `Substitute` and `Variables` for two-operand nodes.
55 + duplicated intent + create a generic `BinaryExpr` type or a helper to handle `Substitute` and `Variables` for two-operand nodes.",99,7940,
analysis/domain/effect/returns/labels.go,q0,101: awkward abstraction + The `returnTypeEquals` function and its associated `normalizeX` helpers use a repetitive pattern of type switching and pointer dereferencing; implement an `Equals(other ReturnType) bool` method on the `ReturnType` interface to let each type handle its own comparison logic.,64,3546,
analysis/domain/lattice/lift/product.go,q0,NONE,2,815,
analysis/domain/effect/ownership/labels.go,q0,"31, 51, 75, 101, 118, 136, 153, 170, 187: duplicated intent that a small type would unify; create a generic `ParametricLabel[T]` struct or a common interface implementation to handle the repetitive `Param effect.ParamRef`, `String()`, and `Equals()` logic.",88,5168,
analysis/domain/path/address/local.go,q0,NONE,2,99,
analysis/domain/path/address/parser.go,q0,NONE,2,369,
analysis/domain/effect/mutation/transforms.go,q0,"46 + god function + move equality logic into a `Equals(other TypeTransform) bool` method on the `TypeTransform` interface to avoid the massive type switch.
46 + duplicated intent + replace the manual pointer/value type switching and normalization functions with a unified interface method.",60,5712,
analysis/domain/lattice/factset/factset.go,q0,"43: boolean-trap parameter (Intersect) + Replace the `bool` with a typed `Polarity` enum (e.g., `type Polarity int; const (May Polarity = iota; Must)`) to clarify intent and prevent accidental inversion.",56,4297,
analysis/domain/effect/postcondition/labels.go,q0,"12-14: primitive obsession: use a custom type for `Kind` instead of raw strings.
88: duplicated intent: `refinementEquals` and `refinementString` could be unified by making `Refinement` a non-nilable type or handling nil within the interface implementation.",64,5717,
analysis/domain/path/address/root.go,q0,NONE,2,253,
analysis/domain/lattice/lift/must.go,q0,16 + duplicated intent + Create a generic `MustLane[T]` type or a shared interface to unify the `bottom` flag and `values` map logic used by both `MustMapLane` and `MustSetLane`.,48,2507,
analysis/domain/path/address/address.go,q0,"48: god function doing too much: `StableFromKey` handles multiple parsing strategies (symbol, resolver, named root, plain); split into specialized parser functions.",35,2050,
analysis/domain/effect/row.go,q0,"101: primitive obsession - The `Equals(other any)` method uses `any` instead of a specific interface or type, making it prone to runtime errors; change to `Equals(other Row) bool`.
138: duplicated intent - The `String()` method manually implements complex formatting logic that could be unified into a single `formatRow(parts []string, tail *Var) string` helper to ensure consistency between pure, open, and closed rows.",97,5294,
analysis/domain/path/path.go,q0,NONE,2,378,
analysis/check/fixpoint/summary/summary_lattice.go,q0,"14 + duplicated intent + create an `IsEmpty()` method on `Summary` to unify the repeated checks for empty fields used in `Normalize`, `Join`, `Widen`, and `summaryBottom`.
141 + duplicated intent + create an `IsEmpty()` method on `Summary` to unify the repeated checks for empty fields used in `Equal`, `LessOrEq`, `Join`, and `Widen`.
188 + duplicated intent + create an `IsEmpty()` method on `Summary` to unify the repeated checks for empty fields used in `LessOrEq`, `Join`, and `Widen`.
234 + duplicated intent + create an `IsEmpty()` method on `Summary` to unify the repeated checks for empty fields used in `Join` and `Widen`.
278 + duplicated intent + create an `IsEmpty()` method on `Summary` to unify the repeated checks for empty fields used in `Widen`.
333 + duplicated intent + create an `IsEmpty()` method on `Summary` to unify the repeated checks for empty fields used in `summaryBottom`.",227,13640,
analysis/domain/path/placeholder.go,q0,NONE,2,316,
analysis/domain/path/address/key.go,q0,"64: leaky layering - `ParseResolverPath` is called from a domain/address package, implying the address layer knows about the resolver layer; move resolver logic to a higher orchestration layer or a dedicated resolver package.",45,2163,
analysis/domain/effect/dispatch/reserved.go,q0,"15 + duplicated intent that a small type would unify + Create a base struct or interface implementation for the `String()` and `Equals()` methods to avoid repeating the same logic for every label type.
31 + duplicated intent that a small type would unify + Create a base struct or interface implementation for the `String()` and `Equals()` methods to avoid repeating the same logic for every label type.",82,7861,
analysis/domain/effect/compose.go,q0,"22: duplicated intent that a small type would unify: The logic for merging or selecting a `tail` (Var) is manually implemented in `Union` and `Intersect`; move this to a `mergeTails(v1, v2 *Var) *Var` method on `Var` or `Row`.
48: duplicated intent that a small type would unify: The nested loop for finding common labels in `Intersect` is a manual implementation of a set intersection; create a `Labels.Intersect(other Labels) Labels` method.
61: duplicated intent that a small type would unify: The nested loop for checking label existence in `Subset` is a manual implementation of a subset check; create a `Labels.IsSubsetOf(other Labels) bool` method.",162,8395,
analysis/domain/placement/placement.go,q0,NONE,2,549,
analysis/domain/path/placeholder_path.go,q0,49: primitive obsession - Replace `int` with a `PlaceholderIndex` type to clarify intent and encapsulate validation.,25,993,
analysis/domain/state/key/value.go,q0,NONE,2,650,
analysis/domain/value/axis/evidence/evidence.go,q0,NONE,2,375,
analysis/domain/lattice/lift/map.go,q0,"43: a god function doing too much: `Map` defines and implements the entire lattice logic (Equal, LessOrEq, Join, Widen, Meet) and internal helpers; extract the lattice implementation into a dedicated struct type.

148: an awkward abstraction: `canonicalizeTop` is a specialized helper that only exists to bridge the gap between the sentinel logic and the lattice interface; integrate this logic directly into the `Meet` closure or use a unified `isTop` check.",103,5545,
analysis/domain/value/axis/key.go,q0,NONE,2,497,
analysis/domain/value/axis/presence/presence.go,q0,NONE,2,584,
analysis/domain/value/axis/reducer.go,q0,NONE,2,149,
analysis/domain/effect/returns/reserved.go,q0,"91 + duplicated intent that a small type would unify + Replace the repetitive `normalizeX` functions with a single generic helper `normalize[T any](r ReturnType) (T, bool)`.

111 + duplicated intent that a small type would unify + Replace the repetitive `normalizeX` functions with a single generic helper `normalize[T any](r ReturnType) (T, bool)`.

124 + duplicated intent that a small type would unify + Replace the repetitive `normalizeX` functions with a single generic helper `normalize[T any](r ReturnType) (T, bool)`.

137 + duplicated intent that a small type would unify + Replace the repetitive `normalizeX` functions with a single generic helper `normalize[T any](r ReturnType) (T, bool)`.",171,7936,
analysis/domain/value/axis/registry.go,q0,NONE,2,149,
analysis/domain/value/axis/assertion/assertion.go,q0,138: duplicated intent that a small type would unify: `Meet` and `Combine` have identical implementations; unify them or remove one.,31,1673,
analysis/domain/constraint/numeric/modular/modular.go,q0,"46: primitive obsession: use a `Variable` type instead of `string` for map keys.
53: primitive obsession: use a `Range` struct instead of `[2]int64` to clarify `lower` and `upper`.
85: primitive obsession: use a `Variable` type instead of `string`.
105: primitive obsession: use a `Variable` type instead of `string`.
105: primitive obsession: use `ModularFact` instead of passing `modulus, residue int64` separately.
123: primitive obsession: use a `Variable` type instead of `string`.
123: primitive obsession: use `ModularFact` instead of passing `modulus, residue int64` separately.",164,11433,
analysis/domain/value/axis/runtimekind/runtimekind.go,q0,NONE,2,138,
analysis/domain/value/axis/spec.go,q0,NONE,2,198,
analysis/domain/path/address/suffix.go,q0,115 + leaky layering + `RelativeStaticMemberSuffixKey` and `RelativeStaticMemberSuffixSegments` depend on `pathdom.PathKey` (a higher-level domain type) to perform low-level suffix transformations; move the key conversion logic to the `path` layer or use a more primitive type here.,67,3072,
analysis/domain/path/identity.go,q0,"14 + duplicated intent + Create a `hasSymbolIdentity()` helper to unify the symbol/version check logic used in both `Equal` and `Less`.
38 + duplicated intent + Create a `hasSymbolIdentity()` helper to unify the symbol/version check logic used in both `Equal` and `Less`.",66,2895,
analysis/domain/path/segment/segment.go,q0,"83: god function doing too much: `parseFormattedSegmentsSlow` handles character-level parsing, state management for different segment types, and error handling; split into specialized sub-parsers for fields, strings, and integers.",47,2217,
analysis/check/fixpoint/summary/summary_slots.go,q0,"13 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
23 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
33 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
41 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
46 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
51 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
55 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
58 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
60 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
61 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
62 + duplicated intent + Create a method on `Summary` or a helper type to handle indexed access with a default value.
63 +",300,16033,
analysis/domain/path/address/structural_key.go,q0,"14: primitive obsession + replace `local bool` and `stable Stable` with a single `AddressSpace` interface or a tagged union type to avoid checking `k.local` repeatedly.
104: duplicated intent + the logic for checking if a key is ""local"" vs ""stable"" and then performing segment transformations is repeated; unify the transformation logic into a method on `StructuralKey`.",83,3693,
analysis/domain/value/axis/escape/escape.go,q0,"74: duplicated intent that a small type would unify: `Bottom()`, `Top()`, `Fresh()`, and `Escaped()` are redundant factory functions for a type that is already exported and has clear constant-like behavior; use the type/constants directly or consolidate.",58,2002,
analysis/domain/value/product/runtime_hash.go,q0,NONE,2,87,
analysis/domain/value/product/runtime_validation.go,q0,NONE,2,87,
analysis/domain/value/product/registry.go,q0,NONE,2,728,
analysis/domain/value/product/runtime_compare.go,q0,NONE,2,568,
analysis/domain/value/product/domain.go,q0,78 + awkward abstraction + The `mergeRuntime` function uses a complex signature of function arguments to handle different merge logic; unify these into a single `Merger` interface or a configuration struct.,41,1109,
analysis/domain/value/axis/identity/identity.go,q0,"21: primitive obsession: replace `Kind string` and `Site string` with typed constants or a specific `IdentityKind` type to prevent invalid string values.
38: primitive obsession: replace `graphID` and `exprRef` with specific types (e.g., `GraphID`, `ExprRef`) to prevent accidental swapping of uint64 arguments.",77,2514,
analysis/domain/value/typevalue/literal.go,q0,NONE,2,396,
analysis/domain/value/typevalue/normalize.go,q0,NONE,2,119,
analysis/domain/value/product/value.go,q0,63: primitive obsession: replace `key string` in `slot` with a dedicated `AxisID` type to prevent mixing up arbitrary strings with axis identifiers.,34,825,
analysis/domain/value/refinement/refinement.go,q0,24: primitive obsession (passing `int` for depth) + use a dedicated `RecursionDepth` type or a context object to manage traversal state.,33,852,
analysis/domain/value/product/intern.go,q0,"39: awkward abstraction - `normalizePresence` uses a switch on equality checks to return the same value; replace with a direct validation or a type-safe enum check.
58: awkward abstraction - `normalizeShape` is a redundant identity function for valid shapes; replace with a `Validate()` method on the `Shape` type.",70,1733,
analysis/domain/value/typevalue/reconstruct.go,q0,NONE,2,80,
analysis/domain/value/axis/variantorigin/variantorigin.go,q0,"33: primitive obsession: use a `FamilyID` type instead of `uint64`.
34: primitive obsession: use a `CaseIndex` type instead of `int`.
135: boolean-trap parameter: replace `equal bool` with a custom type (e.g., `MatchMode`) to clarify intent.",72,1804,
analysis/domain/value/typevalue/presence_projection.go,q0,NONE,2,178,
analysis/domain/value/typevalue/presence.go,q0,NONE,2,178,
analysis/domain/value/typevalue/reconstruct_evidence.go,q0,NONE,2,145,
analysis/domain/constraint/numeric/diff/difference.go,q0,"43 + primitive obsession + Use a custom `NodeID` type instead of `int` to distinguish between graph indices and other integer values.
78 + primitive obsession + Use a custom `VariableName` type instead of `string` to clarify that these strings represent domain entities.
138 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `x` parameter.
143 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `y` parameter.
143 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `x` parameter.
143 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `y` parameter.
154 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `x` parameter.
164 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `x` parameter.
176 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `x` parameter.
187 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `x` parameter.
187 + primitive obsession + Use a custom `VariableName` type instead of `string` for the `y` parameter.",300,13925,
analysis/domain/value/typevalue/structural.go,q0,NONE,2,345,
analysis/domain/value/variant/internal/discriminant/conflict.go,q0,NONE,2,344,
analysis/domain/path/format.go,q0,"14: primitive obsession - replace `func(symbol.ID) string` with a `NameResolver` interface or type.
38: duplicated intent - the logic for formatting symbol-only paths (`""$sym"" + strconv.FormatUint...`) is duplicated from the `Key()` method; unify this into a helper or a `Symbol` type.
74: leaky layering - `Path.Key()` and `Path.Hash()` are performing low-level string/hash construction that relies on the internal structure of `segment`; consider if `segment` should provide its own `Key()` or `Hash()` to encapsulate its internal representation.",133,4814,
analysis/domain/path/construction.go,q0,"23: primitive obsession - Use a `FieldName` type instead of `string` to distinguish between general strings and identifiers.
31: primitive obsession - Use a `StringKey` type instead of `string` to distinguish between general strings and map keys.
39: primitive obsession - Use an `Index` type instead of `int` to clarify the domain meaning of the integer.
108: duplicated intent - The logic for checking if a segment is a string-based access is split between `DirectFieldName` and `segment.Segment`. Unify this by adding a `IsStringAccess()` method to the `segment.Segment` type.",136,4992,
analysis/domain/value/product/runtime.go,q0,111: duplicated intent that a small type would unify - The manual loop and slice copying for `rt.reducers` (lines 144-149) duplicates the intent of a slice conversion; use a helper or direct slice construction if the view allows.,57,2096,
analysis/domain/value/variant/internal/discriminant/paths.go,q0,NONE,2,386,
analysis/domain/value/variant/internal/discriminant/presence.go,q0,NONE,2,510,
analysis/domain/value/returnpresence/returnpresence.go,q0,"36: primitive obsession (trigger and target are ints) + Create a `Slot` or `Index` type to represent register/slot indices.
36: primitive obsession (trigger and target are ints) + Create a `Slot` or `Index` type to represent register/slot indices.",62,1763,
analysis/domain/value/product/runtime_canonical.go,q0,"23: primitive obsession - Use a custom `AxisKey` type instead of `string` for `slot.key` and map keys.
41: duplicated intent - The validation logic (checking `presence.Key.ID()` and `rt.axis(slot.key)`) is duplicated in both `canonicalSlots` and `slotsAlreadyCanonical`; extract to a `validateSlot` method.",83,2725,
analysis/domain/value/variant/cache.go,q0,118 + boolean-trap parameter + Replace `negate bool` in `originByPathLiteral` with two distinct methods or a custom type to avoid caller confusion.,36,1367,
analysis/domain/value/typevalue/materialize.go,q0,"41: primitive obsession - `cache *Cache` is passed to `fromType` but `FromType` passes `nil`, which will cause a panic on line 29; use a default empty cache or a non-nil sentinel.",51,1781,
analysis/domain/value/variant/internal/discriminant/detector.go,q0,31: awkward abstraction: The `if d == nil` check inside methods suggests the `Detector` is being used as a value type or is being improperly initialized; ensure callers use `NewDetector` or handle nil receivers at the call site.,51,1371,
analysis/domain/value/axis/typewitness/typewitness.go,q0,"42: primitive obsession - The `state` field uses a custom `uint8` type, but the `Of` function uses `nil` checks and `typ.IsAny` logic that could be unified into a single state-based transition or a more robust type-checking interface.

54: duplicated intent - The `Of` function performs multiple manual checks for `nil`, `IsAny`, `IsUnknown`, and specific type pointer cases (`*typ.TypeParam`, etc.) that could be unified into a single `typ.IsWitnessable(t)` method to avoid repeating this logic elsewhere.",126,3393,
analysis/domain/value/typevalue/runtime_kind.go,q0,"15: god function doing too much - Split the large switch statement into smaller, specialized functions (e.g., `runtimeKindFromLiteral`, `runtimeKindFromUnion`).
35: god function doing too much - Extract the union member iteration and joining logic into a dedicated helper function.
65: god function doing too much - Extract the default kind mapping logic into a separate function.",82,1983,
analysis/domain/value/typevalue/cache.go,q0,"48 + primitive obsession + replace `family uint64` and `cases []int` with a `VariantOrigin` struct.
58 + primitive obsession + replace `family uint64` and `cases []int` with a `VariantOrigin` struct.
68 + primitive obsession + replace `family uint64` and `cases []int` with a `VariantOrigin` struct.
78 + primitive obsession + replace `family uint64` and `cases []int` with a `VariantOrigin` struct.
88 + primitive obsession + replace `family uint64` and `cases []int` with a `VariantOrigin` struct.",140,3216,
analysis/domain/value/variant/internal/discriminant/residual.go,q0,"49: duplicated intent that a small type would unify: The logic for checking if a field/member is ""non-literal and non-optional"" is repeated in both loops; encapsulate this into a `isMergeableField(f)` helper.",52,2192,
analysis/domain/value/variant/origin_catalog.go,q0,"34: god function doing too much: `storeOriginFamily` handles validation, cloning, concurrency, poisoning logic, compatibility checking, merging, and revision tracking; split into `validate`, `merge`, and `commit` steps.",48,1725,
analysis/domain/value/variant/internal/discriminant/tags.go,q0,111 + awkward abstraction + The `requiredTagSet` uses a manual state machine (switching between single fields and a map) to optimize for the 1-element case; replace with a simple map to unify the logic and remove complexity.,51,2249,
analysis/domain/value/variant/narrow.go,q0,"61 + duplicated intent + The logic for `narrowByPathLiteral` and `narrowByPathLiteralNot` is nearly identical except for the boolean inversion of `pathAdmitsLiteral`. Unify these into a single recursive function that accepts a predicate or a ""negate"" flag.",59,2337,
analysis/engine/effectlowering/callable_value.go,q0,NONE,2,174,
analysis/engine/effectlowering/postconditions.go,q0,NONE,2,108,
analysis/domain/value/variant/origin_ints.go,q0,33: duplicated intent that a small type would unify: Create an `IntSet` type with `Contains` and `Equals` methods to replace manual slice manipulation and map conversions.,38,1665,
analysis/engine/effectlowering/module_load.go,q0,NONE,2,238,
analysis/domain/value/product/reduce.go,q0,"11: god function doing too much - Split the convergence loop logic from the initial state setup and the presence/shape synchronization.
53: duplicated intent that a small type would unify - The `(Shape, presence.Value, bool)` return pattern in `reducePresenceShape` should be a `ReductionResult` struct.
74: primitive obsession - The `values map[string]any` uses raw strings; use a typed `AxisKey` or similar if the domain allows.
104: primitive obsession - The `key string` parameter in `SetAny` and `GetAny` should be a domain-specific type to prevent passing arbitrary strings.
148: duplicated intent that a small type would unify - The `isProductBottom` logic (checking both presence and slot values) suggests a `ProductState` type that encapsulates ""bottomness"".",181,4665,
analysis/domain/value/variant/path_access.go,q0,26: primitive obsession (depth int) + wrap depth in a `Context` struct or a `RecursionTracker` type to encapsulate traversal state.,32,1639,
analysis/engine/dynamicindex/dynamicindex.go,q0,NONE,2,519,
analysis/engine/calloutcome/merge.go,q0,74: duplicated intent that a small type would unify: The manual appending of nearly 20 individual slice fields in `withSupplementalFacts` suggests `NormalReturnFacts` and `Param...` fields should be encapsulated in a struct with a `Merge(other)` method.,57,1644,
analysis/engine/calloutcome/authority.go,q0,22: duplicated intent that a small type would unify: Create a `HasAnyNonEmptyFields()` method on `CallOutcome` or its sub-structs to encapsulate the long list of length checks.,43,2285,
analysis/engine/effectlowering/provider.go,q0,"56: a god function doing too much: The anonymous function returned by `SignatureOutcomeProvider` handles signature lookup, instantiation, effect application, and complex result construction; extract the logic into a dedicated `OutcomeResolver` type or separate methods.",50,1335,
analysis/domain/value/variant/narrow_truthy.go,q0,"30: boolean-trap parameter; replace `wantTruthy bool` with a custom type like `TruthinessMode` (e.g., `ModeTruthy` or `ModeFalsy`) to clarify intent at the call site.
55: duplicated intent; `typeCanBeTruthy` and `typeCanBeFalsy` share nearly identical recursive traversal logic; unify them into a single `canSatisfy(t typ.Type, predicate TruthinessPredicate, depth int)` function.",103,3895,
analysis/engine/callproducer/callproducer.go,q0,"67 + duplicated intent + unify `eligible` and `eligibleView` into a single interface-based check or a shared helper.
108 + duplicated intent + unify `strictResultTarget` and `strictResultTargetView` logic into a shared interface or helper.",57,2010,
analysis/engine/effectlowering/provider_returns.go,q0,"46 + boolean-trap parameter + replace `array bool` in `callbackReturnValue` with a custom type or specific function variants.
148 + boolean-trap parameter + replace `array bool` in `callbackReturnValue` with a custom type or specific function variants.",57,1583,
analysis/engine/callpayload/call_outcome.go,q0,43: boolean-trap parameter (PostReturnAuthority); replace with a typed `AuthorityLevel` or `AuthorityStatus` enum to allow for future states beyond just true/false.,38,2177,
analysis/engine/callboundary/normal_return_facts.go,q0,138 + awkward abstraction + The `EscapeEventEffectSite` and `EscapeEventFromEffectSite` functions use string manipulation to encode/decode state (kind and recursion) into an `effectdelta.Site`. This logic should be unified into a single type or a structured `Effect` type rather than relying on string parsing.,68,3145,
analysis/domain/value/variant/origin.go,q0,"11 + primitive obsession + return `(originFamilyID uint64, caseIndices []int, ok bool)` or a dedicated `OriginEvidence` struct instead of raw primitives.
49 + boolean-trap parameter + replace `lit bool` with a named type like `type InvertMatch bool` or split into two distinct functions to avoid passing `true/false` blindly.
55 + boolean-trap parameter + replace `lit bool` with a named type or split into two distinct functions.
61 + duplicated intent + create an `OriginEvidence` struct to unify the `(uint64, []int, bool)` pattern used across multiple functions.
83 + duplicated intent + create an `OriginEvidence` struct to unify the `(uint64, []int, bool)` pattern used across multiple functions.",173,4023,
analysis/engine/factapply/branch_path.go,q0,148 + boolean-trap parameter + Replace `equal bool` with a custom type or two distinct functions `applyPathOriginRelationEqual` and `applyPathOriginRelationNotEqual`.,39,1627,
analysis/engine/factapply/call_outcome.go,q0,NONE,2,388,
analysis/engine/factapply/branch_presence.go,q0,61: primitive obsession (passing `pathdom.Path` and `projectPath` separately) + encapsulate path resolution logic into a single `PathResolver` type or context.,37,1384,
analysis/engine/effectlowering/provider_operational.go,q0,"158: primitive obsession (passing `object.ID` as a string to `allocationTemplateIdentity`) + create a specific type for `AllocationTemplateID` to wrap the string.

188: primitive obsession (using `fmt.Sprintf(""manifest:%d"", i)` to create a site string) + create a `dynamicindex.Site` type or constructor to handle manifest-based site generation.",85,2132,
analysis/engine/factapply/channel_select_fact.go,q0,NONE,2,547,
analysis/engine/factapply/call_edges.go,q0,148 + awkward abstraction + The `callOutcomeRelationActiveIn` function uses a manual fixed-point iteration loop to compute reachability/liveness; this logic should be encapsulated in a dedicated dataflow or reachability solver type.,49,1399,
analysis/engine/factapply/paths.go,q0,55: boolean-trap parameter (clearStructuralWitness) -> Replace with a typed enum or a specific configuration struct.,25,648,
analysis/domain/path/address/root_suffix.go,q0,"30: primitive obsession (passing `string` to `parseDecimalRootEnd` instead of a domain type) + use a specific type for prefixes or a structured key.
30: primitive obsession (passing `string` to `parseDecimalRootEnd` instead of a domain type) + use a specific type for prefixes or a structured key.
30: primitive obsession (passing `string` to `parseDecimalRootEnd` instead of a domain type) + use a specific type for prefixes or a structured key.
30: primitive obsession (passing `string` to `parseDecimalRootEnd` instead of a domain type) + use a specific type for prefixes or a structured key.
30: primitive obsession (passing `string` to `parseDecimalRootEnd` instead of a domain type) + use a specific type for prefixes or a structured key.
30: primitive obsession (passing `string` to `parseDecimalRootEnd` instead of a domain type) + use a specific type for prefixes or a structured key.
30: primitive obsession (passing `string` to `parseDecimalRootEnd` instead of a domain type) + use a specific type for prefixes or a structured key.
30: primitive obsession (passing `string` to `parseDecimalRootEnd` instead of a domain type) + use a specific type for prefixes or a structured key.
30: primitive obsession (passing `string` to `",300,10798,
analysis/engine/factapply/postconditions.go,q0,NONE,2,400,
analysis/engine/factapply/source_read.go,q0,NONE,2,86,
analysis/engine/factapply/branch_refinement.go,q0,"148 + awkward abstraction + The `apply...Cached` and `apply...WithoutImplicationsCached` pattern creates a deep, repetitive call stack to handle nil caches; unify these into a single function with optional parameters or a configuration struct.",50,2276,
analysis/engine/factapply/channel_select_result.go,q0,81: boolean-trap parameter (hasDefault) + Wrap `hasDefault` and `cases` into a `ChannelSelectConfiguration` struct to clarify intent.,34,1897,
analysis/domain/value/variant/origin_project.go,q0,"45: boolean-trap parameter (negate) -> Replace `bool` with a custom type like `type Negation bool` or `type MatchMode int` with constants `Match` and `NoMatch`.

75: boolean-trap parameter (equal) -> Replace `bool` with a custom type like `type ComparisonMode int` with constants `Match` and `Exclude`.

14: primitive obsession (familyID uint64, cases []int) -> Create a `type Origin struct { FamilyID uint64; CaseIndices []int }` to unify the related data.",124,5260,
analysis/engine/factapply/path_fact_apply.go,q0,"106 + primitive obsession + Use a custom type for the string-based site identifier instead of raw string concatenation.
114 + awkward abstraction + `dynamicIndexReadback` uses a switch to return two booleans; replace with a struct or a bitmask to avoid positional boolean confusion.",62,1739,
analysis/engine/factapply/call_outcome_apply.go,q0,"24: a god function doing too much: split the loop-heavy logic into smaller handlers for different fact types (e.g., `applyHeapFacts`, `applyPathRefinements`, `applyRelationFacts`).",46,2160,
analysis/engine/factapply/path_presence_implication.go,q0,81-111: a god function doing too much - The nested loops and multiple `ForEachResultTarget` calls in `callOutcomePresenceRelationCanPublishAt` make it difficult to reason about. Extract the inner target comparison logic into a named helper function.,55,1948,
analysis/domain/value/variant/origin_family.go,q0,"111 LEAKY LAYERING: `internal ""github.com/wippyai/go-lua/analysis/internal/hash""` imports an internal package from a sibling/parent directory; move the hashing logic to a shared domain or public utility package.
113 LEAKY LAYERING: `github.com/wippyai/go-lua/analysis/type/...` imports are high-level type definitions into a value-domain package; ensure the dependency direction is strictly towards the domain.
116 LEAKY LAYERING: `github.com/wippyai/go-lua/analysis/type/unwrap` is a utility from a higher layer; move unwrap logic to the domain or a lower-level utility.
148 PRIMITIVE OBSESSION: `signature: ""closed-record-union""` uses a raw string; use a typed constant or a specific `Signature` type.
185 PRIMITIVE OBSESSION: `path string` in `originTag` and `signature` construction uses raw strings; use a `Path` type to encapsulate path logic and validation.
196 PRIMITIVE OBSESSION: `signature: taggedRecordFamilySignature(tags)` uses a manually built string; use a structured type or a dedicated `Signature` type to avoid string parsing/manipulation.",278,6224,
analysis/engine/factflow/call_producer.go,q0,NONE,2,552,
analysis/engine/factflow/call_result.go,q0,NONE,2,550,
analysis/engine/factflow/branch.go,q0,133 + boolean-trap parameter + Replace `hasTrueValue` and `hasFalseValue` bools with a single `ValueRefinement` pointer or a specialized `OptionalValueRefinement` type to avoid passing multiple booleans that must be kept in sync.,56,1467,
analysis/engine/factapply/applicator.go,q0,"46: a god function doing too much: `NewFactsNodeTransfer` contains a massive closure that orchestrates many different types of fact applications (assignments, invalidations, refinements, etc.); decompose the closure into a registry of fact handlers or a loop over a slice of specialized applicator functions.

68: duplicated intent that a small type would unify: `FactsNodeTransferConfig` and `FactsEdgeTransferConfig` share many identical fields (`Facts`, `CallOutcome`, `Visibility`, `ProjectPath`, `TypeValues`); unify these into a shared `FactsTransferConfig` base struct or embed a common configuration type.",126,4555,
analysis/engine/factflow/dynamic_index.go,q0,NONE,2,410,
analysis/engine/factflow/expression_refinement.go,q0,NONE,2,82,
analysis/engine/factapply/objects.go,q0,"38: duplicated intent that a small type would unify: The `read` function signature `func(cfg.Point) state.State` is repeated across multiple function signatures; encapsulate this in a `type StateReader func(cfg.Point) state.State`.

118: duplicated intent that a small type would unify: The repeated logic of checking `!kind.IsTop() && !kind.IsBottom()` for multiple keys suggests a unified way to apply ""non-trivial"" properties from an expected value to a target value.",112,3067,
analysis/engine/factflow/expression_operation.go,q0,21: primitive obsession (op string) + use a typed constant or enum for operation types to prevent invalid strings.,25,615,
analysis/engine/factapply/path_state.go,q0,"87: boolean-trap parameter: replace `includeDescendantAliases bool` with a custom type (e.g., `MutationScope`) to clarify intent.
134: boolean-trap parameter: replace `descendantsOnly bool` with a custom type (e.g., `InvalidationScope`) to clarify intent.
155: boolean-trap parameter: replace `includeDescendantAliases bool` with a custom type.
188: boolean-trap parameter: replace `descendantsOnly bool` with a custom type.",114,3143,
analysis/engine/factapply/channel_select_branch.go,q0,"78 + primitive obsession + replace `string(selectFact.Select)` with a dedicated `SelectID` type to avoid repeated casting and clarify intent.
115 + primitive obsession + replace `string(selectFact.Select)` with a dedicated `SelectID` type.
148 + primitive obsession + replace `string(selectID)` with a dedicated `SelectID` type.
185 + primitive obsession + replace `string(selectFact.Select)` with a dedicated `SelectID` type.",108,3702,
analysis/engine/factflow/object.go,q0,NONE,2,499,
analysis/engine/factflow/path_copy.go,q0,NONE,2,116,
analysis/engine/factflow/branch_path_evidence.go,q0,"53, 75, 96, 117, 138, 159, 179, 199, 219, 239: boolean-trap parameter; replace `activeOnTrue bool, activeOnFalse bool` (or similar) with a dedicated `EdgeScope` type or an enum to clarify the relationship between the booleans.",88,2395,
analysis/engine/factflow/branch_relation.go,q0,71: leaky layering (importing `cfg.Point` in a domain/factflow package); move `cfg.Point` to a more generic identifier or pass it as a generic type to decouple the factflow logic from the CFG implementation.,52,1956,
analysis/engine/factflow/call_target.go,q0,67-71: duplicated intent that a small type would unify. Create an interface or use the existing `CallResultTargetView` to unify the accessors for both `CallResultTarget` and `CallResultTargetView`.,48,1445,
analysis/engine/factapply/call_materialize.go,q0,"61: god function doing too much - Split `materializeCallOutcome` into smaller functions handling specific logic branches (producer logic, outcome provider logic, and channel select logic).
79: awkward abstraction - `callContextAt` mutates and returns a `transfer.NodeContext` to simulate a new scope; consider passing specific required fields or using a constructor for a new context instead of mutating the existing one.
101: god function doing too much - `materializeCallOutcome` manages multiple distinct responsibilities (heap materialization, producer slot clearing, outcome application, and channel selection); decompose into specialized sub-functions.",131,4176,
analysis/engine/factflow/branch_path_relation.go,q0,"41 + boolean-trap parameter: Replace `activeOnTrue` and `activeOnFalse` with a single `EdgeActivation` type/struct to avoid passing multiple booleans.
61 + boolean-trap parameter: Replace `activeOnTrue` and `activeOnFalse` with a single `EdgeActivation` type/struct to avoid passing multiple booleans.",76,2180,
analysis/engine/factflow/refs.go,q0,NONE,2,727,
analysis/engine/factflow/return.go,q0,NONE,2,727,
analysis/engine/factquery/root_declaration.go,q0,NONE,2,439,
analysis/engine/factflow/static_member.go,q0,NONE,2,577,
analysis/engine/effectlowering/provider_facts.go,q0,"118 + duplicated intent that a small type would unify + The `switch` statement in `signatureEscapeEvents` repeatedly checks for both `T` and `*T` for every ownership type; a helper or a unified interface for `effect.Label` would remove this duplication.

168 + duplicated intent that a small type would unify + The `switch` statement in `signatureFrozenTables` and `signatureStoreRelations` repeats the `T` vs `*T` pattern; use a unified normalization method.

203 + duplicated intent that a small type would unify + The `switch` statement in `signatureStoreRelations` repeats the `T` vs `*T` pattern.

258 + duplicated intent that a small type would unify + The `switch` statement in `signaturePostconditionValue` repeats the `T` vs `*T` pattern for `postcondition.Present` and `postcondition.Absent`.",196,6203,
analysis/engine/factflow/channel_select.go,q0,"33 + duplicated intent + use an `Optional[T]` type or a pointer to unify the `Value` and `HasValue` boolean pairs.
36 + duplicated intent + use an `Optional[T]` type or a pointer to unify the `Value` and `HasValue` boolean pairs.
39 + duplicated intent + use an `Optional[T]` type or a pointer to unify the `Value` and `HasValue` boolean pairs.",96,2227,
analysis/engine/state/channel_select.go,q0,NONE,2,432,
analysis/engine/factflow/expression_condition.go,q0,"43 + boolean-trap parameter + Replace `value bool` with a custom type (e.g., `ExpressionResult`) to improve readability and prevent accidental inversion of logic.
43 + duplicated intent + Create a small struct/type to group `Refinements` and `PathRelations` together, as they are always accessed via the same boolean branch.",75,1988,
analysis/engine/factflow/facts.go,q0,77 + god function doing too much + `NewFacts` performs a massive amount of manual map copying; consider a more automated way to initialize or group these maps into sub-structures.,39,1764,
analysis/engine/factflow/call_site.go,q0,"33 + duplicated intent + unify `ReceiverPath`, `MethodPath`, and `ReceiverSource` into a single `Receiver` type that encapsulates the path, the name, and the source/presence logic.
38 + duplicated intent + unify `ExprRef` and `ExprIndex` into an `Expr` type that encapsulates the reference and its position.
148 + duplicated intent + unify the repeated field/method patterns between `CallSite` and `CallSiteView` by having the view delegate to the underlying site or using a shared interface.",115,2766,
analysis/engine/state/frozen_table.go,q0,NONE,2,116,
analysis/engine/state/dynamic_index_lane.go,q0,NONE,2,415,
analysis/engine/state/heap_table_lane.go,q0,NONE,2,103,
analysis/engine/factflow/assignment.go,q0,"31: duplicated intent that a small type would unify: The fields `hasDeclaredValue` and `declaredValueContracts` in `RootAssignment` are being used to manage state that could be encapsulated in a single `DeclaredValueInfo` struct or a more robust type to avoid manual boolean flag management.

118: duplicated intent that a small type would unify: The `copy...Map` functions (lines 118, 129, 142, 157) repeat the same pattern of map iteration and cloning; these could be unified using a generic map copier if the types implemented a `Cloner` interface.",135,3968,
analysis/engine/state/effect_delta_lane.go,q0,NONE,2,539,
analysis/engine/state/effectdelta/effectdelta.go,q0,NONE,2,536,
analysis/engine/state/dynamic_index.go,q0,"21: primitive obsession - Replace `key.Table == """"` check with a dedicated `key.IsEmpty()` method or a specific `Key` type constant to avoid checking raw string properties.",39,929,
analysis/engine/state/internal/floor/floor.go,q0,NONE,2,143,
analysis/engine/state/len_floors.go,q0,NONE,2,185,
analysis/engine/state/lenbound/lenbound.go,q0,NONE,2,185,
analysis/engine/factflow/postcondition.go,q0,126 + duplicated intent that a small type would unify + The `copyPostconditionRefinementMap` and `copyPostconditionPathRelationMap` functions (and their corresponding `merge` wrappers) perform identical logic for different types; they could be unified using a generic map copy function.,60,2170,
analysis/engine/factflow/return_presence_relation.go,q0,"18: primitive obsession: use a dedicated `SlotIndex` type instead of `int` to distinguish indices from other integers.
18: duplicated intent: the pair of `(index, presence)` is repeated for both trigger and target; unify them into a `SlotPresence` type.",61,2156,
analysis/engine/sourcevalue/source_values.go,q0,148 + awkward abstraction + `valueOfExpressionOperation` and `valueOfOperationSource` use a manual `active map[factflow.ExprRef]bool` to track recursion; this should be encapsulated in a dedicated `visitor` or `context` type to avoid passing the map through every call.,62,1553,
analysis/engine/state/heap_table.go,q0,24: primitive obsession (passing `identity.ID` as a zero-value check) + use a dedicated `identity.IsNil(id)` or `id.IsZero()` method to clarify intent.,44,902,
analysis/engine/state/num_floors.go,q0,NONE,2,488,
analysis/engine/state/numbound/numbound.go,q0,NONE,2,491,
analysis/engine/state/heapidentity/heapidentity.go,q0,"83: boolean-trap parameter: replace `descendantsOnly bool` with a custom type (e.g., `RemovalMode`) to clarify the meaning of `true` vs `false` at the call site.",46,1136,
analysis/engine/state/effect_delta.go,q0,15: boolean-trap parameter (The `delta.Change` check acts as a control flow flag; consider splitting into `RemoveEffectDelta` and `UpdateEffectDelta` to make intent explicit).,42,1807,
analysis/engine/factapply/roots.go,q0,"24: primitive obsession (passing `read func(cfg.Point) state.State` as a raw function instead of a defined type)
25: primitive obsession (passing `in state.State` and `out state.State` as separate arguments instead of a state container or context)
31: boolean-trap parameter (the `bool` return value in `applyRootAssignmentFact` is ambiguous; return a named error or status type)
43: primitive obsession (passing `hasDeclared bool` and `declaredContracts bool` as separate booleans; group into a `DeclarationInfo` struct)
58: awkward abstraction (the `rootAssignmentTarget` logic is a manual check of multiple fields that could be a method on `pathdom.Path` or a specialized `Target` type)
101: primitive obsession (passing `source factflow.ValueSource` where the logic depends heavily on its internal structure; consider a more specific interface)",202,5624,
analysis/engine/state/channelselectfact/channelselectfact.go,q0,"31: duplicated intent that a small type would unify: `HasPayload` and `Payload` (the presence of a payload is implied by the value or a specific type) or `HasDefault` and `Kind` (if default is a kind).

43: duplicated intent that a small type would unify: `Bottom` and `Top` in `Snapshot` are boolean flags that could be represented by a single `State` enum or by the presence/absence of facts.",100,2383,
analysis/engine/state/len_floor_lane.go,q0,"39: primitive obsession - Use a specific type for `lo` (e.g., `lenbound.Floor` or a custom `Length` type) instead of `int64` to ensure consistency with the domain model.",49,1452,
analysis/engine/state/path_evidence.go,q0,138 + primitive obsession + replace empty string checks with a dedicated `PathKey` validation method or type.,24,1095,
analysis/engine/sourcevalue/path_read.go,q0,"43: primitive obsession (pathKey is a string) + Create a `PathKey` type to encapsulate string operations and validation.
73: duplicated intent (manual identity/root/bottom checks) + Unify the pattern of ""extracting an identity and verifying it belongs to a specific object"" into a single domain method.
105: boolean-trap (hasValue) + Replace the boolean with a `product.Value` check or a specific `Option` type to avoid passing disconnected state.",106,2880,
analysis/engine/state/pathevidence/lane.go,q0,13-20: duplicated intent that a small type would unify. Group the four `bool` bottom markers and their corresponding maps into a generic `Sublane[T]` type to unify the structure.,43,819,
analysis/engine/state/placement_lane.go,q0,NONE,2,244,
analysis/engine/state/pathevidence/refinement.go,q0,NONE,2,596,
analysis/engine/sourcevalue/source_floor.go,q0,"33: god function doing too much - Split `numFloorForSource` into distinct logic paths for literal lookup, path resolution, and operation recursion.
48: god function doing too much - `numFloorForSource` manages both value retrieval and recursion cycle detection (the `active` map); separate the cycle detection logic from the value derivation logic.
54: primitive obsession - The `active map[factflow.ExprRef]bool` is used for cycle detection; wrap this in a `type Visited map[factflow.ExprRef]bool` to clarify intent and provide helper methods.",126,2990,
analysis/engine/state/placement.go,q0,NONE,2,549,
analysis/engine/state/value_lane.go,q0,NONE,2,235,
analysis/engine/state/pathevidence/static_member.go,q0,NONE,2,721,
analysis/engine/state/map_lane.go,q0,"43: boolean-trap parameter - Replace `bottom bool` in `mustSetLane` with a custom type or a sentinel value to clarify its meaning.
84: boolean-trap parameter - Replace `top bool` in `mapLane` with a custom type or a sentinel value to clarify its meaning.",65,2121,
analysis/engine/solve/solver.go,q0,"168 + awkward abstraction + The `edge[Cell]` struct and `dependEdge` map are used solely to check for existence before insertion; a simple `map[edge[Cell]]struct{}` is fine, but the `edge` struct itself is a heavy way to represent a pair of comparable types that could be handled by a more direct dependency tracking mechanism or a composite key.

215 + awkward abstraction + The `indexOf` method returns `len(s.order)` as a sentinel value for missing cells. This ""magic number"" approach is an awkward way to handle the distinction between a valid index and a missing cell; using a pointer or a specific `(int, bool)` return would be clearer.",149,3833,
analysis/engine/state/pathevidence/domain.go,q0,"53-83: duplicated intent that a small type would unify. The repetitive pattern of calling `ops.field.Method(ops.fieldLane(a), ops.fieldLane(b))` for every lattice operation suggests that `Lane` should implement its own lattice methods or use a generic product-type wrapper to handle the component-wise delegation.",73,1839,
analysis/internal/hash/hash.go,q0,NONE,2,381,
analysis/internal/recursion/recursion.go,q0,NONE,2,136,
analysis/engine/visibility/flow.go,q0,"33 + a god function doing too much + extract the fixed-point iteration loop and the version map merging logic into smaller, testable components.",31,753,
analysis/engine/transfer/transfer.go,q0,46: boolean-trap parameter (EdgeContext.HasCond) + Replace `HasCond bool` with a typed `EdgeType` or `BranchStatus` enum to clarify what the boolean represents.,42,1148,
analysis/internal/mapedit/mapedit.go,q0,NONE,2,631,
analysis/ir/dominance/doc.go,q0,NONE,2,339,
analysis/engine/visibility/visibility.go,q0,61: primitive obsession (passing `root string` to `Define`) + create a `VersionRoot` type or use a more specific identifier if `root` represents a specific semantic concept.,40,970,
analysis/ir/dominance/dominators.go,q0,NONE,2,80,
analysis/ir/dominance/info.go,q0,NONE,2,75,
analysis/engine/state/num_floor_lane.go,q0,"48: primitive obsession - The `pathKey == """"` check suggests `pathKey` should be a type with a defined `IsZero()` or `IsValid()` method to avoid checking raw string properties.
63: primitive obsession - The `lo int64` parameter should be a domain type (e.g., `FloorValue`) to ensure consistency with the `numbound.Floor` type used elsewhere.",87,3163,
analysis/engine/state/pathevidence/path_presence_implication.go,q0,"78: duplicated intent that a small type would unify: The manual comparison of `Trigger` and `Target` in `pathPresenceImplicationLess` is repeated logic; consider a dedicated `PathKey` comparison or a unified `ImplicationKey` type.
84: duplicated intent that a small type would unify: The manual comparison of `TriggerPresence` and `TargetPresence` using `.String()` is brittle and repetitive; implement a `Compare` method on `presence.Value` to unify ordering logic.",106,2413,
analysis/engine/factflow/value_source.go,q0,"23: boolean-trap parameter: NewValueSourceShape(final, expanded, adjusted, openTail bool) is hard to call correctly; use a ValueSourceShape struct or functional options.
63: duplicated intent: ValueSource contains redundant boolean fields (Final, Expanded, Adjusted, OpenTail) that are already encapsulated in the ValueSourceShape type.
74: boolean-trap parameter: NewExpressionValueSource and similar constructors use a long list of positional arguments including multiple ints and a struct; use a configuration struct.
88: boolean-trap parameter: NewCallValueSource uses a long list of positional arguments; use a configuration struct.
104: boolean-trap parameter: NewVarargValueSource uses a long list of positional arguments; use a configuration struct.",163,4993,
analysis/engine/state/store_relation.go,q0,"39: primitive obsession - The `Bottom` and `Top` booleans in `StoreRelationsSnapshot` should be replaced by a single `Status` type (e.g., `Bottom`, `Top`, `Contains`) to avoid invalid states where both are true.",56,1780,
analysis/internal/keycodec/codec.go,q0,"8: primitive obsession - Create a `DecimalKey` struct to encapsulate `prefix`, `value`, and `suffix` instead of passing them as loose parameters.",33,1107,
analysis/engine/visibility/resolver.go,q0,78: duplicated intent that a small type would unify - The logic for checking `sym == 0 || version <= 0` and the subsequent cache/lookup logic is repeated or split awkwardly; unify the cache key and lookup into a single internal method.,53,1517,
analysis/engine/state/state.go,q0,"143 + a god function doing too much + The `Domain` function and its helper `fromLanes` are responsible for orchestrating the construction, equality, ordering, and joining of 11 different complex lanes; consider moving the lattice logic for the `State` product into a dedicated factory or using a generic product lattice utility to reduce the manual boilerplate.",75,1898,
analysis/ir/dominance/postdominance.go,q0,NONE,2,432,
analysis/ir/cfg/cfg.go,q0,"148: primitive obsession + replace `bool` in `AddEdge` with a `BranchDirection` type (e.g., `Then`, `Else`) to clarify the meaning of the boolean.",43,1301,
analysis/lua/bind/symbols.go,q0,149 + primitive obsession + replace `name string` with a `SymbolName` type to distinguish between raw strings and validated identifier names.,30,930,
analysis/lua/bind/bind.go,q0,NONE,2,1079,
analysis/lua/bind/name_normalization.go,q0,NONE,2,984,
analysis/ir/ssa/ssa.go,q0,NONE,2,1159,
analysis/engine/state/pathevidence/invalidation.go,q0,"114 + awkward abstraction + The `expandDescendantInvalidationPrefixes` function uses two separate queues and two separate loops to process subtrees and descendants, which could be unified into a single traversal logic or a more cohesive state machine.

168 + duplicated intent + The logic for checking if a `pathKey` is a subtree or descendant is repeated across `pathKeyInSubtree`, `pathKeyInDescendants`, `deletePathKeySubtrees`, and `deletePathKeyDescendantPrefixes`; these could be unified by a single `PathRelation` type or method.",122,3876,
analysis/lua/bind/types.go,q0,116: god function doing too much: `bindTypeExpr` handles a massive switch statement covering every possible type expression; consider using a visitor pattern or splitting into `bind[Type]Expr` methods.,44,1050,
analysis/engine/state/pathevidence/branch_proof.go,q0,"33: primitive obsession - The `AddBranchProof` method returns `(Lane, bool)`. The boolean is used to signal if the operation was a ""no-op"" or a change; consider returning a specific status type or just the Lane if the caller can handle identity checks.

71: awkward abstraction - `EquivalentPathKeys` uses a manual BFS queue and a `seen` map to traverse equality proofs. This logic could be encapsulated into a `ProofSet` type with a `FindEquivalents(key)` method to hide the traversal mechanics.

113: duplicated intent - `deleteBranchProofsMatching` manually manages map cloning and ""changed"" state tracking. This pattern of ""filter and return new if changed"" is repeated in `AddBranchProof`. A unified `ProofSet` type would unify these operations.",173,4270,
analysis/ir/dominance/immediate.go,q0,26: primitive obsession: the use of `int(point)` and `int(block)` throughout the file suggests `cfg.Point` is a type alias for an integer; wrap the indexing logic in a method on `immediateDominatorData` to hide the casting.,57,1699,
analysis/lua/cfgbuild/builder_symbols.go,q0,NONE,2,470,
analysis/lua/cfgbuild/cfgbuild.go,q0,NONE,2,444,
analysis/lua/cfgfacts/assignments.go,q0,NONE,2,132,
analysis/engine/state/pathevidence/snapshot.go,q0,"12 + duplicated intent + create a generic `Snapshot[T]` type to unify the `Bottom`, `Top`, and data fields.
31 + duplicated intent + create a generic `Snapshot[T]` type to unify the `Bottom`, `Top`, and data fields.
51 + duplicated intent + create a generic `Snapshot[T]` type to unify the `Bottom`, `Top`, and data fields.
68 + duplicated intent + create a generic `Snapshot[T]` type to unify the `Bottom`, `Top`, and data fields.",116,3839,
analysis/lua/cfgfacts/control.go,q0,NONE,2,128,
analysis/lua/cfgfacts/copy.go,q0,NONE,2,172,
analysis/lua/cfgbuild/builder_edges.go,q0,NONE,2,1234,
analysis/ir/dominance/frontier.go,q0,"11: primitive obsession (passing multiple slices/arrays representing the same graph state) + encapsulate `rpo`, `rpoNum`, `idomByPoint`, and `hasIDom` into a single `DominanceContext` or `GraphAnalysis` struct.
76: primitive obsession (passing a map where a specialized structure would be more efficient) + use the same `DominanceContext` struct used in the dense implementation to avoid re-calculating or re-mapping data.",102,2674,
analysis/lua/bind/walk.go,q0,"141: boolean-trap parameter + Wrap the `bool` in a named type (e.g., `isMethod bool`) or use a functional option/enum to clarify what `true` represents in `b.bindFunction`.",50,1764,
analysis/lua/channelruntime/channel_runtime.go,q0,NONE,2,168,
analysis/ir/dominance/helpers.go,q0,"48: primitive obsession (passing `int` for `graphSize` instead of a domain-specific type or using the graph itself) + Use a `Graph` type or a slice to encapsulate size.
53: primitive obsession (passing `int` for `graphSize`) + Use a `Graph` type or a slice to encapsulate size.
66: primitive obsession (using `len(rpoNum)` as a proxy for graph size) + Pass the graph or a dedicated `Size` type.",107,2863,
analysis/lua/cfgfacts/doc.go,q0,NONE,2,842,
analysis/lua/semantics/branch_condition.go,q0,NONE,2,130,
analysis/lua/bind/result.go,q0,"23: duplicated intent that a small type would unify: The `Result` struct contains many maps that group AST nodes by `symbol.ID` or vice versa (e.g., `functionSymbols`, `functionsBySymbol`, `declaringFunctions`). Group these into a `SymbolMetadata` or `Scope` type to unify the relationship between symbols and their AST origins.",77,2792,
analysis/lua/cfgfacts/declarations.go,q0,48: primitive obsession (HasTargetSymbol/TargetPath) + replace with a single optional type or a pointer to a struct containing both fields.,30,1182,
analysis/lua/cfgbuild/expr_support.go,q0,"148: primitive obsession + Use a custom type (e.g., `type Operator string`) instead of a raw string for the `operator` parameter in `shortCircuitRHSCond`.",41,2089,
analysis/lua/semantics/channel_select.go,q0,NONE,2,699,
analysis/engine/state/snapshot.go,q0,"14 + duplicated intent that a small type would unify + Create a generic `Snapshot[T]` type to unify the `Top bool` and `Map/Slice` pattern used across all snapshot structs.
31 + duplicated intent that a small type would unify + Create a generic `Snapshot[T]` type to unify the `Bottom/Top bool` and `Slice` pattern.
44 + duplicated intent that a small type would unify + Create a generic `Snapshot[T]` type to unify the `Top bool` and `Map` pattern.
58 + duplicated intent that a small type would unify + Create a generic `Snapshot[T]` type to unify the `Top bool` and `Map` pattern.
72 + duplicated intent that a small type would unify + Create a generic `Snapshot[T]` type to unify the `Top bool` and `Map` pattern.",184,5471,
analysis/lua/branchcond/branch_condition.go,q0,"147 + boolean-trap parameter + Replace `truthy bool` in `polarityChecks` with a custom type or enum (e.g., `Polarity`) to clarify whether the function is collecting checks for the true or false branch.",51,2570,
analysis/lua/semantics/extract_branch.go,q0,NONE,2,145,
analysis/lua/cfgbuild/loopfacts.go,q0,51 + duplicated intent that a small type would unify + Create a `SymbolSet` type with `Add(id symbol.ID)` and `Contains(id symbol.ID) bool` methods to replace passing multiple `map[symbol.ID]struct{}` arguments.,57,2428,
analysis/lua/pathexpr/path.go,q0,67: primitive obsession + replace `name.Method` (string) and `name.Receiver` (ast.Expr) with a more structured `MethodCall` type or ensure the `ast.FuncName` type encapsulates these more robustly.,51,1395,
analysis/lua/pathexpr/table_field.go,q0,53: primitive obsession (passing `*int` for `arrayIndex`) + Pass a dedicated `TableContext` struct or a custom `ArrayIndex` type to manage state and avoid pointer manipulation.,41,1396,
analysis/lua/cfgfacts/metadata.go,q0,"11-91: duplicated intent that a small type would unify. Replace the multiple maps and repetitive getter/setter pairs with a single map of a generic `Fact` interface or a sum type (e.g., `map[cfg.Point]Fact`).",55,2231,
analysis/lua/semantics/result_index.go,q0,NONE,2,104,
analysis/lua/semantics/result.go,q0,NONE,2,453,
analysis/lua/semantics/result_metadata.go,q0,NONE,2,157,
analysis/lua/semantics/sem.go,q0,NONE,2,102,
analysis/lua/pathexpr/object_literal.go,q0,"38: leaky layering (calling `sourceprovenance.ProofInner` inside a `pathexpr` package); move provenance unwrapping to a higher-level orchestration layer or pass the unwrapped expression in.
65: leaky layering (calling `sourceprovenance.ValueShape` inside a `pathexpr` package); move shape analysis to a higher-level layer or pass the shape information in.",85,2187,
analysis/lua/sourceprovenance/expr_nil.go,q0,NONE,2,143,
analysis/lua/semantics/extract.go,q0,61: god function doing too much - The `extractStmt` switch statement is a large dispatcher that handles every possible statement type; consider using a visitor pattern or a map of handler functions to decouple the logic.,44,1669,
analysis/lua/semantics/extract_call_return.go,q0,"39: duplicated intent that a small type would unify: The `context, exprs, exprIndex` pattern is manually reconstructed with different variable counts/types in both functions; unify these into a `CallContext` struct.",47,1295,
analysis/lua/semantics/object_literal.go,q0,38: boolean-trap parameter; replace `final bool` with a typed constant or a small struct to clarify what `true` vs `false` represents in the context of `SourceForExpr`.,42,1256,
analysis/lua/cfgbuild/builder_statements.go,q0,"31: god function doing too much - `buildStmt` is a large switch statement managing many distinct statement types; consider a registry of handlers or splitting the switch into smaller dispatchers.
143: awkward abstraction - `branchPath(branch.current, true)` uses a boolean to represent a semantic path (then/else); replace with a named type or explicit `thenPath`/`elsePath` functions.
144: awkward abstraction - `branchPath(branch.current, false)` uses a boolean to represent a semantic path; replace with explicit path functions.",120,3978,
analysis/lua/bind/functions.go,q0,"41 + awkward abstraction + `FunctionOrigin` contains both high-level metadata (`Kind`, `Parent`) and low-level implementation details (`Stmt`, `LocalIndex`, `Method`); split into `OriginMetadata` and `OriginDetails`.
156 + awkward abstraction + `functionOriginDetails` is a separate struct used only to pass parameters to `registerFunction`; merge it into `FunctionOrigin` or use a builder pattern.
165 + god function + `registerFunction` performs multiple distinct responsibilities: generating a new symbol, updating multiple maps, appending to slices, and constructing the `FunctionOrigin` object; decompose into smaller registration steps.
198 + god function + `bindFunction` manages scope, type parameters, parameter symbol generation, slot layout, and statement binding; split into `bindParameters`, `bindTypeParams`, and `bindBody`.",181,5415,
analysis/lua/transferfacts/assert_postconditions.go,q0,NONE,2,95,
analysis/lua/bind/result_copy.go,q0,"11 + duplicated intent + create a generic `cloneSlice[T any](s []T) []T` function to unify the repetitive slice copying logic.
20 + duplicated intent + create a generic `cloneSlice[T any](s []T) []T` function to unify the repetitive slice copying logic.
29 + duplicated intent + create a generic `cloneSlice[T any](s []T) []T` function to unify the repetitive slice copying logic.
38 + duplicated intent + create a generic `cloneSlice[T any](s []T) []T` function to unify the repetitive slice copying logic.
47 + duplicated intent + create a generic `cloneSlice[T any](s []T) []T` function to unify the repetitive slice copying logic.
56 + duplicated intent + create a generic `cloneSlice[T any](s []T) []T` function to unify the repetitive slice copying logic.",198,5471,
analysis/lua/sourceprovenance/producer.go,q0,68: duplicated intent that a small type would unify: The `AssertionInner` and `ProofInner` functions both implement manual loops to unwrap specific AST types; unify this unwrapping logic into a single helper or a method on `ast.Expr`.,53,1119,
analysis/lua/semantics/extract_assign.go,q0,"15 + god function + split `extractLocalAssign` into smaller methods for call fact building and assignment fact building.
48 + god function + split `extractAssign` into smaller methods for call fact building and assignment fact building.
55 + duplicated intent + create a `CallContext` type to unify the logic used to determine context, expressions, and targets.
85 + duplicated intent + create a `SymbolInfo` type to unify the `id` and `hasSymbol` logic used in both loops.",108,2262,
analysis/lua/transferfacts/assertions.go,q0,NONE,2,114,
analysis/lua/callorder/callorder.go,q0,"78 + a god function doing too much + split `collectExpr` into smaller visitor functions for specific AST node types.
145 + duplicated intent that a small type would unify + `containsCall` and `collectExpr` share identical traversal logic; unify them using a visitor pattern or a shared traversal helper.
188 + leaky layering + `typePredicateCallCovered` imports `branchcond`, which appears to be a higher-level semantic layer; move this logic to the caller or a semantic-specific package.
225 + leaky layering + `channelSelectCallCovered` imports `channelruntime`, which is a specialized runtime-aware layer; move this logic to the caller or a runtime-specific package.",152,4644,
analysis/lua/semantics/value_list.go,q0,"71: duplicated intent that a small type would unify: `assignmentValueSource`, `returnValueSources`, `iteratorValueSources`, and `argumentValueSources` all wrap `sourceprovenance.ValueListSources` with different boolean flags; unify these into a single domain-specific type or enum for the ""mode"".",65,1486,
analysis/lua/transferfacts/branch_path_relations.go,q0,NONE,2,250,
analysis/lua/sourcevalue/expression_operation.go,q0,"46: primitive obsession - replace `op.Op() != ""and"" && op.Op() != ""or""` with a typed constant or method on `factflow.ExpressionOperation`.",40,1249,
analysis/lua/sourcevalue/object_literal.go,q0,"23: a god function doing too much: `ObjectLiteralTypeCached` handles cache lookup, expected type resolution, entry iteration, path construction, and complex validation logic; extract the entry processing loop into a separate function.",46,1278,
analysis/lua/transferfacts/branch_refinement_edges.go,q0,"26: boolean-trap parameter; replace `cond bool` with a custom type (e.g., `BranchDirection`) to clarify meaning at the call site.",35,704,
analysis/lua/transferfacts/assignments.go,q0,"143 + duplicated intent + `reachesArray` and `recordWithCallableField` both implement manual recursion depth logic; unify this into a single `walkType(t typ.Type, fn func(typ.Type) bool, depth int)` helper.",55,1263,
analysis/lua/semantics/result_copy.go,q0,"12 + duplicated intent + use a generic slice copy function or a single type-parameterized function to replace the repetitive `copyExprs`, `copyTypeExprs`, `copyStrings`, and `copySymbols`.
36 + duplicated intent + use a generic slice copy function to replace the repetitive `copyStrings`, `copySymbols`, and `copyValueSources`.
48 + primitive obsession + change `want int` to a specific type like `symbol.Count` or similar if applicable, though the primary smell here is the duplication of the slice-copying logic seen throughout the file.",123,2991,
analysis/lua/semantics/call_fact.go,q0,"63: a god function doing too much: `buildCallFact` orchestrates too many distinct sub-tasks (flag calculation, shape resolution, path resolution, source resolution, and fact assembly); decompose into smaller, specialized builder functions.

79: an awkward abstraction: `callListFlags` returns three booleans which are highly coupled; encapsulate these into a `CallContextFlags` struct.

101: an awkward abstraction: `resolveCallPaths` returns six values (three pairs of `(value, bool)`); encapsulate the results into a `CallPathInfo` struct.

125: a boolean-trap parameter: `methodReceiverSource` uses `hasReceiverPath` (a boolean) to control logic flow; consider passing a more descriptive state or using a specialized type.",165,4310,
analysis/lua/transferfacts/branch_value_constraints.go,q0,24: primitive obsession (typeName string) -> Use a domain type or `runtimekind.Tag` directly to avoid manual parsing.,28,1364,
analysis/lua/sourceprovenance/shape.go,q0,"28: boolean-trap parameter (final, allowExpansion, openTail) -> Create a `ValueShapeOptions` struct to encapsulate these flags.
36: boolean-trap parameter (final, allowExpansion, openTail) -> Create a `ValueShapeOptions` struct to encapsulate these flags.
43: boolean-trap parameter (final, allowExpansion, openTail) -> Create a `ValueShapeOptions` struct to encapsulate these flags.
48: boolean-trap parameter (final, allowExpansion, openTail) -> Create a `ValueShapeOptions` struct to encapsulate these flags.",124,2833,
analysis/lua/transferfacts/channel_selects.go,q0,108: primitive obsession: use a structured ID type or a dedicated formatter instead of manual string concatenation with `strconv.Itoa`.,28,1079,
analysis/lua/transferfacts/expr_refs.go,q0,NONE,2,686,
analysis/lua/transferfacts/branches.go,q0,48: primitive obsession: pass `fact.Check.LiteralString` and `fact.Check.TypeName` as specific types or a unified `Literal` type instead of raw strings.,39,1638,
analysis/lua/transferfacts/path_static_value.go,q0,NONE,2,170,
analysis/lua/transferfacts/symbol_types.go,q0,NONE,2,121,
analysis/lua/semantics/extract_metadata.go,q0,"46 + god function + split the logic for creating the `FunctionDefinitionFact` and the `OrdinaryAssignmentFact` into two distinct methods.
85 + duplicated intent + create a helper method to handle the repetitive ""check if points length is 1/2 and return error"" validation logic.
118 + duplicated intent + create a helper method to handle the repetitive ""check if points length is 1/2 and return error"" validation logic.
135 + duplicated intent + create a helper method to handle the repetitive ""check if points length is 1/2 and return error"" validation logic.
148 + duplicated intent + create a helper method to handle the repetitive ""check if points length is 1/2 and return error"" validation logic.
161 + duplicated intent + create a helper method to handle the repetitive ""check if points length is 1/2 and return error"" validation logic.",194,4243,
analysis/lua/transferfacts/branch_guard_proofs.go,q0,"41: boolean-trap parameter: rename `cond bool` to `isTruthy bool` or similar to clarify its role in the logic.
143: primitive obsession: `typeName string` should be a typed `runtimekind.Tag` or similar to avoid repeated parsing.
144: primitive obsession: `matchesType bool` and `cond bool` are adjacent booleans; consider a small type or more descriptive names to prevent accidental swapping.",95,2230,
analysis/lua/transferfacts/call_targets.go,q0,28: duplicated intent that a small type would unify: The manual mapping of `semantics.CallResultTargetKind` to `factflow.CallResultTargetKind` via a switch statement suggests these enums should share a common type or a conversion method.,53,1798,
analysis/lua/sourcevalue/iterator.go,q0,"12: boolean-trap parameter (hasAssertedSourceType) + wrap assertedSourceType and hasAssertedSourceType into an optional struct or a specialized type.
12: primitive obsession (variableIndex) + use a custom type (e.g., `type Index int`) or an enum to clarify the meaning of the integer.
12: primitive obsession (sourceValue, assertedSourceType) + these are passed through almost every function; consider grouping them into a `Context` or `SourceInfo` struct to reduce parameter noise.",113,2899,
analysis/lua/transferfacts/branch_root_refinements.go,q0,"46: boolean-trap parameter: replace `cond bool` with a domain-specific type or enum to clarify its relationship to the `check.Kind`.
46: duplicated intent: the logic in `truthyBooleanRootRefinements` and `truthyBooleanRootRefinementOnEdge` repeats the same mapping of `CheckKind` + `bool` to `LiteralBool`; unify this into a single lookup or helper.",90,2233,
analysis/lua/transferfacts/calls.go,q0,"21-41: duplicated intent that a small type would unify. Instead of passing multiple boolean flags (e.g., `HasReceiverPath`, `HasMethodPath`) alongside their corresponding values, encapsulate the value and its presence into an `Option[T]` or a small struct type.",61,1742,
analysis/lua/transferfacts/objects.go,q0,87 + duplicated intent + unify `reachesRecordDepth` and `reachesTableContractDepth` into a single function that accepts a predicate or a type-check callback.,37,923,
analysis/lua/transferfacts/type_cast_calls.go,q0,NONE,2,521,
analysis/lua/typeannotation/annotation.go,q0,NONE,2,210,
analysis/lua/transferfacts/path_presence_narrowing.go,q0,20: boolean-trap parameter; replace `cond bool` with a domain-specific type or named constants to clarify the two different `NewBranchRefinement` construction paths.,37,1141,
analysis/lua/transferfacts/lower.go,q0,"46: a god function doing too much: `Lower` handles initialization, complex state setup, and a massive loop iterating over every possible semantic fact type; extract the loop body and the `input` initialization into smaller, specialized methods.",49,1591,
analysis/lua/transferfacts/type_refs.go,q0,17: primitive obsession - Change `typ any` to a specific interface or type representing an AST node to avoid type assertions/runtime errors.,30,1329,
analysis/lua/moduleidentity/projection.go,q0,"61: a god function doing too much: `New` performs graph traversal, assignment analysis, root identification, and alias collection; split into specialized collector methods.
148: a boolean-trap parameter: `includeStableGlobals` in `signatureNameForPath` is a flag that changes logic flow; consider using two distinct methods or a configuration struct.
154: a boolean-trap parameter: `includeStableGlobals` in `signatureNameForPath` is a flag that changes logic flow; consider using two distinct methods or a configuration struct.
238: a boolean-trap parameter: `inherited` in `addRootAlias` and other methods is used to toggle logic; consider using two distinct methods or a specific type to represent the origin.
248: a boolean-trap parameter: `inherited` in `addRootAlias` and other methods is used to toggle logic; consider using two distinct methods or a specific type to represent the origin.
278: a boolean-trap parameter: `inherited` in `addRootAlias` and other methods is used to toggle logic; consider using two distinct methods or a specific type to represent the origin.
303: a boolean-trap parameter: `inherited` in `addRootAlias` and other methods is used to toggle logic; consider using two distinct methods or a specific type to represent the origin.
318: a boolean-trap parameter: `inherited",300,7053,
analysis/lua/transferfacts/values_exprref.go,q0,16: primitive obsession: `sourceExprRefKey` uses many individual bool/int fields that likely mirror `sourceprovenance.ASTSource`; consider embedding the source or a unified metadata type.,41,1485,
analysis/lua/typecall/metamethod.go,q0,NONE,2,249,
analysis/lua/typecall/shared.go,q0,NONE,2,71,
analysis/lua/transferfacts/path_literal_narrowing.go,q0,"21: boolean-trap parameter (the `cond bool` parameter in `rootLiteralRefinement` makes the call site ambiguous; replace with a named type or specific method)
39: primitive obsession (the `literal string` parameter in `literalBranchRefinement` should be a `typ.Type` to match the logic's intent and avoid repeated conversion)",77,2428,
analysis/lua/typecall/callable_return.go,q0,23: boolean-trap parameter - Replace `depth int` with a dedicated `Context` struct or a `Depth` type to prevent accidental integer passing and allow for future metadata expansion.,39,1290,
analysis/lua/typeannotation/typeannotation.go,q0,26 LEAKY LAYERING: Importing `github.com/wippyai/go-lua/compiler/ast` into an analysis package suggests the analysis layer is coupled to the compiler's internal AST representation; consider defining a local interface or a shared intermediate representation.,57,1565,
analysis/lua/transferfacts/return_presence.go,q0,"103 + awkward abstraction + The nested triple loop (trigger, target, presence) used to generate permutations is hard to read; extract the permutation logic into a dedicated generator or iterator.

133 + primitive obsession + The `returnpresence.Point` struct uses two parallel slices (`Presence []presence.Value` and `Known []bool`) to track state; unify these into a single slice of a new type (e.g., `[]PresenceState`) that encapsulates both presence and knowledge.",103,2546,
analysis/lua/typeoperator/metamethod.go,q0,NONE,2,95,
analysis/lua/typeoperator/concat.go,q0,NONE,2,443,
analysis/lua/typeoperator/numeric_literals.go,q0,NONE,2,185,
analysis/lua/transferfacts/values_source.go,q0,"46: duplicated intent that a small type would unify: The manual manipulation of `TargetIndex` and `ResultIndex` (incrementing by 1) to represent an error state is a pattern that should be encapsulated in a method on `sourceprovenance.ASTSource`.

53: god function doing too much: `valueSource` is responsible for both extracting expression references/paths and performing a complex switch-based factory mapping; split the path-adding logic from the object construction.",101,2637,
analysis/lua/typecall/generic_call.go,q0,178 + god function + `inferTypeParamBindings` is a massive switch statement handling every type variant; extract each case into a specific `infer[Type]Bindings` function.,39,1495,
analysis/lua/sourceprovenance/source.go,q0,"31: boolean-trap parameter: Replace the four `bool` parameters in `NewSourceShape` with a `SourceShape` struct or a functional options pattern.
63: duplicated intent: The `Final`, `Expanded`, `Adjusted`, and `OpenTail` fields in `ASTSource` are duplicates of the `SourceShape` type; embed `SourceShape` directly into `ASTSource`.
78: boolean-trap parameter: Replace the four `bool` parameters in `NewSourceShape` (via `astSourceWithShape`) with a `SourceShape` struct.
89: boolean-trap parameter: Replace the four `bool` parameters in `NewSourceShape` (via `astSourceWithShape`) with a `SourceShape` struct.
103: boolean-trap parameter: Replace the four `bool` parameters in `NewSourceShape` (via `astSourceWithShape`) with a `SourceShape` struct.
153: god function: The `Valid()` method contains complex, branching validation logic for every possible state; decompose into smaller `validateKind()` or `validateShape()` methods.",238,6135,
analysis/lua/typeoperator/unary.go,q0,NONE,2,95,
analysis/lua/transferfacts/type_is_active_in.go,q0,"48: primitive obsession (resultIndex int) + replace with a specific type or enum if indices represent semantic roles (e.g., ErrIndex, ValIndex).
78: duplicated intent (typeIsKillsPath) + unify the logic of checking if a path is part of the target set into a method on `typeIsTargets`.
101: awkward abstraction (returning []bool) + return a structured type or a bitset to represent edge presence/absence more clearly.",102,3200,
analysis/lua/typecall/distribute.go,q0,"23, 38, 53, 71, 88: duplicated intent that a small type would unify. The pattern of iterating over members, skipping nil/never, and collecting results into a slice to be passed to a `normalize` function is repeated in every function; unify this with a generic iterator or a higher-order ""map/filter"" utility for `typ.Type` collections.",87,2361,
analysis/lua/typeprojection/constructor_path.go,q0,NONE,2,441,
analysis/lua/typeresolve/binding_lookup.go,q0,NONE,2,202,
analysis/lua/semantics/facts.go,q0,"48 + duplicated intent that a small type would unify + Wrap `Symbol` and `HasSymbol` into a single `OptionalSymbol` struct.
54 + duplicated intent that a small type would unify + Wrap `Path` and `HasPath` into a single `OptionalPath` struct.
57 + duplicated intent that a small type would unify + Wrap `ContainerPath` and `HasContainerPath` into a single `OptionalPath` struct.
78 + duplicated intent that a small type would unify + Wrap `CalleePath` and `HasCalleePath` into a single `OptionalPath` struct.
80 + duplicated intent that a small type would unify + Wrap `ReceiverPath` and `HasReceiverPath` into a single `OptionalPath` struct.
82 + duplicated intent that a small type would unify + Wrap `MethodPath` and `HasMethodPath` into a single `OptionalPath` struct.
85 + duplicated intent that a small type would unify + Wrap `ReceiverSource` and `HasReceiverSource` into a single `OptionalSource` struct.
88 + duplicated intent that a small type would unify + Wrap `CalleeSymbol` and `HasCalleeSymbol` into a single `OptionalSymbol` struct.
131 + duplicated intent that a small type would unify + Wrap `ChannelPath` and `HasChannelPath` into a single `OptionalPath` struct.
14",300,7555,
analysis/lua/typecall/member_call.go,q0,"53: boolean-trap parameter - The `bool` return in `MemberCallable` is an anonymous flag; return a custom type or a more descriptive status to clarify what `true` represents.
113: duplicated intent - `memberCallDepth` and `indexedMemberCallDepth` share nearly identical recursive traversal logic for `Union`, `Intersection`, `Optional`, `Alias`, and `Instantiated`; unify the traversal into a single higher-order function.",96,2098,
analysis/module/importlookup/source.go,q0,NONE,2,74,
analysis/lua/valueexpr/classify.go,q0,NONE,2,335,
analysis/lua/typeoperator/comparison.go,q0,"13: primitive obsession - Replace `op string` with a custom `ComparisonOp` type to restrict valid inputs.
26: primitive obsession - Replace `op string` with a custom `ComparisonOp` type to restrict valid inputs.",50,2004,
analysis/lua/typeoperator/operand_predicates.go,q0,"13, 22, 27, 32, 41, 53, 61, 71: duplicated intent that a small type would unify. Replace the repeated `t = operatorSurface(t, 0)` pattern with a helper function or a wrapper type that handles surface extraction automatically.",70,1567,
analysis/lua/typeoperator/arithmetic.go,q0,"10: primitive obsession (op string) + define an `Operator` type or enum.
23: primitive obsession (op string) + define an `Operator` type or enum.
51: primitive obsession (op string) + define an `Operator` type or enum.
51: duplicated intent (op to metamethod mapping) + use a map or a method on an `Operator` type to unify the relationship.",91,2331,
analysis/lua/typeresolve/type_name_walk.go,q0,10-45: awkward abstraction + Replace the two separate callback functions (`visitRef` and `visitPrimitive`) with a single `Visitor` interface to unify the traversal logic.,38,945,
analysis/lua/transferfacts/type_is_recognition.go,q0,"46 + god function + split `addTypeIsBranchRefinements` into smaller logical units (e.g., one for finding establish points and one for applying refinements to branches).
103 + awkward abstraction + `branchRefinementOnEdge` uses a boolean `cond` to decide which side of a refinement to populate; consider a more descriptive type or a factory method that accepts the specific outcome.
114 + boolean-trap parameter + `typeIsConditionSuccessEdge` returns `(bool, bool)`; the second boolean is a ""success"" flag that would be clearer if returned as a named type or if the logic was encapsulated.",135,4260,
analysis/lua/typeprojection/object_literal_expected.go,q0,48: primitive obsession (passing `depth int` for recursion control) + wrap depth in a `Context` or `RecursionGuard` type.,32,1382,
analysis/lua/typeprojection/apply.go,q0,48: duplicated intent that a small type would unify: `ApplySegments` and `SegmentKeyType` both manually switch on `segment.Segment` kinds; unify this logic into a method on `segment.Segment` or a shared projection helper.,52,1494,
analysis/lua/transferfacts/values_expression.go,q0,"38: god function doing too much: `addExpressionCondition` handles refinement extraction, truthy/falsy check normalization, and relation mapping; split into `extractRefinements` and `extractRelations`.
101: duplicated intent that a small type would unify: `addExpressionOperation` and `scalarOperationType` both use large switch statements to map AST nodes to operators; unify the operator mapping logic.
143: duplicated intent that a small type would unify: `expressionValue` and `expressionOperandType` share similar logic for traversing AST nodes to find types/values; unify via a visitor or shared traversal helper.
189: duplicated intent that a small type would unify: `scalarOperationType` and `binaryExpressionOperation` repeat the same AST type-switching logic; consolidate the operator-to-type mapping.",177,4340,
analysis/lua/typeresolve/resolver.go,q0,"67: leaky layering (Resolver calls typeannotation.Type/TypeWithGuard, which appears to be a higher-level orchestration layer) -> Move the orchestration logic out of the Resolver or invert the dependency.",43,1270,
analysis/module/manifest/effect_codec_support.go,q0,NONE,2,642,
analysis/module/manifest/effect_codec_projection.go,q0,NONE,2,942,
analysis/lua/typeoperator/logical.go,q0,"11: primitive obsession (op string) + use a custom `LogicalOp` type or enum.
11: primitive obsession (depth int) + wrap in a `Context` struct if depth is part of a traversal state.
48: primitive obsession (op string) + use a custom `LogicalOp` type or enum.
101: primitive obsession (op string) + use a custom `LogicalOp` type or enum.",94,2918,
analysis/lua/visibilityfacts/visibility.go,q0,"33: a god function doing too much: `Definitions` handles state initialization, closure definition, and multiple distinct iteration loops; extract the loop logic into smaller, specialized functions.
88: duplicated intent that a small type would unify: `addPath` and `addProofPath` perform nearly identical logic; unify them into a single function or a method on a collector type.",79,2128,
analysis/module/manifest/effect_codec.go,q0,"106 + primitive obsession + Use a typed constant or enum for `effectLabelWire.Kind` instead of raw strings to prevent typos and improve maintainability.
156 + primitive obsession + Use a typed constant or enum for `effectLabelWire.Kind` instead of raw strings to prevent typos and improve maintainability.",68,1977,
analysis/module/signature/function.go,q0,NONE,2,883,
analysis/lua/typecall/callable.go,q0,"15 + primitive obsession + replace `depth int` with a `depth` type or use a context object to avoid passing raw integers through the recursion.
38 + primitive obsession + replace `depth int` with a `depth` type or use a context object to avoid passing raw integers through the recursion.
47 + primitive obsession + replace `depth int` with a `depth` type or use a context object to avoid passing raw integers through the recursion.
58 + primitive obsession + replace `depth int` with a `depth` type or use a context object to avoid passing raw integers through the recursion.
74 + primitive obsession + replace `depth int` with a `depth` type or use a context object to avoid passing raw integers through the recursion.
90 + primitive obsession + replace `depth int` with a `depth` type or use a context object to avoid passing raw integers through the recursion.
101 + primitive obsession + replace `depth int` with a `depth` type or use a context object to avoid passing raw integers through the recursion.",225,5703,
analysis/module/manifest/type_codec_structured.go,q0,147 + primitive obsession + replace the manual `if/else` chain for type checking with a unified `annotation.Payload` encoder/decoder or a visitor pattern.,36,1366,
analysis/module/signaturelookup/source.go,q0,NONE,2,465,
analysis/symbol/symbol.go,q0,NONE,2,103,
analysis/module/manifest/manifest.go,q0,"77 + a god function doing too much + split `Encode` into smaller methods for handling `Export`, `Types`, and `FunctionSignatures` separately.",34,2062,
analysis/lua/typeoperator/operator.go,q0,"11 + primitive obsession + replace `op string` with a `LuaOp` type to prevent invalid operator strings.
20 + primitive obsession + replace `op string` with a `LuaOp` type.
30 + primitive obsession + replace `op string` with a `LuaOp` type.
115 + primitive obsession + replace `op string` with a `LuaOp` type.
145 + primitive obsession + replace `op string` with a `LuaOp` type.
156 + primitive obsession + replace `op string` with a `LuaOp` type.
173 + primitive obsession + replace `op string` with a `LuaOp` type.",149,3867,
analysis/module/manifest/effect_codec_refinement.go,q0,"48: primitive obsession: use a typed constant/enum for ""mutation.unchanged"", ""mutation.elementUnion"", etc., instead of raw strings.
58: primitive obsession: use a typed constant/enum for ""mutation.unchanged"", ""mutation.elementUnion"", etc., instead of raw strings.",66,2397,
analysis/test/value/standard/registry.go,q0,NONE,2,240,
analysis/type/access/access.go,q0,NONE,2,241,
analysis/type/access/field_projection.go,q0,NONE,2,167,
analysis/type/access/index.go,q0,NONE,2,167,
analysis/module/manifest/effect_codec_expr.go,q0,"22: primitive obsession + use a typed constant or enum for `Kind` instead of raw strings.
88: primitive obsession + use a typed constant or enum for `Kind` instead of raw strings.
133: primitive obsession + use a typed constant or enum for `Kind` instead of raw strings.
143: duplicated intent + `encodeRequiredExpr` and `decodeRequiredExpr` use strings for `parent` and `side` to build error messages; unify this into a structured error type or a specific `ExprError` type to avoid string manipulation.",121,3094,
analysis/type/access/index_composite.go,q0,NONE,2,123,
analysis/module/manifest/type_codec.go,q0,"14 primitive obsession: `w.Kind` is a string; use a typed constant/enum for type kinds.
136 primitive obsession: `w.Kind` is a string; use a typed constant/enum for type kinds.",51,2536,
analysis/module/manifest/type_codec_primitives.go,q0,"114: leaky layering + `decodeRecord` directly instantiates and manipulates `typetable.NewRecord()`, which is a higher-level construction layer; move the reconstruction logic into a factory method within the `typ` or `typetable` package.",57,2528,
analysis/lua/transferfacts/fact_set_append.go,q0,"11: duplicated intent that a small type would unify: Create a generic `appendRefinements[T any](out map[cfg.Point]Set[T], point cfg.Point, items ...T)` helper to unify the repetitive map-update pattern.
21: duplicated intent that a small type would unify: Create a generic `appendRefinements[T any](out map[cfg.Point]Set[T], point cfg.Point, items ...T)` helper to unify the repetitive map-update pattern.
31: duplicated intent that a small type would unify: Create a generic `appendRefinements[T any](out map[cfg.Point]Set[T], point cfg.Point, items ...T)` helper to unify the repetitive map-update pattern.
41: duplicated intent that a small type would unify: Create a generic `appendRefinements[T any](out map[cfg.Point]Set[T], point cfg.Point, items ...T)` helper to unify the repetitive map-update pattern.
51: duplicated intent that a small type would unify: Create a generic `appendRefinements[T any](out map[cfg.Point]Set[T], point cfg.Point, items ...T)` helper to unify the repetitive map-update pattern.
61: duplicated intent that a small type would unify: Create a generic `appendRefinements[T any](out",300,8083,
analysis/module/manifest/type_wire.go,q0,"6: primitive obsession: use a custom `Kind` type instead of `string`.
31: primitive obsession: use a custom `Kind` type instead of `string`.
54: primitive obsession: use a custom `Kind` type instead of `string`.
66: primitive obsession: use a custom `Kind` type instead of `string`.",75,2376,
analysis/module/typelookup/source.go,q0,"26: primitive obsession: replace `path []string` with a `TypePath` type to encapsulate path logic.
35: primitive obsession: replace `modulePath, name string` with a `QualifiedName` type.",48,1128,
analysis/type/access/index_map.go,q0,NONE,2,419,
analysis/type/access/wrappers.go,q0,NONE,2,133,
analysis/type/ambient/types.go,q0,NONE,2,146,
analysis/type/format/format.go,q0,NONE,2,101,
analysis/test/laws/lattice/laws.go,q0,178 + duplicated intent that a small type would unify + The `checkJoinAssociative` and `checkMeetAssociative` methods contain identical logic for capping the sample size and iterating; this could be unified into a single `checkAssociative` helper.,54,1292,
analysis/type/format/format_container.go,q0,NONE,2,91,
analysis/type/access/missing_field.go,q0,NONE,2,631,
analysis/type/access/field_descent.go,q0,"14 + duplicated intent + The check `if top, ok := SpecialAccessType(t); ok` is performed both at the start of `fieldDepth` and inside the `descendAccessWrappers` callback; unify this logic within the descent helper or the initial check.",58,1468,
analysis/type/access/index_array.go,q0,"14: boolean-trap parameter (replace `true` in `indexByKeyVariants` with a named constant or a specific enum value)
28: primitive obsession (replace `depth int` with a `Depth` type or a context object to prevent accidental integer misuse)",57,1357,
analysis/module/manifest/effect_codec_returns.go,q0,"16, 20, 24, 28, 32, 36, 40: duplicated intent that a small type would unify; use a pointer-receiver or a helper to handle the `*T` cases instead of repeating the switch logic for every type.
63, 65, 67, 69, 71, 73, 75, 77, 79: primitive obsession; `w.Kind` is a raw string, use a defined type (e.g., `ReturnTypeKind`) to ensure type safety and centralize valid values.",135,3969,
analysis/type/format/format_union.go,q0,NONE,2,395,
analysis/type/access/distribute.go,q0,"15: boolean-trap parameter (the `depth int` parameter is used to track recursion state; consider wrapping the query and depth into a context object or a specialized visitor type)
38: awkward abstraction (the `query` function signature in `distributeIntersection` differs from `distributeUnion`, making it difficult to use them polymorphically)",74,1951,
analysis/type/annotation/annotation.go,q0,131 + duplicated intent + The `Hash` function duplicates the logic of `Equal` by manually branching on every type; use a unified visitor or a more generic way to handle the payload's internal state.,45,1043,
analysis/type/internal/nodeid/nodeid.go,q0,NONE,2,81,
analysis/type/kind/kind.go,q0,NONE,2,103,
analysis/type/inspect/seen.go,q0,NONE,2,589,
analysis/module/manifest/operational_effects_codec.go,q0,"147 + a god function doing too much + split `encodeOperationalEffects` into smaller sub-encoders for each slice type (e.g., `encodeReturnPresenceRelations`, `encodePathStaticMembers`, etc.).
223 + a god function doing too much + split `decodeOperationalEffects` into smaller sub-decoders for each slice type.
318 + a god function doing too much + split `canonicalizeOperationalEffectsWire` into smaller functions per field.
438 + a god function doing too much + split `encodeAllocationObjectTemplate` into smaller sub-functions for static members and dynamic entries.
511 + a god function doing too much + split `decodeAllocationObjectTemplate` into smaller sub-decoders.",158,4771,
analysis/module/signaturelookup/internal/stdlib/registry.go,q0,"235: boolean-trap parameter (os.exit ""close"" parameter) -> Use a typed configuration struct or specific function variants if the boolean controls significant behavior.
238: boolean-trap parameter (os.remove return value) -> The return is a boolean, but the signature uses it to signal success/failure alongside an error string; consider a Result type.
241: boolean-trap parameter (os.rename return value) -> Similar to os.remove, the boolean return is a primitive flag for success.
244: boolean-trap parameter (os.exit ""close"" parameter) -> Use a typed constant or configuration object instead of a raw boolean.",143,3520,
analysis/type/access/index_tuple.go,q0,"16: primitive obsession (passing `depth int` and `mode indexMode` where a context struct or specialized type might be clearer)
21: duplicated intent (the loop to copy/normalize `tup.Elements` into `out` is a manual slice operation that could be unified into a `tup.Elements()` or `tup.Clone()` method)",78,2248,
analysis/module/manifest/effect_wire.go,q0,"14: primitive obsession (Kind is a string) + use a defined type (e.g., `EffectKind`)
33: primitive obsession (Kind is a string) + use a defined type (e.g., `TransformKind`)
40: primitive obsession (Kind is a string) + use a defined type (e.g., `RefinementKind`)
44: primitive obsession (Kind is a string) + use a defined type (e.g., `ReturnKind`)
61: primitive obsession (Kind is a string) + use a defined type (e.g., `ExprKind`)
63: primitive obsession (Op is a string) + use a defined type (e.g., `Operator`)
70: primitive obsession (Kind is a string) + use a defined type (e.g., `ProjectionKind`)",182,5036,
analysis/type/format/format_record.go,q0,"15: a god function doing too much: split the logic into separate methods for formatting fields, static members, and map components.
33: duplicated intent that a small type would unify: the logic for checking `field.Readonly` and `field.Optional` is repeated for both fields and static members; encapsulate this in a `formatModifiers` method.",76,1709,
analysis/type/normalize/intersection.go,q0,15: a god function doing too much: extract the recursive flattening logic into a separate `flattenTypes` helper function.,26,636,
analysis/type/format/format_recursive.go,q0,"14 + a god function doing too much + Extract the large `inspect.Visitor` definition into a separate method or factory to separate traversal logic from formatting logic.
88 + duplicated intent that a small type would unify + The logic for formatting generic/instantiated type parameters (looping with `minInt` and `...` suffix) is duplicated; unify this into a `formatTypeParams` helper.",85,1660,
analysis/type/format/format_function.go,q0,"12: a god function doing too much: extract the type parameter list, parameter list, and return type list into separate helper methods.
38: duplicated intent that a small type would unify: the logic for handling ""limit and ellipsis"" (minInt + loop + `...`) is repeated for type params, params, and returns; unify this into a `formatList` helper.",81,1822,
analysis/type/access/index_record.go,q0,"14: boolean-trap parameter - Replace `true` in `indexByKeyVariants` with a named constant or a configuration struct to clarify the meaning of the boolean.
15: duplicated intent - The logic for checking `r.HasMapComponent()` and `r.Open` is repeated across `indexInRecord`, `indexStringInRecord`, and `indexIntInRecord`; unify this into a single method on `*typ.Record`.",93,2491,
analysis/type/access/index_key.go,q0,"13: boolean-trap parameter (missingNil) + wrap missingNil and mode into a configuration struct.
13: primitive obsession (depth) + wrap depth into a context struct or use a recursive type to track state.
13: duplicated intent (missingNil/mode logic) + unify the conditional logic for nil handling into a single method on a configuration type.
48: primitive obsession (literalStringKey/literalIntKey) + create a `Key` interface or a `KeyType` union to unify string/int key extraction.",115,2637,
analysis/type/inspect/walk.go,q0,46: god function doing too much: `WalkChildren` handles the traversal logic for every single type variant; move the child-extraction logic into a `Children() []typ.Type` method on the `typ.Type` interface.,50,1145,
analysis/type/inspect/visit.go,q0,31: awkward abstraction: The `Visit` function contains a massive switch statement that must be manually updated every time a new type is added to the `typ` package; consider using a registry or a more automated dispatch mechanism if the type set is highly volatile.,54,1230,
analysis/type/channelselect/schema.go,q0,"44: boolean-trap parameter: Replace `hasDefault bool` with a custom type or a specific `ResultCase` representing the default to clarify intent.
44: primitive obsession: Replace `selectID string` and `index int` with a `SelectIdentifier` struct to unify the identity of a select case.
118: primitive obsession: Replace `selectID string` and `index int` with a `SelectIdentifier` struct.",94,2134,
analysis/type/inspect/scan.go,q0,"39: duplicated intent that a small type would unify: Replace the large switch statement in `trackConcreteNodeIdentity` with a method on `typ.Type` (e.g., `t.IsConcrete()`) to unify the check.",51,1409,
analysis/type/literal/literal.go,q0,48: duplicated intent that a small type would unify: The `switch t.Kind()` block at the end of `familyBase` duplicates the logic of `PrimitiveBase` and the type switch; unify by ensuring all primitive-like types are handled through a single mapping or type-switch mechanism.,62,1173,
analysis/type/subst/expand.go,q0,NONE,2,754,
analysis/type/subst/expand_generic.go,q0,NONE,2,730,
analysis/type/subst/expand_walk.go,q0,NONE,2,658,
analysis/type/subst/self_ref.go,q0,NONE,2,443,
analysis/type/subst/expand_memo.go,q0,NONE,2,751,
analysis/module/signature/operational_effects.go,q0,"21: duplicated intent that a small type would unify. Create a `Path` wrapper or use a slice of `Path` to unify the many `[]Path` related structs.
68: duplicated intent that a small type would unify. The `Recursive bool` in `EscapeEvent` is a boolean flag that could be part of an `EscapeKind` or a specific `EscapeProperty` type to avoid boolean traps in future extensions.
143: duplicated intent that a small type would unify. The `IsEmpty` function manually checks every slice; a unified `Collection` interface or a more generic way to aggregate these facts would reduce duplication.
156: duplicated intent that a small type would unify. The `Clone` and `Equals` methods are highly repetitive; implementing a generic deep-copy/equal mechanism for the constituent types would reduce boilerplate.",178,4688,
analysis/type/subtype/subtype.go,q0,NONE,2,514,
analysis/type/subst/self.go,q0,76: god function doing too much: `contains` handles too many type variants; split the switch into smaller visitor methods or a type-specific dispatcher.,33,1233,
analysis/type/subtype/containers.go,q0,NONE,2,661,
analysis/type/subtype/guard.go,q0,NONE,2,657,
analysis/type/subtype/functions.go,q0,NONE,2,658,
analysis/type/table/construct.go,q0,NONE,2,218,
analysis/type/subst/params.go,q0,"73: a god function doing too much: `substituteFunctionParams` handles shadowing logic, parameter transformation, variadic handling, return type transformation, and builder reconstruction; split into smaller functions for parameter/return transformation and builder assembly.",49,1633,
analysis/type/normalize/union.go,q0,89: primitive obsession: the `baseMask` bitmask logic is opaque; replace the bitwise operations with a small `TypeSet` type or a struct to clarify which kinds are present.,41,2086,
analysis/type/refinement/free_type_param.go,q0,"211 + awkward abstraction + The `freeTypeParamRecursiveSeen` and `freeTypeParamSeen` structs both implement a custom ""small-array-then-map"" optimization for tracking seen types; this logic should be unified into a single reusable `SeenSet` type.",58,1907,
analysis/type/subst/expand_collections.go,q0,"56: duplicated intent that a small type would unify: The logic for checking if a map/readonly-map has changed or needs a specific policy is repeated; unify the ""expand and check"" logic into a helper.",46,1916,
analysis/type/projection/projection.go,q0,"43: primitive obsession - Use a dedicated type for `Field` name instead of `string` to prevent mixing with other string identifiers.
44: primitive obsession - Use a dedicated type for `Index` instead of `int` if it represents a specific domain concept (like a positional argument index).",63,2297,
analysis/type/table/constructor.go,q0,45: boolean-trap parameter - Replace `Sealed bool` in `ConstructorEntry` with a `ConstructorKind` type to avoid ambiguity and allow for future expansion.,36,769,
analysis/type/refinement/annotation.go,q0,88: awkward abstraction + The `closedUnionOf` function uses a manual `for` loop with a `switch` to unwrap types; this logic is likely duplicated or should be unified into a single `unwrap.Deep` or `unwrap.ToBase` utility to avoid manual traversal logic in every analysis file.,66,2374,
analysis/type/table/doc.go,q0,NONE,2,490,
analysis/type/table/field_shape.go,q0,NONE,2,446,
analysis/type/table/key_domain.go,q0,NONE,2,417,
analysis/type/table/nil_projection.go,q0,NONE,2,185,
analysis/type/table/record_normalization.go,q0,NONE,2,207,
analysis/type/table/table_key.go,q0,NONE,2,188,
analysis/type/subtype/widen.go,q0,"11: a god function doing too much: split the large `switch` and `if` type-checking logic into smaller, type-specific helper methods (e.g., `canWidenLiteral`, `canWidenArray`).",50,1192,
analysis/type/table/metatable.go,q0,NONE,2,452,
analysis/type/table/record_tail.go,q0,NONE,2,259,
analysis/type/subtype/records.go,q0,"48: boolean-trap parameter: replace `subOptional, subReadonly bool, superOptional, superReadonly bool` with a `FieldModifiers` struct.
48: primitive obsession: replace multiple boolean flags with a `FieldAttributes` or `MemberProperties` type.",59,1780,
analysis/type/subst/expand_record.go,q0,"11: a god function doing too much - Extract the field and static member transformation logic into smaller helper functions.
68: a god function doing too much - Extract the map component expansion logic into a separate method or function.
85: a god function doing too much - Extract the logic for determining the source fields/members and rebuilding the record into a dedicated transformation step.",79,2622,
analysis/type/projection/container.go,q0,"25: primitive obsession: replace `depth int` with a `RecursionContext` or a dedicated `Depth` type to prevent accidental misuse of integers.
73: duplicated intent: the logic for iterating over `typ.Union` members and collecting types to pass to `normalize.UnionForEvidence` is duplicated in both `keyOfDepthProject` and `elementOfDepthProject`; unify this into a helper function.",88,3021,
analysis/type/table/top_marker.go,q0,NONE,2,388,
analysis/type/typ/equality_cycle.go,q0,NONE,2,113,
analysis/type/table/key_admission.go,q0,"12 + a god function doing too much + split the large switch statement into smaller, specialized dispatch functions for different type categories.",27,1033,
analysis/type/typ/equality_identity.go,q0,NONE,2,97,
analysis/type/typ/equality_prefilter.go,q0,NONE,2,95,
analysis/type/table/record_builder.go,q0,61: boolean-trap parameter: Replace `optional bool` with a custom type or separate methods to avoid ambiguity at the call site.,29,739,
analysis/type/typ/equality_unwrap.go,q0,NONE,2,96,
analysis/type/subst/expand_function.go,q0,"11: god function doing too much - Split the logic into separate functions for expanding parameters, returns, and variadic types.
11: primitive obsession - Replace `mode expandMode` with a more descriptive configuration struct if `expandMode` is just a flag or integer.
11: duplicated intent that a small type would unify - The logic for ""copy-on-write"" slice updates (checking if `params == nil`, then `make` and `copy`) is repeated for both `params` and `returns`; unify this into a helper function.",117,3020,
analysis/type/typ/equals.go,q0,NONE,2,139,
analysis/type/table/constructor_sort.go,q0,14: duplicated intent that a small type would unify: Create a generic helper function `sortedKeys[K constraints.Ordered](m map[K]*constructorNode) []K` to unify the sorting logic.,44,1739,
analysis/type/table/optional_payload.go,q0,"39, 56: duplicated intent that a small type would unify. Create a shared interface or a generic `Shape` constraint to unify the logic for `typ.Field` and `typ.StaticMember` instead of repeating the `if !item.Optional` and `splitNilableFieldType` logic.",66,1552,
analysis/type/typ/equals_recursive.go,q0,"7: a god function doing too much: split the large switch statement into smaller `equals` methods on each specific type (e.g., `Optional.equals`, `Record.equals`).",40,981,
analysis/type/typ/flags_cached_open_recursive.go,q0,13-43: awkward abstraction; move the `containsOpenRecursive` field/logic into a common interface or a base struct to avoid the repetitive switch-case pattern.,37,1133,
analysis/type/typ/children.go,q0,24: a god function doing too much: move the type-specific traversal logic into a `Children() []Type` method on each concrete type implementation.,33,1635,
analysis/type/typ/flags_cached_recursive.go,q0,11-41: duplicated intent that a small type would unify: Add a `HasRecursiveFlag() bool` method to the `Type` interface to encapsulate the flag check.,38,1176,
analysis/type/transform/rewrite.go,q0,63: god function doing too much: `rewriteDepth` handles the logic for every single type variant; extract each `case` into a dedicated `rewrite[Type]` function.,38,1821,
analysis/type/typ/guard.go,q0,NONE,2,203,
analysis/type/table/constructor_node.go,q0,"30: boolean-trap parameter; replace `sealed bool` with a custom type (e.g., `SealedMode`) to clarify intent at the call site.
14: duplicated intent; the three separate maps (`fields`, `stringIndexes`, `intIndexes`) could be unified into a single map of a specialized `indexKey` type.
75: god function; `build()` handles multiple distinct logic branches for tuples, records, and empty nodes; split into `buildTuple()`, `buildRecord()`, and `buildEmpty()`.",116,3054,
analysis/type/typ/annotated.go,q0,65: awkward abstraction: The `String()` method contains a complex type-switch for formatting annotation arguments; move this logic into a `String()` or `Format()` method on the `annotation.Argument` type.,45,1994,
analysis/type/typ/flags_aggregates.go,q0,"6-65: duplicated intent that a small type would unify; replace the repetitive `known[Property][Target]` pattern with a single generic function or a predicate-based helper (e.g., `func known(targets []Type, predicate func(Type) bool) bool`).",59,1696,
analysis/type/subtype/table.go,q0,"13: primitive obsession: replace `depth int` with a `Context` or `Depth` type to prevent accidental misuse of integer parameters.
38: duplicated intent: `checkRecordToMap` and `checkRecordToReadonlyMap` share significant logic; unify the field/map component iteration into a shared helper.
78: duplicated intent: `checkMapToRecord` and `checkRecordToMap` share similar logic for field/key validation; unify the structural comparison logic.
118: duplicated intent: `checkTupleToReadonlyMap` and `checkTupleToMap` share similar iteration patterns; unify the element-to-key/value mapping logic.",143,3416,
analysis/type/typ/flags_cached.go,q0,"10: duplicated intent that a small type would unify: return a bitmask or a struct of flags instead of four separate booleans.
55: duplicated intent that a small type would unify: use the new flag type/struct to avoid repeated unwrapping and multiple calls to `cachedContainsFlags`.",63,1784,
analysis/type/typ/members.go,q0,NONE,2,408,
analysis/type/typ/optional.go,q0,NONE,2,450,
analysis/type/subtype/core.go,q0,"36: god function doing too much: `checkCore` handles dozens of disparate type relationships; split into specialized handlers (e.g., `checkUnion`, `checkIntersection`, `checkPrimitive`) called by a central dispatcher.
41: duplicated intent that a small type would unify: The logic for comparing `Ref` names against `Alias`, `Ref`, or `Recursive` types is repeated twice (once for `sub` and once for `super`); unify this into a `isCompatibleRef` helper.
145: god function doing too much: The `checkCore` function is becoming a ""God Function"" by managing the entire type hierarchy logic; delegate specific kind-based logic to a map of handler functions.",152,4417,
analysis/type/typ/predicates.go,q0,NONE,2,435,
analysis/type/typ/dedup.go,q0,"14 + duplicated intent + create a `type HashedType struct { typ Type; hash uint64 }` and return a single slice of that struct instead of two parallel slices.
39 + duplicated intent + use the `hashedType` struct to return a single slice of objects instead of managing two separate slices (`types` and `hashes`) that must be kept in sync.",80,2626,
analysis/type/typ/record_static_member.go,q0,NONE,2,91,
analysis/type/typ/flags_field_typeparam.go,q0,5-64: duplicated intent that a small type would unify. Replace the repetitive `knownXFields` and `knownXTypeParams` functions with a single generic predicate function or a method on a collection type that accepts a checker function.,50,1633,
analysis/type/typ/function.go,q0,111: boolean-trap parameter (The `Optional` field in `Param` is a boolean; consider a `ParamKind` type or a specific `OptionalParam` struct to make intent clearer at the call site).,47,1509,
analysis/type/typ/generic.go,q0,41: duplicated intent that a small type would unify: The `contains...` boolean flags (lines 31-36) should be encapsulated into a single `Properties` or `Flags` bitmask/struct to simplify the `Generic` struct and its initialization/update logic.,60,1432,
analysis/type/typ/flags_dynamic.go,q0,"14: primitive obsession (depth int) + wrap depth and maxDepth into a `RecursionContext` struct.
14: primitive obsession (seen map[Type]bool) + wrap the traversal state (seen, depth, maxDepth) into a `TraversalState` struct.
14: duplicated intent (seen, depth, maxDepth) + use a single `Context` type to unify the traversal parameters.",88,2304,
analysis/type/typ/literal.go,q0,79: awkward abstraction - The `String()` method uses a switch on `Base` and type assertions on `Value` to re-implement logic already partially present in the `str` field; unify this by ensuring `str` is always populated during construction or using a more cohesive interface.,60,1301,
analysis/type/typ/nominal.go,q0,43: a god function doing too much: Extract the loop logic that calculates the hash and the various `contains` flags into a separate `interfaceProperties` or `metadata` struct/function.,41,1246,
analysis/type/typ/recursive_graph_closed.go,q0,NONE,2,397,
analysis/type/typ/recursive.go,q0,41: primitive obsession: replace the group of `contains...` boolean fields with a bitmask or a dedicated `TypeFlags` type.,30,657,
analysis/type/typ/recursive_type_children.go,q0,NONE,2,168,
analysis/type/typ/record.go,q0,"65: a god function doing too much: `String()` handles field formatting, static member formatting, map component formatting, and open-record formatting; extract these into smaller `formatFields`, `formatStaticMembers`, etc., methods.",49,1005,
analysis/type/typ/type.go,q0,NONE,2,206,
analysis/type/typ/primitive_names.go,q0,"11: primitive obsession: use a `PrimitiveName` type instead of `string`.
21: primitive obsession: use a `PrimitiveName` type instead of `string`.
21: duplicated intent: replace the two functions with a single map lookup or a method on a `PrimitiveName` type.",65,1539,
analysis/type/typ/recursive_hash_deps.go,q0,NONE,2,650,
analysis/type/typ/recursive_hash.go,q0,NONE,2,711,
analysis/type/typ/recursive_contains.go,q0,"28, 31, 33, 35: duplicated intent that a small type would unify. Create a `containsFlags` struct to group these boolean fields and their associated dirty/computing state.",45,927,
analysis/type/typ/union.go,q0,NONE,2,157,
analysis/type/typ/intersection.go,q0,"48: god function doing too much: `newCanonicalIntersection` handles slice copying, hash re-calculation, and complex flag computation; extract flag computation to a separate method or struct.
48: duplicated intent that a small type would unify: the multiple `contains...` boolean flags in `Intersection` and the local variables in `newCanonicalIntersection` should be unified into a single `IntersectionFlags` bitmask or bitset.",90,2090,
analysis/type/typ/unwrap_internal.go,q0,NONE,2,195,
analysis/type/transform/rewrite_nodes.go,q0,"11: god function doing too much: `rewriteFunction` handles type parameter replacement, parameter rewriting, return type rewriting, and variadic rewriting; extract the collection/replacement logic into a helper.
67: duplicated intent that a small type would unify: the logic for managing ""changed"" state and ""source"" slices (copying slices only when a change is detected) is repeated in `rewriteFunction`, `rewriteGeneric`, and `rewriteRecord`; encapsulate this in a `ChangeTracker` or similar utility.
148: duplicated intent that a small type would unify: the logic for managing ""changed"" state and ""source"" slices is repeated in `rewriteRecord`.",140,4037,
analysis/type/typ/instantiated.go,q0,"21-30: duplicated intent that a small type would unify. Create a `TypeProperties` or `TypeFlags` struct/bitmask to encapsulate the boolean flags (`containsAny`, `containsNever`, etc.) instead of bloating the `Instantiated` struct.

36-61: god function doing too much. The `Instantiate` function is responsible for both constructing the object and performing complex recursive property aggregation; move the property calculation logic into a separate helper or a method on a property-tracking type.",108,2398,
analysis/type/typ/recursive_hash_traversal.go,q0,"143: primitive obsession + replace magic numbers (1, 2, 3) with typed constants or bitflags for Optional/Readonly/Open properties.",35,862,
analysis/type/typ/rebuild_function.go,q0,"33: a god function doing too much: split `newCanonicalFunction` into separate methods for `calculateHash` and `analyzeProperties`.
55: duplicated intent that a small type would unify: create a `FunctionProperties` struct to encapsulate the boolean flags (containsAny, containsNever, etc.) and the logic to compute them.",71,1829,
analysis/type/typ/container.go,q0,"16 + duplicated intent + create a `TypeMetadata` struct to unify the repeated boolean flags (containsAny, containsNever, etc.) and hash across Array, Map, ReadonlyMap, and Tuple.
101 + duplicated intent + create a `TypeMetadata` struct to unify the repeated boolean flags and hash.
148 + duplicated intent + create a `TypeMetadata` struct to unify the repeated boolean flags and hash.
188 + duplicated intent + create a `TypeMetadata` struct to unify the repeated boolean flags and hash.
203 + god function + extract the loop logic that calculates hash and metadata flags into a helper function or a method on a new `TypeMetadata` type.",149,4142,
analysis/type/typ/record_member_flags.go,q0,"6, 15, 24, 33, 42, 51: duplicated intent that a small type would unify. Replace these repetitive functions with a single function that accepts a predicate function: `func hasStaticMember(members []StaticMember, predicate func(Type) bool) bool`.",66,1599,
analysis/type/typ/typeparam.go,q0,23: primitive obsession (multiple boolean flags for properties) + Unify the `contains...` flags into a single bitmask or a `TypeProperties` struct.,35,615,
analysis/type/typ/rebuild_record.go,q0,"41: a god function doing too much: split `newCanonicalRecord` into smaller methods for sorting, hashing, and property detection.
41: boolean-trap parameter: replace `open bool` and `assumeSorted bool` with typed configuration constants or a bitmask.
41: primitive obsession: replace `open bool` and `assumeSorted bool` with domain-specific types (e.g., `RecordMode` or `SortStrategy`).",95,2011,
analysis/type/typ/union_normalize.go,q0,"48: god function doing too much: `newCanonicalUnion` handles hash computation, flag scanning, and object construction; extract the flag scanning loop into a separate `scanUnionFlags` function.",41,922,
analysis/type/typeexpr/typeexpr.go,q0,"36 + god function + extract the recursive flattening logic into a dedicated `flatten` or `normalize` method to separate traversal from union construction logic.
105 + duplicated intent + the recursive `addMember` logic in `Intersection` is nearly identical to the logic in `Union`; unify this into a single type-traversal utility.",71,1371,
analysis/type/unwrap/unwrap.go,q0,118 + duplicated intent that a small type would unify + The `isOptionalLike` function uses a manual `depth` counter and a `map[*typ.Union]bool` to handle recursion and cycle detection; this logic could be encapsulated into a `TypeVisitor` or a specialized `TraversalContext` type to unify how the package traverses type trees.,74,1388,
analysis/type/typ/ref.go,q0,"81+duplicated intent that a small type would unify: The boolean flags `containsAny`, `containsNever`, etc., are duplicated in both `Alias` and `Meta`. Create a `TypeProperties` struct to encapsulate these flags.

135+duplicated intent that a small type would unify: The boolean flags `containsAny`, `containsNever`, etc., are duplicated in both `Alias` and `Meta`. Create a `TypeProperties` struct to encapsulate these flags.",99,1781,
analysis/type/typ/recursive_hash_scratch.go,q0,"14-23: duplicated intent that a small type would unify. Create a `visitedSet` type to encapsulate the hybrid array/map logic used in `visitedContains`, `visitedPush`, and `visitedPop`.

39-53: duplicated intent that a small type would unify. Create a `memoSet` type to encapsulate the hybrid array/map logic used in `memoGet` and `memoSet`.",88,2179,
