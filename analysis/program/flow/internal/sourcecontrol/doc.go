// Package sourcecontrol owns Flow's assembly-local structural control
// geometry.  It is deliberately private to Flow assembly: the package keeps
// no Program identity, public Edge, Outcome, executable, recurrence, or domain
// relation. Sourcecontrol consumes the already-proved lexical owners and
// seals dense source coordinates and structural arcs; function availability is
// transient reachability input and is not retained in the Result.
package sourcecontrol
