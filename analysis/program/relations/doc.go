// Package relations seals the compile-time relation-schema denominator used by
// the Wave-B Program/Link ownership cut. It is deliberately metadata for code
// generation and validation only: it does not retain runtime relation payloads,
// dispatch them, or provide a registry to the analyzer.
package relations
