// Package complete closes one authenticated relation over its mounted
// denominator.  It is tuple-native: the caller supplies one fiber Scope and
// already-authenticated tuples, while the mounted denominator supplies only
// the closed RowID membership and the mounted column catalogue supplies
// absent-cell types.
package complete
