// Package group implements the sealed relational Group operator over the
// immutable tuple.Batch ABI. Cofiber normalization owns Boolean scope
// refinement; the producer Node redeems its sealed GroupBinding, and Group
// only forms ordered key-range batches.
package group
