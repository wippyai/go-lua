// Package join implements the sealed tuple correspondence join.  It consumes
// only the arrangement-issued correspondence binding and immutable tuple
// batches; key grouping and parent selection are separate operators.
package join
