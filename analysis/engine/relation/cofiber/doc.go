// Package cofiber owns the one solve-local bridge between mounted logical
// scopes and the engine's physical Boolean support universe.
//
// Mount remains neutral: it authenticates Region identities but does not know
// the BDD representation.  Bootstrap supplies the one cold translation of
// every declared Region into the exact guard manager.  After construction an
// Authority contains no callback; it can map declared scopes from that sealed
// translation and can normalize every later Boolean partition through the
// mounted append-only scope arena.  Operators therefore exchange Scope
// values, while masks remain inside physical state/kernel code.
package cofiber
