// Package model defines stable logical owner, schema, expression, dependency,
// operation, type, row, relation, column, key, scope, presence, and
// lineage-reference vocabulary. It owns no expression behavior, storage,
// execution, declaration, or domain behavior.
package model
