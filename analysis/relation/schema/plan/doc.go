// Package plan owns the immutable, unchecked ExecutionSchema identified by an
// owner-issued schema ID, complete expression and signature registries,
// dependency graph, SCC structure, and recurrence-policy declarations.
// Content digests identify copied structures; semantic availability and
// cross-reference validity remain the checker's authority.
package plan
