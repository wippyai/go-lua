// Package relation is domain/call's binding surface: the thin typed binding of
// the dispatch family the census compiles, over the thin typed owner-column
// publisher of every call payload that crosses the boundary.
//
// Every zz_ file is emitted by analysis/schema/rule/relbindgen/relbind. The
// hand-authored half is this axis's own dispatch fold and its own lattice.
package relation
