// Code generated by analysis/schema/rule/relbindgen/relbind. DO NOT EDIT.

package relation

import (
	"github.com/wippyai/go-lua/analysis/relation/schema/model"
	"github.com/wippyai/go-lua/analysis/relation/semantic/binding"
	"github.com/wippyai/go-lua/analysis/relation/semantic/signature"
	"github.com/wippyai/go-lua/analysis/schema/rule/relbindgen"
	heapdomain "github.com/wippyai/go-lua/domain/heap"
)

// HeapIngressColumns are the owner column codecs of the
// heap/allocation/ingress binding.
type HeapIngressColumns struct {
	Heap          *relbindgen.Column[heapdomain.Value]
	HeapCandidate *relbindgen.Column[heapdomain.Key]
}

// NewHeapIngressColumns names this family's own owner columns. A column an axis
// other than this one publishes is handed in, never reached for.
func NewHeapIngressColumns(heap *relbindgen.Column[heapdomain.Value], heapCandidate *relbindgen.Column[heapdomain.Key]) (HeapIngressColumns, bool) {
	columns := HeapIngressColumns{Heap: heap, HeapCandidate: heapCandidate}
	if !columns.Available() {
		return HeapIngressColumns{}, false
	}
	return columns, true
}

// Available reports whether every column this binding needs carries an
// owner-issued type and live storage.
func (columns HeapIngressColumns) Available() bool {
	return columns.Heap.Available() && columns.HeapCandidate.Available()
}

// HeapIngressArgument is the decoded frame of one
// heap/allocation/ingress invocation.
type HeapIngressArgument struct {
	Key heapdomain.Key
}

// HeapIngressJudgment is the owner mathematics this binding carries. The axis
// states it by hand: nothing in this file can name a relation, a cell, a
// buffer or an engine value, so the mathematics stays where its owner wrote
// it and this artifact stays free of it.
type HeapIngressJudgment interface {
	relbindgen.Operation[HeapIngressArgument, heapdomain.Value]
	Available() bool
}

// The hand-authored judgment answers this family's own contract, and the
// assertion is what makes the two halves one binding.
var _ HeapIngressJudgment = HeapIngressOperation{}

// BindHeapIngress admits the heap/allocation/ingress family
// under its sealed signature.
func BindHeapIngress(operation signature.Signature, judgment HeapIngressJudgment, columns HeapIngressColumns, refusal model.RefusalID) (binding.Factory, bool) {
	if judgment == nil || !judgment.Available() || !columns.Available() {
		return nil, false
	}
	return relbindgen.Bind(relbindgen.Spec[HeapIngressArgument, heapdomain.Value]{
		Signature: operation,
		Decoder:   heapIngressDecoder{columns: columns},
		Encoder:   heapIngressEncoder{columns: columns},
		Operation: judgment,
		Address:   0,
		Refusal:   refusal,
	})
}

type heapIngressDecoder struct {
	columns HeapIngressColumns
}

func (decoder heapIngressDecoder) Decode(inputs relbindgen.Inputs) (HeapIngressArgument, bool) {
	key, ok := relbindgen.ScalarAt(inputs, 0, decoder.columns.HeapCandidate)
	if !ok {
		return HeapIngressArgument{}, false
	}
	return HeapIngressArgument{Key: key}, true
}

type heapIngressEncoder struct {
	columns HeapIngressColumns
}

func (encoder heapIngressEncoder) Encode(outputs relbindgen.Outputs, value heapdomain.Value) bool {
	return relbindgen.PutColumn(outputs, 0, encoder.columns.Heap, value)
}
