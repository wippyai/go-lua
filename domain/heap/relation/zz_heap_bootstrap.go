// Code generated by analysis/schema/rule/relbindgen/relbind. DO NOT EDIT.

package relation

import (
	"github.com/wippyai/go-lua/analysis/relation/schema/model"
	"github.com/wippyai/go-lua/analysis/relation/semantic/binding"
	"github.com/wippyai/go-lua/analysis/relation/semantic/signature"
	"github.com/wippyai/go-lua/analysis/schema/rule/relbindgen"
	heapdomain "github.com/wippyai/go-lua/domain/heap"
)

// HeapBootstrapColumns are the owner column codecs of the
// heap/bootstrap binding.
type HeapBootstrapColumns struct {
	Heap          *relbindgen.Column[heapdomain.Value]
	HeapCandidate *relbindgen.Column[heapdomain.Key]
}

// NewHeapBootstrapColumns names this family's own owner columns. A column an axis
// other than this one publishes is handed in, never reached for.
func NewHeapBootstrapColumns(heap *relbindgen.Column[heapdomain.Value], heapCandidate *relbindgen.Column[heapdomain.Key]) (HeapBootstrapColumns, bool) {
	columns := HeapBootstrapColumns{Heap: heap, HeapCandidate: heapCandidate}
	if !columns.Available() {
		return HeapBootstrapColumns{}, false
	}
	return columns, true
}

// Available reports whether every column this binding needs carries an
// owner-issued type and live storage.
func (columns HeapBootstrapColumns) Available() bool {
	return columns.Heap.Available() && columns.HeapCandidate.Available()
}

// HeapBootstrapArgument is the decoded frame of one
// heap/bootstrap invocation.
type HeapBootstrapArgument struct {
	Key heapdomain.Key
}

// HeapBootstrapJudgment is the owner mathematics this binding carries. The axis
// states it by hand: nothing in this file can name a relation, a cell, a
// buffer or an engine value, so the mathematics stays where its owner wrote
// it and this artifact stays free of it.
type HeapBootstrapJudgment interface {
	relbindgen.Operation[HeapBootstrapArgument, heapdomain.Value]
	Available() bool
}

// The hand-authored judgment answers this family's own contract, and the
// assertion is what makes the two halves one binding.
var _ HeapBootstrapJudgment = HeapBootstrapOperation{}

// BindHeapBootstrap admits the heap/bootstrap family
// under its sealed signature.
func BindHeapBootstrap(operation signature.Signature, judgment HeapBootstrapJudgment, columns HeapBootstrapColumns, refusal model.RefusalID) (binding.Factory, bool) {
	if judgment == nil || !judgment.Available() || !columns.Available() {
		return nil, false
	}
	return relbindgen.Bind(relbindgen.Spec[HeapBootstrapArgument, heapdomain.Value]{
		Signature: operation,
		Decoder:   heapBootstrapDecoder{columns: columns},
		Encoder:   heapBootstrapEncoder{columns: columns},
		Operation: judgment,
		Address:   0,
		Refusal:   refusal,
	})
}

type heapBootstrapDecoder struct {
	columns HeapBootstrapColumns
}

func (decoder heapBootstrapDecoder) Decode(inputs relbindgen.Inputs) (HeapBootstrapArgument, bool) {
	key, ok := relbindgen.ScalarAt(inputs, 0, decoder.columns.HeapCandidate)
	if !ok {
		return HeapBootstrapArgument{}, false
	}
	return HeapBootstrapArgument{Key: key}, true
}

type heapBootstrapEncoder struct {
	columns HeapBootstrapColumns
}

func (encoder heapBootstrapEncoder) Encode(outputs relbindgen.Outputs, value heapdomain.Value) bool {
	return relbindgen.PutColumn(outputs, 0, encoder.columns.Heap, value)
}
