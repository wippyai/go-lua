// Package relation is domain/heap's binding surface: the thin typed bindings
// of every heap family the census compiles, over the thin typed owner-column
// publisher of every heap payload that crosses the boundary.
//
// Every zz_ file is emitted by analysis/schema/rule/relbindgen/relbind. The
// hand-authored half is this axis's own mathematics: the seeds, the allocation
// fold, the allocation carry, the sealed topology's receiver expansion, and
// the heap lattice's own join.
package relation
