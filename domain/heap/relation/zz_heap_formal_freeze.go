// Code generated by analysis/schema/rule/relbindgen/relbind. DO NOT EDIT.

package relation

import (
	"github.com/wippyai/go-lua/analysis/relation/schema/model"
	"github.com/wippyai/go-lua/analysis/relation/semantic/binding"
	"github.com/wippyai/go-lua/analysis/relation/semantic/signature"
	"github.com/wippyai/go-lua/analysis/schema/rule/relbindgen"
	heapdomain "github.com/wippyai/go-lua/domain/heap"
)

// HeapFormalFreezeColumns are the owner column codecs of the
// heap/formalfreeze binding.
type HeapFormalFreezeColumns struct {
	Heap         *relbindgen.Column[heapdomain.Value]
	HeapRouteTag *relbindgen.Column[uint64]
}

// NewHeapFormalFreezeColumns names this family's own owner columns. A column an axis
// other than this one publishes is handed in, never reached for.
func NewHeapFormalFreezeColumns(heap *relbindgen.Column[heapdomain.Value], heapRouteTag *relbindgen.Column[uint64]) (HeapFormalFreezeColumns, bool) {
	columns := HeapFormalFreezeColumns{Heap: heap, HeapRouteTag: heapRouteTag}
	if !columns.Available() {
		return HeapFormalFreezeColumns{}, false
	}
	return columns, true
}

// Available reports whether every column this binding needs carries an
// owner-issued type and live storage.
func (columns HeapFormalFreezeColumns) Available() bool {
	return columns.Heap.Available() && columns.HeapRouteTag.Available()
}

// HeapFormalFreezeArgument is the decoded frame of one
// heap/formalfreeze invocation.
type HeapFormalFreezeArgument struct {
	RouteTag    uint64
	Predecessor heapdomain.Value
}

// HeapFormalFreezeJudgment is the owner mathematics this binding carries. The axis
// states it by hand: nothing in this file can name a relation, a cell, a
// buffer or an engine value, so the mathematics stays where its owner wrote
// it and this artifact stays free of it.
type HeapFormalFreezeJudgment interface {
	relbindgen.Operation[HeapFormalFreezeArgument, heapdomain.Value]
	Available() bool
}

// The hand-authored judgment answers this family's own contract, and the
// assertion is what makes the two halves one binding.
var _ HeapFormalFreezeJudgment = HeapFormalFreezeOperation{}

// BindHeapFormalFreeze admits the heap/formalfreeze family
// under its sealed signature.
func BindHeapFormalFreeze(operation signature.Signature, judgment HeapFormalFreezeJudgment, columns HeapFormalFreezeColumns, refusal model.RefusalID) (binding.Factory, bool) {
	if judgment == nil || !judgment.Available() || !columns.Available() {
		return nil, false
	}
	return relbindgen.Bind(relbindgen.Spec[HeapFormalFreezeArgument, heapdomain.Value]{
		Signature: operation,
		Decoder:   heapFormalFreezeDecoder{columns: columns},
		Encoder:   heapFormalFreezeEncoder{columns: columns},
		Operation: judgment,
		Address:   0,
		Refusal:   refusal,
	})
}

type heapFormalFreezeDecoder struct {
	columns HeapFormalFreezeColumns
}

func (decoder heapFormalFreezeDecoder) Decode(inputs relbindgen.Inputs) (HeapFormalFreezeArgument, bool) {
	routeTag, ok := relbindgen.ScalarAt(inputs, 0, decoder.columns.HeapRouteTag)
	if !ok {
		return HeapFormalFreezeArgument{}, false
	}
	predecessor, ok := relbindgen.ScalarAt(inputs, 1, decoder.columns.Heap)
	if !ok {
		return HeapFormalFreezeArgument{}, false
	}
	return HeapFormalFreezeArgument{RouteTag: routeTag, Predecessor: predecessor}, true
}

type heapFormalFreezeEncoder struct {
	columns HeapFormalFreezeColumns
}

func (encoder heapFormalFreezeEncoder) Encode(outputs relbindgen.Outputs, value heapdomain.Value) bool {
	return relbindgen.PutColumn(outputs, 0, encoder.columns.Heap, value)
}
