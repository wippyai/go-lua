// Code generated by analysis/schema/rule/relbindgen/relbind. DO NOT EDIT.

package relation

import (
	"github.com/wippyai/go-lua/analysis/relation/schema/model"
	"github.com/wippyai/go-lua/analysis/relation/semantic/binding"
	"github.com/wippyai/go-lua/analysis/relation/semantic/signature"
	"github.com/wippyai/go-lua/analysis/schema/rule/relbindgen"
	heapdomain "github.com/wippyai/go-lua/domain/heap"
)

// HeapEmptyAllocationColumns are the owner column codecs of the
// heap/allocation/empty binding.
type HeapEmptyAllocationColumns struct {
	Heap          *relbindgen.Column[heapdomain.Value]
	HeapCandidate *relbindgen.Column[heapdomain.Key]
}

// NewHeapEmptyAllocationColumns names this family's own owner columns. A column an axis
// other than this one publishes is handed in, never reached for.
func NewHeapEmptyAllocationColumns(heap *relbindgen.Column[heapdomain.Value], heapCandidate *relbindgen.Column[heapdomain.Key]) (HeapEmptyAllocationColumns, bool) {
	columns := HeapEmptyAllocationColumns{Heap: heap, HeapCandidate: heapCandidate}
	if !columns.Available() {
		return HeapEmptyAllocationColumns{}, false
	}
	return columns, true
}

// Available reports whether every column this binding needs carries an
// owner-issued type and live storage.
func (columns HeapEmptyAllocationColumns) Available() bool {
	return columns.Heap.Available() && columns.HeapCandidate.Available()
}

// HeapEmptyAllocationArgument is the decoded frame of one
// heap/allocation/empty invocation.
type HeapEmptyAllocationArgument struct {
	Key         heapdomain.Key
	Predecessor heapdomain.Value
}

// HeapEmptyAllocationJudgment is the owner mathematics this binding carries. The axis
// states it by hand: nothing in this file can name a relation, a cell, a
// buffer or an engine value, so the mathematics stays where its owner wrote
// it and this artifact stays free of it.
type HeapEmptyAllocationJudgment interface {
	relbindgen.Operation[HeapEmptyAllocationArgument, heapdomain.Value]
	Available() bool
}

// The hand-authored judgment answers this family's own contract, and the
// assertion is what makes the two halves one binding.
var _ HeapEmptyAllocationJudgment = HeapEmptyAllocationOperation{}

// BindHeapEmptyAllocation admits the heap/allocation/empty family
// under its sealed signature.
func BindHeapEmptyAllocation(operation signature.Signature, judgment HeapEmptyAllocationJudgment, columns HeapEmptyAllocationColumns, refusal model.RefusalID) (binding.Factory, bool) {
	if judgment == nil || !judgment.Available() || !columns.Available() {
		return nil, false
	}
	return relbindgen.Bind(relbindgen.Spec[HeapEmptyAllocationArgument, heapdomain.Value]{
		Signature: operation,
		Decoder:   heapEmptyAllocationDecoder{columns: columns},
		Encoder:   heapEmptyAllocationEncoder{columns: columns},
		Operation: judgment,
		Address:   0,
		Refusal:   refusal,
	})
}

type heapEmptyAllocationDecoder struct {
	columns HeapEmptyAllocationColumns
}

func (decoder heapEmptyAllocationDecoder) Decode(inputs relbindgen.Inputs) (HeapEmptyAllocationArgument, bool) {
	key, ok := relbindgen.ScalarAt(inputs, 0, decoder.columns.HeapCandidate)
	if !ok {
		return HeapEmptyAllocationArgument{}, false
	}
	predecessor, ok := relbindgen.ScalarAt(inputs, 1, decoder.columns.Heap)
	if !ok {
		return HeapEmptyAllocationArgument{}, false
	}
	return HeapEmptyAllocationArgument{Key: key, Predecessor: predecessor}, true
}

type heapEmptyAllocationEncoder struct {
	columns HeapEmptyAllocationColumns
}

func (encoder heapEmptyAllocationEncoder) Encode(outputs relbindgen.Outputs, value heapdomain.Value) bool {
	return relbindgen.PutColumn(outputs, 0, encoder.columns.Heap, value)
}
