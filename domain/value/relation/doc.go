// Package relation is domain/value's binding surface: the thin typed bindings
// of every correlated-value family the census compiles, over the thin typed
// owner-column publisher of every value payload that crosses the boundary.
//
// Every zz_ file is emitted by analysis/schema/rule/relbindgen/relbind. The
// hand-authored half is this axis's own judgments, and each one is the
// analyzer's existing mathematics: a reduction here is domain/value's own
// reduction, a seed is its own seed, and a carry is its own transform.
package relation
