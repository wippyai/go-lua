// Package relation is domain/effect's binding surface: the thin typed bindings
// of the two call-site families the census compiles, over the thin typed
// owner-column publisher of every effect payload that crosses the boundary.
//
// Every zz_ file is emitted by analysis/schema/rule/relbindgen/relbind. The
// hand-authored half is this axis's own call-site judgments and its own
// lattice. The two families are one judgment under two derivations, which is
// exactly what the ABI expects: a reading is a declaration, not a form.
package relation
