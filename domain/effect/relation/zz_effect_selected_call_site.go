// Code generated by analysis/schema/rule/relbindgen/relbind. DO NOT EDIT.

package relation

import (
	"github.com/wippyai/go-lua/analysis/relation/schema/model"
	"github.com/wippyai/go-lua/analysis/relation/semantic/binding"
	"github.com/wippyai/go-lua/analysis/relation/semantic/signature"
	"github.com/wippyai/go-lua/analysis/schema/rule/relbindgen"
	calldomain "github.com/wippyai/go-lua/domain/call"
	effectfactor "github.com/wippyai/go-lua/domain/effect/factor"
)

// EffectSelectedCallSiteColumns are the owner column codecs of the
// effect/callsite/selected binding.
type EffectSelectedCallSiteColumns struct {
	Call            *relbindgen.Column[calldomain.Value]
	Effect          *relbindgen.Column[effectfactor.Value]
	EffectCandidate *relbindgen.Column[effectfactor.MountedCall]
}

// NewEffectSelectedCallSiteColumns names this family's own owner columns. A column an axis
// other than this one publishes is handed in, never reached for.
func NewEffectSelectedCallSiteColumns(call *relbindgen.Column[calldomain.Value], effect *relbindgen.Column[effectfactor.Value], effectCandidate *relbindgen.Column[effectfactor.MountedCall]) (EffectSelectedCallSiteColumns, bool) {
	columns := EffectSelectedCallSiteColumns{Call: call, Effect: effect, EffectCandidate: effectCandidate}
	if !columns.Available() {
		return EffectSelectedCallSiteColumns{}, false
	}
	return columns, true
}

// Available reports whether every column this binding needs carries an
// owner-issued type and live storage.
func (columns EffectSelectedCallSiteColumns) Available() bool {
	return columns.Call.Available() && columns.Effect.Available() && columns.EffectCandidate.Available()
}

// EffectSelectedCallSiteArgument is the decoded frame of one
// effect/callsite/selected invocation.
type EffectSelectedCallSiteArgument struct {
	Mounted    effectfactor.MountedCall
	Dispatched calldomain.Value
}

// EffectSelectedCallSiteJudgment is the owner mathematics this binding carries. The axis
// states it by hand: nothing in this file can name a relation, a cell, a
// buffer or an engine value, so the mathematics stays where its owner wrote
// it and this artifact stays free of it.
type EffectSelectedCallSiteJudgment interface {
	relbindgen.Operation[EffectSelectedCallSiteArgument, effectfactor.Value]
	Available() bool
}

// The hand-authored judgment answers this family's own contract, and the
// assertion is what makes the two halves one binding.
var _ EffectSelectedCallSiteJudgment = EffectSelectedCallSiteOperation{}

// BindEffectSelectedCallSite admits the effect/callsite/selected family
// under its sealed signature.
func BindEffectSelectedCallSite(operation signature.Signature, judgment EffectSelectedCallSiteJudgment, columns EffectSelectedCallSiteColumns, refusal model.RefusalID) (binding.Factory, bool) {
	if judgment == nil || !judgment.Available() || !columns.Available() {
		return nil, false
	}
	return relbindgen.Bind(relbindgen.Spec[EffectSelectedCallSiteArgument, effectfactor.Value]{
		Signature: operation,
		Decoder:   effectSelectedCallSiteDecoder{columns: columns},
		Encoder:   effectSelectedCallSiteEncoder{columns: columns},
		Operation: judgment,
		Address:   0,
		Refusal:   refusal,
	})
}

type effectSelectedCallSiteDecoder struct {
	columns EffectSelectedCallSiteColumns
}

func (decoder effectSelectedCallSiteDecoder) Decode(inputs relbindgen.Inputs) (EffectSelectedCallSiteArgument, bool) {
	mounted, ok := relbindgen.ScalarAt(inputs, 0, decoder.columns.EffectCandidate)
	if !ok {
		return EffectSelectedCallSiteArgument{}, false
	}
	dispatched, ok := relbindgen.ScalarAt(inputs, 1, decoder.columns.Call)
	if !ok {
		return EffectSelectedCallSiteArgument{}, false
	}
	return EffectSelectedCallSiteArgument{Mounted: mounted, Dispatched: dispatched}, true
}

type effectSelectedCallSiteEncoder struct {
	columns EffectSelectedCallSiteColumns
}

func (encoder effectSelectedCallSiteEncoder) Encode(outputs relbindgen.Outputs, value effectfactor.Value) bool {
	return relbindgen.PutColumn(outputs, 0, encoder.columns.Effect, value)
}
