// Package variantorigin is the finite origin/case axis for values whose runtime
// shape is a structural variant but whose alternatives must remain distinct even
// when their user-visible record fields are identical.
package variantorigin
