{
  "description": "Loop accumulator widened to [0,+inf) must be re-bounded to [0,#xs] by narrowing so a guarded xs[count] read is proven in-bounds.",
  "check": {},
  "run": {
    "skip": "checker-only narrowing-precision fixture"
  }
}
