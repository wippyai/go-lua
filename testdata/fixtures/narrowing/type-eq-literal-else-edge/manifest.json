{ "description": "Negative type narrowing: the else edge of type(v) == \"number\" narrows v to string (the complement). Clean only if the witness is reduced by the runtime-kind exclusion.", "files": ["main.lua"] }
