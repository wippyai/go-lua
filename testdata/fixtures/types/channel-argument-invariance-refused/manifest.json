{
  "description": "Channel<T> is invariant in its payload: a Channel<Event> is refused where a Channel<Timer> is required, even though the declaration body never mentions T and both applications expand to the same nominal Channel.",
  "packages": [
    "channel"
  ],
  "check": {
    "diagnostics": [
      {
        "file": "main.lua",
        "line": 9,
        "severity": "error",
        "code": "type.call.direct.argument_type",
        "message_contains": [
          "Event",
          "Timer"
        ],
        "render_contains": [
          "error[type.call.direct.argument_type]",
          "return sink(inbox)",
          "because:"
        ],
        "help_contains": [
          "Pass `inbox` as a value compatible with the parameter type, or change the callee signature if that argument is valid."
        ],
        "label_contains": [
          "argument value"
        ],
        "allow_empty_evidence": true
      }
    ]
  },
  "run": {
    "skip": "diagnostic-only fixture"
  }
}
