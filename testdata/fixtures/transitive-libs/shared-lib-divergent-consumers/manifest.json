{
  "description": "One library allocation site (lib.lua box) analyzed once yields a relational escape/placement summary over boundary placeholders; three consumers with divergent escape profiles each substitute their own facts and reach different placement conclusions for the same box (send -> Shared, local derive-then-drop -> proven-local, ownership.store -> ActorLocal retained).",
  "packages": ["process", "ownership"],
  "files": ["lib.lua", "consumer_send.lua", "consumer_local.lua", "consumer_store.lua", "main.lua"],
  "check": {
    "placement": {
      "require_complete": true,
      "min_shared_heap": 1,
      "min_owned_heap": 1,
      "min_shared_heap_kind": {
        "lua.table": 1
      },
      "min_owned_heap_kind": {
        "lua.table": 1
      },
      "max_unknown": 0
    }
  },
  "run": {
    "skip": "checker-only placement fixture"
  }
}
