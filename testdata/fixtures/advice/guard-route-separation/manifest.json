{
  "description": "A branch narrowing belongs to the arm that proves it: the complementary arm never inherits it, and a point two arms reconverge on keeps only what both of them prove.",
  "check": {
    "diagnostic_rules": [
      {
        "code": "advice.always_true_guard",
        "enabled": true,
        "severity": "hint"
      },
      {
        "code": "advice.always_false_guard",
        "enabled": true,
        "severity": "hint"
      }
    ],
    "diagnostics": [
      {
        "file": "main.lua",
        "line": 5,
        "column": 8,
        "severity": "hint",
        "code": "advice.always_false_guard",
        "message_contains": [
          "condition is proven always false"
        ],
        "min_evidence": 1,
        "evidence_contains": [
          "condition is proven to be false on every reachable path"
        ],
        "render_contains": [
          "hint[advice.always_false_guard]",
          "condition is proven always false"
        ],
        "render_not_contains": [
          "main.lua:20:6"
        ],
        "help_contains": [
          "Remove the unreachable branch"
        ],
        "label_contains": [
          "constant guard"
        ],
        "min_labels": 1
      },
      {
        "file": "main.lua",
        "line": 15,
        "column": 10,
        "severity": "hint",
        "code": "advice.always_true_guard",
        "message_contains": [
          "condition is proven always true"
        ],
        "min_evidence": 1,
        "evidence_contains": [
          "condition is proven to be true on every reachable path"
        ],
        "render_contains": [
          "hint[advice.always_true_guard]",
          "condition is proven always true"
        ],
        "render_not_contains": [
          "main.lua:20:6"
        ],
        "help_contains": [
          "Remove the guard"
        ],
        "label_contains": [
          "constant guard"
        ],
        "min_labels": 1
      }
    ]
  },
  "run": {
    "skip": "diagnostic-only fixture"
  }
}
