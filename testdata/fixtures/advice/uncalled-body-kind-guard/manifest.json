{
  "description": "The same guard judged in a called body and in a live body no call reaches: what an analysis proves about a function may not depend on whether the analyzed program happens to invoke it.",
  "check": {
    "diagnostic_rules": [
      {
        "code": "advice.always_true_guard",
        "enabled": true,
        "severity": "hint"
      }
    ],
    "diagnostics": [
      {
        "file": "main.lua",
        "line": 3,
        "column": 8,
        "severity": "hint",
        "code": "advice.always_true_guard",
        "message_contains": [
          "condition is proven always true"
        ],
        "min_evidence": 1,
        "evidence_contains": [
          "condition is proven to be true on every reachable path"
        ],
        "render_contains": [
          "hint[advice.always_true_guard]",
          "condition is proven always true"
        ],
        "help_contains": [
          "Remove the guard"
        ],
        "label_contains": [
          "constant guard"
        ],
        "min_labels": 1
      },
      {
        "file": "main.lua",
        "line": 12,
        "column": 8,
        "severity": "hint",
        "code": "advice.always_true_guard",
        "message_contains": [
          "condition is proven always true"
        ],
        "min_evidence": 1,
        "evidence_contains": [
          "condition is proven to be true on every reachable path"
        ],
        "render_contains": [
          "hint[advice.always_true_guard]",
          "condition is proven always true"
        ],
        "help_contains": [
          "Remove the guard"
        ],
        "label_contains": [
          "constant guard"
        ],
        "min_labels": 1
      }
    ]
  },
  "run": {
    "skip": "diagnostic-only fixture"
  }
}