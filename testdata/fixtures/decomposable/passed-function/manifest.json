{
  "description": "Phase 1 is conservative: passing a table to a function blocks decomposability.",
  "check": {
    "placement": {
      "require_complete": true,
      "min_allocation_sites": 1,
      "max_decomposable": 0,
      "max_unknown": 0
    }
  },
  "run": { "skip": "checker-only decomposable fixture" }
}
