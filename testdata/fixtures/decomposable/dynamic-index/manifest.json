{
  "description": "Dynamic indexing is not a static member operation and blocks decomposability.",
  "check": {
    "placement": {
      "require_complete": true,
      "min_allocation_sites": 1,
      "max_unknown": 0
    }
  },
  "run": { "skip": "checker-only decomposable fixture" }
}
