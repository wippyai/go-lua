{
  "description": "Imported not_nil + eq(type(x),'table') remove a typed-sequence field's optionality, but an arbitrary index read into the unknown-length sequence stays optional",
  "files": [
    "test.lua",
    "main.lua"
  ]
}
