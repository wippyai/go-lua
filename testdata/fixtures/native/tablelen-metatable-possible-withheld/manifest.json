{
  "description": "setmetatable makes __len possible, so the proved-absent disposition becomes unknown and the raw-length row is withheld rather than downgraded silently.",
  "files": [
    "main.lua"
  ],
  "check": {
    "errors": 0,
    "native": {
      "facts": [
        {
          "name": "at most 0 table_length.raw row(s)",
          "family": "table_length",
          "value_contains": [
            "raw"
          ],
          "max": 0
        }
      ],
      "invalidation": [
        {
          "name": "table_length is revoked by meta.set",
          "lane": "values",
          "family": "table_length",
          "event": "meta.set",
          "min": 1
        }
      ]
    }
  },
  "run": {
    "skip": "checker-only native fact fixture"
  }
}
