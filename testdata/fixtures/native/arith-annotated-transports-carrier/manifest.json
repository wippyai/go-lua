{
  "description": "Annotated numeric operands make the same `or 0` fallback shape dispatch primitively: the numeric carrier must survive the `or` logical selection into both operator operands.",
  "files": [
    "main.lua"
  ],
  "check": {
    "errors": 0,
    "native": {
      "facts": [
        {
          "name": "at least 1 proven scalar_operator row(s)",
          "lane": "values",
          "family": "scalar_operator",
          "trust": "proven",
          "min": 1
        },
        {
          "name": "scalar_operator row",
          "lane": "values",
          "family": "scalar_operator",
          "representation": "number",
          "left": "number",
          "right": "number",
          "trust": "proven",
          "min": 1
        }
      ]
    }
  },
  "run": {
    "skip": "checker-only native fact fixture"
  }
}
