{
  "description": "Every fallible time member answers two normal arms, the value with no error or nil with the module error, and each call reads both: the nil the module answers is observed on one path and the value is used on the other.",
  "packages": [
    "time"
  ],
  "files": [
    "main.lua"
  ],
  "check": {
    "errors": 0
  },
  "run": {
    "skip": "declaration-surface fixture"
  }
}
