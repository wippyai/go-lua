{
  "description": "Every declared member of the Wippy v1 store module - store.get and the nine Store methods - is called against the reference host surface with each fallible result read on both of its arms, so the whole module boundary is exercised without a single contradiction.",
  "packages": ["store"],
  "files": [
    "main.lua"
  ],
  "check": {
    "errors": 0
  },
  "run": {
    "skip": "host-surface coverage fixture; the store module has no fixture runtime"
  }
}
