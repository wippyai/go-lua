{
  "description": "An uninitialized local is nil on the path that skips the conditional assignment; that nil must survive the if-without-else merge, so assigning the merged value to a non-nil annotation is rejected. Proves uninitialized nil joins identically to an explicit = nil and is not silently absorbed as join-identity.",
  "check": {
    "diagnostics": [
      {
        "file": "main.lua",
        "line": 6,
        "severity": "error",
        "code": "type.assignment",
        "message_contains": ["cannot assign", "number?", "number"],
        "min_evidence": 2,
        "evidence_contains": ["source expression", "annotated number", "no boundary proof establishes number"],
        "min_labels": 2,
        "label_contains": ["assigned value", "declared type"]
      }
    ]
  }
}
