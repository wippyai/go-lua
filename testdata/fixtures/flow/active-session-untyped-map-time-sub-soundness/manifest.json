{
  "description": "Iterating a locally populated active_sessions map preserves inferred time.Time fields without treating the unannotated map as any",
  "packages": ["time"],
  "check": {"errors": 0}
}
