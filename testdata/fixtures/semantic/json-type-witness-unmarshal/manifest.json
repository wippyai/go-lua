{
  "description": "json.decode-style APIs project return types from explicit typed witnesses, including array witnesses, instead of trusting casts or any.",
  "run": {
    "skip": "checker-only typed witness surface"
  }
}
