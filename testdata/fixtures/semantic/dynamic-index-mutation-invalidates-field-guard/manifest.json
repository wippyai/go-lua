{
  "description": "A direct dynamic-index write through an alias invalidates a prior presence guard on the matching static field.",
  "check": {
    "diagnostics": [
      {
        "file": "main.lua",
        "line": 11,
        "severity": "error",
        "code": "type.assignment",
        "message_contains": ["cannot assign", "string"],
        "min_evidence": 2,
        "evidence_contains": ["source expression", "after is annotated"],
        "min_labels": 2,
        "label_contains": ["assigned value", "declared type"]
      }
    ]
  }
}
