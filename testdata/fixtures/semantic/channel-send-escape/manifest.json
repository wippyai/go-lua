{
  "description": "A locally-built record sent over a channel escapes; placement must mark the payload graph shared.",
  "packages": ["channel"],
  "check": {
    "placement": {
      "require_complete": true,
      "min_shared_heap": 1,
      "min_shared_depth": 2,
      "max_unknown": 0
    }
  },
  "run": {
    "skip": "placement-only"
  }
}
