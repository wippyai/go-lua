{
  "description": "Hub parks each inbound message into actor-local state by retained handle (zero-copy); the scaffolding tables are actor-local.",
  "check": {
    "placement": {
      "min_owned_heap": 2,
      "min_shared_heap": 1
    },
    "skip": "pending: field-granular / derivation-sensitive / closure-capture placement precision (task 150f00f9) not yet landed"
  },
  "run": {
    "skip": "checker-only placement fixture"
  }
}
