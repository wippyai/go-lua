{
  "description": "Actor main event loop: per-iteration scratch stays frame-local, the module cache is actor-local, and only the input-dependent forward is a deferred+promote escape.",
  "packages": [
    "channel"
  ],
  "check": {
    "placement": {
      "min_stack": 3,
      "min_owned_heap": 1
    }
  },
  "run": {
    "skip": "checker-only placement fixture"
  }
}
