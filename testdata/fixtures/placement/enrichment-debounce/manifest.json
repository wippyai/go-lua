{
  "description": "Debounce keeps pending events and timestamps in actor-local maps; the freshness test derives only frame-local scalars.",
  "check": {
    "placement": {
      "min_owned_heap": 2,
      "min_stack": 1,
      "min_shared_heap": 1
    }
  },
  "run": {
    "skip": "checker-only placement fixture"
  }
}
