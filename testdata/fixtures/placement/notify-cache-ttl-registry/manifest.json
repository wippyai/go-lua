{
  "description": "TTL registry: the cache map and its stored entries are actor-local; per-call lookup handles are frame-local borrows.",
  "check": {
    "placement": {
      "min_owned_heap": 2,
      "min_stack": 1
    }
  },
  "run": {
    "skip": "checker-only placement fixture"
  }
}
