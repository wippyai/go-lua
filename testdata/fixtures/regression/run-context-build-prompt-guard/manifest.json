{
  "description": "dataflow 0.7.16 run_context lib with its prompt builder: build_prompt proves its prompt present once the error is ruled out.",
  "files": ["prompt.lua", "prompt_builder.lua", "main.lua"]
}
